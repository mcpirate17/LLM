/*
 * Fused CPU kernels placeholder.
 *
 * Ownership note (DRY slice 1):
 * - All fused symbols previously defined here are canonicalized in
 *   src/cpu/kernels.cpp for current build targets.
 * - This file remains as a reserved location for future split ownership,
 *   but must not duplicate exported aria_* symbols.
 */
