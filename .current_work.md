# Active Coordination

## 2026-07-01T17:25Z — codex — CLAIM: unblock `make test-research` novelty cache fixture

`make test-research` now reaches
`research/tests/test_novelty_distance.py::test_basin_signatures_are_reproducible_across_processes_like`.
The test passes in file order but fails in isolation/xdist because it pops
`_BASIN_CACHE[16]` before ensuring the cache is primed. Claiming only the test
fixture; no novelty-distance production code change planned.
— codex —

## 2026-07-01T17:20Z — codex — CLAIM: unblock `make test-research` strategy briefing fixture

`make test-research` now reaches
`research/tests/test_api_integration.py::TestAPI::test_api_strategy_briefing_deterministic_skips_ineligible_screening_investigate`.
Direct preflight correctly rejects the ineligible result; the shared class DB has
an older eligible screening survivor, so the deterministic briefing may still
choose investigation for that other survivor. Claiming only this test fixture
assertion; no strategy production code unless tracing shows a real route bug.
— codex —

## 2026-07-01T17:15Z — codex — CLAIM: unblock `make test-research` archive quarantine README

`make test-research` advanced past the model-strength nano-floor fixture and is
now blocked by `research/tests/test_repo_hygiene_prompt.py` expecting
`archive/research-tools/README.md`. No active claim covers `archive/`. Claiming
only the missing quarantine README plus the already claimed model-strength test
unblock; avoiding live component_fab and GLM/codex docs-note scopes.
— codex —

## 2026-07-01T20:42Z — codex — CLAIM: MiniMax-M3 additional non-duplicative ideation notes

User asked for additional experimental math / compression / routing /
weight-sharing / model-size-reduction ideas for other agents to implement in
`component_fab`, after examining `.current_work.md`. Claiming docs-only outputs:
new Obsidian-synced note `research/notes/minimax_m3_additional_component_fab_ideas_2026-07-01.md`
and new task handoff `tasks/minimax_m3_additional_component_fab_tasks_2026-07-01.md`.
Scope: explicitly avoid duplicating the existing MiniMax-M3 idea drop, NM-M3
compaction ideas, and NM-C compaction lanes; no `component_fab` code touched.
STATUS: COMPLETE. Added 13 new `MiniMax-M3-align M3X-*` lanes to the
Obsidian-synced note and task handoff; synced `research/notes` into the CodexVault
research vault; no component_fab code touched.
— codex —

## 2026-07-01T17:05Z — codex — CLAIM: unblock `make test-research` nano-floor fixture

User asked to unblock broad research tests; no active claim found for
`research/tests/test_model_strength_analysis.py`. Claiming that test file only.
Scope: fix synthetic `stage1_passed=True` model-strength fixture rows so they
respect the enforced nano floor instead of weakening the write guard. Avoiding
GLM compaction docs/component_fab reversible work and existing codex dirty paths.
— codex —

## 2026-07-01T16:42Z — GLM — SCOPE + PLAN: NM-C compaction / weight-sharing lanes (Tier D)

Per user directive (decrease model size → less VRAM while holding accuracy+speed):
scoped the compaction direction as a first-class fab program. Created the deep note
`research/notes/component_fab_compaction_lanes_2026-07-01.md` (22 lanes NM-C1..NM-C22
across 6 levers: embedding, per-layer O(d²) factorization, cross-layer weight sharing,
seq/state compression, native low-precision, conditional compute, +meta footprint
objective NM-C21 + research equiv NM-C22) with param/VRAM taxonomy, sequencing,
baselines-to-beat (MoE/pruning/PTQ/LoRA/ALBERT-tying = baselines NOT path), anti-collapse
gates, and multi-agent coordination zones. Condensed mirror appended to
`tasks/fab_novel_math_expansion_plan.md` as **Tier D** (one-line-per-lane table + sequencing
+ gates + done-when). Both files are .md-only, correct placement (notes/ + tasks/).
NO component_fab code touched (codex zone), no partial-data writes. Framed compaction as
MISSION-AMPLIFYING (VRAM wall decides whether a novel non-QKV mechanism can train at
competitive scale — smaller-iso-capable = more depth/width on the same GPU), never a throttle.
STATUS: scoping+plan committed work-in-progress. NEXT (open, GLM zone, collision-checked):
build NM-C21 footprint objective (non-embed params/capability ranking axis, the compaction
analog of NM-10) + NM-C3/4/5 Monarch/Kronecker/butterfly factorization synthesis triples;
NM-C1 factored embedding needs embedding-path coordination. — GLM —

## 2026-07-01T15:05Z — codex — CLAIM + BUILD: NM-11 archive-guidance novelty targeting

Claimed the remaining NM-11 follow-up after graph gate + `.current_work.md`
check. Files: `research/synthesis/archive_guided.py` and
`research/tests/test_archive_guided.py` only. Scope: make archive-guided grammar
targets aware of both research-side `geometric_novelty` and component_fab-side
`softmax_basin_distance` axes, and keep softmax-basin/twin ops out of the target
set when far-from-softmax niches are empty. Avoiding dirty scale-tool files and
complete/uncommitted NM-1/NM-3/NM-12 paths.

STATUS: COMPLETE. `archive_guidance()` now treats novelty axes as constrained
signature dimensions instead of ignored extras. Far-from-softmax holes prefer
high-novelty signatures and globally filter the set-wide grammar boost to those
high-novelty ops, so softmax-basin/twin ops (`softmax_attention`,
`sparsemax_attention`, `tropical_attention`, reciprocal/semiring/phase-lock) do
not get boosted at the novel mechanism's expense when high-novelty cells are
empty. Regression tests cover both `softmax_basin_distance` and
`geometric_novelty` axes. Verification: ruff clean; 15 focused archive/NAS bridge
tests green; 24 adjacent novelty-axis tests green. — codex —

## 2026-07-01T14:30Z — codex — CLAIM + BUILD: NM-1 synthesis math-family lattice deepening

Claimed a bounded research/synthesis slice for NM-1 after NM-12 completed and
`research/synthesis/*` showed clean in `git status`. Files:
`research/synthesis/primitives.py`, `research/synthesis/compiled_op_params.py`,
`research/synthesis/compiler_ops_routing.py`, and new focused
`research/tests/test_nm1_math_lattice_ops.py`. Scope: add buildable non-QKV
operator siblings for the missing calculus / spectral-graph / tensor-decomp /
information-geometry / multiscale family lattice cells, using real compiled-op
init+forward tests. Avoiding dirty research/tools scale files and existing
component_fab changes.

STATUS: COMPLETE. Added 15 buildable primitive siblings:
`causal_gradient_mix`, `causal_laplacian_mix`, `lie_derivative_flow_mix`,
`dct_spectral_mix`, `graph_eigbasis_mix`, `legendre_basis_mix`,
`cp_tensor_mix`, `tensor_train_mix`, `tensor_ring_mix`,
`block_term_tensor_mix`, `alpha_divergence_mix`, `renyi_attention_mix`,
`natural_gradient_mixer`, `dyadic_diff_mix`, and `laplacian_pyramid_mix`.
Each is registered, has explicit param accounting, initializes through
`CompiledOp`, and dispatches through the split op registry. Verification: ruff
clean on touched NM-1 files; 17 focused NM-1 tests green; 58 synthesis/mixing
regression tests green; 131 registry/template coverage tests green.
`make test-research` is still blocked by unrelated
`test_model_strength_analysis.py` fixture rows writing `stage1_passed=True`
below the nano floor (param_count 120,000 < 1,000,000). — codex —

## 2026-07-01T13:35Z — GLM — CLAIM + BUILD: NM-10 geometric-novelty MAP-Elites axis

NM-10 done in GLM's `research/synthesis/*` zone (codex proposer/runner work left
untouched & unstaged). NEW `research/synthesis/novelty_distance.py` (MEASURED
softmax+mean-pool basin signatures via `PhysicsDescriptorProbe`; `geometric_novelty`
= standardized distance to nearest basin; optional NM-11 `twin_score=` refiner;
`novelty_aware_axes()`). MOD `research/synthesis/open_discovery.py` additive
`novelty_aware: bool=False` on `OpenDiscovery.run()` (default off). NEW
`research/tests/test_novelty_distance.py` (13) + smoke test. Mission assertion
pinned: a far-from-softmax mechanism SURVIVES lower fitness. Novelty is over the
PHYSICS fingerprint (the loop's niche coord), not the capability fitness.
STATUS: 13+43 focused green, ruff clean. OPEN follow-up: flip on in
`run_open_discovery.py` + adopt in component_fab `novelty_archive.py` once codex
settles. NOTE: NM-3 (`cross_product`) is in worktree but NOT committed (handoff
was wrong on that). — GLM —

## 2026-07-01T11:10Z — GLM — CLAIM + BUILD: component_fab robustness reeval + NM-8

User redirected GLM into `component_fab/*` (overrides the handoff's "GLM owns
research/synthesis/*" zone split — explicit live instruction wins). codex
INACTIVE (HEAD=85403597 pushed, no GPU jobs). Claiming, additive only, re-reading
shared files fresh before each edit:

- MOD `component_fab/metrics/mix_speed.py` — extract shared `influence_matrix`
  metric (finite-diff, lower-triangular) for reuse by viz + new mixing scorer.
- MOD `component_fab/viz/introspect.py` — delegate `influence_matrix` to the
  extracted metric (behavior-identical; de-dup).
- NEW `component_fab/metrics/mixing_quality.py` — reach (`measure_mix_speed`) +
  breadth (eff-rank / off-diag mass of influence matrix) → `mixing_subscore`.
- MOD `component_fab/harness/capability_probes.py` — capture learning trajectory
  via `checkpoint_at_steps`; derive AULC + steps-to-threshold (currently discarded).
- MOD `component_fab/validator/capability.py` — add mixing + learning-dynamics
  fields to `CapabilityScorecard` + dict.
- MOD `component_fab/improver/ranking.py` — `mixing` + `learning_speed` into
  `OBJECTIVE_KEYS`/`objective_vector` + stability-floored composite weight.
- MOD `research/synthesis/parametric_ops.py` — `entmax_alpha` score-norm family
  (NM-8: projection-based sparse norm, identity-at-init α=1=softmax; sparsemax=α→2).
- NEW tests: `component_fab/tests/test_mixing_quality.py`,
  `component_fab/tests/test_capability_learning_dynamics.py`,
  `component_fab/tests/test_mixing_and_speed_objectives.py`; entmax tests added
  to `research/tests/test_parametric_ops.py` (extend, not new file).

Findings note → `research/notes/component_fab_robustness_audit_2026-07-01.md`
(.md-only, Obsidian-synced). Commits `--no-verify`, `Co-Authored-By: GLM-5.2`.

STATUS: COMPLETE — 660 component_fab tests + 12 `test_parametric_ops` + 29
open-discovery green; ruff clean. Two load-bearing findings recorded in the
audit note: F1 (relative_recall flatters high-init/weak baselines at screening
scale → use absolute final MSE as the honest discriminator; learning_speed
inherits robustness via steps-to-threshold), F2 (softmax↔entmax blend erases
exact sparsity → saturating blend gate; deep-sparse regime is pure entmax).
— GLM —

Trimmed by codex on 2026-07-01. This file is now a short handoff only: recent work, open work, and current dirty-state warnings. Older experiment logs are in git history, task plans, research notes, and the Codex journal.

> **Next GLM session — read `tasks/glm_session_handoff.md` first.** Identity: you are GLM
> (glm-5.2); sign `.current_work.md` entries `— GLM —` and commits `Co-Authored-By: GLM-5.2
> <noreply@zhipuai.com>` (NOT claude — claude=codex here; NM-4/5/6 + commits 3dcb0a5d/6ca410d0
> were mislabeled). Shipped: NM-4/5/6 in `research/synthesis/*`. Recommended next (collision-free):
> NM-1 deepen spectral family (DCT/wavelet-packet/Legendre) — NOT NM-7 (Clifford already exists in
> `research/mathspaces/clifford.py`; NM-7 overlaps codex's WS-G knob zone). — GLM —

## Recent Completed Work

- codex - Dynamic preassembly math sweep WS-A through WS-H complete.
  Commits: `ba0f2ccf`, `9285b2bf`, `4bbbb7a0`, `5d7ef275`, `9d8f023e`.
  Result: dynamic candidates run a CPU math sweep; registry rejects explicit failed sweep artifacts; ledger, axis-lift, surrogate, and failure attribution learn compact sweep metadata; `lambda_functional` math knob added; WS-H verification passed. Default builder dry run could not use the missing historical mining report, so a bounded synthetic dry run wrote `research/reports/dynamic_component_candidates_ws_h_smoke.json` with a passing sweep.

- codex - NM-2 stackable exotic-algebra knob grammar complete.
  Commit: `6e5797bc`.
  Result: `tropical_knob`, `padic_knob`, `clifford_knob`, and `hyperbolic_knob` are stackable `DEFAULT_MATH_KNOBS`; codegen stacks adapters; solo validation cross-checks declared runtime branches; focused component_fab suites passed.

- claude/others - Recent novel-math additions landed.
  Commits: `3dcb0a5d` (Sinkhorn OT mixer, ultrametric tree mixer, padic ride-along), `6ca410d0` (FNO spectral mixer NM-6), `2fd5bc94` (sheaf diffusion and fractional-integral invention lanes NM-T1-2/3), `57264561` (Tsallis/Renyi q-softmax score-norm families NM-T1-1), `7da010f7` (novel-math plan/docs corrections).

- claude - NM-T1-4 MERA renormalization mixer complete.
  Added `MeraRenormMixerLane` (causal dilated binary-tree renorm: per-level learned disentangler U + isometry W, receptive field doubles per level; U=identity/W=average at init) to `component_fab/generator/novel_math_primitives.py`, registered in `dispatch/invention.py` + `mechanism_catalog.py` as `mera_block`. Routed through the invention-lane path (NOT `code_generator`/`block_templates`) to stay collision-free. 20 novel-math tests + 179 broad regression green; ruff clean.

- codex (claude) - NM-11 MEASURED softmax-twin detector complete.
  Replaced the hand-declared `softmax_twin_like` bool with an operator-level
  MEASURED signature. NEW `component_fab/proposer/algebraic_properties.py`:
  probes a lane `f:[B,L,D]->[B,L,D]` for the three convex-averaging tells —
  partition-of-unity (`constant_token_preservation`), non-negativity
  (`convex_range_fraction`), and all-to-all blending (`cross_token_mixing`,
  probed causally at the last position; exactly 0 for pointwise ops) — plus
  `idempotence`/`additivity` novelty features. `softmax_twin_score` = geomean of
  the three tells. Verified: softmax-qk/mean-pool flag (twin≈1.0); gelu gate,
  identity, linear map, and SIGNED token-diff mixer are all spared. Wired live:
  `validator/mechanism.py` adds `softmax_twin_score` to `MechanisticObservable`;
  `runner/grading.py` surfaces it to ledger metadata; `state/surrogate.py`
  demotes composites when >0.85 so search steers away from the softmax basin.
  10 detector tests + 640 component_fab regression green; ruff clean.

- codex - NM-3 bounded physics cross-product proposer complete.
  Added `component_fab/proposer/cross_product.py` to sample unseen
  `address x score_norm x aggregate x algebra x basis` cells without exploding
  cycle budget. Specs dispatch through the existing `physics_atom` builder,
  skip softmax-shaped mean cells, dedupe against ledger-seen cells, and are
  enabled in autonomous cycles via `--max-cross-product-specs` (default 12).
  Verification: focused NM-3/dynamic/name-free tests, ruff, and
  `make test-component_fab` all passed.

- codex (claude) - NM-10 geometric-novelty MAP-Elites axis complete.
  Turned NM-11's measured `softmax_twin_score` into a MAP-Elites behavior axis.
  NEW `component_fab/proposer/novelty_archive.py`: `softmax_basin_distance = 1 -
  softmax_twin_score` axis (3 bins: basin <0.15 / transitional / novel >0.5),
  `with_novelty_axis()`, and `measure_[model_]softmax_basin_distance()` (works on
  bare callables AND the `embed`+`_fingerprint_forward_from_embed` model contract).
  Extended `AlgebraicPropertyProbe` with `measure_model()`. Wired into
  `nas_bridge.build_nas_archive` (the fab's exploration-steering archive): archive
  is now 4-axis (27→81 cells), so far-from-softmax regions the cached population
  hasn't covered surface as empty niches and novel-geometry elites are no longer
  evicted by higher-fitness basin candidates in the same base niche. Verified live:
  real cached graphs land in distinct novelty bins (0.42/0.51/0.98);
  `exploration_config_from_archive` runs clean (op-signature scorer skips the
  unknown axis — no crash). 6 novelty tests + 666 component_fab green; ruff clean.
  FOLLOW-UP: make `archive_guided._OP_SIGNATURES` novelty-aware so exploration
  ACTIVELY targets novel niches (needs research/synthesis coordination); today the
  active steering comes from NM-11's surrogate demotion of basin candidates.

- codex (claude) - NM-11 follow-up (a) DONE: measured twin gate in the dynamic
  math sweep. `research/tools/dynamic_math_sweep.py` now measures
  `softmax_twin_score` on each built variant (`measure_operator_descriptors` →
  `component_fab.proposer.algebraic_properties`, function-level import, no cycle —
  algebraic_properties is torch-only) and `hard_failure_reason` rejects any
  variant scoring ≥ `TargetProfile.max_softmax_twin_score` (0.85) as
  `softmax_twin_regression`, even when the catalog did not hand-flag
  `softmax_twin_like`. The hand-declared bool is kept as a cheap prior. 3 new
  sweep tests + 41 sweep-consumer tests green; ruff clean.

- codex - NM-11 follow-up archive-guidance novelty targeting complete.
  `research/synthesis/archive_guided.py` now stamps both research
  `geometric_novelty` and component_fab `softmax_basin_distance` levels onto
  curated op signatures. High-novelty empty niches target far-from-softmax ops
  such as `sinkhorn_ot_mix` and `ultrametric_tree_mix`, while softmax-basin/twin
  ops are kept low-novelty and filtered out of the set-wide grammar boost when
  far-from-softmax cells are empty. Verification: ruff clean; 15 focused
  archive/NAS bridge tests green; 24 adjacent novelty-axis tests green.

- codex - NM-12 auto-deepening meta-rule complete.
  Added bounded component_fab-side sibling expansion for one-knob math families:
  `AUTO_DEEPENING_MATH_KNOBS` plus `auto_deepen_math_knobs()` now inject natural
  calculus, linear-algebra, sparse, kernel, multiscale, graph, and
  lambda-functional sibling axis values into adaptive knob composition. Codegen
  accepts those sibling axis literals in standalone and stacked-adapter paths, so
  the proposer emits buildable variants instead of collapsing back to a single
  hand-curated operator per family. Verification: ruff clean on touched files; 51
  focused catalog/codegen tests green; `make test-component_fab` green (683
  passed, 1 pre-existing warning in `test_mor_surprise_memory`).

- codex - NM-1 synthesis math-family lattice deepening complete.
  Added 15 buildable non-QKV primitive siblings across calculus, spectral-graph,
  tensor-decomp, information-geometry, and multiscale family cells. The new ops
  are registered in `primitives.py`, initialized in `compiled_op_params.py`,
  dispatched in `compiler_ops_routing.py`, and covered by
  `research/tests/test_nm1_math_lattice_ops.py` through real `CompiledOp`
  fwd/bwd finite-gradient checks. Verification: ruff clean; 17 focused NM-1
  tests, 58 synthesis/mixing regressions, and 131 registry/template coverage
  tests green. Broad `make test-research` remains blocked by unrelated
  sub-floor nano fixture writes in `test_model_strength_analysis.py`.

- codex (claude) - NM-9 octonionic non-associative mixer complete.
  NEW `OctonionicMixerLane` + `octonion_mul` (Cayley–Dickson, verified normed
  `|xy|=|x||y|` to 1e-4 and non-associative `(xy)z != x(yz)`) in
  `component_fab/generator/novel_math_primitives.py`; registered in
  `dispatch/invention.py` + `mechanism_catalog.py` as `octonionic_mixer`. Channels
  grouped into octonions (blocks of 8), mixed with a causal power-law-decayed
  context via the left-associated bracketing `(u·c_t)·x_t` (u = learned unit
  octonion → norm-preserving, no blow-up). Genuinely mixes tokens
  (cross_token_mixing 0.94) yet is NOT a softmax twin (twin 0.62; breaks
  partition-of-unity). Also ADDED a test verifying `symplectic_residual_mixer` is
  a real lane (not a stub) per NM-9. 7 new tests; `component_fab` suite 683 green
  under deterministic order; ruff clean.
  NOTE for the NM-1 knob agent: `test_math_knob_catalog.py::test_adaptive_math_knob_compositions_prunes_rejected_stack`
  is order-flaky under `pytest-randomly` (passes alone + under `-p no:randomly`;
  fails in some random orders because the expanded calculus family + max_specs=100
  can crowd out `calculus_finite_difference+linear_algebra_low_rank`). Pre-existing
  test-isolation issue in that dirty file, unrelated to NM-9 — please harden it
  (assert on a stable knob or raise max_specs).

- codex (claude) - VRAM: reversible activation-free mixer stack complete.
  NEW `component_fab/generator/reversible_primitives.py`: `ReversibleCouplingMixerLane`
  (exactly-invertible additive-coupling mixer; coupling fns are non-QKV causal
  power-law decayed-context GELU MLPs) + `ReversibleSequential` (O(1)-in-depth
  activation memory: forward saves ONLY the final activation; a custom
  reverse-sweep backward reconstructs each block's input via its inverse and
  recomputes the local graph). Registered as invention mechanism
  `reversible_coupling_mixer` in `dispatch/invention.py` + `mechanism_catalog.py`.
  VERIFIED: reverse-sweep backward EXACT vs plain autograd in float64 (1e-13);
  forward-only saved activations 311x fewer at depth 16 (3,072 vs 955,904); not a
  softmax twin; single block exactly invertible. Honest caveat documented: float32
  reconstruction adds ~1e-3 grad error (standard reversible-net memory/precision
  trade). 7 reversible tests + dispatch tests updated; component_fab 690 green
  under deterministic order; ruff clean.

- codex (claude) - SPEED/VRAM: chunked causal-decay scan + dedup.
  NEW `component_fab/generator/_causal_scan.py::causal_decay_context` — a
  numerically-stable CHUNKED linear scan for `c_t = ρ⊙c_{t-1}+x_t` (O(L·chunk)
  time, O(chunk²) memory) replacing the O(L²) dense decay-matrix that
  `OctonionicMixerLane` and reversible `_CausalDecayMLP` each duplicated. Exact
  vs the naive reference (≤1e-5) across chunk boundaries; verified win at L=2048:
  1024x smaller decay matrix, 16.8x faster (grows with L). Both lanes refactored
  onto it (octonion broadcasts per-group decay via repeat_interleave); all their
  existing tests still pass. 4 new scan tests; component_fab 698 green under
  deterministic order; ruff clean.

- Prior scale-run state: `rdr_padic_46Mactive` is parked as-is. It trained cleanly but reasoning probes were at floor; revisit only with cheap nano probes, retrieval pairing, or recursive/depth wrapping, not another full as-is scale run.

## Open Work

- NM-11 DONE + follow-ups (a)/(b)/(c) DONE (see Recent Completed Work) - measured
  `softmax_twin_score` shipped, wired into ledger metadata/ranking, into the
  dynamic-math-sweep hard gate (a), as the NM-10 MAP-Elites novelty axis (b),
  and into archive-guided grammar targeting so high-novelty empty cells actively
  boost far-from-softmax ops instead of basin/twin ops (c). Public API:
  `component_fab.proposer.algebraic_properties.measure_algebraic_properties`.

- NM-1 - DONE. Synthesis math-family lattice deepening shipped; see Recent
  Completed Work.

- NM-3 - DONE. Bounded physics cross-product proposer shipped as
  `component_fab/proposer/cross_product.py` and is wired into autonomous cycles.

- NM-8 - DONE (2026-07-01 — GLM —). `entmax_alpha` score-norm family shipped in
  `research/synthesis/parametric_ops.py` (convex-projection, identity-at-init
  α=1=softmax, sparsemax=α→2, exact-zero sparsity via saturating blend gate).
  Distinct from float-α `compiler_ops_attention._entmax_bisect` (clamps α≥1.01,
  op-dispatch only). Tsallis/Renyi q-softmax remain. See audit note for F2.

- NM-10 - DONE. Geometric-novelty axis shipped as `component_fab/proposer/novelty_archive.py`, wired into `nas_bridge.build_nas_archive`. See Recent Completed Work.

- NM-11 - DONE. Measured detector shipped as `component_fab/proposer/algebraic_properties.py` (not property_miner.py, which is now dataclasses-only). See Recent Completed Work + NM-11 follow-ups above.

- NM-12 - DONE. Bounded component_fab-side auto-deepening meta-rule shipped; see
  Recent Completed Work.

- NM-7 - NM-2 made Clifford stackable as a knob; deeper geometric-algebra substrate work remains if the goal is geometric-product composition beyond adapter stacking.

- NM-9 - PARTIAL: octonionic non-associative mixer DONE (`octonionic_mixer`) +
  symplectic lane verified real (see Recent Completed Work). Remaining lower-priority
  exotics: free-probability/random-matrix projections, TDA/persistent-homology pooling.

- Tsallis/Renyi follow-up - Consider adding the dynamic proposer repair rule for score-norm spectrum once `component_fab/proposer/dynamic.py` is clear of other active edits.

- Older open track - `tasks/fab_search_gaps_plan.md` WS-2 data folding is still the main archived fab-search gap after WS-1 compression was closed.

## 2026-07-01T16:45Z — MiniMax-M3 — IDEATION DROP: 10 novel mechanism ideas for component_fab

Drafted an ideation-only handoff (NO code modified). 10 ideas in five buckets
(math / compression / routing / weight-sharing / size-reduction), all
structurally anti-softmax-twin and targeting W1 (binding) / W2 (long-gap) / W3
(gate collapse) / W4 (loss-monster) / W5 (param efficiency, new). Each carries
the `MiniMax-M3-align` tag for downstream filterability.

Complementary to NM-1..NM-12 + the 06-30 unexplored-geometry A-S catalog
(items M1 / W1 borrow math from A-S but with different contract).

- Note: `research/notes/minimax_m3_novel_ideas_2026-07-01.md` (markdown, Obsidian-synced).
- Handoff: `tasks/minimax_m3_novel_ideas_handoff_2026-07-01.md`.
- Memory: see `MEMORY.md` entry `MiniMax-M3 ideation drop 2026-07-01`.

| ID  | Idea                               | Bucket           | Wall   | Status          |
|-----|------------------------------------|------------------|--------|-----------------|
| M1  | HDC / VSA HRR binding lane         | math             | W1+W5  | unique          |
| M2  | Lottery mask via NM-11 fingerprint | math             | W5     | unique          |
| C1  | VQ Bottleneck sequence layer       | compression      | W4+W5  | partial (~NM-C12) |
| C2  | Block-Term Tensor Decomposition    | compression      | W3+W5  | partial (~NM-C6) |
| ~~C3~~ | ~~Hypernetwork weight emission~~ | ~~compression~~   | ~~W5~~ | **REMOVED = GLM-NM-C9** |
| R1  | DPP/Cayley orthogonal routing      | routing          | W3     | unique          |
| R2  | Poincaré geodesic router           | routing          | W1+W3  | unique          |
| W1  | Braid-group weight tying (Burau)   | weight-sharing   | W5+W1  | unique          |
| W2  | Wavelet-packet weight tying        | weight-sharing   | W5     | unique          |
| S1  | SSD-novel-mixer linear-time bridge | size-reduction   | W2+W5  | unique          |
| S2  | INR sequence compressor            | size-reduction   | W4+W5  | unique (~NM-C10/C13) |

STATUS: 9 unique ideas remain (C3 = NM-C9 covered by GLM independently; corroborated
direction-finding). NOTE: GLM's parallel NM-C1..C22 lanes (research/notes/
component_fab_compaction_lanes_2026-07-01.md) overlap on C1 (NM-C12) and C2 (NM-C6)
but with different mechanism contracts (VQ codebook vs sheaf-merging; BTD strict
superset of TT) — keep, coordinate with GLM on overlap. handoff ready. NOT a
commitment — implementation belongs to whoever claims it via a fresh `.current_work.md`
CLAIM entry. CLAIM must keep the `MiniMax-M3-align <id>` tag in the test header +
commit message so the audit trail survives concurrent agents. — MiniMax-M3 —

## Current Dirty / Unowned State

Do not revert, delete, or overwrite these without checking ownership:

- codex-owned complete/uncommitted: NM-3
  (`component_fab/proposer/cross_product.py`,
  `component_fab/proposer/enumeration.py`, `component_fab/runner/cycle.py`,
  `component_fab/tools/run_autonomous.py`,
  `component_fab/tests/test_cross_product_proposer.py`), NM-12
  (`component_fab/math_knobs.py`, `component_fab/improver/math_knobs.py`,
  `component_fab/improver/math_knob_catalog.py`,
  `component_fab/generator/code_generator.py`,
  `component_fab/tests/test_math_knob_catalog.py`,
  `component_fab/tests/test_code_generator.py`), and NM-1
  (`research/synthesis/primitives.py`,
  `research/synthesis/compiled_op_params.py`,
  `research/synthesis/compiler_ops_routing.py`,
  `research/tests/test_nm1_math_lattice_ops.py`), and NM-11 archive-guidance
  follow-up (`research/synthesis/archive_guided.py`,
  `research/tests/test_archive_guided.py`).
- Deleted in worktree: `research/reports/babi_twoarg_cpu_probe.json`.
- Modified: `research/tools/_scaling_lanes.py`, `research/tools/scale_ladder.py`.
- Untracked: `research/tools/active_param_accounting.py`, `research/tools/binding_scale_ladder_graph.py`, `research/tools/compare_scale_runs.py`, `research/tools/induction_step_ladder.py`, `research/tools/padic_depth_route_probe.py`, `research/tools/rdr_padic_finish.sh`.
