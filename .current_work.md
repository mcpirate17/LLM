# Active Coordination

**Date:** 2026-05-04 ET
**Purpose:** short coordination surface for Codex + the two Claude sessions. Keep
this file current-only. Completed work should be summarized here briefly and left
in reports/tests/artifacts, not carried as active work.

## Global Guardrails

- Treat the worktree as user-owned. Do not revert unrelated changes.
- Do not delete/truncate research data, DBs, logs, profiling artifacts, or
  backups.
- Before any intentional DB mutation, run backup freshness or make a backup.
- Do not wire new metrics, gates, scoring, grammar weights, or continuous-mode
  policy without Tim's explicit approval unless this file says the wiring is
  already complete and tested.
- `program_results.stage1_passed=True` test fixtures must include core post-S1
  metrics or use an allowed fixture/backfill trust label.

## Cleared From Active Work

These are complete and should not stay on the active task list unless Tim
explicitly reopens them:

- Controlled sentence diagnostic, Nano-BLiMP v3 audit, Nano controlled holdout:
  all report-only/withdrawn from scoring.
- Real LM quickcheck audit: completed report-only.
- Op-composition audit: completed read-only zero-GPU audit.
- Nano controlled-corpus binding / tuning / strict scans: completed; no scoring
  wiring approved here.
- NanoBind 17K backfill: completed; artifacts live under
  `research/reports/nano_bind_backfill/`.

## Codex Active Lane — Meta Profile Strategy Integration

**Owner:** Codex
**Status:** implemented and validated. Routing scaffold follow-up is complete;
no Codex file claims are active after this handoff.

### Files Claimed / Recently Touched

- `research/meta_analysis/metadata_db.py`
- `research/tools/build_meta_analysis_db.py`
- `research/tools/meta_profile_ml_analysis.py`
- `research/tools/meta_experiment_queue.py`
- `research/tools/profile_component_scaffolds.py`
- `research/scientist/runner/dashboard_hypothesis.py`
- `research/scientist/llm/decision.py`
- `research/scientist/llm/prompts.py`
- tests:
  - `research/tests/test_meta_analysis_db.py`
  - `research/tests/test_meta_experiment_queue.py`
  - `research/tests/test_meta_profile_strategy_integration.py`
  - `research/tests/test_profile_component_scaffolds.py`
  - touched fixture in `research/tests/test_selection_policy.py`

### What Is Now Active In Strategy

- `meta_analysis.db` now materializes component profiling:
  - `op_profile_catalog`
  - `op_pair_profile_catalog`
  - `op_triplet_profile_catalog`
  - `graph_profile_observations`
  - `eval_metric_catalog`
  - `external_component_prior_catalog`
- Latest generated reports:
  - `research/reports/meta_profile_ml_analysis_20260504.{md,json}`
  - `research/reports/meta_experiment_queue_20260504.{md,json}`
  - refreshed after Claude backfill and routing scaffold coverage:
    `research/reports/meta_profile_ml_analysis_20260504_routing.{md,json}`
    and `research/reports/meta_experiment_queue_20260504_routing.{md,json}`
  - profile/compression CSVs and scaffold command JSONL with same prefix.
- Continuous / LLM-governed strategy now sees a compact
  `meta_profile_strategy` brief in the next-experiment summary.
- The structured planner prompt tells Sonnet / LLM planner to use that brief.
- Fallback planner also applies the brief if the LLM is disabled, unavailable,
  over budget, or omits the meta-profile bias.

### Current Strategy Bias

The live brief currently biases next strategy toward:

- mode: `synthesis`
- strategy: `profile_refresh_guided_routing_compression_synthesis`
- boost scaffoldable candidates:
  - `token_class_proj`
  - `signal_conditioned_compression`
  - `hybrid_token_gate`
  - `hybrid_sparse_router`
- now scaffoldable via `hybrid_routing` family:
  - `hybrid_token_gate`
  - `sparse_span_builder`
  - `hybrid_sparse_router`
  - `default_path`
  - `lane_conditioned_block`
  - `calibrated_branch_merge`
- remaining unsupported profile-refresh item currently downweighted:
  - `gated_linear_attention`

### Smoke Profile Results

No-persist CPU scaffold smoke profiles:

- `token_class_proj`: ok, replacement scaffold, loss delta `-0.0226`
- `adjacent_token_merge`: ok, replacement scaffold, loss delta `+0.0115`
- `signal_conditioned_compression`: `screen_fail` in replacement scaffold
- `hetero_moe`: ok, FFN scaffold, loss delta `-0.0066`
- `spectral_filter`: ok, FFN scaffold, loss delta `-0.0026`
- `kronecker_linear`: ok, FFN scaffold, loss delta `-0.0016`
- `hybrid_token_gate`: ok, hybrid routing scaffold, loss delta `-0.009`
- `sparse_span_builder`: ok, hybrid routing scaffold, loss delta `-0.007`
- `hybrid_sparse_router`: ok, hybrid routing scaffold, loss delta `-0.011`
- `default_path`: ok, hybrid routing scaffold, loss delta `-0.011`
- `lane_conditioned_block`: ok, hybrid routing scaffold, loss delta `-0.016`
- `calibrated_branch_merge`: ok, hybrid routing scaffold, loss delta `-0.008`

Artifacts:

- `research/reports/scaffold_profile_meta_queue_gpt2_replace_20260504.json`
- `research/reports/scaffold_profile_meta_queue_gpt2_ffn_20260504.json`
- `research/reports/scaffold_profile_hybrid_routing_20260504.json`
- `research/runtime/scaffold_profile/scaffold_profile_hybrid_routing_20260504.log`

### Validation

Last targeted validation:

```bash
python -m pytest \
  research/tests/test_meta_profile_strategy_integration.py \
  research/tests/test_meta_experiment_queue.py \
  research/tests/test_meta_analysis_db.py \
  research/tests/test_meta_surrogate_analysis.py \
  research/tests/test_meta_surrogate_screening_proposal.py \
  research/tests/test_profile_component_scaffolds.py \
  research/tests/test_selection_policy.py::test_auto_recommend_records_next_experiment_plan_decision \
  -q
# 22 passed

python -m ruff check \
  research/scientist/runner/dashboard_hypothesis.py \
  research/scientist/llm/decision.py \
  research/scientist/llm/prompts.py \
  research/tools/meta_experiment_queue.py \
  research/tools/meta_profile_ml_analysis.py \
  research/tests/test_meta_profile_strategy_integration.py \
  research/tests/test_meta_experiment_queue.py \
  research/tests/test_selection_policy.py
# All checks passed

python -m pytest research/tests/test_profile_component_scaffolds.py -q
# 15 passed

python -m ruff check \
  research/tools/profile_component_scaffolds.py \
  research/tests/test_profile_component_scaffolds.py
# All checks passed
```

## Next Codex Task

No active Codex-owned task is claimed here. Good next candidates if Tim asks:

1. Add a `gated_linear_attention` scaffold/harness; it is now the only
   unsupported op in `meta_profile_strategy.needs_scaffold_harness_ops`.
2. Add a component-specific harness for `signal_conditioned_compression`,
   because it is scaffoldable but failed the generic replacement smoke.
3. Persist a short, approved scaffold profiling run into `lab_notebook.db` after
   backup freshness is verified, so scaffold evidence can feed
   `scaffold_guided` priors instead of remaining report-only.

## Claude-1 Coordination — Template-Pass + Dynamic Slots (DONE through Phase C)

**Owner:** Claude  
**Status:** Phase 1+2+3+4+5 + dynamic-slot wiring + UI toggle all SHIPPED. No
file claims active.

**Commits (in order, 2026-05-04):**

- `d1cb2b0` Phase 1+2+3 — template-pass classification, sa_factor blender, 3 Bucket D mines
- `3f12734` Phase 4 — derived slot constraints (loader), soft-A elevator, test fixes
- `160e787` Phase 5 — auto-mine sub-patterns from passing-cohort graphs
- `f7603da` Phase A+B+C — wire `derive_slot_classes` into `_pick_compatible_motif` via `_use_derived_slot_classes` flag (default OFF), trial template harness, auto-demotion script
- `31f6b5f` Continuous + UI — `RunConfig.use_derived_slot_classes`, dashboard checkbox in AdvancedConfigPanels
- `7e97be1` Miner V2 — per-template coverage index + novelty filter + Python skeleton emit. On default V1 output: 0 novel / 30 rare / 197 common. Surfaces `semi_structured_2_4_linear` and gated-attention `mul` chains as actionable rare patterns.
- `e15290a` `tpl_sparse_24_linear_block` (rare-chain conversion) + math-space deep-dive report (`research/reports/novel_math_ops_proposal_20260504.md`) identifying `alternative_math_candidate_catalog`'s 4 unimplemented frameworks (lambda_calculus 0.82, combinatory_logic 0.74, boolean_algebra 0.62, category_theory 0.55) and gaps within existing non-Euclidean families.
- `9198d57` Clifford companion ops `clifford_inverse` + `versor_apply` + `tpl_geometric_product_versor_block` template. Geometric_product was the highest-passing non-Euclidean op (84.7% with eval=150) and family had only 5 ops; this fills the obvious gap. Template weight 3.0; smoke-tested. Ops register through `mathspaces/registry.py:register_all_mathspaces`.

**How to use the dynamic-slot feature:**

- UI: `Advanced Config → Dynamically Learned Slots` checkbox (rebuilt
  `dashboard/build/` already; restart dashboard to load — see
  `Claude-3` section below for the dashboard restart marker).
- Programmatic: `RunConfig(use_derived_slot_classes=True, ...)`. Propagates
  to `GrammarConfig` via `_build_grammar_config` in `execution_candidates.py`
  and onward to the picker via `graph.metadata`. Hardcoded class tuples
  remain as fallback when the meta DB has no qualifying data for the slot.

**Tools added (research/tools/):**

- `template_slot_inventory.py`, `template_slot_realization.py`,
  `phase1_mixer_analysis.py`, `phase1_meta_analysis.py`,
  `template_classification.py`, `template_pass_summary.py`,
  `mine_template_subpatterns.py`, `sample_template_distribution.py`,
  `auto_demote_trial_templates.py`

**Reports/artifacts:**

- `research/reports/slot_inventory.json`, `slot_realization.csv`,
  `template_overall.csv`, `slot_mixer_credit_v2.csv`,
  `op_mixer_certification_v2.csv`, `high_capability_slot_fills.csv`,
  `template_classification.csv`, `template_pass_20260504_summary.md`,
  `slot_tightening_proposal.json`, `new_templates_proposal.json`,
  `mined_chain_proposals.{csv,json}`
- `research/runtime_events/template_weights.ndjson` — per-template
  `sa_factor` events from the blender (185+ entries)

**Open follow-ups for the next Claude (none blocking):**

- Miner V2: ✅ DONE in commit `7e97be1`. Skeletons live in
  `research/reports/mined_novel_chain_proposals.json` for human review.
  Codegen is intentionally NOT auto-applied — skeletons need shape/config
  review per `_motif_rules.py` constraints before becoming real templates.
- Cron loop: schedule meta_analysis.db rebuild → classifier → weight
  refresh on a daily timer.
- Cleanup: remove redundant explicit derivations in
  `_templates_attention_tail.py` (Phase 4.1 wired them per-template;
  the central wire in `_pick_compatible_motif` covers it now).
- After Claude-3's backfill chain finishes (`[chain] All phases complete`),
  rerun `auto_demote_trial_templates.py` against any trial templates
  registered between now and then.
- Top rare-chain candidates from V2 worth promoting to a real template:
  `softmax_attention -> linear_proj -> mul` (gated attention, n_pass=194,
  pass=0.94, only in 2 templates) and the `semi_structured_2_4_linear`
  chains (n_pass=200, pass=0.81, only in latent_compress_*).

**No active claims.** Synthesis files are unlocked.

## Claude-2 Coordination — Probe / NanoBind / Scoring Caution

**Owner:** Claude or Tim-assigned session
**Status:** no active scoring wiring approved in this file.

Current caution:

- Nano controlled-corpus and strict variants found useful failure modes, but
  previous scoring claims were withdrawn or narrowed. Treat them as diagnostic
  unless Tim explicitly approves wiring.
- Token-merge low-PPL degeneracy is consistent with Codex profile findings:
  compression can help WikiText while increasing NanoBind/frequency-collapse
  risk when no positional/content mixer follows.
- Any proposal to hard-gate compression/token-merge should be reviewed against:
  - NanoBind
  - controlled language
  - WikiText
  - TinyStories
  - routing fast-lane

## Claude-3 Coordination — Slot Fix + Backfill + nano-AR-INV Pilot

**Owner:** Claude (this session)
**Status:** Slot-fix shipped + tested. Backfill Phase A + most of Phase B
persisted. Phase C (meta_analysis.db rebuild) + Phase D (auto-demote)
completed manually after the chain process exited mid-run. nano-AR-INV
probe + corpus + tests + pilot tool landed; smoke pilot validates the
discriminator. Dashboard SAFE TO RESTART.

**1. Slot fix:** `_tpl_novel_mixing_block` in
`research/synthesis/_templates_attention_advanced.py` was deterministic →
all 7 sibling templates produced 1–2 fingerprints regardless of seed.
Fixed: rng-driven norm + FFN motif picks; emits `primary_core`/
`complement_core` slot bindings. Diversity 2/500 → 42 unique / 45 valid
per 50 seeds. 96 template tests pass. **Uncommitted.**

**2. Backfill outcome:**
- Phase A: 3 Phase-3.1 templates persisted (62 fresh program_results).
- Phase B: ran ~110 fresh program_results across 5/7 fixed templates
  (`difficulty_routed_attention_block` 33, `long_conv_hyena_block` 31,
  `gated_progressive_attention_block` 22, `mixture_of_recursions_block`
  20, `associative_memory_block` 15) before the chain process exited
  early. `strided_attention_block` and `gated_linear_attention_block`
  retained their historical 152 / 119 evals — no fresh data on those.
- Phase C: rebuilt `meta_analysis.db`. **slot_catalog 529 → 557 (+28
  new bindings)**. 196 templates, 56,429 slot observations.
- Phase D: `auto_demote_trial_templates` ran read-only — 0 trial
  templates registered → 0 demotion proposals.
- `research/reports/backfill_20260504/` directory was wiped during the
  chain shutdown — only the DB persistence remains.

**3. nano-AR-INV (new investigation-tier probe, NOT WIRED YET):**
The legacy `ar_auc` probe trains a wikitext-pretrained model on synthetic
random-token sequences — across 8,062 DB rows, the max ar_auc is 0.015 vs
the 0.15 pass threshold. **Zero discrimination.** New design uses real
3-slot (noun, adj, object) sentences re-using nano_bind's vocab + a
**wikitext-2500-step warmup** (mimics investigation-tier S1) before
fine-tuning on nano-AR. The wikitext warmup is the key — it forces
language priors to compete for capacity with the binding task,
exposing failure modes that small-corpus probes miss.

Files added (uncommitted):
- `research/eval/nano_ar_inv_corpus.py` — corpus generator (NOUNS+ADJECTIVES
  from nano_corpus_v0; new OBJECTS pool of 30 single-token cl100k_base nouns)
- `research/eval/nano_ar_inv.py` — probe runner mirroring nano_bind (deepcopy
  + fine-tune + eval). `from_s1=True` for production, `from_s1=False` for
  pilot. Pilot path runs wikitext warmup since stored leaderboard rows have
  no live S1 weights.
- `research/tools/nano_ar_inv_pilot.py` — read-only pilot CLI
- `research/tools/nano_ar_inv_pilot_top20_sweep.py` — V3: top-by-composite 3-seed
- `research/tools/nano_ar_inv_pilot_midtier_sweep.py` — V4: composite-100-300 3-seed
- `research/tests/test_nano_ar_inv.py` — 12 tests (corpus determinism,
  pair-match grader, vocab single-token check, held-out separation, probe
  smoke). All pass.

**Iteration log V0→V4** (full details in
`research/reports/nano_ar_inv_findings.md`):

- V0/V1: corpus too small without warmup → only token_merge fails. Useless.
- V2: 2500-step wikitext warmup added → ref_gpt2 drops to 0.83, real differentiation appears.
- V3 (top-20, 3-seed): 100% agreement with nano_bind binary classification
  (NB-passers ≥ 0.83, NB-failers ≤ 0.03). All 10 NB-passers hit in_pair=1.00.
- V4 (mid-tier, 3-seed): **2D classification space emerges**.

**4-tier 2D classification (in_pair × held_class):**

| tier | n in V4 | in_pair | held_class | meaning |
|---|---|---|---|---|
| Pass + Generalize | 1 | 1.00 | 0.87-0.93 | learned bindings AND frame |
| Pass + Memorize-only | 5 | 1.00 | 0.00-0.67 | bindings but no frame generalization |
| **Frame-only (weak retrieval)** | **10** | 0.10-0.37 | 0.87-1.00 | **NB passes; nano-AR catches** |
| Degenerate | 1 | 0.00 | 0.00 | full frequency collapse (NB fail) |

The **Frame-only tier (10/20 mid-tier archs)** is the headline — these are
nano_bind PASSERS that nano-AR-INV catches as weak-retrieval. Templates
affected: `latent_attn_sparse_ffn`, `residual_block`, `routed_bottleneck`,
`latent_attn_moe`, `latent_compress_block`, etc.

**Wall cost:** ~20s/arch on cuda 5090 (mirror of investigation-tier scale).

**Recommended composite:**
```
nano_ar_inv_score = 0.6 * in_dist_pair_match_acc + 0.4 * held_class_acc
binding_composite_v2 = 0.4 * nano_ar_inv_score + 0.3 * induction_auc + 0.3 * binding_auc
# (replaces the dead 0.4 * ar_auc weight in binding_composite)
```

**Followups (not in this session):**

- Wire `nano_ar_inv` into post-S1 probe pipeline at investigation tier
  (`research/scientist/runner/execution_training_post.py:560` is where
  `associative_recall_score(model, ...)` runs today). DB schema needs new
  columns; needs explicit user approval per CLAUDE.md guardrails.
- Replace dead `ar_auc` weight in `binding_composite` with `nano_ar_inv_score`.
- Resume Phase B for `strided_attention_block` / `gated_linear_attention_block`
  if their slot bindings are needed (low priority — both have 152/119
  historical evals).
- 7 other deterministic templates emit zero slot bindings: `token_merge_block`,
  `token_merge_conv`, `recursive_attn_ssm_hybrid`, `recursive_moe_attn`,
  `rwkv_double_norm`, `attn_normalized_matmul_pinned`, `induction_matmul_block`.

**Reports:**
- `research/reports/nano_ar_inv_findings.md` — full V0→V4 iteration writeup
- `research/reports/nano_ar_inv_top20_heavy_warmup_3seed.{md,json}` — V3
- `research/reports/nano_ar_inv_midtier_3seed.{md,json}` — V4

## Open Questions For Whoever Picks Up Next

- Do the unsupported routing blocks need a new composite scaffold family, or can
  they be safely wrapped in existing `gpt2_replace` / `mamba_mixer` scaffolds?
- Should `signal_conditioned_compression` get a component-specific harness after
  failing the generic replacement scaffold?
- After scaffold coverage improves, should the continuous strategy brief boost
  `hybrid_*` blocks instead of downweighting them?
- Should meta-profile strategy bias be guarded by a new explicit config flag, or
  is the current read-only advisory + fallback-bias behavior acceptable?
