# Active Coordination

## 2026-07-02T01:50Z — codex — CLAIM + BUILD: perf-scan #11/#12 remainder (gmqar inference-mode, telemetry grad-pass dedup)

User-approved #11/#12. ANOTHER session already has the fast-path loss sync
(`training_core.py`), `utils.py` accumulators, and binding-probe autocast/eval-batch
IN FLIGHT (uncommitted, unclaimed — same pattern as #8/#10; leaving those hunks alone).
Claiming the UNTOUCHED remainder:
- `research/eval/gmqar.py` (CLEAN): `score_cell` → `torch.inference_mode()` (numerically
  identical to no_grad) + OPT-IN bf16 autocast knob (default OFF — gMQAR is the metric of
  record; forward precision must not drift silently).
- `research/eval/training_core.py` TELEMETRY branch only (disjoint region from the
  in-flight fast-path hunk): kill the 3rd full grad pass per step — the native clip is a
  single uniform scalar `c=clamp(max/(total+1e-6),0,1)` (verified in
  `_training_native.cpp:486`), so post-clip stats = pre-clip stats × c, derived exactly
  from the clip's returned total norm (one scalar sync replaces a full
  `grad_stats_fused` pass).
STATUS: COMPLETE (mine) + ADOPTED (theirs, verified). Mine: gmqar inference_mode +
opt-in autocast (default OFF, metric-of-record comparability preserved); telemetry
grad-pass dedup (derived post-clip == re-measured pass, worst rel 7.7e-8 over 5 seeds;
2 pinned regression tests added to test_training_core_diagnostics.py). Theirs
(verified + adopted, same unclaimed-WIP pattern as #8/#10): utils.py
measure_loss/compute_perplexity device-side accumulators + inference_mode (EXACT
parity vs HEAD, diff 0.0e+00); training_core fast-path loss sync removal;
binding_multislot bf16 autocast on CUDA + eval_batch_size 64 (DISCLOSURE: binding
numerics on CUDA no longer bit-comparable to history — matches sandbox/induction
sibling-probe convention; CPU unchanged). Suites: diagnostics 14, binding 5,
retrieval/interp/gmqar 47 — all green; ruff clean. — codex —

## 2026-07-02T01:37Z — Claude (Fable 5) — CLAIM + BUILD: NM-F9.1 CDMA addressing fixes (v2) + probe rerun

Implementing the three de-risked F9.1 fixes in `research/synthesis/cdma_slot_binding.py`
(my lane; oracle diagnostic proved everything downstream of addressing is solved):
1. **Tie addressing** (`tie_addressing=True` default): `query_lift ≡ key_lift` — the
   task queries with the key token; halves the co-alignment problem.
2. **Annealed selection** (`selection="annealed"` default): Lorentzian
   bounded-reciprocal weighting over the correlation gap (the validated non-softmax,
   NM-11-clean form), blended toward hard top-1 STE via a trainer-settable
   `selection_hardness ∈ [0,1]`; hardness 1.0 == v1 deploy behavior.
3. **Code-alignment aux loss**: `aux_loss` stashed per forward (1 − max-cos between
   key lift and code bank); harness weights it, decaying to 0.
Harness: `train_model` anneals hardness 0→1 and aux weight 0.1→0 for CDMA ops.
Updating `research/tests/test_cdma_slot_binding.py` for the new defaults. Probe rerun
at n_keys 32 (oracle-comparable) then 64. Files: same as prior claims
(`cdma_slot_binding.py`, its test, `nm_f_capability_probes.py`, findings note).
— Claude (Fable 5) —

## 2026-07-02T01:25Z — codex — CLAIM + BUILD: perf-scan #17 aria_core LTO + GIL release

Perf-scan follow-up (#16 rayon shipped `b1551d47`). Claiming `aria_core/setup.py` +
`aria_core/bindings/bind_kernels.cpp` + `aria_core/bindings/bind_ops.cpp`:
(a) add host LTO (`-flto=auto`) + `-fno-math-errno` to the cxx/link flags (only
`-O3 -march=native -fopenmp` today); (b) release the GIL during the pure-tensor
kernel bindings (~115 `m.def`s hold it for the whole kernel; only
`sensitivity_collect_f32` has `py::call_guard<py::gil_scoped_release>` today) —
skipping `get_op_registry` + anything touching py objects, and `bind_graph.cpp`
if py-object-shaped. Rebuild `build_ext --inplace`, run `aria_core/tests`.
STATUS: COMPLETE. 144 pure-tensor bindings guarded (66+3 in bind_kernels, 75 in
bind_ops; mechanical `, nogil)` diff — get_op_registry/cum-lambdas/IO defs and all
of py-object-shaped bind_graph.cpp untouched). Host LTO + -fno-math-errno in
cxx+link flags; nvcc objects untouched. Rebuilt in place. VERIFIED: kernel
correctness vs torch; GIL release proven live (Python counter thread runs at
0.62x idle rate DURING native matmuls — was fully blocked before);
aria_core/tests 85 green. NOTE: dashboard/API must restart to pick up the new
_C .so (per aria_core/CLAUDE.md). — codex —

## 2026-07-02T01:21Z — Claude (Fable 5) — CLAIM + RUN: F9.1 oracle-assist diagnostic (CDMA addressing vs superposition)

Extends the NM-F probe harness (`research/tools/nm_f_capability_probes.py`, commit
`4b437590`, my claim) with `oracle{32,64,128}` mixers: CDMA superposition with
GROUND-TRUTH slot addressing derived from token identity (bijective key→slot at
n_keys=32); only value_compress + out_lift are learned. Factorizes the F9 probe
failure: oracle solves ⟹ blocker is addressing (F9.1 fixes 1–3 suffice, Welch curve
becomes measurable); oracle fails ⟹ payload d_v is the constraint. Same file claim as
previous entry + findings appended to `research/notes/nm_f_probe_results_2026-07-02.md`.
— Claude (Fable 5) —

**STATUS: COMPLETE** — verdict: **addressing is 100% of the gap; the binding law is
PROVEN in-model.** Oracle CDMA [3 seeds, all identical]: 1.0/1.0/1.0/**0.999** at
2/4/8/**16** pairs — 16 bindings superposed in ONE 256-d state vector — on both block
and scatter layouts, converging in ~200 steps (~30× faster than the attn control).
Capacity trade lands where designed: chips 32≈64 (decoder tolerates interference below
the analytic bound); chips 128 (d_v=2 payload) degrades at 8–16 pairs ⟹ payload-limited,
chips 32–64 is the dim-256 operating range. Learned-addressing cdma32 at matched
difficulty stays 1/n-shaped (0.31@2). F9.1 fixes (tie W_qry≡W_key, anneal top-1,
code-align aux) now have an existence proof that everything downstream is solved; the
Welch-curve probe is unblocked once one lands. Findings in note; raw JSON
20260701_212317. — Claude (Fable 5) —

## 2026-07-02T00:05Z — Claude (Fable 5) — CLAIM + RUN: NM-F nano-contract capability probes (F9 binding curve, F4 retention flatness)

Training probes for the 4 shipped NM-F ops (commits `ac5541dd`, `31ce9f3d`), per user
directive. Nano contract config (dim 256, vocab 256, 2 blocks, ~1.2M non-embed), 3 seeds
+ median, randomized-query controls built into both tasks. NO runs.db/notebook writes —
standalone synthetic-task probe, JSON to `research/reports/nm_f_probes/` (auto-pruned).
- **Probe A (F9)**: gMQAR-style multi-slot binding, accuracy vs #pairs {2,4,8,16} at
  chips {32,64,128} — the falsifiable Welch-interference-curve prediction. Tiny 2-layer
  transformer = positive-control probe (explicitly allowed; NOT a new baseline run).
- **Probe B (F4)**: bind→distractors→query retention, train gaps ≤128, eval gaps
  {16,64,256,1024} (extrapolation) — flatness vs a learned-decay EMA (the
  decay-by-parameterization baseline) and the transformer control.
GPU checked free (1.6/32GB). Claiming `research/tools/nm_f_capability_probes.py` +
findings note `research/notes/nm_f_probe_results_2026-07-02.md`. No component_fab/
edits; not touching dirty `research/eval/*` probe files (codex lane). — Claude (Fable 5) —

**STATUS: COMPLETE**. Findings in `research/notes/nm_f_probe_results_2026-07-02.md`:
- **F4 retention flatness CONFIRMED** [3 seeds, median]: 1.0/1.0/1.0/**0.992** at gaps
  16/64/256/**1024** (8× train length); matched learned-decay EMA (identical except the
  state law) solves in-distribution then collapses to 0.033/0.020 at 256/1024;
  ≈ attention control at 1024 with zero position params. The p-adic retrieval-gap fix
  has a validated structural component.
- **F9 probe VALID, v1 addressing doesn't train**: attention control clears 0.98+ up to
  16 pairs; CDMA ≈ chance (best seed 0.21@2) — binding ALGEBRA verified in unit tests,
  blocker is hard-top-1-STE addressing co-alignment. F9.1 fix lane (tie W_qry≡W_key,
  anneal hard select, code-alignment aux, oracle-assist diagnostic) scoped in the note
  + lane doc. Welch curve untested (blocked), not falsified.
- Harness lessons recorded (dense multi-query supervision required; state mixers need a
  kernel-4 local conv or (k,v) pairs are structurally invisible; body-length 48→64
  cliff collapses even the attention control — screening should sweep body length).
Probe runs: 20260701_{200821,203409}_nm_f_probes.json. — Claude (Fable 5) —

## 2026-07-02T00:15Z — codex — CLAIM + BUILD: perf-scan #16 rayon batch feature extraction

Perf-scan follow-up (#8/#10 adopted+pushed `aec15c88`; #5/#6/#7/part-of-#13 are IN FLIGHT
by another session in `compiler_ops_{sequence,routing}.py` — staying clear). Claiming
`research/runtime/native/rust/aria-scheduler/{Cargo.toml,src/intelligence.rs}`:
parallelize the five serial `*_batch*` graph-feature loops with rayon `par_iter`
(independent graphs, immutable shared maps; callers `gnn_predictor.py:876-947`).
No `python_bridge.rs` changes (rayon workers don't need the GIL). Rebuild via maturin,
verify parity vs serial + benchmark.
STATUS: COMPLETE. All five `*_batch*` loops (`extract_topology_features_batch_json`,
`extract_topology_feature_maps_batch`, both `*_with_imodel_batch`,
`extract_edge_op_pairs_batch_json`) now `par_iter().collect::<Result<...>>()`;
rebuilt via maturin (thin-LTO profile). Verified on 64 REAL `program_results` graphs:
batch == per-graph singles EXACTLY (every key, order preserved); 512-graph batch
5.3x vs serial singles. `test_intelligence_native_hotpaths` + native-consumer
tests 28 green. — codex —

## 2026-07-02T00:04Z — GLM — CLAIM + BUILD: NM-C16 p-adic low-precision mixer

Continuing the Tier-D compaction program after NM-C2 (`496c0182`). Built
`research/synthesis/padic_lowprec_mix.py` `PadicLowPrecMixer` + tests: the validated
collapse-proof padic highway gate `g=sigmoid(Wg·x+Wv·v_p(x)+b)` (mirrors
`_op_padic_gated_mixer`), but the gated projection is carried through `padic_truncate`
(keep `n_digits` base-`p` significant digits, STE). Truncation error bounded at
`p^(1-n_digits)/2` RELATIVE magnitude, scale-INDEPENDENT (the "low bit-width is lossless
for discarded scales" property; `n_digits`=precision knob, default ~4-bit activations).
Compaction angle = activation PRECISION, complementary to NM-C3/C5/NM-C15/NM-C2; DISTINCT
from NM-C15 (activations vs ternary weights). Identity-at-init (ReZero); per-token sigmoid ⟹
passes NM-11 softmax-twin detector; NM-10-measurable. 24 tests; 108 sibling-regression
passed; ruff clean. Registry wiring DEFERRED (NM-C3/C5/C7/C10/C15 convention). Claiming
`research/synthesis/padic_lowprec_mix.py`, `research/tests/test_padic_lowprec_mix.py` only —
NOT touching codex's dirty `compiler_ops_*`/ECC/perf lanes. — GLM —

## 2026-07-01T23:58Z — codex — CLAIM + ADOPT: perf-scan #8/#10 probe batching (verified orphan WIP)

User-approved adoption of the UNCLAIMED, verified-good worktree changes implementing
perf-scan items #8 (batch `influence_matrix` L→1 forward + `measure_mix_speed` trials;
`component_fab/metrics/mix_speed.py`) and #10 (thread precomputed `fx`/`fy` through the
physics descriptors + NM-11 tells; `research/synthesis/physics_descriptors.py`,
`component_fab/proposer/algebraic_properties.py`). Authored by an unidentified concurrent
session with NO claim entry; sat dirty >1h. My verification vs HEAD algorithms: physics +
algebraic drift EXACTLY 0.0; influence_matrix ≤7.6e-9; measure_mix_speed drifts by design
(RNG draw shape changed — same estimator, not seed-comparable to historical scorecards).
Adding parity-pinning regression tests to the three EXISTING test files, then committing
with attribution note + drift disclosure, then pushing master per user instruction.
— codex —

## 2026-07-01T23:59Z — codex — CLAIM + BUILD: WS-2 adjustable data folding search axis

User asked to pick up WS-2 data folding and make folding aggressively tunable:
amount of data folded, fold count, fold direction, sparse/intermittent modes,
vertical/horizontal folding, and search visibility so mixers see more distant
tokens sooner. Scope: extend existing `research/synthesis/data_pipeline_grammar.py`
(do not duplicate route machinery), wire `enumerate_data_route_variants` through
`component_fab/proposer/enumeration.py` and LM/autonomous controls, update
`research/tools/data_route_ab.py`, axis variants, and focused tests. Avoiding
active Fable `research/synthesis/{nilpotent_lie_scan,port_hamiltonian_mix}.py`
and unrelated dirty ECC/routing/perf lanes. — codex —

STATUS: COMPLETE. Shipped adjustable data-folding controls in
`data_pipeline_grammar.py`: fold fraction/amount, fold count, direction
(`alternate`/`forward`/`backward`), dense/sparse/intermittent patterns, and
horizontal/vertical fold orientation. Added an implemented route catalog with
aggressive fold variants and routed it into axis variants,
`enumerate_data_route_variants`, LM probe enumeration, autonomous cycle controls
(`--enable-data-routes`, `--max-data-route-specs`), and `data_route_ab.py`.
Focused route/proposer tests passed; ruff clean; `make test-component_fab` green
(768 passed, 1 pre-existing warning). — codex —

## 2026-07-01T23:55Z — Claude (Fable 5) — CLAIM + BUILD: NM-F3 nilpotent-Lie signature scan + NM-F5 port-Hamiltonian mixer

Continuing the NM-F lane (F9+F4 shipped, commit `ac5541dd`). User directive: F3 then F5.
- **NM-F3** `research/synthesis/nilpotent_lie_scan.py` `NilpotentLieScan`: sequence
  mixing by an exact associative scan over a step-2 nilpotent Lie group (truncated path
  signature; Chen's identity ⟹ associativity is algebra, not approximation). The level-2
  cross term accumulates ORDERED key–value second-order statistics — the mixing law has
  ZERO learned params (only lifts + zero-init readout ⟹ identity-at-init). Order
  sensitivity for free: distinguishes anagrams sum/EMA pooling cannot.
- **NM-F5** `research/synthesis/port_hamiltonian_mix.py` `PortHamiltonianMixer`:
  recurrence in port-Hamiltonian normal form ẋ = (J−R)∇H + Bu (J banded skew, R⪰0 diag,
  H learned convex quadratic), discretized by the Cayley/implicit-midpoint transform ⟹
  the state transition is a CONTRACTION in the energy norm by THEOREM — collapse-proof
  and length-stable by construction (principled successor to the p-adic gate's empirical
  collapse-proofing). Conservative (R→0) transport circulates state without decay.
  Honest baseline note: diagonal-stable SSMs (S4/LRU) are the baseline to beat — this is
  structurally distinct (non-normal banded skew coupling, learned energy metric,
  dissipation separated from transport, passivity certificate testable in-suite).
Graph + grep checked: no path-signature / Hamiltonian op in repo ("signature" hits are
physics-fingerprint terminology). Convention = NM-C3/C5/C15 (torch-only, identity-at-init,
`num_parameters`, registry wiring DEFERRED). Claiming
`research/synthesis/{nilpotent_lie_scan,port_hamiltonian_mix}.py`,
`research/tests/test_{nilpotent_lie_scan,port_hamiltonian_mix}.py`. No component_fab/
edits. — Claude (Fable 5) —

**STATUS: COMPLETE** (commit `31ce9f3d`; F9+F4 committed as `ac5541dd`). Shipped
`nilpotent_lie_scan.py` (exact Chen-identity semigroup scan verified in-suite;
structural anagram discrimination: level-1 invariant under permutation, level-2
separates; zero-param mixing law) + `port_hamiltonian_mix.py` (Cayley-discretized
J−R form; contraction certificate `energy_contraction_norm() ≤ 1` verified for 5
random parameter draws; monotone energy dissipation after impulse; EXACT energy
conservation at R→0 over 512 steps; bounded at 32× length under persistent input).
16 + 12 tests; full NM-F + monarch/butterfly/ternary regression 119 passed; ruff
clean. NM-F lane now 4/9 shipped (F3, F4, F5, F9); F1/F2/F6/F7/F8 remain UNOWNED in
`tasks/nm_f_operator_families_2026-07-01.md`. — Claude (Fable 5) —

## 2026-07-01T23:14Z — codex — CLAIM + BUILD: NM-7 Clifford rotor-sandwich substrate

User asked to continue unworked open items. Claiming the component_fab-only
NM-7 gap: deeper geometric-algebra substrate beyond the existing
`clifford_knob`/`CliffordAttention` alias. Scope: add a stackable Cl(2,0)
rotor-sandwich adapter (`R x ~R`) with identity-at-init and auto-deepened
`clifford_rotor_sandwich` knob wiring. Files:
`component_fab/math_knobs.py`,
`component_fab/generator/primitive_templates/_lanes_b.py`,
`component_fab/generator/primitive_templates/__init__.py`,
`component_fab/generator/code_generator.py`, focused math-knob/codegen tests.
Avoiding active Fable `research/synthesis/*` files and dirty ECC/routing/scaling
lanes.
STATUS: COMPLETE. Added vectorized `CliffordRotorSandwichLane` plus
`CliffordRotorAdapterLane`, wired direct `op_clifford_adapter="rotor_sandwich"`
and stackable `clifford_rotor_sandwich` knob dispatch, and exposed it as an
auto-deepened Clifford sibling. Focused codegen/math-knob tests passed; ruff
clean; `make test-component_fab` green (764 passed, 1 pre-existing warning).
— codex —

## 2026-07-01T23:11Z — Claude (Fable 5) — CLAIM + BUILD: NM-F9 CDMA slot binding + NM-F4 integral-control gate

New NM-F lane (forced-structure operator families from control theory / signal processing
/ abstract algebra): 9 families scoped in `tasks/nm_f_operator_families_2026-07-01.md`.
User directive: build **F9 then F4** first.
- **NM-F9** `research/synthesis/cdma_slot_binding.py` `CDMASlotBinding`: multi-slot
  binding via fixed ±1 spreading codes (Gold preferred-pair LFSR family, near-Welch-bound
  cross-correlation; Walsh–Hadamard exact-orthogonal option). Value payload spread over
  chips, superposed into ONE running state vector by exact causal prefix sum (linear
  time); retrieval = despreading (gain = chips). Binding law has ZERO learned params;
  slot select = hard top-1 STE on code correlation; gate = sigmoid highway — NON-softmax
  throughout. Targets the field-wide multi-slot binding wall with a falsifiable
  Welch-bound interference curve. Adjacent-not-duplicative to codex M3X-C1 ECC embedding
  (codes for state multiplexing, not vocab-table compression) — graph-checked, no
  existing CDMA/spreading code in repo.
- **NM-F4** `research/synthesis/integral_control_gate.py` `IntegralControlMixer`:
  internal-model-principle integral controller — error-driven diagonal integrator
  (s ← s + k_i·g·e), zero steady-state error ⟹ holds bound state indefinitely with NO
  decay-by-parameterization (the structural fix for the p-adic scale run's retrieval
  gap); anti-windup clamp; O(D) control gains; sigmoid-highway write gate (validated
  non-twin form).
Convention = NM-C3/C5/C15 (self-contained torch-only ⟹ NM-10-measurable,
identity-at-init, `num_parameters`, registry wiring DEFERRED). Tests include
randomized-query binding control + PhysicsDescriptorProbe measurability. Claiming
`research/synthesis/{cdma_slot_binding,integral_control_gate}.py`,
`research/tests/test_{cdma_slot_binding,integral_control_gate}.py`,
`tasks/nm_f_operator_families_2026-07-01.md`. No component_fab/ edits. — Claude (Fable 5) —

**STATUS: COMPLETE** (commit `ac5541dd`). Shipped
`research/synthesis/cdma_slot_binding.py` (Gold preferred-pair LFSR + Walsh–Hadamard
code banks as fixed ±1 buffers, exclusive causal prefix-sum superposition, hard top-1
STE slot select, sigmoid-highway gate, identity-at-init, ~21K params at D=256/chips=32)
+ `research/synthesis/integral_control_gate.py` (error-driven diagonal integrator,
zero steady-state error, EXACT retention through closed gate vs EMA losing >99% over
512 distractors, anti-windup clamp proven load-bearing by ablation, O(D) control core).
15 + 10 tests incl. randomized-query binding control, Gold t(n) cross-correlation
bound, despread-interference-within-Welch-bound, and PhysicsDescriptorProbe (NM-10)
measurability. 29 passed; monarch/butterfly/ternary sibling regression 55 passed;
ruff clean. Remaining NM-F lane (F1–F3, F5–F8) scoped UNOWNED in
`tasks/nm_f_operator_families_2026-07-01.md`. — Claude (Fable 5) —

## Recent Completed Work

### Compaction / Weight-Sharing (Tier D / MiniMax-M3 Alignment)
- **GLM — NM-C7 & NM-C10 Recurrent-Depth & Persistent-Memory Refiners**: Shipped `recurrent_depth_refine.py` (Lorentzian p-adic gate, resists collapse) and `persistent_memory_refine.py` (ultrametric/tropical top-k persistent slots) + tests. Passes NM-11 measured softmax-twin detector. (Commit `d0ca0691`)
- **GLM — NM-C2 Compositional Feature-Hash (Bloom) Embedding**: Shipped `feature_hash_embedding.py` (zero V×d table; `emb(t)=⊕_k W[k,f_k(t)]` over K fixed independent bit-mixing hashes into a shared `K×h×D` codebook; params independent of vocab V — ~49× cut of the cl100k tied embedding block; hybrid anchor fallback) + 15 tests (param-independence-from-V, compositionality, hash-aperiodicity fix, collision-resistance-vs-K). Self-contained token-embedding, NO `tiny_lm.py` edit ⟹ no M3X-C1 collision (distinct from codex's single-hash `ModuloHashEmbedding` control). Registry wiring deferred. — GLM —
- **GLM — NM-C15 Ternary-Native Sign-Semiring Mixer**: Shipped `ternary_sign_mix.py` (weights ∈ {-1,0,1}, packed 2-bit VRAM optimization, rank-r factored mode) + tests. (Commit `5c5b8ec5`)
- **GLM — NM-C21 Footprint Objective**: Plumbed capability-gated non-embedding param compactness axis (`footprint`) into candidate ranking composite scoring in `ranking.py`/`capability.py` + tests. (Commit `5e45c535`)
- **GLM — NM-C5 Butterfly Mixer**: Shipped O(d log d) orthogonal-flow butterfly stage mixer `butterfly_mix.py` + tests. (Commit `8c288478`)
- **GLM — NM-C3 Monarch-Structured Mixer**: Shipped Monarch block factorized mixer `monarch_mix.py` with involution-safe identity-at-init + tests. (Commit `d94ca086`)
- **GLM — Compaction Scoping**: Scoped 22 compaction/weight-sharing lanes (NM-C1..NM-C22) in `component_fab_compaction_lanes_2026-07-01.md`. (Commit `c1df58df`)
- **codex — M3X-C1 ECC Codeword Embeddings & Promotion Wiring**: Shipped `ECCCodewordEmbedding` + output head, and wired into `TinyLM` harness + promotion probes against modulo-hash/JL low-rank controls. (Commit `a50fc095` + in-progress local updates)
- **codex — M3X-R1 Auction Capacity Router**: Shipped `AuctionCapacityRouter` (hard one-hot routing, exact capacity balance) in `routing_primitives.py` + tests. (Commit `a50fc095` + in-progress local updates)
- **codex — M3X-M2 Signed Expander Mixer**: Shipped `SignedExpanderMixerLane` with expander-gap and anti-softmax-twin verification + tests. (Commit `a50fc095`)
- **Antigravity — MiniMax-M3 Compaction/Routing/VRAM Ideation**: Formulated 26 aligned ideas in `research/notes/minimax_m3_aligned_compaction_ideas_2026-07-01.md` and implemented task backlog P0-P6 in `tasks/minimax_m3_alignment_tasks.md`.

### Proposer & Optimization
- **codex — Tsallis/Renyi dynamic proposer score-norm repair**: Added score-norm spectrum repair logic in `dynamic.py` (escapes measured softmax-basin collapse by searching non-softmax spectra like `tsallis_q`, `renyi`, and `entmax_alpha` with semiring aggregation) + tests. Focused dynamic proposer tests passed; ruff clean; `make test-component_fab` green. (Uncommitted)
- **codex — NM-7 Clifford rotor-sandwich substrate**: Added a true Cl(2,0) rotor-sandwich geometric-product adapter (`R x ~R`) distinct from `CliffordAttention`, exposed as direct Clifford dispatch and auto-deepened stackable knob. Focused codegen/math-knob tests passed; ruff clean; `make test-component_fab` green. (Uncommitted)
- **codex — WS-2 adjustable data folding search axis**: Added fold fraction/count/direction plus sparse, intermittent, vertical, and horizontal folding controls, exposed them through the data-route catalog, search axis variants, LM/autonomous enumeration, and A/B route conditions. Focused route/proposer tests passed; ruff clean; `make test-component_fab` green. (Uncommitted)
- **codex — runs.db WAL Fix**: Restored persistent WAL mode for `runs.db` by ensuring database maintenance and backfill connections correctly utilize WAL instead of DELETE journal mode. (Commit `6a9cda78`)
- **codex — CPU grading hot-path fixes**: Added native CPU C++ scans for surprise-memory (`native_postread_surprise.cpp`) and MoR refine CPU scan (`native_mor_refine.cpp`); vectorized gMQAR batch construction in `gmqar.py` (9.5x speedup, bit-identical); enabled thin LTO Rust optimization profile in `aria-scheduler` + `aria-db`. (Commits `78586853`, `a9d708fd`, `d410e76a`)

### Test Suite & Infrastructure
- **codex — test-research unblocking**: Resolved parallel worker failures by isolating simulator stubs, novelty cache fixtures, strategy briefings, and modeling strength nano-floor rows in `test_model_strength_analysis.py`.
- **codex & GLM — prior novelty/algebraic completions**: Shipped NM-10 geometric-novelty MAP-Elites behavior axis (`novelty_archive.py`), NM-11 measured `softmax_twin_score` (`algebraic_properties.py`), and NM-12 auto-deepening math knobs.

[Older completed tasks trimmed; see git history and Codex journal for full records.]

## Open Work

- **NM-9 (Exotics)**: Free-probability/random-matrix projections and TDA/persistent-homology pooling remain as lower-priority exotics.
- **MiniMax-M3 Alignment Tasks**: Implementation backlog P0-P6 in `tasks/minimax_m3_alignment_tasks.md`.

## Current Dirty / Unowned State

Do not revert, delete, or overwrite these without checking ownership:

- **Uncommitted modified files** (in-progress scale-promotion wiring, dynamic repairs, & performance adjustments):
  - `component_fab/generator/_causal_scan.py`
  - `component_fab/generator/code_generator.py`
  - `component_fab/generator/ecc_codeword_embedding.py`
  - `component_fab/generator/primitive_templates/__init__.py`
  - `component_fab/generator/primitive_templates/_lanes_b.py`
  - `component_fab/generator/routing_primitives.py`
  - `component_fab/harness/harder_binding_tasks.py`
  - `component_fab/harness/tiny_lm.py`
  - `component_fab/harness/training_probe.py`
  - `component_fab/improver/axis_variants.py`
  - `component_fab/improver/math_knob_catalog.py`
  - `component_fab/math_knobs.py`
  - `component_fab/metrics/mix_speed.py`
  - `component_fab/proposer/algebraic_properties.py`
  - `component_fab/proposer/dynamic.py`
  - `component_fab/proposer/enumeration.py`
  - `component_fab/runner/cycle.py`
  - `component_fab/tests/test_axis_variants.py`
  - `component_fab/tests/test_code_generator.py`
  - `component_fab/tests/test_dynamic_proposer.py`
  - `component_fab/tests/test_ecc_codeword_embedding.py`
  - `component_fab/tests/test_math_knob_catalog.py`
  - `component_fab/tools/run_autonomous.py`
  - `component_fab/tools/run_lm_probe.py`
  - `research/eval/binding_multislot_probe.py`
  - `research/eval/training_core.py`
  - `research/eval/utils.py`
  - `research/synthesis/butterfly_mix.py`
  - `research/synthesis/compiler_ops_attention.py`
  - `research/synthesis/compiler_ops_routing.py`
  - `research/synthesis/compiler_ops_sequence.py`
  - `research/synthesis/data_pipeline_grammar.py`
  - `research/synthesis/parametric_ops.py`
  - `research/synthesis/physics_descriptors.py`
  - `research/tests/test_binding_multislot_probe.py`
  - `research/tests/test_data_pipeline_grammar.py`
  - `research/tests/test_discovery_mixing_primitives.py`
  - `research/tests/test_novelty_distance.py`
  - `research/tools/data_route_ab.py`
- **Untracked files**:
  - `component_fab/harness/ecc_promotion.py`
  - `component_fab/harness/ecc_rare_token_probe.py`
  - `component_fab/tests/test_ecc_promotion.py`
  - `component_fab/tests/test_ecc_rare_token_probe.py`
  - `research/synthesis/cdma_slot_binding.py`
  - `research/synthesis/integral_control_gate.py`
  - `research/tests/test_cdma_slot_binding.py`
  - `research/tests/test_integral_control_gate.py`
