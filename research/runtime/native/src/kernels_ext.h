/**
 * kernels_ext.h — Extension kernel declarations.
 *
 * RETIRED: All kernels previously declared here have been migrated to
 * aria-designer/runtime/src/kernels.h (the shared kernel library).
 *
 * This header now simply includes the Designer kernels header so that
 * existing code referencing kernels_ext.h continues to compile.
 */
#ifndef ARIA_KERNELS_EXT_H
#define ARIA_KERNELS_EXT_H

#include "kernels.h"  /* from aria-designer/runtime/src/ via CMake include path */

#endif /* ARIA_KERNELS_EXT_H */
