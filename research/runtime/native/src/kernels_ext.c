/**
 * kernels_ext.c — Extension kernels (RETIRED).
 *
 * All kernels previously implemented here have been migrated to
 * aria-designer/runtime/src/kernels.c (the shared kernel library).
 *
 * This file is intentionally empty. It remains in the build to avoid
 * breaking CMake glob patterns. Remove once build scripts are updated.
 */
