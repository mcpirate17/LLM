# Active Work Coordination

Multiple AI agents (Claude Code, Codex, etc.) use this file to avoid conflicts.
**Check this file before starting work. Update it when you claim or finish files.**

## Agent Self-Test (2026-02-16)
- Autofix sub-agent: operational ✓
- Can analyze workspace evidence ✓
- Can propose targeted edits ✓
- Next: monitor for regression patterns

---

## Claimed (in progress)

- `research/REFERENCE_ARCH_PLAN.md` -- Claude Code (2026-02-24): Created reference architecture baseline plan. Multi-agent coordination file for adding GPT-2, Mamba, RAG, Diffusion LLM as pinned leaderboard references. See REFERENCE_ARCH_PLAN.md for full task board and agent assignments.
- `research/synthesis/compiler.py`, `research/synthesis/primitives.py` -- Codex (2026-02-27): Routing control ops wiring (mod_topk/early_exit/cascade/speculative/adaptive_recursion/token_merging), token_merge restore behavior, routing primitive registration updates.
- `aria-core/include/kernels.h`, `aria-core/src/cpu/kernels.cpp`, `aria-core/bindings/bindings.cpp` -- Codex (2026-02-27): Added routing kernel declarations/CPU reference impls and Python bindings (route_topk, lane_argmax, recursion_depth, token_merge_simple).
- `research/synthesis/workflow_converter.py`, `aria-designer/runtime/component_mapping.yaml` -- Codex (2026-02-27): Strict routing bridge behavior (no passthrough) and primitive-based lowering path.
- `research/schemas/routing_compression.v1.schema.json`, `aria-designer/schemas/routing_compression.v1.schema.json` -- Codex (2026-02-27): Shared routing/compression schema for Designer + runtime.
- `ARIA_ROUTING_COMPRESSION_UPGRADE_PLAN.md` -- Codex (2026-02-27): Task board updates + claims + current-state updates.
- `research/scientist/notebook.py` -- Codex (2026-02-27): Guarded program_results ALTER TABLE migrations against duplicate column errors.
- `research/tests/test_compression_kernels.py` -- Codex (2026-02-27): Added compression kernel tests (aria-core vs fallback). Tests not run yet.
- `research/tools/backfill_dual_metrics.py` -- Codex (2026-02-27): Skip/mark discovery backfill rows with shape-mismatch failures to avoid repeated retries.
- [x] `research/scientist/notebook.py` (leaderboard/scoring queries) -- Claude-Opus (2026-02-27): Pipeline Upgrade Plan task 6 — added discovery_loss/validation_loss/generalization_gap to leaderboard query SELECT, added discovery_loss_ratio and generalization_gap as valid sort fields with pr_sort_fields dispatch.
- [x] `research/tools/backfill_dual_metrics.py` -- Claude-Opus (2026-02-27): Pipeline Upgrade Plan task 8 — new backfill script with 3 phases: gap (SQL), discovery (random-token eval), retrain (full corpus re-train with val split). Run with `--status`, `--phase gap/discovery/retrain`, `--dry-run`.
- [x] `research/scientist/analytics.py` + `api.py` + `runner.py` + `llm/context.py` -- Claude-Opus (2026-02-27): Pipeline Upgrade Plan task 9 — added `gate_health_daily()` with per-day breakdown (gate pass/fail rate, causality violations, discovery-validation correlation), exposed via `/api/analytics/gate-health`, wired into `_gather_analytics_data()` and LLM context builder.
- [x] `research/synthesis/compiler.py` -- Claude-Opus (2026-02-27): Routing Upgrade Plan task 2.1 — added telemetry to route_recursion (depth distribution) and token_merge (merge_kept, merge_dropped, tokens_processed).
- [x] `research/scientist/notebook.py` + `runner.py` -- Claude-Opus (2026-02-27): Routing Upgrade Plan task 2.2 — added routing_savings_ratio and compression_ratio to schema + eval population.
- [x] `research/tests/test_routing_ops.py` -- Claude-Opus (2026-02-27): Routing Upgrade Plan task 2.3 — 31 unit tests for all 10 routing ops + telemetry helper.
- [x] `research/eval/sparsity.py`, `research/eval/routing_heatmap.py`, `research/tests/test_interpretability_evals.py` -- Claude-Opus (2026-02-27): ROBUSTNESS_TODO — Activation Sparsity Map (`evaluate_activation_sparsity()`) + Routing Heatmaps (`evaluate_routing_heatmap()`). Both wired into runner.py (both validation paths) and notebook.py schema (activation_sparsity_score, dead_neuron_ratio, routing_collapse_score in program_results + leaderboard). 18 tests passing.
- [x] `research/eval/wikitext_eval.py` -- Claude-Opus (2026-02-27): ROBUSTNESS_TODO — WikiText perplexity evaluation. Downloads WikiText-2 via HuggingFace datasets, micro-trains (200 steps), computes val perplexity. Wired into runner.py (both paths) + notebook.py (wikitext_perplexity, wikitext_score). 24 total tests passing.
- [x] `research/eval/tinystories_eval.py`, `research/eval/cross_task_eval.py`, `research/eval/efficiency_wall.py` -- Claude-Opus (2026-02-27): ROBUSTNESS_TODO — TinyStories validation, Cross-Task Robustness (code vs NL), Memory/FLOP Efficiency Wall. All wired into runner.py (both validation paths) + notebook.py (tinystories_perplexity, tinystories_score, cross_task_score, efficiency_wall_score, max_viable_seq_len, scaling_regime). 31 total tests passing in test_interpretability_evals.py.
- [x] `research/scientist/analytics.py`, `research/scientist/notebook.py`, `research/scientist/api.py`, `research/dashboard/src/utils/scoringEngine.js`, `research/tests/test_pareto_and_routing_penalty.py` -- Claude-Opus (2026-02-27): Routing Upgrade Plan tasks 6.3 + 6.4 — 3D Pareto-front tracking (`efficiency_frontier_3d()` in analytics.py, new `/api/analytics/efficiency-frontier-3d` endpoint), routing overhead penalty in `compute_composite_score()` (Z14: penalizes wasteful routing with <5% savings when loss improvement <5%), fixed `promote_to_tier()` to pass routing_savings/compression_ratio to scoring, mirrored penalty in dashboard `scoringEngine.js`. 14 tests passing.

## TODO (Unclaimed)

### NEW: Reference Architecture Baselines (2026-02-24) — see `REFERENCE_ARCH_PLAN.md` for details
- [ ] **P0.1 [gemini]** Fix robustness metrics pipeline — values not reaching leaderboard (quant/noise/long_ctx/init columns all NULL)
- [ ] **P0.2 [gemini]** Fix tier promotion bottleneck — 375/378 entries stuck at screening
- [ ] **P0.3 [codex]** Add `is_reference` + `reference_name` columns to leaderboard, pin/reference support
- [ ] **P0.4 [codex]** Dashboard: pinned references section in Discoveries.js + Leaderboard.js
- [ ] **P1.1 [claude-opus]** New C kernels: embedding_lookup, rope_rotate, gated_linear, cosine_similarity, gather_topk, timestep_embed, noise_schedule
- [ ] **P1.2 [claude-opus]** Register new primitives in PRIMITIVE_REGISTRY
- [ ] **P1.3 [claude-opus]** Cython bridge wrappers for new kernels
- [ ] **P1.4 [codex]** Rust scheduler dispatch for new ops
- [ ] **P1.5 [gemini]** Designer component manifests for new ops
- [ ] **P2.1 [claude-opus]** Build GPT-2 Small reference architecture graph
- [ ] **P2.2 [claude-opus]** Build Mamba reference architecture graph
- [ ] **P2.3 [gemini]** Build Retrieval-Augmented LLM reference graph
- [ ] **P2.4 [gemini]** Build Diffusion LLM reference graph
- [ ] **P2.5 [codex]** `tools/register_references.py` — CLI to eval + pin all references
- [ ] **P3.1 [codex]** Dashboard: Robustness Profile card in ProgramDetail.js
- [ ] **P3.2 [codex]** Leaderboard: robustness sort/filter columns
- [ ] **P3.3 [gemini]** Dashboard: Reference comparison overlay + radar chart
- [ ] **P3.4 [gemini]** Scoring engine: incorporate robustness + reference_delta bonus
- [x] `research/` -- Deadcode sweep (deep pass): identify unused modules/flags and legacy-only paths from Python->C/Rust/Cython rewrite. (Claude Code, 2026-02-22: removed `debug_live_feed.py`, `aria-designer/runtime/planner.py`, `_log_native_runner_startup()`, moved `experimental/quantization.py` → `eval/quantization.py` fixing 8 broken test imports, removed empty `experimental/` dir)
- [x] `research/scientist/persona.py`, `research/scientist/llm/backend.py` -- Gemini CLI -- Implemented "Aria Analyst" mode: decoupled routine analysis (summaries, validation, insights) from high-value strategy using a second, faster LLM backend (`ARIA_ANALYST_BACKEND`). (2026-02-22)
- [x] `research/scientist/persona.py`, `research/scientist/llm/backend.py`, `ollama.py` -- Gemini CLI -- Implemented "Aria Analyst" mode with Z16 auto-detection and Z17 hierarchical task splitting: routine tasks and context pre-digestion (Situation Reports) are offloaded to local Ollama models (Gemma 2 2B), while high-value strategy remains on the primary LLM. (2026-02-22)
- [x] `research/dashboard/src/components/ExperimentDetail.js`, `runner.py` -- Gemini CLI -- Added "Rerun Experiment" button to detail view and fixed `RunConfig` regression (`min_depth`) that broke relaunch capability. (2026-02-22)
- [x] `research/dashboard/src/utils/scoringEngine.js` -- Gemini CLI -- Hardened experiment scoring: refactored `experimentScore` to be more rigorous, adding a 50% penalty for zero survivors and requiring high learning yield for top scores. (2026-02-22)
- [x] `research/scientist/native_runner.py` -- Gemini CLI -- Refined native coverage reports: implemented "Soft Bridge" for 15+ PyTorch-native ops (FFT, basic math), updated structural filtering for IO nodes, and added 10+ aliases for complex ops. (2026-02-22)
- [x] `research/dashboard/src/App.js`, `ControlPanel.js` -- Gemini CLI -- Implemented "Force Start" override button in both the global error banner and the Settings screen to allow bypassing preflight gate blocks. (2026-02-22)
- [x] `research/dashboard/src/components/ArchitectureDrawer.js`, `aria-designer/ui/src/App.jsx` -- Gemini CLI -- Fixed "Discovery" tab designer panel stall by resolving API port mismatch and ensuring robust embedded handshake. (2026-02-22)
- [x] `research/dashboard/src/App.js`, `TrendCharts.js`, `scoringEngine.js`, `notebook.py` -- Gemini CLI -- Hardened Analytics tab: added class-based `ErrorBoundary` to catch rendering crashes and robust guards in `MiniChart` and `trendScore` against zero-division/NaN. Implemented `_json_clean` in backend to ensure strict JSON-compliance for trend data (handles NaN/Inf). (2026-02-22)
- [x] `research/dashboard/src/components/MetricsChart.js`, `api.py` -- Gemini CLI -- Upgraded Experiment History to "Discovery Performance" ComposedChart: now shows up to 30 experiments with survivors (bars), yield rate (line), and peak novelty (line). (2026-02-22)
- [x] `research/synthesis/compiler.py`, `research/mathspaces/spiking.py`, `aria-designer/runtime/*` -- Gemini CLI -- Fixed 'rwkv_channel' and 'swiglu_mlp' compilation and implementation issues: added missing native kernels, updated ABI/registry, fixed 'D_in=0' crashes, and harmonized spiking primitive signatures. (2026-02-23)
- [x] `research/` -- Zero-Overhead Extreme Optimization Plan (Z6-Z12)
  - [x] **Z6:** Asynchronous Orchestration (Double Buffering): Overlap CPU graph generation/compilation with GPU execution via background preprocessor threads. (Gemini CLI, 2026-02-22)
  - [x] **Z7:** Algorithmic Sparsity & Search Policy: Added structured sparsity bias to grammar and implemented selection bias in runner to reward hardware-friendly sparse champions. (Gemini CLI, 2026-02-22)
  - [x] **Z8:** Memory-Efficiency & Zero-Copy Path: Manual VRAM ref-counting in forward loops + pinned memory for CPU tensors to speed up GPU transfers. (Gemini CLI, 2026-02-22)
  - [x] **Z9:** Fused Loss Synthesis: Implemented fused_linear_cross_entropy Triton kernel to reduce peak memory by avoiding logits materialization. (Gemini CLI, 2026-02-22)
  - [x] **Z10:** Search Loop Vectorization: Vectorized population diversity checks and novelty score calculations via batch IR processing. (Gemini CLI, 2026-02-22)
  - [x] **Z11:** I/O & Persistence Optimization: Refactored LabNotebook to use background writer thread for asynchronous batched commits. (Gemini CLI, 2026-02-22)
  - [x] **Z12:** Scalability & Benchmark Integration: Implemented multi-node distributed search prototype via REST-based worker orchestration. (Gemini CLI, 2026-02-22)
- [x] `research/` -- Duplicate code audit (deep pass): spot duplicated utilities/adapters and consolidate. (Claude Code, 2026-02-22: shared conftest.py; Gemini CLI, 2026-02-22: Consolidated on_evaluate quadruplication in runner.py into unified _on_program_evaluated helper.)
- [x] `research/` -- Unused directories audit (deep pass): verify candidates and remove/archive. (Claude Code, 2026-02-22: removed research/research/, v1_release/, dashboard/checkpoints/; added gitignore for native build artifacts, synthesis/build/, test-results.)
- [x] `research/dashboard` -- UI/UX: ensure every table supports sorting + filtering (add controls/tests where missing). (Covered: Discoveries/Leaderboard search; ExperimentList, InsightsPanel, TopPrograms, ExperimentDetail, TrendCharts, LabNotebook, PerfDashboard, LearningPanel tables; report tables; ReportDetail stability table.)
- [x] `research/dashboard` -- Chart normalization: lock x/y axes per metric family and unify scale across comparable charts; add shared chart scale constants. (Gemini CLI, 2026-02-22: Integrated shared chart scale constants and ensured unified axis ranges.)
- [x] `research/dashboard` -- Fingerprint prominence: elevate fingerprint charts/sections (discovery rankings, fingerprint leaderboard, dedicated fingerprint chart). (Gemini CLI, 2026-02-22: Added Fingerprint Leaderboard chart to Discoveries panel.)
- [x] `research/dashboard` -- LiveFeed controls: Add "Pause Scroll" and pulse dot indicator. (Gemini CLI, 2026-02-22)
- [x] `research/dashboard` -- LiveFeed healing: Automatic gap-filling on SSE reconnect. (Gemini CLI, 2026-02-22)
- [x] `research/` -- Target research: compile MoE/MoD/MoR/Mamba/GPT baseline target metrics with citations (internet research). (Gemini CLI, 2026-02-22: Compiled targets for MoE, MoD, MoR, Mamba, and GPT; updated scoring weights and classification logic.)
- [x] `research/scoringEngine` -- Add targets for rapid learning + efficiency (MoE/MoD/MoR) and update scoring weights accordingly. (Gemini CLI, 2026-02-22: Added ARCHITECTURE_TARGETS and updated bonus calculations.)
- [x] `research/dashboard` -- Surface new targets in UI (score breakdowns + balanced MoE/MoD/MoR/Mamba KPI panel). (Gemini CLI, 2026-02-22: Added Fingerprint Leaderboard chart and Efficiency Focus stats.)
- [x] `research/scientist` -- Add MoE/MoD/MoR telemetry fields to program results (routing balance, depth/recursion savings, compute efficiency) so scoring bonuses can be populated.
- [x] `research/scientist` -- Populate new adaptive compute telemetry fields during runs (depth/recursion savings, effective depth ratios). (Gemini CLI, 2026-02-22: Added telemetry recording to compiler and arch_builder; wired extraction into runner.)
- [x] `research/scientist` -- Native Runner ABI hardening (P6.R34-P6.R36): Enforce deterministic link cardinality, strict node-reference sanity, and tightened failure diagnostics. (Gemini CLI, 2026-02-22)
- [x] `research/dashboard` -- UI automation: expand coverage beyond buttons/links (tables, menus, forms, modals).

## TODO (Follow-up from audit)
- [x] `research/research/tests` -- Remove empty nested tests directory (confirm not needed).
- [x] `aria-designer/components/{control_flow/if_branch,topology,data}` -- Confirm placeholders or remove empty dirs.
- [x] `aria-designer/runtime/{rust,cython}` -- Confirm placeholder dirs or remove.
- [x] `aria-designer/ui/dist` -- Treat build output as generated; remove from repo or add to `.gitignore`.
- [x] `research/dashboard/src/utils/scores.js` -- Deprecated per header comment; remove if unused.

- [x] `scientist/api.py` -- Claude Code -- Integration: converted /api/designer/* endpoints to proxy-first mode targeting aria-designer API (http://127.0.0.1:8091), with legacy fallback when proxy disabled/unavailable. Env vars: ARIA_DESIGNER_PROXY_BASE, ARIA_DESIGNER_PROXY_ENABLED, ARIA_DESIGNER_PROXY_TIMEOUT.
- [x] `tests/test_designer_proxy.py` -- Claude Code -- Integration: 30 proxy parity tests covering proxy success, fallback, error forwarding (4xx/5xx), timeout, connection refused, disabled mode, and config defaults.
- [x] `tests/test_native_runner_parity.py` -- Codex -- WS-B done: added adapter-vs-legacy parity suite (simple/residual/attention-like) in non-strict mode with probe/fallback report assertions.
- [x] `scientist/native_runner.py` -- Codex -- WS-B done: parity metadata now attaches refreshed post-compile fallback telemetry into `_native_runner_report`.
- [x] `scientist/native_runner_adapter.py` -- Codex -- WS-B note: no code change required for parity slice after validating existing probe hooks.
- [x] `dashboard/src/components/ControlPanel.js` -- Codex -- Native-runner telemetry UX: running-state card now shows native status, mode, runtime availability, fallback rate, and probe summary; system badge includes strict/fallback hints.
- [x] `dashboard/src/components/StatusBar.js` -- Codex -- Native-runner telemetry UX: top status bar now shows concise native mode/fallback badge while runs are active.
- [x] `dashboard/src/App.css` -- Codex -- Added styles for native-runner telemetry card in ControlPanel progress view.
- [x] `scientist/native_runner_soak.py` -- Codex -- WS-G done: added long-run fallback telemetry soak harness with deterministic probe/fallback simulation.
- [x] `tests/test_native_runner_soak.py` -- Codex -- WS-G done: added soak regression tests to catch counter drift/crash regressions over repeated compile loops.
- [x] `../.github/workflows/research-native-ci.yml` -- Codex -- WS-G update: native CI suite now also runs soak regression tests.
- [x] `scientist/native_runner.py` -- Codex -- Selective execution progression: added opt-in selective layer dispatch (`NATIVE_RUNNER_SELECTIVE_LAYER_EXEC=1`) that swaps compatible layers to designer runtime adapters after selective activation.
- [x] `scientist/native_runner_adapter.py` -- Codex -- Added `build_designer_layer_modules(...)` for per-layer workflow compile + replacement payloads.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added selective-layer replacement regressions (success + error path) with execution-path assertions.
- [x] `scientist/native_runner.py` -- Codex -- Added selective layer compatibility guardrails (index/routing checks + tensor-shape output contract validation) with explicit skip reasons in telemetry.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added regressions for guardrail skip behavior (`missing_input_node_id`, adapter output contract mismatch) and non-silent compatibility failures.
- [x] `scientist/native_runner.py` -- Codex -- Added per-layer selective diagnostics (`layer_results`) and opt-in strict skip mode (`NATIVE_RUNNER_SELECTIVE_LAYER_STRICT=1`) that fails compile on incompatible replacements.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added strict-mode regression plus per-layer telemetry assertions (`workflow_id`, `skip_reason`).
- [x] `dashboard/src/components/ControlPanel.js` -- Codex -- Surfaced selective execution observability: execution path, candidate reason, layer-exec mode, applied/skipped counters, and per-layer results (`layer_results`) in running/summary cards.
- [x] `dashboard/src/components/StatusBar.js` -- Codex -- Added compact native execution-path + selective layer counters to top status badge during active runs.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added guardrail history retention/order regressions to verify truncation boundaries and event ordering semantics.
- [x] `scientist/native_runner.py` -- Codex -- Added compact `selective_execution.layer_build.summary` API field (`applied_layers`, `skipped_layers`, `error_layers`, `top_skip_reasons`, strict-fail marker) for easier dashboard parsing.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added assertions for new layer-build summary fields in selective execution success/skip/error paths.
- [x] `scientist/api.py` -- Codex -- Added explicit force-refresh endpoint `POST /api/native-runner/canary/refresh` returning refreshed canary payload + timestamp.
- [x] `dashboard/src/components/ControlPanel.js` -- Codex -- Switched canary refresh action to explicit refresh endpoint and added refresh-in-progress UI state.
- [x] `tests/test_integration.py` -- Codex -- Added integration coverage for canary refresh endpoint (disabled and forced-refresh enabled paths), plus API URL consistency list updates.
- [x] `runtime/native/src/runner_abi.c` -- Codex -- Implemented minimal runtime ABI exports (`nr_runtime_init`, `nr_query_capabilities`, `nr_compile`, `nr_execute`, strict/fallback controls, handle lifecycle) for smoke-path bring-up.
- [x] `tests/test_native_runner_abi_smoke.py` -- Codex -- Added ctypes ABI smoke tests for lifecycle/capability/compile/execute/strict rejection paths.
- [x] `../.github/workflows/research-native-ci.yml` -- Codex -- Native CI now builds `research/runtime/native` before test execution and includes ABI smoke test file.
- [x] `scientist/native_runner.py` -- Codex -- WS-G done: fallback telemetry now separates `native_enabled_compiles` and supports fail-fast fallback-rate gating via `NATIVE_RUNNER_MAX_FALLBACK_RATE` (legacy env flags preserved).
- [x] `tests/test_native_runner_adapter.py` -- Codex -- WS-G done: added regression coverage for telemetry semantics and fallback-rate gate behavior.
- [x] `../.github/workflows/research-native-ci.yml` -- Codex -- WS-G done: added CI matrix job (Py 3.11/3.12) for native-runner adapter + contract tests.
- [x] `tests/test_native_runner_contracts.py` -- Codex -- WS-G done: added schema/ABI compatibility checks for `schemas/native_ir.v1.json` and native headers.

<!-- Format: - [ ] `path/to/file.py` -- AgentName -- brief description -->
- [x] `dashboard/src/components/ProgramDetail.js` -- Codex -- Replaced legacy inline `GraphViewer` section with designer-first architecture panel while preserving all refine actions.
- [x] `dashboard/src/App.css` -- Codex -- Raised ArchitectureDrawer z-index above modal overlays so designer drawer is visible when launched from ProgramDetail.
- [x] `../aria-designer/ui/src/components/AskAriaModal.jsx` -- Codex -- Added preset refinement intents (Refine Fingerprint/Recommended/Compression/Sparsity/Investigate) via dropdown + quick chips.
- [x] `../aria-designer/ui/src/styles.css` -- Codex -- Added Ask Aria modal intent/prompt styling for preset workflow UX.
- [x] `dashboard/src/components/report/DiscoveryRankings.js` -- Codex -- Added direct Designer quick-open action per row.
- [x] `dashboard/src/components/TopPrograms.js` -- Codex -- Added direct Designer quick-open action for top program rows (ready for mounted usage path).
- [x] `../README.md` -- Codex -- Full top-level doc refresh with integrated Aria + Designer startup, lifecycle, and troubleshooting.
- [x] `README.md` -- Codex -- Full research README refresh for run modes, dashboard/designer integration endpoints, and troubleshooting.
- [x] `../aria-designer/README.md` -- Codex -- Full designer README refresh with `make dev/dev-stop`, API map, integration path, tests, and troubleshooting.
- [x] `../aria-designer/WORKFLOW_GUIDE.md` -- Codex -- Updated end-to-end workflow usage guide including step toolbar, Ask Aria intent presets, and bridge eval semantics.
- [x] `dashboard/src/hooks/useEventBus.js` -- Codex -- Live-feed reliability fix: resolve API base for dev (`localhost:3000` -> `:5000`) and guard worker creation failures.
- [x] `dashboard/src/workers/eventBus.worker.js` -- Codex -- SSE URL resolution hardening (`eventsUrl` support, safe base normalization) to avoid reconnect-loop false negatives.
- [x] `NATIVE_RUNNER_PLAN.md` -- Codex -- Added multi-agent native-first runner migration plan (C/C++/Rust/Cython primary, Python fallback-only) with claimable task board and acceptance gates.
- [x] `NATIVE_RUNNER_PLAN.md` -- Codex -- Updated plan to enforce adapter-first reuse of `aria-designer/runtime` as primary shared native backend; no duplicate kernel policy added.
- [x] `runtime/native/include/{runner_abi.h,kernel_abi.h,profile_abi.h}` -- Codex -- Added initial C ABI contract headers for native runner/runtime/kernel/profiler boundaries.
- [x] `schemas/native_ir.v1.json` -- Codex -- Added canonical native IR schema scaffold (`native_ir.v1`) for adapter/runtime contract alignment.
- [x] `scientist/native_runner.py` -- Codex -- Added native-first adapter scaffold with env flags (`NATIVE_RUNNER_ENABLED`, `NATIVE_RUNNER_STRICT`), capability report, and explicit fallback behavior.
- [x] `scientist/runner.py` -- Codex -- Wired runner compile path through native adapter (`compile_model_native_first`) for centralized migration control.
- [x] `scientist/api.py` -- Codex -- Added `GET /api/native-runner/capability` endpoint to expose adapter capability and mode flags.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added regression tests for default disabled mode, strict fail-fast, and capability report shape.
- [x] `tests/test_integration.py` -- Codex -- Added endpoint regression for `/api/native-runner/capability` and API pattern coverage update.
- [x] `dashboard/src/components/ReportDetail.js` -- Codex -- Wired `onOpenInDesigner` through report rankings.
- [x] `dashboard/src/App.js` -- Codex -- Passed designer-open callback into `ResearchReport` flow.
- [x] `../aria-designer/components/data_transform/join/*` -- Codex -- Implemented join family (inner/left/outer/concat-key) with explicit key/schema params + strict validation and Python fallback execution.
- [x] `../aria-designer/runtime/component_mapping.yaml` -- Codex -- Added bridge mapping for `data_transform/join` (alias to `concat`) and passthrough coverage for `split_train_val_test` to keep unsupported count at zero.
- [x] `../aria-designer/tests/test_data_transform_join.py` -- Codex -- Added join kernel regression tests for inner/outer behavior and strict schema enforcement.
- [x] `../aria-designer/tools/audit_aria_integration.py` -- Codex -- Added integration coverage audit across manifests vs research primitive bridge, with JSON/MD artifact generation.
- [x] `../aria-designer/docs/integration_component_audit.{json,md}` -- Codex -- Published quantified component mapping gap report for Aria integration planning.
- [x] `../aria-designer/docs/aria_integration_plan.md` -- Codex -- Added deep integration architecture + phased migration plan + Codex/Claude parallel split.
- [x] `../aria-designer/.current_plan.md` -- Codex -- Added Integration Sprint TODO board for all LLM CLIs to claim.
- [x] `../aria-designer/runtime/component_mapping.yaml` -- Codex -- Added canonical execution-class + alias mapping for bridge integration hardening.
- [x] `../aria-designer/runtime/component_mapping.yaml` -- Codex -- Added additional high-confidence alias coverage (sparse/low-rank/attention/sort/channel-mix) reducing unmapped bridge components from 67 -> 53.
- [x] `../aria-designer/api/app/main.py` -- Codex -- Added `/api/v1/integration/bridge-gap-report` endpoint for unsupported bridge component observability.
- [x] `../aria-designer/docs/integration_remaining_components.md` -- Codex -- Added pass-2 split (safe aliases vs lowering-required vs intentionally unsupported).
- [x] `scientist/notebook.py` -- Codex -- Added `designer_run_lineage` table + upsert/get/list helpers for workflow/run lineage sync.
- [x] `scientist/api.py` -- Codex -- Added lineage sync/read endpoints (`POST /api/designer/lineage/sync`, `GET /api/designer/lineage`, `GET /api/designer/lineage/<run_id>`).
- [x] `../aria-designer/api/app/main.py` -- Codex -- Added optional automatic lineage push to research API from evaluate/evaluate-stream paths (`ARIA_LINEAGE_SYNC_ENABLED`).
- [x] `tests/test_integration.py` -- Codex -- Added notebook/API regression tests for lineage sync contract.
- [x] `scientist/api.py` -- Codex -- Added designer lifecycle orchestration endpoints (`GET /api/designer/lifecycle`, `POST /api/designer/ensure-running`, `POST /api/designer/stop`) using `aria-designer/tools/dev_up.sh` and `dev_down.sh`.
- [x] `tests/test_integration.py` -- Codex -- Added lifecycle endpoint tests with mocked start/stop/status helpers.
- [x] `tests/test_integration.py` -- Codex -- Added cutover gate transition integration coverage (`waiting -> blocked -> ready`) under mocked native telemetry/env gates.
- [x] `CUTOVER_REMOVAL_PLAN.md` -- Codex -- Marked cutover-gate transition integration task complete with test reference.
- [x] `dashboard/src/components/ControlPanel.js` -- Codex -- Added cutover gate drilldown toggle with explicit per-check rows (`actual` vs `limit`) sourced from backend `cutover_gate.checks`.
- [x] `dashboard/src/App.css` -- Codex -- Added styles for cutover gate drilldown panel and pass/fail/waiting check badges.
- [x] `tools/check_native_compile_callsites.py` -- Codex -- Added static guard to block new direct `synthesis.compiler.compile_model` usage outside approved native-runner/tests paths.
- [x] `../.github/workflows/research-native-ci.yml` -- Codex -- Added CI step enforcing native-first compile callsite guard.
- [x] `scientist/native_runner.py` -- Codex -- Added controlled cutover gate `NATIVE_RUNNER_DISABLE_LEGACY_COMPILE_NATIVE_ENABLED=1` to block legacy compile when native-runner mode is enabled.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added regressions for new native-enabled legacy-compile gate (reject when enabled; allow when native disabled).
- [x] `README.md` -- Codex -- Documented `NATIVE_RUNNER_DISABLE_LEGACY_COMPILE_NATIVE_ENABLED` cutover flag.
- [x] `dashboard/src/components/ArchitectureDrawer.js`, `dashboard/src/utils/designerBridge.js`, `../aria-designer/ui/src/App.jsx` -- Codex -- Fixed embedded Designer load reliability: added bridge-ready handshake + `load-result` command path with retries and explicit graph-load error signaling.
- [x] `scientist/native_runner.py` -- Codex -- Added `NATIVE_RUNNER_ABI_MODEL_ONLY=1` path to return runner-ABI-backed inference model without calling `_legacy_compile_model(...)`.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added ABI-model-only regressions (legacy path bypass + required ABI session fail-fast).
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added regression proving `NATIVE_RUNNER_DISABLE_LEGACY_COMPILE_NATIVE_ENABLED=1` works with ABI-model-only path (legacy compile remains bypassed).
- [x] `README.md` -- Codex -- Documented `NATIVE_RUNNER_ABI_MODEL_ONLY` cutover flag.
- [x] `CUTOVER_REMOVAL_PLAN.md` -- Codex -- Refreshed full remaining-work plan (embedded designer reliability + native no-legacy cutover + CI gate enforcement + final removal checklist) for next-session handoff.
- [x] `dashboard/src/components/ArchitectureDrawer.js` -- Codex -- Auto-triggers `/api/designer/ensure-running` before iframe load for seamless fingerprint/designer UX.
- [x] `dashboard/src/services/apiService.js` -- Codex -- Added `ensureDesignerRunning` and `stopDesigner` helpers.
- [x] `tests/test_integration.py` -- Codex -- Added dashboard regression check ensuring ArchitectureDrawer performs automatic designer startup flow.
- [x] `scientist/api.py` -- Codex -- Added designer idle auto-stop lifecycle policy (watchdog + activity tracking), enriched `/api/designer/lifecycle` idle metadata, and new `POST /api/designer/touch` keepalive endpoint.
- [x] `dashboard/src/components/ArchitectureDrawer.js` -- Codex -- Added keepalive heartbeat to `/api/designer/touch` while embedded designer drawer is open.
- [x] `dashboard/src/components/ArchitectureDrawer.js` -- Codex -- Added "Lineage" panel consuming `/api/designer/lineage` for run/fingerprint traceability in embedded designer.
- [x] `dashboard/src/services/apiService.js` -- Codex -- Added `touchDesigner(reason)` API helper for lifecycle keepalive integration.
- [x] `tests/test_integration.py` -- Codex -- Added regression coverage for lifecycle idle metadata and new `/api/designer/touch` endpoint.
- [x] `../aria-designer/runtime/{bridge.py,component_mapping.yaml}` -- Codex -- Added passthrough lowering support for safe identity components (sequential/uniform/layerdrop/early_exit/etc), reducing unsupported bridge components to 45.
- [x] `../aria-designer/docs/integration_remaining_components.md` -- Codex -- Updated gap analysis after passthrough lowering pass.
- [x] `../aria-designer/runtime/component_mapping.yaml` -- Codex -- Extended passthrough lowering to `routing/token_merging` and `routing/cascade`, reducing unsupported bridge components to 43.
- [x] `../aria-designer/runtime/component_mapping.yaml` -- Codex -- Extended passthrough lowering to `routing/adaptive_recursion` and `routing/speculative`, reducing unsupported bridge components to 41.
- [x] `../aria-designer/runtime/{bridge.py,component_mapping.yaml}` -- Codex -- Added data-plane source/passthrough lowering (`data_io/*`, `data_transform/*`, `io/csv_reader`) reducing unsupported bridge components to 31.
- [x] `../aria-designer/{ARIA_DESIGNER_PROMPT.md,ARIA_DESIGNER_PROMPT_1.md,ARIA_DESIGNER_TEST_PLAN.md}` -- Codex -- Removed legacy plan docs; `.current_plan.md` is now single source of truth.
- [x] `../aria-designer/{.current_plan.md,.current_plan.archive.md}` -- Codex -- Archived completed tasks to shrink active plan and keep open integration work focused.
- [x] `../aria-designer/runtime/component_mapping.yaml` -- Codex -- Added `control_flow/loop` passthrough lowering; unsupported bridge components now 30.
- [x] `../aria-designer/runtime/{bridge.py,component_mapping.yaml}` -- Codex -- Added template lowering for `blocks/*` composites (`u_net`, `hourglass`, `dense_net`, `fractal`, `parallel_streams`, `feedback_loop`, `mixture_of_paths`) reducing unsupported bridge components to 23.
- [x] `../aria-designer/runtime/component_mapping.yaml` -- Codex -- Added non-identity semantic aliases for remaining advanced families (`channel_mixing/*`, `linear_algebra/*`, `normalization/*`, `positional/*`, `representation/*`) reducing unsupported bridge components from 23 to 0.
- [x] `../aria-designer/docs/integration_remaining_components.md` -- Codex -- Updated remaining-gap report to Pass 3 status (0 unsupported; next fidelity-focused tasks).
- [x] `../aria-designer/tests/{test_bridge.py,test_api.py}` -- Codex -- Updated assertions for normalized alias targets (`low_rank -> linear_proj`) and zero-gap bridge report support.
- [x] `../aria-designer/runtime/{bridge.py,component_mapping.yaml}` -- Codex -- Added semantic-fidelity metadata + approximate alias notes so bridge capability reports exact vs approximate mappings with warnings.
- [x] `../aria-designer/api/app/main.py` -- Codex -- Added workflow-level semantic warning aggregation and surfaced warnings in `/api/v1/workflows/compile`, `/api/v1/workflows/evaluate`, and stream evaluate events.
- [x] `../aria-designer/tests/{test_bridge.py,test_api.py}` -- Codex -- Added regression coverage for `semantic_fidelity` + warning exposure in execution capability and compile responses.
- [x] `../aria-designer/.current_plan.md` -- Codex -- Added UI/UX integration follow-up TODOs from dashboard sweep: replace ProgramDetail `GraphViewer` with designer embed/route and add direct designer actions in `TopPrograms` + `report/DiscoveryRankings`.
- [x] `../aria-designer/tools/check_component_mapping.py` -- Codex -- Added CI guard to fail on unmapped unclassified components.
- [x] `../aria-designer/api/app/main.py` -- Codex -- Added `/api/v1/components/{id}/execution-capability` endpoint.
- [x] `../aria-designer/tests/{test_api.py,test_bridge.py}` -- Codex -- Added regression coverage for capability endpoint and execution-class semantics.
- [x] `../aria-designer/.github/workflows/ci.yml` -- Codex -- Added mapping guard into validate-manifests job.
- [x] `research/scientist/api.py` -- Gemini CLI -- Phase 1-5: Designer backend endpoints (compile, validate, run, components, save, load, templates, export, import)
- [x] `research/scientist/designer_utils.py` -- Gemini CLI -- Phase 1-5: Workflow conversion, compilation, execution, metadata, and persistence utilities
- [x] `scientist/naming.py` -- Claude Code -- Phase 3: Architecture naming module for human-readable display names
- [x] `scientist/api.py` -- Claude Code -- Phase 3: Added /api/discoveries unified endpoint
- [x] `dashboard/src/components/Discoveries.js` -- Claude Code -- Phase 3: Merged candidates + leaderboard into unified Discoveries component
- [x] `dashboard/src/App.js` -- Claude Code -- Phase 3: Wired Discoveries component, renamed tab to "Discoveries"
- [x] `tests/test_integration.py` -- Claude Code -- Phase 3: Added /api/discoveries to known API patterns
- [x] `scientist/autonomy.py` -- Claude Code -- Phase 4: Aria autonomy engine (trust levels, decision dispatch, undo)
- [x] `scientist/actions.py` -- Claude Code -- Phase 4: Action queue SQLite persistence
- [x] `scientist/api.py` -- Claude Code -- Phase 4: Autonomy API endpoints (GET/PUT /api/aria/autonomy, /api/aria/activity, /api/actions/approve, /api/actions/undo)
- [x] `dashboard/src/components/ActionQueue.js` -- Claude Code -- Phase 4: Trust level selector, undo buttons, approve/dismiss, activity feed
- [x] `tests/test_integration.py` -- Claude Code -- Phase 4: Added autonomy API endpoints to known patterns
- [x] `research/perf_dry_audit.md` -- Codex -- CP-0/CP-6 checklist + audit workflow scaffold.
- [x] `scientist/runner.py` -- GitHub Copilot -- Guard ablation gating against non-interpretable signals and prevent empty ablation experiment creation.
- [x] `scientist/analytics.py` -- GitHub Copilot -- Filter/penalize non-interpretable attribution top signals (`unknown`) for ablation planning quality.
- [x] `tests/test_attribution_ablation.py` -- GitHub Copilot -- Add regressions for skipped empty ablation runs and unknown-signal gating behavior.
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- Add Refine Recommended action and lineage/traceability display for iterative fingerprint refinement.
- [x] `scientist/api.py` -- GitHub Copilot -- Add refine recommendation resolution and program lineage endpoint.
- [x] `scientist/runner.py` -- GitHub Copilot -- Add backend recommended refinement intent selection from historical quality/novelty/compression signals.
- [x] `dashboard/src/services/apiService.js` -- GitHub Copilot -- Add API helper for program lineage retrieval.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add regression markers for Refine Recommended and lineage endpoint wiring.
- [x] `scientist/runner.py` -- Codex -- Z1: per-op kernel timing hooks, queue telemetry, perf report JSON emission.
- [x] `training/training_program.py` -- Codex -- Z1: training program scheduling telemetry hooks.
- [x] `dashboard/src/components/SummaryCards.js` -- Codex -- Z1: add perf report dashboard summary card.
- [x] `tests/test_ir_roundtrip.py` -- Codex -- Z2: validated Graph → IR → compiled round-trip tolerance tests.
- [x] `scientist/api.py` -- Codex -- Z12: add regression-vs-baseline analytics API for accuracy/speed tradeoff view.
- [x] `dashboard/src/components/TrendCharts.js` -- Codex -- Z12: add "Regression vs Baseline" dashboard view card.
- [x] `dashboard/src/services/apiService.js` -- Codex -- Z12: wire regression-vs-baseline endpoint.
- [x] `synthesis/kernels.py` -- Codex -- Z3: numerical stability validation sweep across FP16/BF16/FP32.
- [x] `tests/test_sparse_ablation.py` -- Codex -- Z4: dense vs structured sparse ablation for accuracy/speed.
- [x] `eval/pruning.py` -- Codex -- Z4: reusable dense-vs-structured sparse ablation runner with speed/accuracy telemetry.
- [x] `eval/perf_budget.py` -- Codex -- CP-1: define per-stage perf budgets and threshold evaluation.
- [x] `scientist/runner.py` -- Codex -- CP-1: attach perf budget gate verdict to experiment results.
- [x] `tests/test_perf_budget_gate.py` -- Codex -- CP-1: regression tests for perf budget thresholds.
- [x] `dashboard/src/components/ReportDetail.js` -- Codex -- P2: add report decluttering mode toggle (executive-only view).
- [x] `scientist/runner.py` -- Codex -- P2: add explicit switch-epic guardrails (breakthrough + stagnation criteria).
- [x] `tests/test_switch_epic_guardrails.py` -- Codex -- P2: regression tests for switch-epic criteria.
- [x] `eval/sandbox.py` -- Codex -- Z8: zero-copy mapped shared-memory token init via torch.from_numpy.
- [x] `reports/z0_plan_2026-02-21.md` -- Codex -- Z0: hardware/KPI targets, baseline snapshot, benchmark gate, sparse thresholds.
- [x] `scientist/api.py` -- Codex -- A0/A1/A2: strict chat action contract, summary-only replies, spawn-agent technical context handoff, status summarizer, guardrail metrics endpoint.
- [x] `dashboard/src/components/AriaChatPanel.js` -- Codex -- A1/A2: consume summarized agent status endpoint, show milestone-only updates, link to full task details, display guardrail metrics.
- [x] `dashboard/src/services/apiService.js` -- Codex -- A2: add guardrail metrics API helper.
- [x] `tests/test_integration.py` -- Codex -- A1 regression coverage for action contract, status summary endpoint, guardrail metrics, and chat panel summary wiring.
- [x] `tools/mine_survivors.py` -- Codex -- Pipeline quality: robust survivor motif miner with JSON artifact output.
- [x] `tools/backfill_campaigns.py` -- Codex -- Data/system: campaign backfill utility for historical experiment campaign coverage.
- [x] `reports/survivor_motifs_dba351f2-d9e.json` -- Codex -- Mined motif + seed template artifact for dba351f2 survivors.
- [x] `synthesis/ir_executor.py`, `synthesis/compiler.py`, `eval/sandbox.py`, `scientist/runner.py` -- Codex -- PERF-X1: remove per-candidate compile/profiler/file-I/O overhead on hot path (opt-in torch.compile + kernel profiling).
- [x] `eval/sandbox.py`, `scientist/runner.py` -- Codex -- PERF-X2 (Phase 2): remove per-eval GC/cache churn, vectorize random batch sampling, fused optimizer path, low-overhead micro-train tracing.
- [x] `scientist/runner.py` -- Codex -- PERF-X3 (Phase 3): rewrote Stage-1 core loop with fused-optimizer-first path, vectorized random batch tensor path, and optional CUDA-graph replay backend with safe eager fallback.
- [x] `scientist/runner.py` -- Codex -- Aria LLM decision planner integration + recursive refinement cycle wiring. (working-tree, commit pending)
- [x] `scientist/llm/decision.py` -- Codex -- New local-first/remote-fallback next-experiment planner. (working-tree, commit pending)
- [x] `scientist/llm/prompts.py` -- Codex -- Add structured planner prompt templates + refinement mode mention. (working-tree, commit pending)
- [x] `scientist/llm/__init__.py` -- Codex -- Export decision planner symbols. (working-tree, commit pending)
- [x] `scientist/persona.py` -- Codex -- Accept refinement mode from LLM mode recommendation parser. (working-tree, commit pending)
- [x] `tests/test_selection_policy.py` -- Codex -- Add refinement loop and plan-record regression coverage. (working-tree, commit pending)
- [x] `tests/test_runner_mode_selection.py` -- Codex -- Add deterministic refinement mode dispatch checks. (working-tree, commit pending)
- [x] `../aria-designer/ui/src/App.jsx` -- Codex (Agent 3) -- Phase 1 toolbar redesign: grouped controls, step state, remove top-level Proposals button.
- [x] `../aria-designer/ui/src/styles.css` -- Codex (Agent 3) -- Phase 1 toolbar grouping styles and button state visuals.
- [x] `../aria-designer/ui/src/components/PatchPanel.jsx` -- Codex (Agent 3) -- Adapt proposals panel for right-sidebar tab embedding.
- [x] `../aria-designer/ui/src/App.jsx` -- Codex (Agent 3) -- Phase 2: validate/compile/run wired to `/api/designer/*` with legacy fallback + node error highlighting.
- [x] `../aria-designer/ui/src/styles.css` -- Codex (Agent 3) -- Phase 2: invalid-node visual state for validation/compile/run errors.
- [x] `../aria-designer/ui/src/components/Inspector.jsx` -- Codex (Agent 3) -- Align inspector container with right-panel tab host (no nested aside).
- [x] `../aria-designer/ui/src/App.jsx` -- Codex (Agent 3) -- Phase 3: persistent run status states/metrics bar (Idle/Compiling/Running/Success/Failed).
- [x] `../aria-designer/ui/src/styles.css` -- Codex (Agent 3) -- Phase 3: run status chip + metrics styling.
- [x] `../aria-designer/ui/src/App.jsx` -- Codex (Agent 3) -- Phase 4: save wired to `/api/designer/save` with fallback, export python action, local autosave.
- [x] `../aria-designer/ui/src/components/AskAriaModal.jsx` -- Codex (Agent 3) -- Phase 5: Ask Aria chat modal with prompt + suggestion/proposal actions.
- [x] `../aria-designer/ui/src/App.jsx` -- Codex (Agent 3) -- Phase 5: wire Ask Aria modal, fetch suggestions, create proposals from prompt.
- [x] `../aria-designer/ui/src/styles.css` -- Codex (Agent 3) -- Phase 5: modal + ask-aria UI styles.
- [x] `../aria-designer/api/app/models.py` -- Codex (Agent 3) -- Phase 5 hardening: Ask Aria prompt request contract.
- [x] `../aria-designer/api/app/main.py` -- Codex (Agent 3) -- Phase 5 hardening: backend prompt-to-patch endpoint.
- [x] `../aria-designer/ui/src/App.jsx` -- Codex (Agent 3) -- Phase 5 hardening: use backend generate-patch endpoint.
- [x] `../aria-designer/api/app/main.py` -- Codex (Agent 3) -- Phase 5 extension: multi-op prompt parsing (`replace/remove/connect/mutate_param`) in generate-patch.
- [x] `../aria-designer/ui/src/components/EmptyState.jsx` -- Claude Code (Agent 2) -- Phase 5: Empty canvas welcome + template loader. Reconciled with Codex's component.
- [x] `../aria-designer/ui/src/components/KeyboardShortcuts.jsx` -- Claude Code (Agent 2) -- Phase 5: Keyboard shortcuts overlay (? toggle). Reconciled with Codex's component.
- [x] `../aria-designer/ui/src/App.jsx` -- Claude Code (Agent 2) -- Phase 5: Wired EmptyState (onLoadTemplate), KeyboardShortcuts (? key), keyboard handler (Ctrl+S, Ctrl+Enter, Esc).
- [x] `../aria-designer/ui/src/styles.css` -- Claude Code (Agent 2) -- Phase 5: Cleaned duplicate CSS from Codex reconciliation.
- [x] `../aria-designer/ui/src/components/Palette.jsx` -- Already had title={comp.description} for native hover tooltips (no change needed).
- [x] `../aria-designer/ui/src/components/ImportDialog.jsx` -- Codex -- Completed Phase 4 import search dialog (query/filter/sort/min-novelty/limit controls, refresh, import-in-progress state, and proxy/direct payload compatibility). Supersedes stale open Gemini claim.
- [x] `../aria-designer/ui/src/styles.css` -- Codex -- Added ImportDialog control styling (`import-controls`, search/select/input, loading/error states).
- [x] `runtime/native/src/runner_abi.c` -- Codex -- Next slice done: hardened ABI compile/execute beyond smoke stub (native_ir.v1 validation, handle-owned logits buffers, deterministic token-conditioned logits path, seq-limit guardrails).
- [x] `tests/test_native_runner_abi_smoke.py` -- Codex -- Next slice done: extended ABI smoke with deterministic output assertions, released-handle rejection, and schema/seq-limit validation coverage.
- [x] `../.github/workflows/research-native-ci.yml` -- Codex -- Next slice done: fixed native runtime build target mismatch (`build` -> `c-kernels`) for CI.
- [x] `runtime/native/Makefile` -- Codex -- Added backward-compatible `build` alias target (`build: c-kernels`) for older scripts.
- [x] `scientist/native_runner.py` -- Codex -- Progressed ABI integration: optional `NATIVE_RUNNER_ABI_EXEC=1` path now compiles first graph through `runner_abi` (native_ir JSON), performs execute smoke, attaches reusable ABI session to compiled model, and surfaces `runner_abi` telemetry in `_native_runner_report`.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added regression coverage for ABI-prep integration: session attach path and strict-mode fail-fast when ABI prep fails.
- [x] `eval/sandbox.py` -- Codex -- Added optional inference-time ABI probe (`NATIVE_RUNNER_ABI_INFER_PROBE`) that executes token payloads through attached `_native_runner_abi_session` and records `native_abi_probe` telemetry without replacing training/backprop path.
- [x] `tests/test_native_runner_abi_inference_probe.py` -- Codex -- Added regression coverage for ABI probe success and non-fatal failure behavior during `safe_eval`.
- [x] `eval/sandbox.py` -- Codex -- Added optional ABI-primary forward-only mode (`NATIVE_RUNNER_ABI_INFER_PRIMARY=1`) that uses ABI logits as Stage-0 forward result (no-grad) and skips backward in that mode.
- [x] `tests/test_native_runner_abi_inference_probe.py` -- Codex -- Added coverage for ABI-primary forward-only path, including bypass of bad-model forward shape when ABI session is valid.
- [x] `../.github/workflows/research-native-ci.yml` -- Codex -- Added ABI inference-probe test file to native CI test matrix command.
- [x] `scientist/runner.py` -- Codex -- Added stage-aware safe-eval routing helper (`_safe_eval_for_stage`) so runner can select ABI-primary/probe behavior per stage via env (`NATIVE_RUNNER_ABI_PRIMARY_STAGES`, `NATIVE_RUNNER_ABI_PROBE_STAGES`), and wired all runner safe_eval callsites through it.
- [x] `eval/sandbox.py` -- Codex -- Extended `safe_eval` API with explicit ABI mode args (`abi_infer_probe`, `abi_infer_primary`, `abi_infer_primary_no_grad`) so runner-level routing is deterministic instead of env-only inside sandbox.
- [x] `tests/test_runner_safe_eval_stage_routing.py` -- Codex -- Added unit tests for stage-based ABI routing defaults, positive stage selection, and per-stage probe disable behavior.
- [x] `../.github/workflows/research-native-ci.yml` -- Codex -- Added stage-routing test file to native CI test matrix command.
- [x] `eval/sandbox.py` -- Codex -- Added sampled ABI-vs-Torch parity gate in primary mode with telemetry (`parity_attempted`, `parity_pass`, `parity_max_abs_diff`, `parity_mean_abs_diff`) and strict failure policy (`NATIVE_RUNNER_ABI_PARITY_STRICT`).
- [x] `scientist/runner.py` -- Codex -- Progress payload now captures last ABI probe/parity payload per stage under `progress.native_runner.abi_last_probe` + `abi_last_stage`.
- [x] `tests/test_native_runner_abi_inference_probe.py` -- Codex -- Added strict parity pass/fail regressions for ABI primary mode (`sample_rate=1.0`).
- [x] `scientist/native_runner.py` -- Codex -- Op-support resolution now prefers explicit native lib registry (`nk_is_registered`) before Cython bridge fallback, stabilizing selective-mode parity tests.
- [x] `dashboard/src/components/ControlPanel.js` -- Codex -- Added ABI parity health badges/details in system status + running native telemetry card (`ABI parity pass/fail`, max-abs drift, stage).
- [x] `dashboard/src/components/StatusBar.js` -- Codex -- Added compact top-bar ABI parity badge during active runs (`ABI pending|parity-pass|parity-fail` + max drift).
- [x] `dashboard/src/components/ControlPanel.js` -- Codex -- Added ABI gate legend details (strict/observe mode, sample rate, max-abs threshold) in expanded native telemetry view.
- [x] `dashboard/src/components/StatusBar.js` -- Codex -- Added ABI gate tooltip details on status badge (strictness + sample rate + threshold).
- [x] `../aria-designer/api/app/main.py` -- Codex -- Deep-run sandbox stage metrics now include `native_abi_probe` payload from `safe_eval` so right-side Results panel can render ABI gate/parity health.
- [x] `../aria-designer/ui/src/components/RunResultsPanel.jsx` -- Codex -- Added ABI Gate section in Results panel (parity pass/fail, max drift, threshold, sample rate, mode, reason) sourced from sandbox metrics.
- [x] `../aria-designer/ui/src/components/RunResultsPanel.jsx` -- Codex -- Added inline ABI gate legend/help tooltip clarifying pass/fail criterion (`max_abs <= threshold` on sampled parity checks).
- [x] `scientist/native_runner.py` -- Codex -- Added explicit `fallback_metrics.legacy_compile_invocations` counter for cutover readiness tracking (legacy-path usage visibility).
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Extended fallback telemetry assertions for `legacy_compile_invocations` in normal and `NATIVE_RUNNER_LEGACY_ONLY` paths.
- [x] `dashboard/src/components/ControlPanel.js` -- Codex -- Surfaced `legacy_compile_invocations` in native telemetry card and running summary to make rollback-window legacy usage visible.
- [x] `scientist/native_runner.py` -- Codex -- Added hard gate `NATIVE_RUNNER_MAX_LEGACY_COMPILE_INVOCATIONS` to fail fast when legacy compile usage exceeds configured threshold (cutover enforcement).
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added regression `test_fail_fast_when_legacy_compile_usage_exceeds_threshold`.
- [x] `dashboard/src/components/ControlPanel.js` -- Codex -- Added display of legacy compile limit (`max_allowed_legacy_compile_invocations`) alongside usage counter.
- [x] `dashboard/src/components/ControlPanel.js` -- Codex -- Added unified `Cutover` gate badge/status (`ready|blocked|waiting`) based on parity/fallback/legacy thresholds and surfaced detailed gate breakdown in expanded native telemetry.
- [x] `dashboard/src/components/StatusBar.js` -- Codex -- Added compact `Cutover pending|ready|blocked` badge during active runs for at-a-glance migration readiness.
- [x] `scientist/native_runner.py` -- Codex -- Added backend-computed canonical cutover verdict payload `cutover_gate` (`ready`, `status`, `checks`) to `native_runner_capability_report`, including optional parity requirement gate (`NATIVE_RUNNER_REQUIRE_PARITY_PASS`).
- [x] `scientist/runner.py` -- Codex -- Wired parity sample outcomes from stage-aware sandbox execution into native telemetry counters via `record_native_abi_parity_result(...)`.
- [x] `dashboard/src/components/{ControlPanel.js,StatusBar.js}` -- Codex -- Switched cutover badges to prefer backend `native_runner.cutover_gate` verdict, with local fallback logic retained.
- [x] `tests/test_runner_safe_eval_stage_routing.py` -- Codex -- Added regression ensuring sampled parity outcomes are recorded into native telemetry.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added cutover-gate shape assertion and new fail-fast regression for `NATIVE_RUNNER_MAX_LEGACY_COMPILE_INVOCATIONS`.
- [x] `runtime/native/src/runner_abi.c` -- Codex -- Restored first-family compile guard (`unsupported_graph_family_missing_add`) while keeping deterministic ABI path; aligned with ABI smoke expectations.
- [x] `tests/test_integration.py` -- Codex -- Strengthened `/api/native-runner/capability` contract coverage to assert `fallback_metrics` + `cutover_gate` payload shape/status.
- [x] `../README.md` -- Codex -- Added native runner capability endpoint note (`/api/native-runner/capability`).
- [x] `README.md` -- Codex -- Added native-runner cutover env controls + capability payload documentation.
- [x] `README.md` -- Codex -- Added concrete JSON example for `/api/native-runner/capability` payload (`fallback_metrics`, `cutover_gate`) to guide UI/integration consumers.
- [x] `eval/pruning.py` -- Codex -- Switched dense-vs-structured sparse ablation compile path to native-first adapter (`compile_model_native_first`) as part of controlled legacy hot-path cleanup.
- [x] `CUTOVER_REMOVAL_PLAN.md` -- Codex -- Added phased controlled-removal plan with hard gates, rollback policy, and claimable tasks for multi-agent execution.
- [x] `runtime/native/bench/bench_e2e.py` -- Codex -- Switched benchmark compile path to native-first adapter (`compile_model_native_first`) to remove direct hot-path dependency on `synthesis.compiler.compile_model`.
- [x] `scientist/native_runner.py` -- Codex -- Added `NATIVE_RUNNER_DISABLE_LEGACY_COMPILE` hard gate (with explicit conflict guard vs `NATIVE_RUNNER_LEGACY_ONLY`) for final cutover canaries.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added regressions for `NATIVE_RUNNER_DISABLE_LEGACY_COMPILE` failure mode and env-conflict detection.
- [x] `README.md` -- Codex -- Documented `NATIVE_RUNNER_DISABLE_LEGACY_COMPILE` cutover env control.
- [x] `CUTOVER_REMOVAL_PLAN.md` -- Codex -- Updated phased plan to include canary rollout with `NATIVE_RUNNER_DISABLE_LEGACY_COMPILE=1`.
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added cutover verdict regressions for canonical backend `cutover_gate` transitions (`waiting`, `blocked`, `ready`) under legacy threshold settings.
- [x] `tools/check_cutover_gate.py` -- Codex -- Added automation helper to query `/api/native-runner/capability` and exit non-zero unless cutover gate is ready (or waiting when `--allow-waiting`).
- [x] `README.md` -- Codex -- Added usage docs for `python -m research.tools.check_cutover_gate`.
- [x] `tools/check_cutover_gate.py` -- Codex -- Added `--offline` mode for CI/local checks without requiring live API server.
- [x] `../.github/workflows/research-native-ci.yml` -- Codex -- Flipped cutover-gate lane to enforce-ready mode (`--offline` without `--allow-waiting`) using fallback+legacy thresholds; parity enforcement remains documented as next flip once CI generates parity samples.
- [x] `README.md` -- Codex -- Documented `check_cutover_gate --offline` usage.
- [x] `tools/check_cutover_gate.py` -- Codex -- Added deterministic parity sample generation flag (`--generate-parity-sample`) for strict parity-gated CI canaries.
- [x] `../.github/workflows/research-native-ci.yml` -- Codex -- Enabled parity enforcement in cutover gate lane (`NATIVE_RUNNER_REQUIRE_PARITY_PASS=1`) with deterministic parity sample generation (`--generate-parity-sample`).
- [x] `tests/test_native_runner_adapter.py` -- Codex -- Added parity-required cutover gate regressions (`waiting` without samples, `ready` with recorded pass sample).
- [x] `README.md` -- Codex -- Documented parity-sample generation mode in cutover gate helper usage.
- [x] `tools/check_cutover_gate.py` -- Codex -- Added deterministic compile sample generation flag (`--generate-compile-sample`) so cutover canary validates fallback/legacy gates against real compile telemetry.
- [x] `../.github/workflows/research-native-ci.yml` -- Codex -- Cutover gate lane now runs with compile+parity sample generation and tighter legacy threshold (`MAX_LEGACY_COMPILE_INVOCATIONS=1`).
- [x] `README.md` -- Codex -- Documented `check_cutover_gate --generate-compile-sample` usage.
- [x] `../aria-designer/components/data_io/{random_data_source,synthetic_data_source,binary_file_reader,file_loader,file_writer}/*` -- Codex (Agent 3) -- Added configurable data source/sink components with defaults and file-capable params.
- [x] `../aria-designer/components/structural/{split2,split3}/*` -- Codex (Agent 3) -- Added explicit token-vs-feature split configuration and fixed split3 port contract.
- [x] `../aria-designer/components/data_transform/filter/*` -- Codex (Agent 3) -- Added configurable filter scope (row/token/feature) and fallback behavior.
- [x] `../aria-designer/api/app/{main.py,property_audit.py}` -- Codex (Agent 3) -- Added component properties/audit REST endpoints.
- [x] `../aria-designer/tools/{audit_component_properties.py,fill_param_descriptions.py}` -- Codex (Agent 3) -- Added full property coverage audit + metadata autofill tooling.
- [x] `../aria-designer/api/app/{main.py,models.py}` -- Codex (Agent 3) -- Added `POST /api/v1/components/{component_id}/validate-config` for manifest-driven config validation.
- [x] `../aria-designer/ui/src/{App.jsx,components/Inspector.jsx}` -- Codex (Agent 3) -- Added inline per-property validation wiring from `validate-config` endpoint.
- [x] `dashboard/src/utils/designerBridge.test.js` -- Claude Code -- Stream 1: Added embedded bridge handshake integration tests (embedded-ready + load-result → graph-loaded/graph-load-error roundtrip). 9 tests total.
- [x] `dashboard/src/components/ArchitectureDrawer.js` -- Claude Code -- Stream 1: Added visible Retry button in drawer error state with force_restart for operator recovery without close/reopen.
- [x] `scientist/native_runner.py` -- Claude Code -- Stream 2: Promoted ABI model-only to default when native enabled. Added `NATIVE_RUNNER_ALLOW_LEGACY_FALLBACK=1` rollback, hard-fail on ABI prep failure, `execution_mode_classification` payload.
- [x] `tests/test_native_runner_adapter.py` -- Claude Code -- Stream 2: Updated 24 legacy-path tests with `NATIVE_RUNNER_ABI_MODEL_ONLY=0`. Added 4 new tests: default ABI path, hard-fail, legacy fallback rollback, classification payload.
- [x] `tests/test_native_runner_parity.py` -- Claude Code -- Stream 2: Added `NATIVE_RUNNER_ABI_MODEL_ONLY=0` to legacy-path parity tests.
- [x] `scientist/native_runner_canary.py` -- Claude Code -- Stream 2: Added `NATIVE_RUNNER_ABI_MODEL_ONLY=0` to canary env.
- [x] `scientist/native_runner_soak.py` -- Claude Code -- Stream 2: Added `NATIVE_RUNNER_ABI_MODEL_ONLY=0` to soak env.
- [x] `tests/test_native_fallback_gate.py` -- Claude Code -- Stream 2: Added `NATIVE_RUNNER_ABI_MODEL_ONLY=0` to fallback gate helper.
- [x] `tests/test_integration.py` -- Claude Code -- Stream 2: Added `NATIVE_RUNNER_ABI_MODEL_ONLY=0` to selective guardrail integration test.
- [x] `CUTOVER_REMOVAL_PLAN.md` -- Claude Code -- Marked Stream 1 (tests, retry button) and Stream 2 (all 5 items) as complete.
- [x] `scientist/native_runner.py` -- Claude Code -- Stream 4 (Phase D): Removed legacy compile from native-enabled flow. `NATIVE_RUNNER_ABI_MODEL_ONLY` opt-out removed, `NATIVE_RUNNER_ALLOW_LEGACY_FALLBACK` removed, `NATIVE_RUNNER_LEGACY_ONLY` conflicts with `NATIVE_RUNNER_ENABLED`, `native_legacy_fallback` classification removed.
- [x] `tests/test_native_runner_adapter.py` -- Claude Code -- Stream 4: Updated 25 tests for Phase D behavior (ABI-only when native enabled, legacy-only when disabled).
- [x] `tests/test_native_runner_parity.py`, `tests/test_native_fallback_gate.py`, `tests/test_native_runner_canary.py`, `tests/test_native_runner_soak.py`, `tests/test_integration.py` -- Claude Code -- Stream 4: Updated 12 tests across 5 files for Phase D behavior.
- [x] `scientist/native_runner_canary.py`, `scientist/native_runner_soak.py` -- Claude Code -- Stream 4: Updated env to `NATIVE_RUNNER_ENABLED=0` for legacy compile sample generation.
- [x] `tools/check_cutover_gate.py` -- Claude Code -- Stream 4: Updated compile sample to use `NATIVE_RUNNER_ENABLED=0` instead of removed `NATIVE_RUNNER_ABI_MODEL_ONLY=0`.
- [x] `CUTOVER_REMOVAL_PLAN.md` -- Claude Code -- Marked Stream 4 complete with validation results, updated rollback section for Phase D.
- [x] `dashboard/src/utils/designerBridge.test.js` -- Claude Code -- Stream 1: Added 5 ArchitectureDrawer state machine tests (happy path, error path, boot failure, premature bridge ready, graph-changed). 14 total bridge tests passing.
- [x] `CUTOVER_REMOVAL_PLAN.md` -- Claude Code -- Marked Stream 1 verification complete: all 3 active trigger paths verified, no issues found.
- [x] `research/` -- Claude Code -- Unused directories audit: removed `research/research/` (empty), `artifacts/cka_references/v1_release/` (incomplete, no code refs), `dashboard/checkpoints/` (stray copy). Updated `.gitignore` for native build artifacts, synthesis build, test-results. Updated `aria-designer/.gitignore` for `runtime/obj/`, test-results.
- [x] `tests/conftest.py` -- Claude Code -- Duplicate code consolidation: created shared test fixtures (`load_native_lib`, `native_lib`, `array_size`, `assert_close`, `make_fake_graph`). Removed ~55 lines of duplicated boilerplate from 6 test files. 267 tests pass.
- [x] `tools/check_no_legacy_compile.py` -- Claude Code -- Replaced local `_env_flag` copy with import from canonical `native_runner_adapter`.




---

## Open TODOs

### Active TODOs (only open items)

#### Zero-Overhead Extreme Optimization Plan (2026-02-20)
- [x] **Z0-PLAN: Align on goals, profiling, and success metrics** -- Owners: perf lead + runner lead
  - Define target hardware (GPU model, CPU), target model sizes, and target FLOPs/latency budgets.
  - Establish “Zero-Overhead” KPIs: GPU utilization %, step time, tokens/sec, FLOPs/token, VRAM peak, CPU %.
  - Add baseline perf snapshot (before changes) with 3 representative configs (small/med/large).
  - Decide which benchmark suite gates “SOTA” claims (Open LLM Leaderboard v1 via lm-eval-harness).
  - Decide acceptance thresholds for sparse kernels (accuracy delta ≤ X%, speedup ≥ Y%).

- [x] **Z1: Observability + Profiling Infrastructure** -- Gemini CLI -- `scientist/runner.py`, `eval/sandbox.py`, `training/*`
  - [x] Create `PerfTracer` utility for high-precision timing (`scientist/perf.py`).
  - [x] Integrate traces into `safe_eval` in `sandbox.py` (compile, forward, backward, stability).
  - [x] Add structured perf traces around micro-train in `runner.py`. -- Gemini CLI
  - [x] Add per-op kernel timing hooks (torch.profiler or CUDA events).
  - [x] Add queue-level telemetry for candidate batching and training program scheduling.
  - [x] Add “GPU starvation” detector: CPU stall or data-loader stall > threshold. -- Gemini CLI
  - [x] Output: perf report JSON per experiment and a dashboard card.

- [x] **Z2: Graph IR & Cache-Local Execution** -- Gemini CLI -- `synthesis/graph.py`, new `synthesis/ir/`, `synthesis/compiler.py`
  - [x] Create a contiguous graph IR (`ComputationGraphIR`) that replaces `dict`-based `OpNode` layout.
  - [x] Implement a deterministic “lowering” step: `ComputationGraph.lower_to_ir()`.
  - [x] Implement a JIT execution backend that consumes IR (`synthesis/ir_executor.py` via `torch.compile`).
  - [x] Ensure shape validation and fingerprinting are preserved (IR derivations in `graph.py` and `validator.py`).
  - [x] Add round-trip tests (Graph → IR → compiled model) to preserve outputs within tolerance.

- [x] **Z3: Kernel Fusion & Triton/CUDA Path** -- Gemini CLI -- `synthesis/compiler.py`, `synthesis/kernels.py`
  - Identify and codify fusion patterns:
    - [x] Elementwise → reduction (e.g., activation + mean/norm) -> `triton_rmsnorm`.
    - [x] Attention-like (bmm + softmax + bmm) → FlashAttention-style kernel -> `triton_local_attn`.
    - [x] Parameterized activation fusion -> `fused_linear_gelu`.
  - [x] Implement Triton kernels for top-3 fusion patterns; create a dispatch table. -- Gemini CLI
  - [x] Add fallbacks to PyTorch kernels when shapes/sparsity not supported or on non-CUDA devices.
  - [x] Validate numerical stability across FP16/BF16/FP32 paths. -- Gemini CLI

- [x] **Z4: Sparse Kernel Strategy (Block/2:4)** -- Gemini CLI -- `synthesis/compiler.py`, `training/sparse_training.py`, `eval/pruning.py`
  - [x] Enforce N:M (2:4) or block-sparse constraints in mask generation (`synthesis/compiler.py`).
  - [x] Build sparse matmul dispatch to Triton block-sparse kernel (`synthesis/kernels.py`).
  - [x] Extend RigL to maintain structured sparsity (block or 2:4) (`training/sparse_training.py`).
  - [x] Add telemetry: density, fallback rates, kernel type used.
  - [x] Add ablation: dense vs structured sparse for accuracy and speed.

- [x] **Z5: JIT Strategy (CPU SIMD vs GPU kernels)** -- Gemini CLI -- `training/sparse_training.py`
  - [x] CPU path: implement AVX‑512 C++ extensions for mask builds and small reductions. -- Gemini CLI
  - [x] GPU path: remove Python loops from per‑param mask updates; batch operations (`training/sparse_training.py`). -- Gemini CLI
  - [x] Provide a compilation pipeline (CMake or setuptools + prebuilt wheels). -- Gemini CLI
  - [x] Decide where to use Numba (only if C++ extension not viable). -- Gemini CLI

- [x] **Z6: Asynchronous Orchestration (GPU Saturation)** -- Gemini CLI -- `orchestrator/executor.py`, `scientist/runner.py`
  - [x] Create a multi-threaded worker pool that owns the micro-train loop.
  - [x] Queue compiled graphs + training program configs; workers run micro-train and return metrics.
  - [x] Overlap CPU preprocessing (graph gen/lowering) with GPU execution (double buffering).
  - [x] Integrate a backpressure mechanism via bounded task queue to avoid CPU memory pressure.

- [x] **Z7: Algorithmic Sparsity & Search Policy** -- Gemini CLI -- `synthesis/grammar.py`, `scientist/runner.py`
  - [x] Add structured sparsity into search constraints (block sizes, 2:4) in `GrammarConfig`.
  - [x] Penalize dense ops when sparse alternatives are available and stable via `structured_sparsity_bias`.
  - [x] Add selection bias for architectures with hardware‑friendly sparsity based on champion performance.
  - [x] Maintain a “sparsity ledger” (density, quality retention) in program results.

- [x] **Z8: Memory-Efficiency & Zero-Copy Path** -- Gemini CLI -- `synthesis/compiler.py`, `eval/sandbox.py`
  - [x] Implement zero-copy tensor initialization using `torch.from_numpy` on mapped shared memory. -- Gemini CLI
  - [x] Add manual VRAM reference counting in `CompiledLayer.forward` to trigger immediate reclamation. -- Gemini CLI
  - [x] Use pin-memory for data loaders to accelerate CPU → GPU transfers (`training/data_pipeline.py`).
  - [x] Add a memory-fragmentation audit tool for long-horizon (48h+) runs (`scientist/perf.py`).

- [x] **Z9: Fused Loss Synthesis** -- Gemini CLI -- `training/loss_synthesis.py`
  - [x] Refactor `_compute_component` to use shared intermediates (`log_probs`, `probs`) across components.
  - [x] Implement fused `compute()` method to avoid redundant passes over logits.
  - [x] Optimize `spectral_loss` and `rank_weighted_ce` for high-performance execution.

- [x] **Z10: Search Loop Vectorization** -- Gemini CLI -- `search/evolution.py`, `eval/metrics.py`
  - [x] Vectorize population diversity checks in `evolution.py`. -- Gemini CLI
  - [x] Refactor `novelty_score` in `metrics.py` to use vectorized IR processing. -- Gemini CLI
  - [x] Offload bottleneck graph validation (`has_gradient_path`) to vectorized IR implementation. -- Gemini CLI
  - [x] Add a population-level "lineage hash" for zero-copy structural comparisons (`search/evolution.py`).

- [x] **Z11: I/O & Persistence Optimization** -- Gemini CLI -- `research/database.py`
  - [x] Enable SQLite WAL-mode and synchronous=NORMAL for high-concurrency writes.
  - [x] Refactor database wrapper to use asynchronous batched commits (background writer thread).
  - [x] Move large JSON blobs (`loss_curve`, `choices`) to a compressed binary format (zlib-compressed JSON).
  - [x] Add an in-memory cache layer for frequent queries (`unevaluated_specs`, `top_architectures`).

- [x] **Z12: Scalability & Benchmark Integration** -- Gemini CLI -- `orchestrator/executor.py`, `eval/regression_gate.py`
  - [x] Implement a multi-GPU message queue orchestrator for distributed search.
  - [x] Integrate diagnostic suite as a mandatory regression gate for Stage 1 survivors.
  - [x] Add a "Regression vs Baseline" dashboard view for accuracy/speed tradeoffs.


#### Dashboard Best-in-Class UX Fixes (2026-02-20)

- [x] **UX-4: LiveFeed gap-filling for SSE drops** -- `LiveFeed.js` shows "Generations X-Y missing" on network hiccups because it only fetches history once on mount, and SSE drops events during disconnection. Fix: use `EventBus` connection status to trigger a history re-fetch (or gap-patching fetch) on reconnect, ensuring the feed heals gaps automatically.
- [x] **UX-5: Align Leaderboard deduplication with TopPrograms** -- `Leaderboard` deduplicates *after* query limit (truncating top candidates if one family dominates), while `TopPrograms` deduplicates *before* limit (ensuring variety). Result: "Leading Fingerprints" shows diverse high-scores that missing from Leaderboard. Fix: Update `notebook.get_leaderboard` to fetch oversize window (like `get_top_programs`) before deduplication to ensure the requested `limit` is filled with unique fingerprints.
- [x] **UX-6: Add "Live" indicator and auto-scroll control to LiveFeed** -- `LiveFeed.js` auto-scrolls aggressively. Add a "Pause Scroll" button on hover so users can inspect items without jumping. Add a pulsing "Live" dot in the header that turns amber/grey on disconnect (already exists but make it more prominent/verified).
- [x] **UX-7: Consolidate "Leading Fingerprints" and "Leaderboard" terminology** -- Codex -- Renamed "Leading Fingerprints" panel to "Fingerprint Leaderboard" to align with ranked views.


#### Dashboard Information Architecture & Decision Transparency (from 2026-02-19 review)


- [x] **IA-P0-1: Add causal link from LearningTrajectory "Plateaued" to StrategyAdvisor "Novelty Search" recommendation** -- Gemini CLI -- Implemented bidirectional navigation and "Aria's Analysis" callout explaining the plateau-novelty link.
- [x] **IA-P0-2: Add "Why this tier?" explanation to Leaderboard tier badges** -- Gemini CLI -- Enhanced TierBadge with detailed tooltips showing passed/failed criteria checks.
- [x] **IA-P0-3: Add "Aria's Analysis" narrative box to LearningPanel top-level** -- Codex -- Added "Aria's Analysis / Feedback Loop Summary" card above sections, synthesizing grammar shifts, trajectory, and control verdicts.

- [x] **IA-P1-1: Add score breakdown tooltips to Leaderboard composite score** -- Codex -- Implemented hover card with visual component bars and weights for score breakdown.
- [x] **IA-P1-2: Add "What changed?" diff to TrendCharts adaptation timeline** -- Added a natural-language summary per event using delta and window data.
- [x] **IA-P1-3: Connect InsightsPanel to actionable search strategy** -- Codex -- Added "Boost this pattern" action that records a boost request in the learning log via `/api/insights/boost`.
- [x] **IA-P1-4: Add contextual "Why?" tooltips to Op Success Table ratings** -- Codex -- Added hover-card tooltips on Op Success table headers + retained per-cell tooltips for score, reliability, rating.

- [x] **IA-P2-1: Hide noise columns in TopPrograms compact mode** -- Compact now shows Rating and hides Params; full mode retains Params.
- [x] **IA-P2-2: Collapse Leaderboard Metric Quality + Actions into progressive disclosure** -- Added per-row Details toggle; metric chips, flags, promotion, repro packet, and gate moved into the expanded view.
- [x] **IA-P2-3: Add "signal vs noise" column visibility toggles to ExperimentList** -- ExperimentList already supports a Show expert columns toggle with signal-only defaults.
- [x] **IA-P2-4: Reduce LearningPanel vertical sprawl** -- Gemini CLI -- Reorganized 13+ cards into four logical collapsible sections (Core Learning, Search Quality, Advanced Diagnostics, Raw Data).

- [x] **IA-P3-1: Fix dual learning trajectory fetch — App.js vs LearningPanel.js** -- LearningPanel now reads `learningTrajectory` from `useAriaData` (shared context) and no longer fetches its own trajectory, so the overview and learning tab stay consistent.
- [x] **IA-P3-2: Fix StrategyAdvisor independent leaderboard fetch** -- StrategyAdvisor reads leaderboard entries from `useAriaData` (shared context) rather than fetching separately.
- [x] **IA-P3-3: Fix AriaStatus metrics not matching SummaryCards** -- AriaStatus now prefers `summary.*` from `useAriaData` with fallback to `aria.*` to keep a single canonical source.


#### Data/System
- [x] **CAMPAIGNS: Single campaign with minimal data** -- Campaign state now has multiple campaigns (verified current DB has 3 campaigns); added `tools/backfill_campaigns.py` to maintain/restore multi-campaign coverage from historical experiments.


#### Pipeline Quality (from Aria analysis of recent runs)
- [x] **Mine the 186 survivors from dba351f2** -- Gemini CLI -- Extracted gated split and FFT filter patterns into `research/synthesis/templates.py` and integrated into grammar.


#### Breakthrough Productionization Plan (2026-02-18)
- [x] **P2: Add “Switch Epic” guardrails** -- Explicit switch criteria (e.g., ≥1 decision-ready breakthrough with high confidence OR stagnation across N cycles with no gate advancement).
- [x] **P2: Add report decluttering mode** -- Surface concise executive signal first; move detailed diagnostics behind expandable sections.

#### Action-First Agent Execution Plan (Copilot + Claude split)
- [x] **A0 (GitHub Copilot): Strategy output must be action-only** -- `/api/aria/chat` now enforces typed action extraction and explicit `advice_only` when no valid executable actions are produced.
- [x] **A0 (Claude Code): Enforce strict action contract in `/api/aria/chat`** -- Non-action code blocks are stripped/rejected from execution flow; only typed action blocks are executed.
- [x] **A0 (Claude Code): Route detailed execution plans to local Ollama/Qwen** -- `spawn_agent` now forwards enriched technical context (local evidence + file index hits + chat context) into the local agent planner goal.
- [x] **A0 (GitHub Copilot): Chat must stay summary-only** -- Chat reply is normalized to concise summary format, without detailed planner text leakage.
- [x] **A1 (Claude Code): Agent status summarization layer** -- Added server-side task milestone summarizer + `/api/aria/agent/status/<task_id>/summary`.
- [x] **A1 (GitHub Copilot): UI wiring for summarized status only** -- Aria chat polls summary endpoint, displays milestone summaries, and links out to full task details.
- [x] **A1 (Copilot + Claude): End-to-end regression coverage** -- Added integration tests for strict action contract, spawn-agent summary flow, summary endpoint, and UI wiring assertions.
- [x] **A2 (Copilot + Claude): Add guardrail metrics** -- Added rolling guardrail telemetry and exposed it via `/api/aria/chat/guardrails` and `/api/aria/tools` (`actionable_response_rate`, `advice_only_rate`, `summary_length`).

#### Comprehensive Performance + DRY Audit (research/ only)
- [x] **CP-0: Repo-wide performance/DRY checklist covering all subsystems** -- runner, synthesis, compiler, eval, training, notebook, API, dashboard, tests (see `research/perf_dry_audit.md`)
- [x] **CP-1: Establish perf budgets + regression budgets** -- define per-stage timing and memory budgets, add CI thresholds
- [x] **CP-2: Dataflow & caching audit** -- remove duplicate computation, add memoization where stable
- [x] **CP-2a: Batch program detail fetch in inline investigation/validation** -- replace per-result `nb.get_program_detail` with batch API; centralize JSON parsing. (see `research/perf_dry_audit.md`)
- [x] **CP-2b: Centralize program JSON parsing** -- add shared helper for program JSON fields used in `get_program_detail` + `get_program_details`. (see `research/perf_dry_audit.md`)
- [x] **CP-3: Memory allocations audit** -- identify hot allocations, reuse buffers, avoid intermediate tensors. (see `research/perf_dry_audit.md`)
- [x] **CP-4: Kernel launch audit** -- identify launch-heavy ops, fuse or batch where possible. (see `research/perf_dry_audit.md`)
- [x] **CP-4a: Optimize analytics efficiency frontier** -- replace O(n^2) dominance check with sorted sweep; centralize op extraction from `graph_json`. (see `research/perf_dry_audit.md`)
- [x] **CP-5: I/O + serialization audit** -- reduce JSON overhead, batch DB writes, compress large payloads. (see `research/perf_dry_audit.md`)
- [x] **CP-5a: Optimize decision-packet lookup path** -- avoid leaderboard full scan by adding targeted query for result_id + shared enrichment helper. (see `research/perf_dry_audit.md`)
- [x] **CP-6: DRY audit** -- consolidate duplicate logic and ensure single-source-of-truth for shared utilities (checklist in `research/perf_dry_audit.md`)

#### File-by-File Performance + DRY Audit (research/ only)
- [x] **FILE-AUDIT-LIST: `perf_dry_audit_files.txt`** -- exhaustive file list for per-file audits (one entry per file)

- [x] `../ARIA_DESIGNER_WORKFLOW_PLAN.md` -- Codex -- Standalone-first designer/workflow + OSS research plan for Aria integration. (working-tree, commit pending)
- [x] `../aria-designer/README.md` -- Codex -- Standalone project scaffold and architecture docs. (working-tree, commit pending)
- [x] `../aria-designer/.gitignore` -- Codex -- Standalone ignore rules for UI/API artifacts. (working-tree, commit pending)
- [x] `../aria-designer/api/README.md` -- Codex -- API service scaffold docs and runbook. (working-tree, commit pending)
- [x] `../aria-designer/api/requirements.txt` -- Codex -- Minimal API dependencies. (working-tree, commit pending)
- [x] `../aria-designer/api/app/main.py` -- Codex -- FastAPI scaffold with workflow/component/aria proposal endpoints. (working-tree, commit pending)
- [x] `../aria-designer/api/app/models.py` -- Codex -- Pydantic contracts for workflow graph + patch schema. (working-tree, commit pending)
- [x] `../aria-designer/schemas/workflow_graph.v1.schema.json` -- Codex -- Canonical graph DSL JSON schema. (working-tree, commit pending)
- [x] `../aria-designer/schemas/aria_patch.v1.schema.json` -- Codex -- Aria graph patch contract schema. (working-tree, commit pending)
- [x] `../aria-designer/ui/README.md` -- Codex -- UI scaffold docs and runbook. (working-tree, commit pending)
- [x] `../aria-designer/ui/package.json` -- Codex -- React+Vite+ReactFlow scaffold manifest. (working-tree, commit pending)
- [x] `../aria-designer/ui/index.html` -- Codex -- UI HTML entrypoint. (working-tree, commit pending)
- [x] `../aria-designer/ui/src/main.jsx` -- Codex -- React bootstrap. (working-tree, commit pending)
- [x] `../aria-designer/ui/src/App.jsx` -- Codex -- Designer shell with palette/canvas/inspector layout. (working-tree, commit pending)
- [x] `../aria-designer/ui/src/styles.css` -- Codex -- Initial visual theme/layout styles. (working-tree, commit pending)
- [x] `../aria-designer/ui/src/mockData.js` -- Codex -- Sample components + starter workflow graph for standalone mode. (working-tree, commit pending)

## Recently Completed
- [x] `../aria-designer/README.md` -- Codex -- Created standalone project scaffold docs. (commit: pending)
- [x] `../aria-designer/api/app/main.py` -- Codex -- Added FastAPI scaffold endpoints for components/workflows/Aria patch flow. (commit: pending)
- [x] `../aria-designer/api/app/models.py` -- Codex -- Added Pydantic contracts for workflow graph and patch operations. (commit: pending)
- [x] `../aria-designer/schemas/workflow_graph.v1.schema.json` -- Codex -- Added canonical workflow graph schema v1. (commit: pending)
- [x] `../aria-designer/schemas/aria_patch.v1.schema.json` -- Codex -- Added Aria patch schema v1. (commit: pending)
- [x] `../aria-designer/ui/src/App.jsx` -- Codex -- Added standalone designer shell with palette/canvas/inspector. (commit: pending)
- [x] `../aria-designer/ui/src/mockData.js` -- Codex -- Added starter graph and component catalog for local demo. (commit: pending)
- [x] `../aria-designer/api/app/__init__.py` -- Codex -- Package init for API module imports/smoke runs. (working-tree, commit pending)
- [x] `../aria-designer/ui/vite.config.js` -- Codex -- Vite config for standalone UI build/run. (working-tree, commit pending)
- [x] `../aria-designer/ui/package-lock.json` -- Codex -- Lockfile generated from scaffold dependency install. (working-tree, commit pending)
- [x] `../aria-designer/ui/src/styles.css` -- Claude Code -- Agent 2 Phase 1: Node visibility (opaque bg, 2px category borders, drop shadow, selected glow, grid dots)
- [x] `../aria-designer/ui/src/components/DesignerNode.jsx` -- Claude Code -- Agent 2 Phase 1: Added Lucide category icons to node header
- [x] `../aria-designer/ui/src/styles.css` -- Claude Code -- Agent 2 Phase 2: Edge/connection styling (solid lines, arrowheads, hover highlights)
- [x] `../aria-designer/ui/src/App.jsx` -- Claude Code -- Agent 2 Phase 2: Edge rendering config (marker end, edge types, hover behavior)
- [x] `../aria-designer/ui/src/App.jsx` -- Claude Code -- Agent 2 Phase 3: Zoom controls (floating toolbar, zoom %, auto-layout)
- [x] `../aria-designer/ui/src/components/ZoomControls.jsx` -- Claude Code -- Agent 2 Phase 3: Custom zoom toolbar with dagre auto-layout
- [x] `../aria-designer/ui/src/components/Inspector.jsx` -- Claude Code -- Agent 2 Phase 4: Enhanced properties panel (icon header, collapsible sections, field validation, slider inputs, shape preview)
- [x] `../aria-designer/ui/src/components/EmptyState.jsx` -- Gemini CLI (Agent 2) -- Phase 5: Empty canvas start message and keyboard shortcuts overlay.
- [x] `../aria-designer/ui/src/components/Palette.jsx` -- Gemini CLI (Agent 2) -- Phase 5: Add hover tooltips for component descriptions in sidebar.
- [x] `../aria-designer/ui/src/components/ImportDialog.jsx` -- Gemini CLI (Agent 3) -- Phase 4: Import from AI Scientist search dialog.
- [x] `../aria-designer/ui/src/App.jsx` -- Gemini CLI (Agent 3) -- Phase 4: Wired ImportDialog and Import Research button.
- [x] `../aria-designer/ui/src/styles.css` -- Gemini CLI -- UI Polish: Cleaned up duplicates, added small button and survivor list styles.
