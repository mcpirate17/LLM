# Active Work Coordination

Multiple AI agents (Claude Code, Codex, etc.) use this file to avoid conflicts.
**Check this file before starting work. Update it when you claim or finish files.**

## Claimed (in progress)

<!-- Format: - [ ] `path/to/file.py` -- AgentName -- brief description -->

## Recently Completed

<!-- Format: - [x] `path/to/file.py` -- AgentName -- brief description (commit hash) -->
- [x] `scientist/analytics.py` -- GitHub Copilot -- #9 change-point follow-up: add `stage1_transition_timing` and `primary_change_point_timing` trajectory features to `experiment_clusters()` and cluster diagnostics (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- #9 change-point regression update extending `test_experiment_clusters_include_trajectory_features` for new feature keys/value ranges (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for #9 change-point clustering follow-up and `126/126` full-suite evidence (uncommitted)
- [x] `scientist/analytics.py` -- GitHub Copilot -- #9 trajectory-shape follow-up: add `outcome_peak_timing` and `recovery_lag` features to experiment clustering diagnostics with no-recovery handling (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- #9 trajectory-shape regression update extending `test_experiment_clusters_include_trajectory_features` (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for #9 trajectory-shape clustering follow-up and `126/126` full-suite evidence (uncommitted)
- [x] `scientist/analytics.py` -- GitHub Copilot -- #9 trajectory follow-up: add temporal trend features (`stage1_momentum`, novelty/loss momentum, outcome volatility) to `experiment_clusters()` and cluster diagnostics (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- #9 trajectory regression `test_experiment_clusters_include_trajectory_features` for matched-aggregate/opposite-trend separation (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for #9 trajectory-level clustering follow-up and `126/126` full-suite evidence (uncommitted)
- [x] `scientist/analytics.py` -- GitHub Copilot -- #9 follow-up: add failure-signature feature enrichment (compile/train/stage1 fail rates + error diversity) to `experiment_clusters()` and include cluster-level diagnostics (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- #9 follow-up regression `test_experiment_clusters_include_failure_signature_features` (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for #9 failure-signature clustering follow-up and `125/125` full-suite evidence (uncommitted)
- [x] `scientist/analytics.py` -- GitHub Copilot -- #9 deeper: upgrade `experiment_clusters()` with deterministic multi-k model selection (silhouette/imbalance scoring) and consensus-aware stability diagnostics (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- #9 deeper regression `test_experiment_clusters_model_selection_and_consensus` (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for #9 deeper clustering implementation and `124/124` full-suite validation evidence (uncommitted)
- [x] `morphological_box.py` -- GitHub Copilot -- #20 optimize tag incompatibility checks via tag->source indexing (removes repeated cross-dimension set intersections) (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- #20 regression `test_tag_incompatibility_detection_via_option_map_patch` to preserve incompatibility behavior (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for #20 implementation and `123/123` integration validation evidence (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- #22 regression `test_program_results_experiment_index_exists` to guarantee index on `program_results(experiment_id)` (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for #22 verification closure and `122/122` integration validation evidence (uncommitted)
- [x] `scientist/analytics.py` -- GitHub Copilot -- optimize `top_op_combinations()` parse path with regex fast extraction + per-row cache + robust JSON fallback (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- add regression `test_top_op_combinations_handles_malformed_graph_json` (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for #21 implementation and `121/121` integration validation evidence (uncommitted)
- [x] `training/checkpointing.py` -- Claude Code -- Track D new CheckpointManager with atomic save/load/cleanup (uncommitted)
- [x] `scientist/runner.py` -- Claude Code -- Track D checkpoint/resume wiring in continuous, investigation, validation threads + start_resume() + RunConfig fields (uncommitted)
- [x] `scientist/notebook.py` -- Claude Code -- Track D get_resumable_experiment() helper (uncommitted)
- [x] `__main__.py` -- Claude Code -- Track D --resume CLI flag (uncommitted)
- [x] `scientist/persona.py` -- GitHub Copilot -- #10 publication-grade breakthrough evidence assessor and stricter announcement labeling (`publication_grade|provisional|underspecified`) (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- #10 regression tests for breakthrough evidence classification and provisional announcement language (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for #10 implementation and `120/120` integration validation evidence (uncommitted)
- [x] `scientist/runner.py` -- GitHub Copilot -- add Track-C fixed routing benchmark harness (`run_routing_benchmark`) comparing routing strategies on identical seed/data budgets with frontier metrics (uncommitted)
- [x] `__main__.py` -- GitHub Copilot -- add `--mode=routing-benchmark` CLI entrypoint and result summary output for Track-C harness (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- add Track-C regression asserting >=3 routing modes compared and frontier metric schema present (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for Track-C implementation and validation evidence (`117/117` full integration) (uncommitted)
- [x] `training/data_pipeline.py` -- GitHub Copilot -- add tiny corpus loader abstraction + tokenizer adapter interface (TXT/JSONL, byte/whitespace tokenization) for corpus-mode batches (uncommitted)
- [x] `scientist/runner.py` -- GitHub Copilot -- add RunConfig corpus controls and deterministic data-source switch (`random|corpus`) across training/sensitivity/OOD loops with safe fallback (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- add Track-B regressions for corpus config round-trip and corpus-mode fallback behavior (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for Track-B corpus/tokenizer MVP implementation and test evidence (uncommitted)
- [x] `scientist/notebook.py` -- GitHub Copilot -- add routing-health telemetry columns to `program_results` schema migration map (uncommitted)
- [x] `scientist/analytics.py` -- GitHub Copilot -- add `routing_health()` aggregation over persisted routing metrics with structured-empty fallback (uncommitted)
- [x] `scientist/api.py` -- GitHub Copilot -- expose `/api/analytics/routing-health` analytics endpoint (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- add routing-health API regression and dashboard route allowlist coverage (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync update for Track-A routing telemetry completion and full-suite validation evidence (uncommitted)
- [x] `synthesis/primitives.py` -- Claude Code -- added 7 new token routing primitives (selective_scan, conv1d_seq, topk_gate, local_window_attn, sliding_window_mask, multi_head_mix, token_pool_restore) (uncommitted)
- [x] `synthesis/compiler.py` -- Claude Code -- added param init and execution for 7 new primitives (uncommitted)
- [x] `synthesis/grammar.py` -- Claude Code -- added config generation (window_size, n_heads) and shape compat checks for new primitives (uncommitted)
- [x] `eval/flops.py` -- Claude Code -- added FLOP estimation cases for 7 new primitives (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- added frontier-LLM glaring-miss scan and execution-ready Phase-1 routing benchmark checklist (uncommitted)
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- added single experiment-clusters card in existing Learning panel with one fetch path (no duplicate tab/component) (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- updated dashboard API URL allowlist for `/api/analytics/experiment-clusters` and validated contracts (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- synced non-duplication audit + dashboard clustering consumer test evidence (uncommitted)
- [x] `scientist/analytics.py` -- GitHub Copilot -- add deterministic experiment clustering summary with stability score (priority #9 follow-up) (uncommitted)
- [x] `scientist/api.py` -- GitHub Copilot -- expose clustering via `/api/analytics/experiment-clusters` and include in `/api/report` payload (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- add clustering endpoint/report contract tests and validate full suite (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- sync updates for clustering implementation/test evidence (uncommitted)
- [x] `eval/baseline.py` -- GitHub Copilot -- complete #48 by adding optimizer/weight-decay/momentum/beta-aware baseline recipe parity and cache keys (uncommitted)
- [x] `scientist/runner.py` -- GitHub Copilot -- complete #48 by propagating candidate optimizer metadata + recipe-aware baseline compare wiring across screening/validation/scale-up (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- expand #48 regression to assert recipe-parity wiring and optimizer metadata capture (uncommitted)
- [x] `scientist/runner.py` -- Claude Code -- fix #57 hyperparameter sensitivity check (±2x LR, ±2x steps) in validation phase (uncommitted)
- [x] `scientist/runner.py` -- Claude Code -- fix #54 OOD robustness testing with reference training recipes (SGD, AdamW variants) in validation phase (uncommitted)
- [x] `scientist/persona.py` -- Claude Code -- fix Fix A LLM context key mismatch (total_s1_passed → stage1_survivors) (uncommitted)
- [x] `scientist/analytics.py` -- Claude Code -- fix #41 control_experiment_comparison() method with z-test (uncommitted)
- [x] `scientist/api.py` -- Claude Code -- expose control comparison and holdout validation in grammar-weights + report endpoints (uncommitted)
- [x] `scientist/persona.py` -- Claude Code -- add control experiment analysis section to reports, fix grammar_weights nested dict handling (uncommitted)
- [x] `scientist/runner.py` -- Claude Code -- add logging to critical-path bare excepts (math spaces, hypothesis validation, campaign/hypothesis linking) (uncommitted)
- [x] `eval/fingerprint.py` -- Claude Code -- fix #28/#43 add CKA heuristic caveat documentation (uncommitted)
- [x] `eval/fingerprint.py` -- Claude Code -- fix #33 add logging to all bare except blocks (uncommitted)
- [x] `search/novelty_search.py` -- Claude Code -- fix #33 add logging to bare except (uncommitted)
- [x] `scientist/notebook.py` -- Claude Code -- fix #49/#50 normalized composite scoring with hard std threshold (uncommitted)
- [x] `scientist/analytics.py` -- Claude Code -- fix #53 holdout_validation() method for grammar weight eval (uncommitted)
- [x] `tests/test_integration.py` -- Claude Code -- updated composite score test for normalized scoring (uncommitted)
- [x] `search/novelty_search.py` -- Claude Code -- fix #27 reservoir sampling for archive eviction (uncommitted)
- [x] `synthesis/primitives.py` -- Claude Code -- fix #32 outer_product description, #35 safe_eval_formula (uncommitted)
- [x] `synthesis/grammar.py` -- Claude Code -- fix #35 use safe_eval_formula (uncommitted)
- [x] `synthesis/graph.py` -- Claude Code -- fix #35 use safe_eval_formula (uncommitted)
- [x] `eval/baseline.py` -- Claude Code -- fix #24 device in cache key + #47 multi-seed baseline (uncommitted)
- [x] `synthesis/compiler.py` -- Claude Code -- fix #25 guard weight access (uncommitted)
- [x] `eval/flops.py` -- Claude Code -- fix #31 use config out_dim for FLOP estimates (uncommitted)
- [x] `eval/metrics.py` -- Claude Code -- fix #29/#45 multiplicative novelty bonus (uncommitted)
- [x] `eval/fingerprint.py` -- Claude Code -- fix #44 increase probes from 8 to 32 (uncommitted)
- [x] `training/loss_synthesis.py` -- Claude Code -- fix #26 import math placement (uncommitted)
- [x] `eval/sandbox.py` -- Claude Code -- fix #23 logits shape validation (uncommitted)
- [x] `eval/baseline.py` -- Claude Code -- fix #46 match baseline n_layers to candidate (uncommitted)
- [x] `scientist/runner.py` -- Claude Code -- pass n_layers to all baseline.compare calls (uncommitted)
- [x] `scientist/analytics.py` -- Claude Code -- fix #42 statistical guard on weight updates (uncommitted)
- [x] `scientist/analytics.py` -- Claude Code -- single-pass correlations, module-level imports (96573a1)
- [x] `synthesis/graph.py` -- Claude Code -- cached graph properties (96573a1)
- [x] `scientist/persona.py` -- Claude Code -- fix key mismatch, richer reports (9f8b049)
- [x] `scientist/runner.py` -- Claude Code -- fix metadata key, markdown save call (9f8b049)
- [x] `scientist/notebook.py` -- Claude Code -- save_report_markdown method (9f8b049)
- [x] `database.py` -- Claude Code -- batch commits, consolidated count query (9f8b049)
- [x] `synthesis/serializer.py` -- Claude Code -- compact JSON (9f8b049)
- [x] `morphological_box.py` -- Claude Code -- O(1) option lookup dicts (9f8b049)
- [x] `search/evolution.py` -- GitHub Copilot -- parent-informed mutation/crossover lineage + explicit eval error metadata (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- evolution lineage/error tests, dashboard API route contract tests, package wiring checks (uncommitted)
- [x] `tools/dead_code_audit.py` -- GitHub Copilot -- non-destructive dead-code audit script with improved import parsing and first-party filtering (uncommitted)
- [x] `mathspaces/__init__.py` -- GitHub Copilot -- explicit module exports for registry linkage (uncommitted)
- [x] `scientist/llm/__init__.py` -- GitHub Copilot -- explicit context/prompts exports for package linkage (uncommitted)
- [x] `__main__.py` -- GitHub Copilot -- fixed CLI entrypoints to use current runner APIs and wait for completion (uncommitted)
- [x] `scientist/notebook.py` -- Claude Code -- optimize update_op_success_rates: targeted SQL, index access, running averages (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `TestPipelineEndToEnd` single-flow execution test covering novelty output, learning-memory updates, experiment persistence, and campaign/report analytics APIs (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- documented prioritized main improvement observations and validation status snapshot (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- appended authoritative Agent Collaboration Protocol, 2-minute sync cadence, test ownership, and milestone gates (uncommitted)
- [x] `CLAUDE.md` -- GitHub Copilot -- added explicit shared-observations workflow instructing all agents to update `copilot.md` (uncommitted)
- [x] `search/evolution.py` -- GitHub Copilot -- added fingerprint duplicate-control to enforce population diversity and avoid clone collapse (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- added regression test `test_evolution_enforces_fingerprint_diversity` and validated full suite (uncommitted)
- [x] `scientist/runner.py` -- GitHub Copilot -- fix #52 by adding periodic control experiments that skip learned grammar weights and emit control metadata (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- regression test for control-experiment scheduling/grammar-weight bypass (`TestInlinePhaseMethods`) (uncommitted)
- [x] `scientist/api.py` -- GitHub Copilot -- fix #37 make SSE timeout configurable via `ARIA_SSE_TIMEOUT_SECONDS` with safe fallback (uncommitted)
- [x] `scientist/llm/anthropic.py` -- GitHub Copilot -- fix #36 use stable Anthropic default model alias with env override (uncommitted)
- [x] `scientist/persona.py` -- GitHub Copilot -- fix #40 warn once for unknown backend cost fallback; fix #39 add pivot/stop recommendation signal after prolonged zero survivors (uncommitted)
- [x] `scientist/notebook.py` -- GitHub Copilot -- fix #55 persist code version in experiment config metadata (`RESEARCH_CODE_VERSION` or git hash) (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- add regression tests for #36/#37/#39/#40/#55 (uncommitted)
- [x] `scientist/runner.py` -- GitHub Copilot -- fix #51 flag campaign success criteria as post-hoc when formulated from prior outcomes (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- add regression test `test_ensure_campaign_marks_post_hoc_criteria` (uncommitted)
- [x] `scientist/runner.py` -- GitHub Copilot -- fix #34 startup stale-run recovery helper and #56 contextual deterministic seeding across training paths (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- add regression tests `test_runner_startup_recovers_stale_experiments` and `test_train_with_program_uses_step_seed_sequence` (uncommitted)
- [x] `scientist/api.py` -- GitHub Copilot -- fix #33 replace silent exception swallowing with warning logs in analysis/dashboard/system status paths (uncommitted)
- [x] `scientist/runner.py` -- GitHub Copilot -- fix #33 replace silent exception swallowing with warning/debug logs in validation, campaign linking, grammar weights, and FLOP estimation paths (uncommitted)
- [x] `scientist/runner.py` -- GitHub Copilot -- partial fix #48: baseline comparison now uses candidate training metrics (`n_train_steps`, `final_lr`) where available (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- add regression `test_baseline_compare_uses_training_metrics` and validate full suite (uncommitted)
- [x] `morphological_box.py` -- GitHub Copilot -- optimize #20 tag incompatibility checks with cached option/tag sets (uncommitted)
- [x] `eval/fingerprint.py` -- Claude Code -- #4 novelty calibration: track analyses_succeeded + quality fields in BehavioralFingerprint (uncommitted)
- [x] `eval/metrics.py` -- Claude Code -- #4 novelty calibration: add novelty_confidence to NoveltyMetrics (uncommitted)
- [x] `scientist/notebook.py` -- Claude Code -- #4 novelty calibration: add novelty_confidence column to program_results schema + migration (uncommitted)
- [x] `scientist/runner.py` -- Claude Code -- #4 novelty calibration: wire novelty_confidence through all record_program_result calls (uncommitted)
- [x] `scientist/runner.py` -- Claude Code -- #10 tighten breakthrough promotion: align with persona publication thresholds + novelty_confidence ≥ 0.5 gate, validation_n_seeds default 3→5 (uncommitted)
- [x] `scientist/notebook.py` -- Claude Code -- add avg_novelty_confidence to op_success_rates schema + migration + accumulation (uncommitted)
- [x] `scientist/analytics.py` -- Claude Code -- discount novelty by confidence in grammar weight computation (uncommitted)
- [x] `scientist/persona.py` -- Claude Code -- add Confidence column to top programs table in reports (uncommitted)
- [x] `tests/test_integration.py` -- Claude Code -- 12 regression tests for novelty calibration (quality, confidence, DB persistence, breakthrough gate, grammar weights) (uncommitted)

## Notes

<!-- Anything the other agent should know: recent refactors, tricky areas, conventions -->
- `scientist/analytics.py` imports `get_primitive` and `GrammarConfig` at module level now (not inside methods)
- `synthesis/graph.py` ComputationGraph has a `_cache` dict cleared by mutation methods -- don't bypass `add_op`/`set_output`
- `database.py` ExperimentDB has a `batch()` context manager for deferred commits
- Tests: run with `/home/tim/venvs/llm/bin/pytest tests/ -x`
- CLI note: `python -m research --mode=synthesize` previously crashed due stale `RunConfig` args; `__main__.py` now starts runner jobs via `start_experiment/start_continuous/start_evolution`.
- Dead code audit note: `tools/dead_code_audit.py` is report-only and now includes `__init__.py` imports in reference corpus to avoid false orphan flags.
- E2E test note: `TestPipelineEndToEnd.test_continuous_pipeline_records_novelty_learning_and_reports` runs a short continuous session (`max_experiments=2`, CPU) and validates `/api/report`, `/api/campaigns`, and `/api/analytics/op-success` outputs.
- Collaboration note: `copilot.md` now includes the canonical prompt/rules for both agents (sync every 2 minutes, explicit testing ownership, and required completion gates).
- Diversity note: evolution now deduplicates same-fingerprint individuals and replaces overflow duplicates with fresh unique candidates before next generation ranking.
- Baseline note: `TransformerBaseline` now trains 3 seeds and averages, includes n_layers+device in cache key. Old cache entries won't match new keys — this is intentional (stale 2-layer baselines shouldn't be reused for 4-layer comparisons).
- Compiler note: parameterized ops now gracefully return input tensor if weight/scale/bias param is missing (instead of crashing).
- FLOP note: `_estimate_op_flops` now reads `config.get("out_dim")` for linear projections instead of hardcoding D//2 and D*2.
- Control note: `RunConfig.control_experiment_interval` defaults to 5 and marks every Nth continuous synthesis run as control (`control_experiment=true`, `use_learned_grammar_weights=false`).
- SSE note: `/api/events` timeout is now configurable via `ARIA_SSE_TIMEOUT_SECONDS` (defaults to 30s, invalid values fall back with warning).
- Versioning note: `start_experiment()` now writes `code_version` into `experiments.config_json` using `RESEARCH_CODE_VERSION` or repo git short hash.
- Test note: full integration currently has an unrelated failure in `TestNotebook.test_composite_score_increases_with_phases`; targeted tests for new fixes are green.
- Test note (updated): full integration now passes (`python -m unittest research.tests.test_integration -v` -> 113/113).
- Novelty archive note: `BehaviorArchive.add()` now uses reservoir sampling (Algorithm R) instead of FIFO — all time periods equally represented.
- Safe eval note: `eval(formula)` in grammar.py and graph.py replaced with `safe_eval_formula()` from primitives.py — AST-based, supports only arithmetic on integers.
- Outer product note: `outer_product` description updated to "Elementwise (Hadamard) product" — implementation does `a * b`, not true outer product. Name kept for backward compat with serialized graphs.
- Observability note: remaining runner/api silent catches touched in Copilot lane now emit explicit logs instead of swallowing exceptions silently.
- Baseline note: baseline comparison now aligns closer to candidate training conditions by reusing observed steps/LR plus optimizer recipe metadata (optimizer type, weight decay, momentum/betas when available).
- Clustering note: `ExperimentAnalytics.experiment_clusters()` now returns deterministic k-means-style clusters over completed experiments (S1 rate, novelty, loss, duration) with a compact stability score; surfaced in `/api/analytics/experiment-clusters` and `/api/report`.
- Dashboard note: clustering UI was intentionally added only once in `LearningPanel` (single fetch match for `/api/analytics/experiment-clusters`) to avoid duplicate consumers.
- Novelty calibration note: `BehavioralFingerprint` now tracks `analyses_succeeded` (0–4) and `quality` ("full"/"partial"/"none"). `NoveltyMetrics.novelty_confidence` (0.0–0.9) reflects fingerprint quality. `program_results.novelty_confidence` column added via migration. Evolution/novelty search paths use 0.2 confidence (structural-only).
- Grammar weight note: novelty factor in grammar weight formula is now `1.0 + raw_novelty * confidence` instead of `1.0 + raw_novelty`. Low-confidence novelty barely moves weights. `op_success_rates` table has new `avg_novelty_confidence` column.
- Breakthrough note: promotion thresholds now match `Aria.PUBLICATION_*` constants (baseline < 0.90, std ≤ 0.03, ≥ 5 seeds, OOD ≥ 0.67, HP ≥ 0.75) + novelty_confidence ≥ 0.5 gate. `validation_n_seeds` default raised from 3 to 5.
