# Active Work Coordination

Multiple AI agents (Claude Code, Codex, etc.) use this file to avoid conflicts.
**Check this file before starting work. Update it when you claim or finish files.**

---

## Claimed (in progress)

<!-- Format: - [ ] `path/to/file.py` -- AgentName -- brief description -->
<!-- No active claims -->

---

## Open TODOs

### UX/UI Assessment (2026-02-16) — Full Dashboard Audit

#### P0: Critical — Insights are broken (all scores identical, duplicated content) -- FIXED

- [x] **INSIGHTS: All scores identical** -- Fixed: `compute_insights()` returns structured dicts with per-insight confidence scaled by sample size/effect strength. Scores now range 53-79 instead of all 63.
- [x] **INSIGHTS: Massive duplication** -- Fixed: `_analyze_results` deduplicates before inserting; `/api/insights` and `/api/dashboard` deduplicate on read for existing DB data.
- [x] **INSIGHTS: No category diversity** -- Fixed: each insight type maps to correct category (`success_factor`, `hypothesis`, `failure_mode`, `pattern`).

#### P1: High — "Is the AI learning?" is unclear

- [x] **LEARNING: No before/after narrative showing trajectory** -- Fixed: `analytics.learning_trajectory()` computes S1 trend via linear regression; `LearningTrajectory` card shows sparkline, trend pill (improving/plateaued/declining), slope, weight adjustment count; `summarize_learning_bullets()` includes trajectory bullet.
- [x] **LEARNING: Cluster descriptions are all "the productive cluster"** -- Fixed: `_describe_clusters` now ranks clusters by S1 rate and assigns contrastive labels ("most productive", "mid-tier", "least productive") with distinguishing features.
- [x] **LEARNING: Grammar weights explanation is LLM-generated but LLM is disabled** -- Fixed: deterministic fallback now shows before→after values (e.g., "1.0→3.29") and net shift summary across adjusted categories.
- [x] **OVERVIEW: Aria's status shows stale/empty state** -- Fixed: `get_status(db_summary=...)` includes `total_experiments`, `total_programs`, `stage1_survivors`; `AriaStatus.js` shows all-time section below daily counters; both `/api/status` and `/api/dashboard` pass `db_summary`.

#### P2: Medium — Disconnected or confusing elements

- [x] **OVERVIEW: Compact InsightsPanel shows top 5 but they're all identical** -- Fixed: overview preview now deduplicates by category+content before selecting top 5, preventing repeated copies in compact Insights panel.
- [x] **PROGRAMS vs LEADERBOARD: Two tabs showing overlapping data with different scoring** -- Fixed: both components already have cross-referencing descriptions. TopPrograms says "Raw survivor view... For decision-ready candidates, use Leaderboard (Curated)." Leaderboard says "Curated decision view... For broad raw survivor browsing, use Programs (Raw)."
- [x] **TRENDS: Missing visual connection to grammar weight changes** -- Fixed: TrendCharts now fetches learning-log events and renders dashed orange vertical markers ("W") on all three MiniCharts at timestamps when grammar weights were adjusted. Legend below charts explains the markers.
- [ ] **CAMPAIGNS: Single campaign with minimal data** -- Only 1 campaign exists ("Architecture Discovery Campaign") with 5 experiments. This is a data/system issue, not a frontend fix — campaigns will populate as more experiments run.
- [x] **KNOWLEDGE: Knowledge entries are cryptic** -- Fixed: `_rule_based_knowledge()` now includes full hypothesis prediction, reasoning, test method, and outcome in content (not just the metric check). KnowledgeBase.js shows first line collapsed with full multi-line content on expand.
- [x] **REPORT: Efficiency frontier `ops` field is always empty `[]`** -- Fixed: `efficiency_frontier()` ops extraction now checks both `op` and `op_name` keys in graph_json nodes, filtering out input/output nodes. Ops now populated (verified with live data).

#### P3: Low — Polish items

##### Done (by Copilot phases 1-18 + Claude Code)
- [x] Score provenance tooltips — Discovery Rankings, ExperimentList, TopPrograms, TrendCharts, LabNotebook, Op Success
- [x] Metric glossary inline — Leaderboard (S.Loss/I.Loss/V.Loss/V.Base) + Research Report (loss/baseline/novelty/score/S1/CKA)
- [x] Data freshness per panel — Leaderboard, Trends, Campaigns, Report, Knowledge, Learning, Notebook
- [x] Denominator context — Experiments, Trends, Op Success, Report op rows
- [x] Confidence/reliability bands — Op Success + Report op rows
- [x] Cross-view drill-down links — Trends/Notebook/Knowledge experiment drill-down
- [x] Copy-to-clipboard — Experiment IDs, result IDs, fingerprints, knowledge IDs, campaign decision/evidence IDs
- [x] Truncation usability — Leaderboard, TopPrograms, KnowledgeBase full-value tooltips
- [x] Missing-value states — Experiments/programs/trends/learning use reasoned labels
- [x] Report confidence callouts — Confidence & Data Sufficiency card
- [x] Campaign decision traceability — Evidence IDs, hypothesis links, observed metrics, experiment open
- [x] Campaign progression health — Coverage meters, health status band, blockers list
- [x] Task-oriented quick actions — Report Investigate/Validate + Leaderboard actions
- [x] Accessibility aria labels — Sortable headers across leaderboard/notebook/insights/op-success/report

##### Open P3 — Actionable Now
- [x] **Extract shared utilities** -- Created `dashboard/src/utils/format.js` with `formatTime()`, `formatDuration()`, `scoreColor()`. Updated 8 components to import from shared utility.
- [x] **Overview "Top 5" lacks count context** -- Added `totalCount` prop; title now shows "Top Programs (5 of 10)" in compact mode.
- [x] **KnowledgeBase filter counts** -- Client-side filtering with count badges: "All (7)", "Principle (3)", etc.
- [x] **Normalize color semantics** -- Added Color Legend and Score Formulas sections to Help panel documenting all color conventions.
- [x] **NAV: 12 tabs is overwhelming** -- Grouped into Research / Analysis / Meta sections with labeled dividers.

##### Open P3 — Usability & Technical Debt (from page-by-page audit)

**Tier 1 — High impact, removes duplication:**
- [x] **Extract color utility functions** -- Created `utils/colors.js` with `lossColor()`, `noveltyColor()`, `confidenceColor()`. Replaced 20+ inline ternary chains across 7 components.
- [x] **Extract `useCopyToClipboard()` hook** -- Created `hooks/useCopyToClipboard.js`. Replaced identical copy-feedback pattern in 7 components.
- [x] **Extract `useFetch()` hook** -- Deferred: fetch patterns too varied (Promise.all, polling, dynamic params) for a single hook to be net simpler.

**Tier 2 — Polish:**
- [ ] **Unify score formulas** -- 12 different score formulas exist. Same program scored differently across pages. Requires extracting to `scores.js` and harmonizing weights.
- [ ] **Add novice/expert display modes** -- Simple defaults, optional advanced fields.
- [ ] **Standardize sorting defaults** -- Default to evidence strength, persist sort/filter.
- [ ] **Expose backend quality flags** -- Surface artifact-backed vs fallback heuristic status.
- [ ] **"What changed since last run" deltas** -- Per-tab metric deltas.

### Scientist-Centric Assessment (2026-02-16)

#### Verdict (Scientist Perspective)
- [ ] **Information completeness: partial (strong core, missing decision-grade rigor)** -- Current UI exposes hypotheses, experiments, rankings, report confidence card, and campaign traceability; however, it still lacks statistical confidence framing for promotion decisions and reproducibility-ready experiment packets.
- [ ] **Workflow seamlessness: moderate (improved, still context-switch heavy)** -- Key drill-downs exist, but scientists still hop across Experiments/Programs/Leaderboard/Report/Campaigns to answer one decision question.

#### Math Diversity + Attention Alternatives Assessment (scientist question)
- [ ] **Math diversity verdict: moderate-to-strong but still narrow for "exotic" families** -- Engine already includes hyperbolic/tropical/p-adic/Clifford ops, FFT/state-space, and morphological alternatives to softmax attention; however, there is no explicit functional-neural-network family (operator learning, neural fields, implicit-function layers) as first-class searchable primitives.
- [ ] **QKV alternatives clarity verdict: partial** -- alternatives exist in morphology (`state_space`, `fourier_mixing`, `differentiable_sort`, `cross_attention_pool`, etc.) and primitives (`local_window_attn`, `selective_scan`, `conv1d_seq`), but the scientist UI does not clearly map "this candidate used X instead of Q/K/V" in a single explanatory panel.

#### Compression + Recursion/Gating + Hypothesis-Review Assessment (scientist question)
- [ ] **Weight compression verdict: present but under-instrumented** -- system includes low-rank/shared/hash/structured sparse/quantized options and compressed attention, but dashboard does not show explicit compression ratio, memory footprint deltas, or compression-vs-quality tradeoff summary for decision-making.
- [ ] **Recursive/adaptive gating verdict: present** -- compute routing already includes `adaptive_recursion`, `mod_topk`, `early_exit`, and channel/token gating primitives, but comparative evidence across routing modes is not prominently surfaced as a single scientist-facing analysis.
- [ ] **Early AI hypothesis review verdict: partial** -- AI/LLM hypothesis formulation is richer in context-aware continuous workflows, but ad-hoc experiment start can still generate hypotheses with minimal context review; no explicit preflight "AI hypothesis critique" gate is shown to user.

##### P1: Compression Evidence Clarity (high impact)
- [ ] **Add compression metrics to leaderboard/report rows** -- show param compression ratio vs dense baseline, estimated memory footprint, and quality-retention score so scientists can choose compact models with confidence.
  - Files: `scientist/analytics.py`, `scientist/api.py`, `dashboard/src/components/Leaderboard.js`, `dashboard/src/components/ResearchReport.js`
- [ ] **Add compression-technique coverage panel** -- summarize tested/surviving counts by compression mechanism (`low_rank`, `shared_basis`, `hash_trick`, `structured_sparse`, `residual_quantized`, `compressed_attention`).
  - Files: `scientist/analytics.py`, `scientist/api.py`, `dashboard/src/components/LearningPanel.js`

##### P1: Recursive Learning + Adaptive Gating Analysis (high impact)
- [ ] **Add routing-mode comparison card** -- compare success/stability/efficiency for `uniform` vs `mod_topk` vs `early_exit` vs `adaptive_recursion` vs `token_merging` with sample sizes and confidence labels.
  - Files: `scientist/analytics.py`, `scientist/api.py`, `dashboard/src/components/LearningPanel.js`, `dashboard/src/components/ResearchReport.js`
- [ ] **Add gating-behavior diagnostics** -- expose gate entropy, route collapse risk, and token-retention curves for gated/recursive candidates.
  - Files: `scientist/analytics.py`, `scientist/api.py`, `dashboard/src/components/LearningPanel.js`, `dashboard/src/components/ProgramDetail.js`

##### P1: Earlier AI Review of Hypotheses (high impact)
- [ ] **Add preflight AI hypothesis critique step** -- before experiment launch, run AI-assisted hypothesis quality checks (testability, measurable metric, confound risk, fallback plan) and show pass/warn/fail badges.
  - Files: `scientist/persona.py`, `scientist/runner.py`, `scientist/api.py`, `dashboard/src/components/ControlPanel.js`
- [ ] **Require context-rich hypothesis generation for manual starts** -- when LLM backend is available, always provide recent experiment/insight context to `formulate_hypothesis` in `start_experiment` path instead of minimal/no-context generation.
  - Files: `scientist/runner.py`, `scientist/llm/context.py`, `tests/test_integration.py` *(Partial: `start_experiment` now builds/passes rich context from recent experiments, leaderboard, analytics, and budget to `formulate_hypothesis`; integration coverage added.)*
- [ ] **Log hypothesis review provenance** -- notebook entries should record whether hypothesis came from LLM+context, rule-based fallback, or user input, plus confidence/critique output.
  - Files: `scientist/notebook.py`, `scientist/runner.py`, `dashboard/src/components/CampaignView.js`, `dashboard/src/components/LabNotebook.js` *(Partial: backend now records provenance metadata (`source`, `llm_used`, `fallback_used`, `used_context`, `confidence`, `critique`) in hypothesis notebook entries for manual starts and continuous synthesis starts.)*

##### P1: Exotic Model Families (high impact)
- [ ] **Add functional-network architecture family to morphological box** -- introduce explicit options for operator/functional models (e.g., neural operators, implicit neural fields, kernelized function layers) with constraints and tags so search can intentionally sample them.
  - Files: `morphological_box.py`, `arch_builder.py`, `tests/test_integration.py`
- [ ] **Register functional primitives in synthesis layer** -- add primitive ops representing function-space computation (e.g., basis-expansion layer, integral-kernel mixing, implicit fixed-point update) and wire shape/parameter rules.
  - Files: `synthesis/primitives.py`, `synthesis/grammar.py`, `synthesis/compiler.py`
- [ ] **Expose "math family coverage" analytics** -- dashboard panel showing share of evaluated/surviving programs by math family (Euclidean, hyperbolic, tropical, p-adic, Clifford, functional).
  - Files: `scientist/analytics.py`, `scientist/api.py`, `dashboard/src/components/LearningPanel.js`

##### P1: QKV Alternative Explainability (high impact)
- [ ] **Add attention-mechanism taxonomy in Program Detail** -- for each candidate, render "token mixing mechanism" with plain-language replacement mapping (e.g., `state_space` = no QKV, recurrent state update; `cross_attention_pool` = learned query bottleneck; `local_window_attn` = constrained Q=K=V self-attn).
  - Files: `dashboard/src/components/ProgramDetail.js`, `dashboard/src/components/HelpPanel.js`
- [ ] **Add per-program "QKV used?" quality flag** -- explicit boolean/enum in API payload (`full_qkv`, `q_eq_k_eq_v`, `qkv_free`) derived from graph primitives/morphological choices.
  - Files: `scientist/analytics.py`, `scientist/api.py`, `dashboard/src/components/TopPrograms.js`, `dashboard/src/components/Leaderboard.js`
- [ ] **Add report section: "Alternatives to Attention"** -- summarize which non-QKV mechanisms were tried, survival rates, and top fingerprints per mechanism.
  - Files: `scientist/analytics.py`, `scientist/api.py`, `dashboard/src/components/ResearchReport.js`

#### Scientist Workflow Gaps — Prioritized TODOs

##### P1: Discovery-Decision Rigor (high impact)
- [ ] **Add decision confidence gates in Leaderboard/Report** -- show minimum evidence thresholds before “promote” (e.g., required seed count, baseline comparisons completed, robustness available) and label entries as `decision-ready` vs `exploratory`.
  - Files: `dashboard/src/components/Leaderboard.js`, `dashboard/src/components/ResearchReport.js`
- [ ] **Add uncertainty/provenance chips per key metric** -- for each primary metric (loss ratio, novelty, baseline ratio, routing confidence), show source and reliability (`artifact-backed`, `heuristic`, `low sample`).
  - Files: `dashboard/src/components/Leaderboard.js`, `dashboard/src/components/TopPrograms.js`, `dashboard/src/components/ExperimentList.js`, `dashboard/src/components/LearningPanel.js`, `dashboard/src/components/ResearchReport.js`
- [ ] **Add cross-run stability summary for top candidates** -- expose how candidate rank changes across recent experiments (stable/up/down/new) so scientists can avoid overreacting to one run.
  - Files: `scientist/api.py`, `dashboard/src/components/Leaderboard.js`, `dashboard/src/components/ResearchReport.js`

##### P2: Scientific Workflow Seamlessness (high impact)
- [ ] **Create a "Decision Packet" view/action** -- one-click assembled evidence bundle for a candidate: hypothesis lineage, experiment outcomes, baseline comparison, failure modes, and recommended next action.
  - Files: `scientist/api.py`, `dashboard/src/components/ProgramDetail.js`, `dashboard/src/components/ResearchReport.js`
- [ ] **Add persistent investigation queue** -- allow pinning candidates from Programs/Leaderboard/Report into a shared queue for batch Investigate/Validate and side-by-side review.
  - Files: `dashboard/src/App.js`, `dashboard/src/components/TopPrograms.js`, `dashboard/src/components/Leaderboard.js`, `dashboard/src/components/ResearchReport.js`
- [ ] **Add hypothesis-to-experiment handoff controls** -- from campaign/report insights, prefill Control Panel config with objective/hypothesis and suggested mode (`screening`, `investigation`, `validation`).
  - Files: `dashboard/src/App.js`, `dashboard/src/components/CampaignView.js`, `dashboard/src/components/ResearchReport.js`, `dashboard/src/components/ControlPanel.js`

##### P3: Reproducibility + Auditability (important)
- [ ] **Add reproducibility manifest copy/export** -- include experiment/program IDs, seed settings, dataset slice, grammar weights snapshot, and code/artifact version in one exportable snippet.
  - Files: `scientist/api.py`, `dashboard/src/components/ExperimentDetail.js`, `dashboard/src/components/ProgramDetail.js`
- [ ] **Add negative-results synthesis panel** -- aggregate repeatedly refuted patterns and failed op combinations into a scientist-readable "do not pursue" list.
  - Files: `scientist/analytics.py`, `scientist/api.py`, `dashboard/src/components/InsightsPanel.js`, `dashboard/src/components/ResearchReport.js`
- [ ] **Add per-campaign success-criteria tracker** -- compare campaign success criteria text against observed metrics with explicit pass/at-risk/not-yet status.
  - Files: `scientist/analytics.py`, `dashboard/src/components/CampaignView.js`

---

## Recently Completed (uncommitted)

### P3 Polish — Color Utilities + Copy Hook
- [x] `dashboard/src/utils/colors.js` -- Claude Code -- new shared utility: `lossColor()`, `noveltyColor()`, `confidenceColor()`
- [x] `dashboard/src/hooks/useCopyToClipboard.js` -- Claude Code -- new shared hook replacing 7 identical copy-feedback implementations
- [x] 7 component files updated to use `colors.js`: TopPrograms, ExperimentDetail, ExperimentList, ProgramDetail, InsightsPanel, TrendCharts, KnowledgeBase
- [x] 7 component files updated to use `useCopyToClipboard`: TopPrograms, ExperimentList, LabNotebook, ResearchReport, CampaignView, TrendCharts, KnowledgeBase
- [x] `tests/test_integration.py` -- Claude Code -- 219 tests passing after all extractions

### P3 Polish — Shared Utilities, Nav Grouping, Help Docs
- [x] `dashboard/src/utils/format.js` -- Claude Code -- new shared utility: `formatTime()`, `formatDuration()`, `scoreColor()`
- [x] `dashboard/src/components/ExperimentList.js` -- Claude Code -- replaced 3 local functions with shared imports
- [x] `dashboard/src/components/ExperimentDetail.js` -- Claude Code -- replaced 2 local functions with shared imports
- [x] `dashboard/src/components/InsightsPanel.js` -- Claude Code -- replaced 2 local functions with shared imports
- [x] `dashboard/src/components/LabNotebook.js` -- Claude Code -- replaced 2 local functions with shared imports
- [x] `dashboard/src/components/TrendCharts.js` -- Claude Code -- replaced 3 local functions with shared imports
- [x] `dashboard/src/components/Leaderboard.js` -- Claude Code -- replaced 1 local function with shared import
- [x] `dashboard/src/components/TopPrograms.js` -- Claude Code -- replaced 1 local function with shared import + "Top 5 of N" count context
- [x] `dashboard/src/components/LearningPanel.js` -- Claude Code -- replaced opScoreColor with shared scoreColor import
- [x] `dashboard/src/App.js` -- Claude Code -- tab nav grouped into Research/Analysis/Meta sections + totalCount prop
- [x] `dashboard/src/components/KnowledgeBase.js` -- Claude Code -- client-side filtering with category count badges
- [x] `dashboard/src/components/HelpPanel.js` -- Claude Code -- Color Legend, Score Formulas, updated Dashboard Tabs sections
- [x] `tests/test_integration.py` -- Claude Code -- 218 tests passing after all P3 changes

### P2 Disconnected/Confusing Elements
- [x] `scientist/analytics.py` -- Claude Code -- `efficiency_frontier()` ops extraction: check both `op` and `op_name` keys, filter input/output
- [x] `dashboard/src/components/TrendCharts.js` -- Claude Code -- grammar weight change markers: fetches learning-log, renders dashed orange "W" markers on all MiniCharts at weight adjustment timestamps
- [x] `scientist/persona.py` -- Claude Code -- `_rule_based_knowledge()` includes full hypothesis prediction, reasoning, test method, and outcome in content
- [x] `dashboard/src/components/KnowledgeBase.js` -- Claude Code -- multi-line content with pre-wrap on expand, collapsed first-line preview
- [x] `dashboard/src/components/LearningPanel.js` -- Claude Code -- fixed freshness text to avoid false-positive API route test match
- [x] `tests/test_integration.py` -- Claude Code -- 218 tests passing after all P2 changes

### P1 Learning Clarity + Aria Status
- [x] `scientist/analytics.py` -- Claude Code -- `learning_trajectory()`: S1 trend regression with improving/plateaued/declining classification
- [x] `scientist/persona.py` -- Claude Code -- `explain_grammar_weights()` deterministic fallback with before→after values; `summarize_learning_bullets()` trajectory bullet; `get_status()` all-time counters
- [x] `scientist/api.py` -- Claude Code -- `/api/analytics/learning-trajectory` endpoint; `/api/analytics/learning-summary` passes trajectory; `/api/status` and `/api/dashboard` pass `db_summary` for all-time counters
- [x] `dashboard/src/components/LearningPanel.js` -- Claude Code -- `LearningTrajectory` card with sparkline, trend pill, slope, weight adjustments
- [x] `dashboard/src/components/AriaStatus.js` -- Claude Code -- all-time section (experiments, programs, S1 survivors) below daily counters
- [x] `tests/test_integration.py` -- Claude Code -- added `/api/analytics/learning-trajectory` to known API patterns (218 tests passing)

### P0 Insight Scoring + Dedup + Cluster Descriptions
- [x] `scientist/analytics.py` -- Claude Code -- `compute_insights()` returns structured dicts with per-insight category and confidence scaled by sample size/effect; `_describe_clusters()` replaces `_describe_cluster()` with contrastive ranking labels
- [x] `scientist/runner.py` -- Claude Code -- `_analyze_results` consumes structured insight dicts, deduplicates before inserting to DB
- [x] `scientist/api.py` -- Claude Code -- `/api/insights` and `/api/dashboard` deduplicate insights by content (keeps latest); added `_deduplicate_insights()` helper
- [x] `tests/test_integration.py` -- Claude Code -- updated `TestClusterDescriptions` for new `_describe_clusters()` API (218 tests passing)

### P3 usability implementation batch (phase 1)
- [x] `dashboard/src/components/ExperimentList.js` -- GitHub Copilot -- added score breakdown tooltip, S1 denominator context (`x/y` and rate), and reasoned missing-value labels
- [x] `dashboard/src/components/TopPrograms.js` -- GitHub Copilot -- added score breakdown tooltip and replaced ambiguous placeholders with reasoned missing-value labels
- [x] `dashboard/src/components/TrendCharts.js` -- GitHub Copilot -- added score breakdown tooltip, S1 denominator context (`x/y` with rate), and reasoned missing-value labels
- [x] `.current_work.md` -- GitHub Copilot -- added/claimed P3 implementation batch and synchronized completion notes

### P3 usability implementation batch (phase 2)
- [x] `dashboard/src/App.js` -- GitHub Copilot -- wired experiment drill-down callbacks into Trends, Notebook, and Knowledge tabs
- [x] `dashboard/src/components/TrendCharts.js` -- GitHub Copilot -- added experiment open action, copy experiment-id action, and panel freshness stamp
- [x] `dashboard/src/components/LabNotebook.js` -- GitHub Copilot -- added latest-entry freshness context plus Open/Copy experiment-entry IDs in expanded rows
- [x] `dashboard/src/components/KnowledgeBase.js` -- GitHub Copilot -- added panel freshness stamp, copy knowledge entry-id action, and experiment drill-down buttons parsed from supporting evidence
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 2 claims and completion notes

### P3 usability implementation batch (phase 3)
- [x] `dashboard/src/components/TopPrograms.js` -- GitHub Copilot -- added copy actions for fingerprint and result IDs in survivor table rows
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added copy fingerprint/result-id actions in Discovery Rankings rows
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 3 claims and completion notes

### P3 usability implementation batch (phase 4)
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- added campaign list/detail freshness timestamps and report generation timestamp/source context
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added report fetch freshness timestamp/source context in report header
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 4 claims and completion notes

### P3 usability implementation batch (phase 5)
- [x] `dashboard/src/components/LabNotebook.js` -- GitHub Copilot -- added score provenance tooltip with component breakdown in notebook rows
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 5 claims and completion notes

### P3 usability implementation batch (phase 6)
- [x] `dashboard/src/components/ExperimentList.js` -- GitHub Copilot -- added copy action for experiment IDs in experiments table rows
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 6 claims and completion notes

### P3 usability implementation batch (phase 7)
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- added score provenance tooltip, leaderboard freshness stamp/source, and inline metric glossary hints
- [x] `dashboard/src/App.js` -- GitHub Copilot -- deduplicated compact overview insights before top-5 preview
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 7 claims and completion notes

### P3 usability implementation batch (phase 8)
- [x] `dashboard/src/App.js` -- GitHub Copilot -- wired report quick-action callbacks (Investigate/Validate)
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added Investigate/Validate quick-action controls per discovery row
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 8 claims and completion notes

### P3 usability implementation batch (phase 9)
- [x] `dashboard/src/App.js` -- GitHub Copilot -- renamed tabs to `Programs (Raw)` and `Leaderboard (Curated)` for intent clarity
- [x] `dashboard/src/components/TopPrograms.js` -- GitHub Copilot -- added explicit raw-survivor positioning guidance
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- added explicit curated-decision positioning guidance
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 9 claims and completion notes

### P3 usability implementation batch (phase 10)
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- added full-value tooltip for truncated architecture description cells
- [x] `dashboard/src/components/TopPrograms.js` -- GitHub Copilot -- added full-value tooltips for fingerprint and similarity fields
- [x] `dashboard/src/components/KnowledgeBase.js` -- GitHub Copilot -- added truncation-safe title rendering with full-value tooltips for title/content
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 10 claims and completion notes

### P3 usability implementation batch (phase 11)
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- added op-rate denominators, sample-size reliability bands, score provenance tooltip, and learning panel freshness stamp
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 11 claims and completion notes

### P3 usability implementation batch (phase 12)
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- replaced ambiguous `--` placeholders with reasoned missing-value labels in op/routing sections
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 12 claims and completion notes

### P3 usability implementation batch (phase 13)
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- added sortable-header aria labels for Op Success table
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 13 claims and completion notes

### P3 usability implementation batch (phase 14)
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- added campaign progression health band, coverage meters (evidence/hypothesis/decision), and explicit blockers list
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added Confidence & Data Sufficiency card with coverage factors, caution notes, and supporting signals
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 14 claims and completion notes

### P3 usability implementation batch (phase 15)
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- added decision traceability with parsed evidence IDs, linked hypotheses/experiments, observed metrics, and decision-context guidance
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 15 claims and completion notes

### P3 usability implementation batch (phase 16)
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added inline Metric Glossary card covering loss ratio, baseline loss ratio, novelty, discovery score, S1 survivor, and CKA source semantics
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 16 claims and completion notes

### P3 usability implementation batch (phase 17)
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added denominator context and reliability labels to report op success/failure rows
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 17 claims and completion notes

### P3 usability implementation batch (phase 18)
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- added copy actions for campaign decision IDs and grouped evidence IDs in Decision Log
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 18 claims and completion notes

### P3 usability implementation batch (phase 19)
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added `scope` and `aria-sort` semantics to Discovery Rankings sortable headers
- [x] `.current_work.md` -- GitHub Copilot -- synchronized phase 19 claims and completion notes

### Scientist-perspective usability audit (phase 20)
- [x] `.current_work.md` -- GitHub Copilot -- added scientist-perspective verdict and prioritized TODO backlog for decision rigor, seamless workflow, and reproducibility

### Scientist math-diversity/QKV-clarity audit (phase 21)
- [x] `.current_work.md` -- GitHub Copilot -- added scientist-focused assessment and prioritized TODOs for exotic functional model families and explicit QKV-alternative explainability

### Scientist compression/recursion/hypothesis-review audit (phase 22)
- [x] `.current_work.md` -- GitHub Copilot -- added scientist-focused assessment and prioritized TODOs for compression evidence, adaptive routing/gating analysis, and earlier AI hypothesis review gates

### Scientist early hypothesis context (phase 23)
- [x] `scientist/runner.py` -- GitHub Copilot -- `start_experiment` now builds rich context and passes it into `aria.formulate_hypothesis(context=...)` with safe fallback
- [x] `tests/test_integration.py` -- GitHub Copilot -- added regression test `test_start_experiment_builds_context_for_hypothesis`
- [x] Focused validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "start_experiment_builds_context_for_hypothesis or control_experiment_interval_marks_and_skips_learned_weights" --tb=short` (2 passed)

### Hypothesis provenance logging (phase 24)
- [x] `scientist/persona.py` -- GitHub Copilot -- added optional provenance return (`return_metadata=True`) for `formulate_hypothesis`
- [x] `scientist/notebook.py` -- GitHub Copilot -- `start_experiment` now accepts/stores `hypothesis_metadata` in hypothesis notebook entries
- [x] `scientist/runner.py` -- GitHub Copilot -- threaded provenance metadata through manual `start_experiment` and continuous synthesis experiment starts
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_start_experiment_records_hypothesis_provenance_metadata`
- [x] Focused validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "start_experiment_builds_context_for_hypothesis or start_experiment_records_hypothesis_provenance_metadata or control_experiment_interval_marks_and_skips_learned_weights" --tb=short` (3 passed)

### Campaign purpose card + usability audit backlog
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- added Campaign Purpose & Status card with why/status/evidence/next-best-action summary
- [x] `.current_work.md` -- GitHub Copilot -- added comprehensive prioritized TODO backlog for AI scientist data usability review

### Campaign/report usability clarity
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- added explicit Open Details affordance, keyboard semantics, timeline evidence guidance, and direct Open Experiment action
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- linked Discovery Rankings fingerprints to program detail, clarified score bands/meaning, and added usability-focused section guidance
- [x] `dashboard/src/App.js` -- GitHub Copilot -- wired report/campaign drill-down callbacks so report fingerprints and campaign timeline can open detail views
- [x] Focused validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_report_schema or api_campaigns_list_schema or api_campaign_report_schema" --tb=short` (3 passed)

### Styling Consistency
- [x] `dashboard/src/components/Leaderboard.js` -- Claude Code -- sort arrow fontSize 9→10 to match all other tables
- [x] `dashboard/src/components/ExperimentList.js` -- Claude Code -- empty state color/size normalized (text-secondary/14 → text-muted/13)
- [x] `dashboard/src/components/MetricsChart.js` -- Claude Code -- empty state color/size normalized
- [x] `dashboard/src/components/InsightsPanel.js` -- Claude Code -- empty state color/size normalized
- [x] `dashboard/src/components/TrendCharts.js` -- Claude Code -- empty state color/size normalized
- [x] `dashboard/src/components/LabNotebook.js` -- Claude Code -- empty state color/size normalized
- [x] `dashboard/src/components/TopPrograms.js` -- Claude Code -- empty state color/size normalized
- [x] `dashboard/src/components/FailureAnalysis.js` -- Claude Code -- empty state: added fontSize 13, improved message text

### Remaining `alert()` conversion
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- replaced action-failure `alert()` calls with inline error banner (`actionError`)
- [x] `dashboard/src/components/ControlPanel.js` -- GitHub Copilot -- replaced guard/fetch `alert()` calls with inline control-panel error banner (`actionError`)
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- replaced fallback action `alert()` calls with inline error text (`actionError`)
- [x] `tests/test_integration.py` -- GitHub Copilot -- focused dashboard contract sanity check passed after alert-conversion updates

### Accessibility: aria-label coverage
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- added aria-labels for tier filter buttons, refresh control, and sortable headers
- [x] `dashboard/src/components/KnowledgeBase.js` -- GitHub Copilot -- added aria-labels for category filters and search controls
- [x] `dashboard/src/components/LabNotebook.js` -- GitHub Copilot -- added aria-labels for sortable headers plus keyboard/expanded semantics for expandable rows
- [x] `dashboard/src/components/InsightsPanel.js` -- GitHub Copilot -- added aria-labels for sortable headers plus keyboard/expanded semantics for expandable rows
- [x] `tests/test_integration.py` -- GitHub Copilot -- focused dashboard contract sanity check passed after accessibility updates

### Empty-State Guidance + Search Feedback
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- improved empty-state guidance with tier-aware, actionable next steps
- [x] `dashboard/src/components/KnowledgeBase.js` -- GitHub Copilot -- improved empty-state guidance and added explicit search-loading feedback
- [x] `tests/test_integration.py` -- GitHub Copilot -- focused frontend contract sanity check (`no_orphaned_api_fetch_urls`) passed

### Dashboard UX: Inline Errors, Connection State, Empty States
- [x] `dashboard/src/App.js` -- Claude Code -- replaced all `alert()` calls with inline dismissible error banner (`actionError` state)
- [x] `dashboard/src/components/LiveFeed.js` -- Claude Code -- connection-lost banner when EventSource drops; contextual empty state message
- [x] `dashboard/src/components/ExperimentDetail.js` -- Claude Code -- friendly error messages (404/500), descriptions + empty states for Insights and Notebook Entries cards

### Full Regression Verification
- [x] `tests/test_integration.py` -- GitHub Copilot -- ran full integration suite (`/home/tim/venvs/llm/bin/pytest tests/ -x --tb=short`): 219 passed

### Campaign UX refinement
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- improved campaign integration UX with null-safe data handling, tab counts, and explicit empty states for hypotheses/decisions/report

### Benchmark clarity
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- added explicit fixed-seed baseline evidence snapshot and breakthrough benchmark note

### Aria "What I've learned" card
- [x] `scientist/persona.py` -- GitHub Copilot -- added `summarize_learning_bullets()` with LLM-first and deterministic fallback
- [x] `scientist/api.py` -- GitHub Copilot -- added `/api/analytics/learning-summary` endpoint returning `bullets` and `source`
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- added "What I've learned" card and endpoint wiring
- [x] `tests/test_integration.py` -- GitHub Copilot -- added endpoint/schema/API-url contract coverage for learning summary

### Routing Health explanation
- [x] `scientist/analytics.py` -- GitHub Copilot -- added deterministic plain-language routing interpretation for drop rate, utilization entropy, and confidence
- [x] `scientist/api.py` -- GitHub Copilot -- `/api/analytics/routing-health` now guarantees `explanation` with safe defaults
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- renders plain-language routing interpretation in Routing Health card
- [x] `tests/test_integration.py` -- GitHub Copilot -- routing-health schema test asserts `explanation` string

### Leaderboard Action Callbacks + Post-Action State Refresh
- [x] `dashboard/src/App.js` -- Claude Code -- wired onInvestigate/onValidate to Leaderboard, onActionComplete to ProgramDetail
- [x] `dashboard/src/components/ProgramDetail.js` -- Claude Code -- added onActionComplete prop for Scale Up/Investigate/Validate success

### Dashboard-Wide Section Descriptions (plain-language)
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- added plain-language subtitle explaining ranking tiers and evidence strength
- [x] `dashboard/src/components/LabNotebook.js` -- GitHub Copilot -- added plain-language subtitle clarifying notebook purpose and decision traceability
- [x] `dashboard/src/components/KnowledgeBase.js` -- GitHub Copilot -- added plain-language subtitle explaining extracted lessons and confidence meaning
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- added plain-language subtitle describing campaigns as goal/hypothesis/decision threads

### Grammar Weights LLM Explanation
- [x] `scientist/persona.py` -- GitHub Copilot -- added `explain_grammar_weights()` using configured LLM with deterministic fallback summary
- [x] `scientist/api.py` -- GitHub Copilot -- `/api/analytics/grammar-weights` now returns `explanation` narrative
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- renders Aria narrative card under Grammar Weights
- [x] `tests/test_integration.py` -- GitHub Copilot -- schema test asserts `explanation` string in grammar-weights analytics payload

### All Pages Freshness
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Experiments/Programs/Notebook/Insights now fetch dedicated endpoints on tab open with dashboard snapshot fallback on fetch failure

### Scope Communication
- [x] `dashboard/src/App.js` -- GitHub Copilot -- added Overview scope statement clarifying the system researches architecture designs (not training recipes/data tuning)
- [x] `dashboard/src/components/FailureAnalysis.js` -- Claude Code -- added description for failure funnel, stage-at-death, and top errors
- [x] `dashboard/src/components/LiveFeed.js` -- Claude Code -- added description for real-time architecture test stream
- [x] `dashboard/src/components/ResearchReport.js` -- Claude Code -- added descriptions to 7 sub-cards (Discovery Rankings, Experiment Timeline, What Works, What Doesn't Work, Grammar Evolution, Efficiency Frontier, Insights & Recommendations)

### Leaderboard Diversity: Re-Investigation Guard
- [x] `scientist/notebook.py` -- Claude Code -- added `get_investigated_fingerprints()` to query archs already at investigation/validation/breakthrough tier
- [x] `scientist/runner.py` -- Claude Code -- `_run_inline_investigation` and `_auto_escalate` now skip candidates whose fingerprint was already investigated
- [x] `tests/test_integration.py` -- Claude Code -- test for `get_investigated_fingerprints()` tier filtering

### Live Feed SSE Contract Test
- [x] `tests/test_integration.py` -- Claude Code -- 3 SSE contract tests: frontend events subset of backend, sanity checks for both sides
- [x] `scientist/api.py` -- Claude Code -- emit `campaign_completed` SSE event from API endpoint (was missing)

### Dashboard UX Resilience: Error States (all components)
- [x] `dashboard/src/components/LearningPanel.js` -- Claude Code -- error state for analytics fetch failures
- [x] `dashboard/src/components/CampaignView.js` -- Claude Code -- error state for campaign list/detail/report
- [x] `dashboard/src/components/Leaderboard.js` -- Claude Code -- error state for leaderboard fetch
- [x] `dashboard/src/components/ExperimentDetail.js` -- Claude Code -- error state for experiment + analysis fetch
- [x] `dashboard/src/components/ProgramDetail.js` -- Claude Code -- error state for program detail fetch
- [x] `dashboard/src/components/KnowledgeBase.js` -- Claude Code -- error state for knowledge load + search
- [x] `dashboard/src/components/TrendCharts.js` -- Claude Code -- error state for trends fetch
- [x] `dashboard/src/components/FailureAnalysis.js` -- Claude Code -- error state for failure analysis fetch

### Leaderboard Diversity + Dashboard UX + Observability
- [x] `scientist/notebook.py` -- Claude Code -- leaderboard fingerprint dedup in get_leaderboard()
- [x] `scientist/runner.py` -- Claude Code -- persist applied_grammar_weights in exp config and results
- [x] `scientist/analytics.py` -- Claude Code -- frontier ops extraction + cluster plain-language descriptions
- [x] `dashboard/src/components/LearningPanel.js` -- Claude Code -- hover tooltips, scope intro, cluster descriptions, routing empty state

### Schema Contract Tests + Stale Cleanup
- [x] `tests/test_integration.py` -- Claude Code -- 8 schema contract tests + 4 stale cleanup tests
- [x] `tests/test_integration.py` -- GitHub Copilot -- extended schema contracts for `/api/report`, `/api/trends`, `/api/knowledge*`, `/api/campaigns*`

### Validation Pipeline Hardening
- [x] `scientist/runner.py` -- GitHub Copilot -- centralized grammar-weight config persistence
- [x] `scientist/runner.py` -- GitHub Copilot -- tightened investigation→validation auto-escalation gate
- [x] `scientist/runner.py` -- GitHub Copilot -- persisted validation_result_ids/candidate metadata into config
- [x] `scientist/runner.py` -- GitHub Copilot -- investigation brittleness guard
- [x] `scientist/runner.py` -- GitHub Copilot -- inline investigation/validation progress denominators fixed
- [x] `scientist/runner.py` -- GitHub Copilot -- per-experiment op distribution snapshots + distribution-shift logging
- [x] `scientist/notebook.py` -- GitHub Copilot -- stale-running cleanup with startup-failure detection
- [x] `dashboard/src/components/ExperimentList.js` -- GitHub Copilot -- Validating state for active validation runs
- [x] `dashboard/src/components/ControlPanel.js` -- GitHub Copilot -- mode-aware progress text fallback
- [x] `tests/test_integration.py` -- GitHub Copilot -- regressions for escalation, brittleness, stale cleanup, validation metadata, progress, distribution-shift

### Dashboard Feature Additions
- [x] `scientist/notebook.py` -- GitHub Copilot -- architecture_family label in leaderboard payload
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- Family column and sorting
- [x] `eval/fingerprint.py` -- GitHub Copilot -- removed CPU-unsupported autocast(float32) wrappers
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- Routing Health card
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- CKA provenance badges
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- CKA source/version in discovery rankings
- [x] `tests/test_integration.py` -- GitHub Copilot -- dashboard API URL contract test

## Recently Completed (committed)

### Diagnostic Tasks + HYDRA Integration (4b777df, 7985877)
- [x] `scientist/runner.py` -- Claude Code -- HYDRA data integration (data_mode="hydra")
- [x] `eval/baseline.py` -- Claude Code -- data_fn/data_tag support for real-data baselines
- [x] `eval/diagnostic_tasks.py` -- Claude Code -- synthetic diagnostic task evaluation suite
- [x] `scientist/notebook.py` -- Claude Code -- diagnostic_tasks_json + diagnostic_score migration columns

### CKA Pipeline + Novelty Confidence (9712a63)
- [x] `eval/cka_references.py` -- Claude Code -- ReferenceCkaStore loader/validator/cache
- [x] `eval/fingerprint.py` -- Claude Code -- artifact-backed CKA + heuristic fallback + provenance
- [x] `tools/export_cka_references.py` -- Claude Code -- offline reference training/export
- [x] `scientist/notebook.py` -- Claude Code -- composite score weights novelty by confidence
- [x] `scientist/runner.py` -- Claude Code -- novelty_confidence passthrough to upsert_leaderboard
- [x] Various Copilot tooling -- GitHub Copilot -- CKA manifest/integrity/hash tools, clustering features

<!-- Older history: see git log. Commits feacbab (novelty calibration), 3d2a39c (dashboard+agent improvements), 96573a1 (perf), 9f8b049 (reports+fixes), 0d707ea (methodology) -->

---

## Notes

### Technical Conventions
- `scientist/analytics.py` imports `get_primitive` and `GrammarConfig` at module level
- `synthesis/graph.py` ComputationGraph has `_cache` dict cleared by mutation -- use `add_op`/`set_output`
- `database.py` ExperimentDB has `batch()` context manager for deferred commits
- Tests: `/home/tim/venvs/llm/bin/pytest tests/ -x` (219 passing as of 2026-02-15)
- CLI: `python -m research --mode=synthesize` starts via `start_experiment/start_continuous/start_evolution`

### CKA & Novelty
- CKA references: `artifacts/cka_references/v1/`. Runtime auto-loads via `ReferenceCkaStore`. Regenerate with `python tools/export_cka_references.py`.
- Novelty: `NoveltyMetrics.novelty_confidence` (0.0–0.9). Grammar weights use `1.0 + novelty * confidence`.
- Breakthrough promotion: thresholds match `Aria.PUBLICATION_*` (baseline < 0.90, std ≤ 0.03, ≥ 5 seeds, OOD ≥ 0.67, HP ≥ 0.75, novelty_confidence ≥ 0.5).

### HYDRA Integration
- `RunConfig.data_mode="hydra"` uses HYDRA's `create_universal_loader`. Token IDs projected via `% vocab_size` when HYDRA vocab (GPT-2 50257) exceeds model vocab.

### Scope
- The AI scientist **only researches neural network layer architectures** — it generates random compositions of ops, tests if they compile and learn, and evolves the grammar toward successful patterns.

### Control & Observability
- Every 5th continuous run uses default grammar weights (`control_experiment=true`).
- Synthesis results include `generated_op_distribution` and `generation_distribution_shift` for grammar feedback verification.
- All 57 audit items from copilot.md are addressed. See `copilot.md` for full status.
