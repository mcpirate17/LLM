# Active Work Coordination

Multiple AI agents (Claude Code, Codex, etc.) use this file to avoid conflicts.
**Check this file before starting work. Update it when you claim or finish files.**

## Claimed (in progress)

<!-- Format: - [ ] `path/to/file.py` -- AgentName -- brief description -->
- [ ] `scientist/runner.py` -- Claude Code -- HYDRA data integration (data_mode="hydra")
- [ ] `eval/baseline.py` -- Claude Code -- data_fn/data_tag support for real-data baselines

## Recently Completed

<!-- Format: - [x] `path/to/file.py` -- AgentName -- brief description (commit hash) -->
- [x] `scientist/runner.py` -- GitHub Copilot -- finalized investigation brittleness guard (loss-ratio multiplier + brittle-risk gating in investigation pass and auto-validation filter) (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_auto_escalate_excludes_brittle_candidates` regression for brittle candidate exclusion (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- added sync 40 observations for brittleness guard + targeted auto-escalation validation (uncommitted)
- [x] `.current_work.md` -- GitHub Copilot -- coordination note: shared observations file `copilot.md` updated in this pass (sync 40) (uncommitted)
- [x] `copilot.md` -- GitHub Copilot -- added sync 39 for dashboard routing-health + CKA provenance wiring and contract-test evidence (uncommitted)
- [x] `.current_work.md` -- GitHub Copilot -- coordination note: shared observations file `copilot.md` updated in this pass (sync 39) (uncommitted)
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- connected `/api/analytics/routing-health` and added in-panel Routing Health card (mode/pass/drop/entropy/confidence) (uncommitted)
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- added CKA provenance badges (`cka_source`, `cka_artifact_version`) in existing CKA similarity section (uncommitted)
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added CKA source/version visibility in discovery rankings table (uncommitted)
- [x] `tests/test_integration.py` -- GitHub Copilot -- verified dashboard API URL contract via `TestDashboardConsistency.test_no_orphaned_api_fetch_urls` (uncommitted)
### #28/#43 CKA Pipeline (commit 9712a63)
- [x] `eval/cka_references.py` -- Claude Code -- #28/#43 Phase A: ReferenceCkaStore loader/validator/cache (9712a63)
- [x] `eval/fingerprint.py` -- Claude Code -- #28/#43 Phase C: artifact-backed CKA + heuristic fallback + provenance (9712a63)
- [x] `tools/export_cka_references.py` -- Claude Code -- #28/#43 Phase B: offline reference training/export (9712a63)
- [x] `artifacts/cka_references/v1/` -- Claude Code -- #28/#43 Phase B: v1 reference artifacts (9712a63)

### Novelty Confidence & Scoring (commit 9712a63)
- [x] `scientist/notebook.py` -- Claude Code -- composite score weights novelty by confidence (9712a63)
- [x] `scientist/runner.py` -- Claude Code -- novelty_confidence passthrough to upsert_leaderboard (9712a63)
- [x] `tests/test_integration.py` -- Claude Code -- 22 new CKA/confidence tests, 162/162 passing (9712a63)

### Copilot Support Tooling (commit 9712a63)
- [x] Various Copilot tooling -- GitHub Copilot -- CKA manifest/integrity/hash tools, clustering features, coordination (9712a63)

<!-- Older history: see git log. Commits feacbab (novelty calibration), 3d2a39c (dashboard+agent improvements), 96573a1 (perf), 9f8b049 (reports+fixes), 0d707ea (methodology) -->

## Notes

<!-- Anything the other agent should know: recent refactors, tricky areas, conventions -->
- `scientist/analytics.py` imports `get_primitive` and `GrammarConfig` at module level
- `synthesis/graph.py` ComputationGraph has `_cache` dict cleared by mutation -- use `add_op`/`set_output`
- `database.py` ExperimentDB has `batch()` context manager for deferred commits
- Tests: `/home/tim/venvs/llm/bin/pytest tests/ -x`
- CLI: `python -m research --mode=synthesize` starts via `start_experiment/start_continuous/start_evolution`
- Novelty calibration: `BehavioralFingerprint` tracks `analyses_succeeded` (0–4), `quality`, `cka_source`, `cka_artifact_version`. `NoveltyMetrics.novelty_confidence` (0.0–0.9). Grammar weights use `1.0 + novelty * confidence`.
- CKA references: `artifacts/cka_references/v1/` has trained transformer/SSM/conv activations. Runtime auto-loads via `ReferenceCkaStore`. Regenerate with `python tools/export_cka_references.py`.
- Breakthrough promotion: thresholds match `Aria.PUBLICATION_*` (baseline < 0.90, std ≤ 0.03, ≥ 5 seeds, OOD ≥ 0.67, HP ≥ 0.75, novelty_confidence ≥ 0.5).
- Control experiments: every 5th continuous run uses default grammar weights (`control_experiment=true`).
- All 57 audit items from copilot.md are addressed. See `copilot.md` for full status.
- HYDRA data integration: `RunConfig.data_mode="hydra"` uses HYDRA's `create_universal_loader` from `/home/tim/Projects/LLM/HYDRA/hydra/data`. Baseline comparisons use matching data via `data_fn`/`data_tag`. Token IDs projected via `% vocab_size` when HYDRA vocab (GPT-2 50257) exceeds model vocab.
