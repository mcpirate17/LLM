# Current Work

## Claimed
<!-- Add entries when starting work on a file to prevent multi-agent conflicts -->

## Recently Completed
- **2026-04-18 — claude-opus — training/ performance audit fixes**
  - `_data_native.cpp` + `.py`: added `jsonl_byte_tokenize_file` (single-pass JSONL byte tokenizer, no Python json), `gather_token_batch_into` (gather into caller-provided pinned buffer, parallel memcpy), `project_int64_modulo_inplace` (parallel mod kernel for vocab projection); cache bumped to `data_native_ext_v2`
  - `data_pipeline.py`: tokenizer adapters now share `encode_to_tensor` (no `tolist()` round-trip, no `isinstance` branching at call sites); `.npy` mmap stays zero-copy when dtype matches and values fit vocab; vocab projection happens once at load via the native in-place kernel; CUDA path gathers directly into a single reused pinned buffer (no per-step `pin_memory` or per-step `torch.remainder`); JSONL byte path delegates to the new single-pass native parser; benign PyTorch read-only-mmap warning silenced at the mmap call site
  - `sparse_training.py`: optimizer momentum reset now targets `grown = new_mask & ~old_mask` only — pruned-weight momentum is left alone (its grad is already zeroed by the mask hook), saving memory traffic on every topology update
  - `_loss_components.py`: removed redundant module-level `_LOSS_NATIVE` cache (the loader is already `lru_cache(1)`)
  - `loss_ops.py`: docstring fixed to reference the actual native module (`research.training._loss_native`, not the retired runner path)
  - `eval/utils.py`: removed unused `_runner_native` import (dead since loss/clip migrated to `_loss_native`)
  - Verified: 59/59 relevant unit tests pass with `-W error::UserWarning` (`test_loss_optimizer_synthesis`, `test_training_perf_regressions`, `test_tokenizer_parity`, `test_eval_runner_native`, `test_backfill_micro_train`, `test_evolution_pareto`, `test_novelty_search_scaling`, `test_toxic_op_fix`)

## Recently Completed
- **Research directory hygiene audit** — claude-opus, 2026-04-01
  - Deleted 15 junk files (MagicMock leaks, :memory: SQLite, stale backups, orphaned DBs)
  - Removed API key from llm_config.json, added env var support
  - Fixed :memory: WAL leak in notebook_core.py
  - Deleted legacy morphological pipeline (explorer.py, database.py, evaluator.py)
  - Fixed broken evaluate_stage1 import in continuous_investigation.py
  - Cached ArchSpec.id property
  - Removed monitor state files from git tracking

## Notes
- `morphological_box.py` and `arch_builder.py` are still used by scientist/runner morphological_box candidate source mode
- `llm_config.json` now uses `api_key_env` field — set `ANTHROPIC_API_KEY` env var
