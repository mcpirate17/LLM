# Research Current Work

Condensed on 2026-04-26. Original full journal is archived at
`../cleanup_20260426/doc_archives/research_current_work_original_20260426.md`.

## Active State

- `research/` is active and should be treated as the main project.
- The important source directories are:
  - `dashboard/`
  - `eval/`
  - `healer/`
  - `mathspaces/`
  - `orchestrator/`
  - `runtime/`
  - `runtime_events/`
  - `schemas/`
  - `scientist/`
  - `search/`
  - `synthesis/`
  - `tests/`
  - `tools/`
  - `training/`
- `corpus/`, `artifacts/`, `db_backups/`, `logs/`, `perf_artifacts/`, and `profiling/` are state/generated areas, not source authority.

## Current Operational Guardrails

- Leave `.continuous_paused` in place unless explicitly resuming continuous.
- Do not delete or move DB snapshots/backups during casual cleanup.
- DB corruption/recovery should be handled as its own project with an explicit retention policy.
- Use writer-lock discipline for any DB-writing tooling.

## Recent Completed Work To Preserve

- Synthesis selector cleanup after profiling:
  - Kept low-risk compatibility/filter reductions.
  - Rejected a custom direct weighted selector because `random.choices` was faster/simpler in A/B.
- Batch native packed graph validation:
  - Added C batch ABI and Python bindings.
  - Integrated packed batch validation where call sites could consume it without duplicate implementations.
- Training/runtime/eval performance audits:
  - Multiple small Python changes were tested, retained only when measured useful, and reverted when neutral or slower.
  - The recurring conclusion is that further one-line Python cleanup is low ROI.
- Eval scorer and packed scorer passes:
  - Consolidated repeated paths and native/packed scoring boundaries.
  - `research/eval/WORKING.md` remains the short active eval notes file.
- Runtime/API/notebook cleanup:
  - API, runner, and notebook god-file cleanup passes split code into smaller helpers.
  - Remaining high findings are mostly native candidate signals and broader source-structure work.

## Remaining High-ROI Work

- Build a native graph-builder boundary that owns graph construction plus validation inputs together.
- Avoid partial native changes that only move overhead around.
- Review `research/tools/REGISTRY.md` and retire one-off tools that are no longer wired into workflows/tests/CI.
- Decide what to do with `research/docs/`, which is currently deleted in git status.
- Decide how to retain/prune DB snapshots and corrupt/recovery copies.

## 2026-04-26 Cleanup Pass

- Removed generated caches/build outputs in `research`, `aria_designer`, and `aria_core`.
- Removed runtime logs and test-run artifacts.
- Left DB files untouched.
- Condensed long current-work journals and archived originals under `../cleanup_20260426/doc_archives/`.
