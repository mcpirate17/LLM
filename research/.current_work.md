# Active Work Coordination

Multiple AI agents (Claude Code, Codex, etc.) use this file to avoid conflicts.
**Check this file before starting work. Update it when you claim or finish files.**

---

## Claimed (in progress)

<!-- Format: - [ ] `path/to/file.py` -- AgentName -- brief description -->


<!-- Claude Code learning-loop + compound-ops claims released — completed -->




<!-- Claude Code Phase 76 claims released — completed -->
<!-- Claude Code Decision Packet claims released — completed -->
<!-- Claude Code ResearchReport queue/handoff claims released — completed -->
<!-- Claude Code negative-results claims released — completed -->
<!-- Claude Code score unification + reproducibility manifest claims released — completed -->
<!-- Claude Code attention taxonomy + deltas claims released — completed -->
<!-- Claude Code Phase 64 Aria AI briefing claims released — completed -->
<!-- GitHub Copilot P0 Aria claims superseded by Claude Code Phase 64 -->

---

## Open TODOs

### Active TODOs (only open items)

#### Data/System
- [ ] **CAMPAIGNS: Single campaign with minimal data** -- Only 1 campaign exists ("Architecture Discovery Campaign") with 5 experiments. This is a data/system issue, not a frontend fix — campaigns will populate as more experiments run.

#### P0: Aria AI Integration — LLM-Powered Research Assistant *(COMPLETE)*

All items below are done. The full Aria loop is operational: LLM-powered briefings, one-click experiment start, autonomous mode with cycle control (start/pause/resume), real-time activity strip, chat with DB persistence and compaction, SSE-driven auto-refresh, and graceful fallback to rule-based mode.

**Goal (achieved):** Open dashboard → read Aria's LLM-generated analysis → click "Run Suggested Experiment" (or "Start Autonomous Mode") → watch it run → Aria analyzes results → suggests next steps → repeat.

##### Backend: Aria Briefing via LLM
- [x] **Create `BRIEFING_PROMPT` template** (`scientist/llm/prompts.py`) — *(Phase 64: Core agent template added. Produces 3-5 sentence analysis + structured SUGGESTED_ACTION with mode/hypothesis/config/reasoning.)*
- [x] **Create `ARIA_ORCHESTRATOR_PROMPT` template** (`scientist/llm/prompts.py`) — *(Superseded: orchestration implemented in code via `run_aria_cycle()` in runner.py (Phase 83) rather than as a single prompt. Uses existing MODE_SELECTION_PROMPT + BRIEFING_PROMPT + ANALYSIS_PROMPT in sequence.)*
- [x] **Add `build_briefing_context()` to `scientist/llm/context.py`** — *(Phase 64: Assembles last 5 experiments, pipeline tiers, learning trajectory, campaign, grammar weights, top programs into <2000 token context.)*
- [x] **Add `generate_briefing()` to persona.py** — *(Phase 64: LLM-backed with 60s cache. Returns {briefing_text, suggested_action, confidence, ai_powered}. Falls back to None when LLM unavailable.)*
- [x] **Add `run_aria_cycle()` orchestration service** — *(Phase 83: Full plan→run→analyze cycle in runner.py with cycle summary builder, `aria_cycle_completed` SSE emission, and `last_cycle_summary` exposure. Pause/resume policy added in Phase 84.)*
- [x] **Upgrade `/api/strategy/briefing` to use LLM** (`scientist/api.py`) — *(Phase 64: Tries LLM first, deterministic fallback. Response includes `ai_powered`, `suggested_config`, `confidence`.)*
- [x] **Add Aria cycle API + SSE events** — *(Phases 82-86: `GET /api/aria/cycle-status`, `POST /api/aria/cycle-control` (start/pause/resume), `GET /api/aria/cycle-history` with filters+CSV export. SSE events: `aria_cycle_phase`, `aria_cycle_completed`.)*

##### Frontend: One-Click "Run Suggested Experiment"
- [x] **Redesign StrategyAdvisor as Aria's briefing card** — *(Phase 64: "Aria's Analysis" section with AI-Powered/Rule-Based badge, hypothesis display, param chips, large "Run Suggested Experiment" button.)*
- [x] **Wire "Run Suggested Experiment" to `/api/experiments/start`** — *(Phase 64: Button sends suggested_config (mode, hypothesis, params). Loading state, calls onStart.)*
- [x] **Enforce exact button placement in Overview** — *(StrategyAdvisor at top of overview grid, autonomous mode + execute buttons prominent, advanced setup collapsed in details element below.)*
- [x] **Add real-time Aria activity strip** — *(Phase 82: "Aria Continuous Activity" strip with phase, cycle index, mode, note. Phase 84: Start/Pause/Resume cycle controls. Phase 85: Cycle Timeline view with history.)*

##### Post-Run: Auto-Refresh Loop
- [x] **Auto-refresh briefing after experiment completes** — *(Phase 64: SSE listener on `experiment_completed` triggers re-fetch with 2s delay. Shows "Aria is analyzing the latest results..." during loading.)*
- [x] **Add experiment completion context to briefing** — *(Phase 76: SSE event data parsed by frontend, experiment_id passed to `/api/strategy/briefing?just_completed=<id>`, highlighted at top of LLM context. AriaChatPanel shows immediate results.)*
- [x] **Persist Aria reasoning transcript as markdown/chat artifact** — *(Phase 78: Chat transcript persisted to sessionStorage, survives page refresh. Clear button to reset. Message count shown.)*
- [x] **Add fallback + health UX for AI backend** — *(Phases 74+78: Rule-based fallback shows reason (not configured/unreachable/error) with "Configure LLM" button that opens advanced setup. AriaChatPanel auto-refreshes on LLM config.)*

##### Stretch (P2, future)
- [x] **Aria conversational chat panel** — *(Phases 71-73: Two-way chat with structured Evidence/Recommendation/Next Action responses, SSE integration, transcript history.)*

##### Acceptance Criteria (Definition of Done)
- [x] **End-to-end Aria loop works without manual setup edits** — Fresh load: Aria suggests a run; one click starts it; upon completion Aria posts analysis and a new recommendation.
- [x] **User sees AI-generated rationale each cycle** — Every suggested run includes concise rationale text (LLM) or data-backed rationale (deterministic).
- [x] **No silent failure mode** — If LLM fails, UI shows "Rule-Based" badge and deterministic fallback. Manual run path always available.

---

## Recently Completed (uncommitted)

### Phase 90
- [x] `scientist/api.py` -- GitHub Copilot -- Added deterministic real-data knowledge backfill pipeline and `POST /api/knowledge/backfill` to populate missing categories (`anti_pattern`, `sweet_spot`, `correlation`, `tool_insight`) from notebook telemetry.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `/api/knowledge/backfill` schema contract test coverage.
- [x] `copilot.md` -- GitHub Copilot -- Observation: real-data backfill endpoint executed successfully against `lab_notebook.db`; knowledge counts now include all requested categories.

### Phase 89 — Close Learning Loop + Compound Math Space Ops
- [x] `scientist/runner.py` -- Claude Code -- Phase 1A: Populate `excluded_ops` from `negative_results_synthesis()` (0% S1 rate, n>=5, confidence>=0.7); Phase 1B: Add `negative_results` key to `_gather_analytics_data()`
- [x] `scientist/llm/context.py` -- Claude Code -- Phase 1C: Render negative results (failed ops, anti-patterns, summary) in `build_rich_context()` after Active Insights
- [x] `mathspaces/hyperbolic.py` -- Claude Code -- `execute_hyperbolic_norm`: log-map → LayerNorm → exp-map compound op
- [x] `mathspaces/tropical.py` -- Claude Code -- `execute_tropical_gate`: tropical distance → sigmoid gate → linear projection compound op
- [x] `mathspaces/clifford.py` -- Claude Code -- `execute_clifford_attention`: geometric-product-based attention compound op
- [x] `mathspaces/padic.py` -- Claude Code -- `execute_padic_residual`: p-adic expansion → per-scale transform → residual compound op
- [x] `mathspaces/registry.py` -- Claude Code -- Registered 4 compound primitives as `MATH_SPACE` with `has_params=True`
- [x] `tests/test_integration.py` -- Claude Code -- 10 new tests: excluded_ops logic, negative results in context, all 4 compound op shapes + registration

### Phase 88
- [x] `scientist/api.py` -- GitHub Copilot -- Added `POST /api/experiments/<experiment_id>/rerun` to relaunch experiments from stored config and mode (including stale rebooted `running` rows).
- [x] `dashboard/src/components/ExperimentList.js` -- GitHub Copilot -- Added `Rerun` button for `running` and `failed` experiments in the Runs table.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Passed refresh callback to `ExperimentList` so cancel/rerun actions refresh dashboard state.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added rerun endpoint tests (success path + busy-runner conflict).

### Phase 87
- [x] `dashboard/src/components/CycleTimeline.js` -- GitHub Copilot -- Added quick preset chips (`Failures`, `Evolution`, `Last 24h`, `Clear`) with active-state behavior and client-side 24h window filtering.

### Phase 86
- [x] `scientist/api.py` -- GitHub Copilot -- Enhanced `GET /api/aria/cycle-history` with `mode`, `status`, and `q` filters plus `format=csv` export output.
- [x] `dashboard/src/components/CycleTimeline.js` -- GitHub Copilot -- Added mode/status/search controls and export actions (JSON/CSV) while preserving refresh + auto-poll behavior.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added cycle-history filter/CSV export integration test coverage.

### Phase 85
- [x] `scientist/runner.py` -- GitHub Copilot -- Persisted `aria_cycle_completed` summaries to notebook `live_feed` entries (`live_feed_type=aria_cycle`) for durable cycle history.
- [x] `scientist/api.py` -- GitHub Copilot -- Added `GET /api/aria/cycle-history` returning persisted cycle summaries from notebook entries.
- [x] `dashboard/src/components/CycleTimeline.js` -- GitHub Copilot -- Added dedicated Cycle Timeline view with auto-refresh and cycle detail cards.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added `cycles` tab wiring and label (`Cycle Timeline`) in Analysis section.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `/api/aria/cycle-history` endpoint test and API pattern coverage for new frontend fetch URL.

### Phase 84
- [x] `scientist/runner.py` -- GitHub Copilot -- Added cycle pause/resume policy (`pause_aria_cycle`, `resume_aria_cycle`), pause gate between iterations, and bounded `cycle_history` in cycle status payload.
- [x] `scientist/api.py` -- GitHub Copilot -- Added `POST /api/aria/cycle-control` supporting `start`, `pause`, and `resume` actions.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added cycle control buttons (Start/Pause/Resume) and Recent Cycles history list in Overview activity strip.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added cycle-control API tests (`start`, `pause/resume`) and API pattern coverage for `/api/aria/cycle-control`.

### Phase 83
- [x] `scientist/runner.py` -- GitHub Copilot -- Added `run_aria_cycle()` orchestration step (plan → run → analyze), cycle summary builder, `aria_cycle_completed` SSE emission, and `last_cycle_summary` exposure in cycle-status polling data.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added SSE listener for `aria_cycle_completed` and appended compact cycle summaries into Aria chat transcript.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended `/api/aria/cycle-status` contract test to validate `last_cycle_summary` structure when present.

### Phase 82
- [x] `scientist/runner.py` -- GitHub Copilot -- Added Aria cycle-phase tracking (`planning/running/analyzing/...`) with SSE event emission and a polling-friendly status snapshot via `get_aria_cycle_status()`.
- [x] `scientist/api.py` -- GitHub Copilot -- Added `GET /api/aria/cycle-status` endpoint for continuous research loop observability.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added Overview “Aria Continuous Activity” strip wired to `/api/aria/cycle-status` with phase, cycle index, mode, and note.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added integration contract test for `/api/aria/cycle-status` and API URL whitelist coverage.

### Phase 81
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- Made “Why this was chosen” evidence chips clickable and mapped each chip to a concrete target dashboard tab.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Wired StrategyAdvisor evidence navigation callback to set active tab (`experiments`, `leaderboard`, `trends`, `learning`, `report`).

### Phase 80
- [x] `scientist/api.py` -- GitHub Copilot -- Added `evidence` payload to `/api/strategy/briefing` (trend, recent S1, zero-S1 ratio, cancellations/failures, pipeline counts).
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- Added “Why this was chosen” panel with compact metric chips next to recommended action flow.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Updated strategy briefing contract test to assert `evidence` exists.

### Phase 79
- [x] `dashboard/src/components/AriaChatPanel.js` -- Claude Code -- Chat transcript persisted to sessionStorage (survives refresh). Added `llm-configured` event listener for auto-refresh. Added Clear button and message count. Skip initial fetch if persisted messages exist.
- [x] `dashboard/src/components/StrategyAdvisor.js` -- Claude Code -- Added "Configure LLM" button in rule-based fallback hint that opens advanced setup section.

### Phase 78
- [x] `scientist/api.py` -- GitHub Copilot -- `/api/aria/chat` now returns `fallback_reason` diagnostics (`llm_not_configured`, `llm_unreachable`, `llm_empty_response`, `llm_error:*`).
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Chat transcript now labels fallback replies with concrete reason text.

### Phase 77
- [x] `scientist/llm/anthropic.py` -- GitHub Copilot -- Hardened Anthropic content parsing to extract text across response block variants and log block types when responses are empty.
- [x] `scientist/api.py` -- GitHub Copilot -- Hardened `/api/aria/chat` against analytics/history/context failures with robust deterministic fallback context.

### Phase 77
- [x] `scientist/llm/context.py` -- Claude Code -- Added `just_completed` param to `build_briefing_context()`: highlights the just-finished experiment at the top of LLM context with "*** JUST COMPLETED ***" section.
- [x] `scientist/api.py` -- Claude Code -- `/api/strategy/briefing` now accepts `?just_completed=<experiment_id>` query param; clears briefing cache when provided so LLM sees fresh context with the completed experiment prominently placed.
- [x] `dashboard/src/components/StrategyAdvisor.js` -- Claude Code -- SSE `experiment_completed` handler now parses event data to extract `experiment_id` and passes it to briefing re-fetch as `just_completed` param.
- [x] `dashboard/src/components/AriaChatPanel.js` -- Claude Code -- SSE `experiment_completed` handler now parses event data and shows immediate results (mode, S1 survivors, loss ratio) in system message before full AI re-analysis.

### Phase 76
- [x] `scientist/persona.py` -- GitHub Copilot -- Hardened `generate_briefing()` and `_parse_briefing()` so imperfect LLM formatting still produces `briefing_text` (using reasoning/raw fallback instead of empty text).
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added parser regression tests for missing `BRIEFING:` block and `Summary:`-style responses.

### Phase 75
- [x] `scientist/persona.py` -- GitHub Copilot -- `get_llm_config()` now reports configured vs reachable and treats availability as backend reachability, not merely object presence.
- [x] `scientist/api.py` -- GitHub Copilot -- `/api/strategy/briefing` now emits `fallback_reason` (`llm_not_configured`, `llm_unreachable`, `llm_empty_response`, `llm_error:*`) when AI briefing is unavailable; `/api/system/status` now reports LLM `available` based on reachability plus `configured` flag.
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- Displays concrete fallback reason in Overview instead of generic rule-based text.

### Phase 75
- [x] `scientist/api.py` -- Claude Code -- LLM config persistence: `_llm_config_path()`, `_load_persisted_llm_config()`, `_save_llm_config()`. Auto-loads on `create_app()`, saves on configure, clears briefing cache.
- [x] `dashboard/src/components/ControlPanel.js` -- Claude Code -- Dispatches `llm-configured` CustomEvent after successful LLM configuration.
- [x] `dashboard/src/components/StrategyAdvisor.js` -- Claude Code -- Listens for `llm-configured` event to auto-refresh briefing (shows "Aria is analyzing..." then fetches AI briefing).
- [x] `tests/test_integration.py` -- Claude Code -- Added `/api/aria/chat` to `known_api_patterns`.

### Phase 74
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- Unified recommendation CTA to one explicit `Execute Recommended Action` button (including breakthrough/report actions) and added clearer rule-based fallback explanation when LLM is not active.

### Phase 73
- [x] `scientist/llm/prompts.py` -- GitHub Copilot -- Updated `CHAT_PROMPT` to require structured analyst sections: `Evidence`, `Recommendation`, `Next Action`.
- [x] `scientist/api.py` -- GitHub Copilot -- Added chat reply normalization so `/api/aria/chat` always returns sectioned analyst output; improved deterministic fallback to same structure.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Enabled multiline transcript rendering (`whiteSpace: pre-wrap`) for readable sectioned responses.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended `test_api_aria_chat` to assert required structured sections.

### Phase 72
- [x] `scientist/api.py` -- GitHub Copilot -- Added `POST /api/aria/chat` with contextual LLM response generation and deterministic fallback when LLM is unavailable.
- [x] `scientist/llm/prompts.py` -- GitHub Copilot -- Added `CHAT_PROMPT` template for grounded interactive Aria responses.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Upgraded panel to two-way chat with input/send, transcript updates, and Aria response rendering.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added chat endpoint contract test (`test_api_aria_chat`).

### Phase 71
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added Aria chat transcript card for Overview with auto-populated entries from briefing, strategy, and recommendation endpoints, plus SSE-triggered post-run analysis updates.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Mounted Aria chat panel directly under Aria status in Overview.

### Phase 70
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- Fixed navigate CTA wiring to emit explicit briefing action payloads (`export_breakthrough` / `monitor_validation`) instead of ambiguous strategy-only object.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- StrategyAdvisor action handler now routes `Export Breakthrough Report` to Report tab and `Review Validation Progress` to Leaderboard tab.

### Phase 69
- [x] `scientist/api.py` -- GitHub Copilot -- Hardened LLM context assembly in `/api/strategy/briefing` so partial analytics failures fall back to minimal context and still attempt AI briefing; normalized suggested action modes/aliases for start-mode compatibility.
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- Normalized AI-suggested run modes (e.g., `evolution` → `evolve`) and fixed hook ordering for strategy propagation.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `/api/strategy/briefing` contract test + AI mode alias normalization regression test.

### Phase 64 (latest)
- [x] `scientist/llm/prompts.py` -- Claude Code -- `BRIEFING_PROMPT`: instructs LLM to analyze research state and produce briefing text + structured suggested_action (mode, hypothesis, config, reasoning, confidence)
- [x] `scientist/llm/context.py` -- Claude Code -- `build_briefing_context()`: compact <2000 token context with last 5 experiments, pipeline tiers, learning trajectory, campaign, grammar weights, top programs
- [x] `scientist/persona.py` -- Claude Code -- `generate_briefing()`: LLM-backed with 60s cache, returns {briefing_text, suggested_action, confidence, ai_powered}. `_parse_briefing()` parses structured response.
- [x] `scientist/api.py` -- Claude Code -- `/api/strategy/briefing` upgraded: tries LLM first (via Aria), falls back to deterministic. Response now includes `ai_powered`, `suggested_config`, `confidence`. Added `_briefing_action_label()` helper.
- [x] `dashboard/src/components/StrategyAdvisor.js` -- Claude Code -- Complete redesign: "Aria's Analysis" card with AI-Powered/Rule-Based badge, hypothesis display, param chips, large "Run Suggested Experiment" button, SSE `experiment_completed` listener for auto-refresh with "Aria is analyzing..." state.

### Phase 63
- [x] `scientist/notebook.py` -- Claude Code -- `cancel_experiment()` method: marks a running experiment as failed with "Cancelled by user"
- [x] `scientist/api.py` -- Claude Code -- `POST /api/experiments/<id>/cancel` + `POST /api/experiments/cleanup-stale` endpoints
- [x] `dashboard/src/components/ExperimentList.js` -- Claude Code -- Red "Cancel" button on running experiments with confirmation dialog
- [x] `tests/test_integration.py` -- Claude Code -- Added `/api/experiments/` to known API patterns

- Completion blocks moved to `.current_work_archive.md` on 2026-02-17 to keep this tracker focused on active/open work.

---

## Notes

### Technical Conventions
- `scientist/analytics.py` imports `get_primitive` and `GrammarConfig` at module level
- `synthesis/graph.py` ComputationGraph has `_cache` dict cleared by mutation -- use `add_op`/`set_output`
- `database.py` ExperimentDB has `batch()` context manager for deferred commits
- Tests: `/home/tim/venvs/llm/bin/pytest tests/ -x` (219 passing as of 2026-02-15)
- CLI: `python -m research --mode=synthesize` starts via `start_experiment/start_continuous/start_evolution`

### CKA & Novelty
- CKA references: `artifacts/cka_references/v1/`. Runtime auto-loads via `ReferenceCkaStore`. Regenerate with `python tools/export_cka_references.py`.
- Novelty: `NoveltyMetrics.novelty_confidence` (0.0–0.9). Grammar weights use `1.0 + novelty * confidence`.
- Breakthrough promotion: thresholds match `Aria.PUBLICATION_*` (baseline < 0.90, std ≤ 0.03, ≥ 5 seeds, OOD ≥ 0.67, HP ≥ 0.75, novelty_confidence ≥ 0.5).

### HYDRA Integration
- `RunConfig.data_mode="hydra"` uses HYDRA's `create_universal_loader`. Token IDs projected via `% vocab_size` when HYDRA vocab (GPT-2 50257) exceeds model vocab.

### Scope
- The AI scientist **only researches neural network layer architectures** — it generates random compositions of ops, tests if they compile and learn, and evolves the grammar toward successful patterns.

### Control & Observability
- Every 5th continuous run uses default grammar weights (`control_experiment=true`).
- Synthesis results include `generated_op_distribution` and `generation_distribution_shift` for grammar feedback verification.
- All 57 audit items from `copilot.md` are addressed. See `copilot.md` for full status.