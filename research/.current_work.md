# Current Work

## Claimed
- **Gate & Scoring v6 Fix** — claude-opus (session A), 2026-03-16
  - **My scope**: `runner/_helpers.py` (corpus-aware gate function), `runner/execution_training.py` (gate wiring), dashboard files (`format.js`, `Leaderboard.js`, `leaderboardConfig.js`)
  - **DONE**: `stage1_learning_gate()` in `_helpers.py`, wired into `execution_training.py`, all 7 test cases pass
  - **DONE**: `compute_composite_v6()` core formula in `leaderboard_scoring.py`, `_recompute_composite()` v6 in `rescore_leaderboard.py`
  - **BLOCKED on Var H rapid eval** — see details below
  - **DONE**: dashboard color updates, audit file, v6 core formula, gate, rescorer

### Var H rapid eval problem (for browser Claude)
User wants one-anchor-per-tier scoring with rapid WikiText PPL (200 steps) as the screening anchor. GPT-2 and all references need the same 200-step eval.

**Problem**: Var H (and GPT-2 reference) don't have `wikitext_ppl_200`. Tried running `screening_wikitext_eval` from cold start — Var H diverges at step 7-20. Initial logits are extreme (pre_ppl=485M). The `micro_train_loop` uses bare AdamW + clip_grad=1.0, no warmup, no scheduler. This works for stable architectures but not for poincare_add→rwkv_channel→gelu→swiglu which needs the full pipeline init.

**58 of 121 entries already have `wikitext_ppl_200`** from screening. The rest (including Var H, GPT-2, all references) need it.

**Options**:
1. Fix `micro_train_loop` to handle extreme-init architectures (add warmup, lower initial LR, retry with smaller LR on divergence)
2. For models that already completed 10K training on WikiText-103 (Var H), use their training loss to derive a calibrated PPL estimate
3. Store model checkpoints at screening so we can re-eval later

**Recommendation**: Option 1 — make `micro_train_loop` more robust. Add 10-step LR warmup and retry at 1/10th LR if loss goes NaN. This fixes the cold-start problem for all architectures.
  - **DO NOT** modify `runner/_helpers.py` or `runner/execution_training.py`

- **Scoring Steps Gate Bug Fix** — claude-opus (session B, user-directed), 2026-03-16
  - **Your scope**: step gate logic inside `compute_composite_v6`, `n_train_steps` wiring in rescorer, `tests/test_scoring_integrity.py`
  - **COORDINATION NOTE TO SESSION A (acknowledged)**: I understand now — the step gate constants (`_V6_MIN_STEPS_FOR_COMPETITIVE`, `_V6_SCREENING_HARD_CAP`, `n_train_steps` param) are from you, not a linter. I will NOT remove them. The c05a bug (500-step screening → 206.20) is a real problem — your step gates are the right fix.
  - **SESSION A → SESSION B**: Please note:
    1. I changed the hard learning gate to use the **lowest** (best) `_best_lr` across all tiers, not first-in-priority. Var H has `val_baseline=0.982` but `inv_lr=0.54` — old code picked 0.982 first and capped at 10pts. New code picks min(0.982, 0.54) = 0.54 which passes.
    2. I added `is_wikitext_tiktoken` param — models tagged `tiktoken_native+wikitext103` use GPT-2 anchor of 6.8 (wikitext), others use 2.6326 (micro_corpus). This prevents apples-to-oranges final_loss comparisons.
    3. I added `gpt2_raw_anchor` kwarg for dynamic normalization — rescorer computes GPT-2's actual raw score and passes it so GPT-2 = exactly 100.0.
    4. Your step gates should work fine with these changes — they're orthogonal concerns (step count vs loss quality).

### Shared files — division of labor
| File | Session A owns | Session B owns |
|------|---------------|---------------|
| `leaderboard_scoring.py` | v6 core formula (perf/conv/eff/novelty/robust terms, hard learning gate, normalization) | step gate logic (constants, `n_train_steps` early-return branches) |
| `rescore_leaderboard.py` | `_recompute_composite` kwargs, GPT-2 anchor computation | `n_train_steps` wiring |
| `runner/_helpers.py` | ALL (corpus-aware gate) | DO NOT TOUCH |
| `runner/execution_training.py` | ALL (gate wiring) | DO NOT TOUCH |
| Dashboard files | ALL | DO NOT TOUCH |
| `tests/test_scoring_integrity.py` | DO NOT TOUCH | ALL |

## Recently Completed
- **Phase 6: Dashboard UI for Trajectory-Aware Eval** — gemini, 2026-03-13
  - Task G-1: PPL Trajectory Sparklines in Leaderboard (`dashboard/src/components/shared/Sparkline.js`)
  - Task G-2: Stability-Quality Quadrant View (`dashboard/src/components/charts/StabilityQualityQuadrant.js`)
  - Task G-3: Status Badges for `[FRAGILE]`, `[ROBUST]`, `[FRONTIER_SIGNAL]`, `[SLOW_BURN]`, `[DIVERGED]`, and `[STABLE_GENERALIZER]` (`dashboard/src/components/shared/StatusBadge.js`)
  - Dashboard Integration: Updated `leaderboardConfig.js`, `LeaderboardRow.js`, and `App.js` to surface trajectory, peak PPL, divergence steps, and robustness metrics.
- **Phase 4: Task 4G, 4H, 4I** — gemini, 2026-03-13
  - Task 4G: Morphological box incompatibility constraints (`research/morphological_box.py`)
  - Task 4H: Pareto frontier chart in dashboard (`dashboard/src/components/charts/ParetoEfficiencyChart.js`)
  - Task 4I: Investigation gating — skip full fingerprint for failing candidates (`eval/fingerprint.py`, `scientist/runner/execution_training.py`)
- **Phase 3: Task 3G, 3H, 3I (and bonus features)** — gemini, 2026-03-13
  - Task 3G: Reference Architecture Dashboard UI (`dashboard/src/components/ReferenceArchitectures.js`)
  - Task 3H: Decision Trace Dashboard View (`dashboard/src/components/DecisionTraces.js`)
  - Task 3I: Feedback Loop Scaffolding (`aria_designer/api/app/main.py`, `aria_designer/api/app/database.py`)
  - Bonus: Multi-Seed Robustness Metric (`scientist/runner/execution_validation.py`, `scientist/leaderboard_scoring.py`)
  - Bonus: Similarity Heatmap for Discoveries (`dashboard/src/components/charts/SimilarityHeatmap.js`, `dashboard/src/components/TrendCharts.js`)
  - Bonus: Automated Hypothesis Extraction (`research/tools/extract_hypotheses.py`)
- **Phase 2: Task 2G, 2H, 2I** — gemini, 2026-03-13
  - Task 2G: Adaptive Training Budget for Novel Architectures (`scientist/runner/execution_training.py`)
  - Task 2H: Routing-Aware Fingerprint Dimensions (`eval/fingerprint.py`)
  - Task 2I: Op-Pair Mining Script (`research/tools/mine_op_pairs.py`)
- **Phase 1: Task 1G, 1H, 1I** — gemini, 2026-03-13
  - Task 1G: Reference Architecture Dashboard Pins (`dashboard/src/components/Leaderboard.js`)
  - Task 1H: Remove Novelty-Loss Gate (`scientist/leaderboard_scoring.py`)
  - Task 1I: Show Routing Template Stats in Dashboard (`dashboard/src/components/LiveFeed.js`, `scientist/runner/execution_search.py`)
- F.12: Add native dispatch for `synthesis/compiler.py::basis_expansion` (codex-gpt5, 2026-03-08) — guarded native `aria_core.basis_expansion_f32` path with strict output-shape fallback
- F.11: Add native dispatch for `synthesis/compiler.py::embedding_lookup` (codex-gpt5, 2026-03-08) — guarded native token-id lookup path with pass-through fallback for pre-embedded tensors
- F.10: Add native dispatch for `synthesis/compiler.py::cosine_similarity` (codex-gpt5, 2026-03-08) — guarded native `aria_core.cosine_similarity_f32` with output-shape validation fallback
- F.9: Add native dispatch for `synthesis/compiler.py::topk_gate` (codex-gpt5, 2026-03-08) — guarded native path (`aria_core.topk_gate_f32`) with strict shape checks and Python fallback
- F.6/F.7/F.8: Add native dispatches for `synthesis/compiler.py::{causal_mask, sort_seq, argsort_seq}` (codex-gpt5, 2026-03-08) — guarded native paths with type/shape-validated fallbacks
- F.5: Add native dispatch for `synthesis/compiler.py::rwkv_time_mixing` (codex-gpt5, 2026-03-08) — guarded native `aria_core.rwkv_time_mixing_f32` path with existing Python fallback preserved
- F.4: Add native dispatch for `synthesis/compiler.py::rope_rotate` (codex-gpt5, 2026-03-08) — native `aria_core.rope_rotate_f32` fast path with guarded Python fallback
- F.3: Add native dispatch for `synthesis/compiler.py::tropical_center` (codex-gpt5, 2026-03-08) — `tropical_center` now uses `aria_core.tropical_center_f32` on CPU/float32/non-grad with causal `cummin` fallback
- C.6: Split `runner/results.py::_auto_escalate` (codex-gpt5, 2026-03-08) — extracted screening/investigation orchestration to `results_auto_escalate_phase7.py`; parent now dispatches by phase
- Delete dead `_runner_dead.py` monolith (15,795 lines) — claude-opus, 2026-03-08
- Phase 4: Split `analytics.py` → `analytics/` package (6 mixin files + __init__.py, `__slots__`, `@staticmethod`, np.percentile), 4,487 lines — claude-opus, 2026-03-08
- C.5: Split `runner/continuous.py::_run_inline_validation` (codex-gpt5, 2026-03-08) — fixed bootstrap return contract (`hypothesis`) and pre-promotion by `entry_id`; parent now delegates to Phase 7 mixin helpers safely
- Phase 2: notebook.py dedup + quality pass — deleted dead `notebook.py`, created `_shared.py`, removed 9,864 lines of duplication from gemini's split, added `__slots__`, unified `sanitize_for_db`, 6,591 lines total (claude-opus, 2026-03-08)
- Phase 0: Dead code cleanup — deleted `legacy/runner.py.bak` (725KB), `api_routes/read.py.bak` (117KB), empty `legacy/` dir (claude-opus, 2026-03-08)
- B.2: Runner import pruning — 599 unused imports removed, 14 broken test patches fixed (claude-opus, 2026-03-08)
- B.2: Test patch targets fixed — `runner.threading` → `runner.control.threading`, etc. (claude-opus, 2026-03-08)
- H.1: React bundle code-splitting (claude-opus, 2026-03-08) — App.js lazy loading
- Test categorization: pytest markers (unit/native/api/pipeline/e2e/designer) + conftest guard (claude-opus, 2026-03-08)
- C.5: Split _rule_based_mode_recommendation (codex-gpt5, 2026-03-08) — d6be57b
- A.6: safe_float dedup (codex-gpt5, 2026-03-08) — bcade27
- A.3-A.5, D.3-D.5, D.7-D.8, E.1-E.3: Batch perf fixes (codex-gpt5, 2026-03-08) — b83459b
