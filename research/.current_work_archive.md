# Archived Work History

This file stores historical completion notes moved out of `.current_work.md` to keep active coordination concise.

Archive date: 2026-02-16
Source: `.current_work.md`

---

## Archived from "Recently Completed (uncommitted)"

The following completion blocks were archived from the active tracker:

- Phase 47: P1 Attention Taxonomy + P3 "What Changed" deltas
- Phase 46: P3 score-unification follow-up
- Phase 45: P3 technical-debt batch; P3 score unification + reproducibility manifest
- Phase 44: P3 technical-debt batch
- Phase 43: P3 technical-debt batch
- Phase 42: P3 technical-debt batch
- Phase 41: P3 negative-results synthesis panel
- Phase 40: P3 technical-debt batch
- Phase 39: P3 technical-debt batch
- Phase 38: P2 ResearchReport queue + hypothesis handoff integration
- Phase 36: P3 technical-debt batch
- Phase 35: P2 decision packet; P3 technical-debt batch
- Phase 34: P3 scientific workflow batch
- Phase 33: P2 scientific workflow batch
- Phase 32: P2 scientific workflow batch
- Phase 31: P1 scientist workflow batch
- Phase 30: P2 workflow seamlessness (provenance/context/cross-view navigation)
- Phase 29: P1 scientist workflow batch
- Phase 28: P1 scientist workflow batch (compression evidence foundations)
- Phase 27: P1 scientist workflow batch (decision gates)
- Phase 26: P1 scientist workflow batch (hypothesis metadata/provenance)
- P3 polish: color utilities + copy hook
- P3 polish: shared utilities, nav grouping, help docs
- P2 disconnected/confusing elements fixes
- P1 learning clarity + Aria status
- P0 insight scoring + dedup + cluster descriptions
- P3 usability batches: phases 1 through 19
- Scientist assessment/audit entries: phases 20 through 22
- Scientist hypothesis context/provenance work: phases 23 through 25
- Campaign/report usability and dashboard resilience blocks
- Routing/grammar explanations and freshness/scope clarity blocks
- Validation hardening and dashboard feature additions blocks

---

## Archived from "Recently Completed (committed)"

- Diagnostic Tasks + HYDRA Integration (`4b777df`, `7985877`)
- CKA Pipeline + Novelty Confidence (`9712a63`)

Older history reference that was previously in `.current_work.md`:
- `feacbab` (novelty calibration)
- `3d2a39c` (dashboard + agent improvements)
- `96573a1` (perf)
- `9f8b049` (reports + fixes)
- `0d707ea` (methodology)

---

## Notes

- Keep `.current_work.md` focused on:
  - current claims,
  - open TODOs,
  - a short "Recently Completed (uncommitted)" window.
- Add future archival entries here when the active completion list grows too large.
- On 2026-02-16 (second cleanup pass), completed checklist entries from the large `Open TODOs` section were removed from `.current_work.md`; only open items remain in the active tracker.

### Robust dashboard action/inaction investigation (findings capture)
- [x] `.current_work.md` -- GitHub Copilot -- added phase-67 TODOs for (1) single best next-step CTA, (2) investigation/validation eligibility guardrails, (3) queue progression constraints, and (4) trend/learning observability fixes based on code + notebook data audit

### Deep-dive investigation: report Discovery Rankings duplication + learning ambiguity
- [x] `.current_work.md` -- GitHub Copilot -- added phase-68 TODOs for fingerprint dedup/grouping, report action gating, per-row stability wiring, and fingerprint-aware learning-weight robustness after DB audit (top-20 report rows concentrated in repeated fingerprints)

### Live feed persistence for generation summaries
- [x] `scientist/runner.py` -- GitHub Copilot -- persisted evolution/novelty generation events as notebook `live_feed` entries with replay payload metadata
- [x] `scientist/api.py` -- GitHub Copilot -- added `/api/live-feed` endpoint to replay persisted live-feed events (chronological order)
- [x] `dashboard/src/components/LiveFeed.js` -- GitHub Copilot -- hydrates recent feed history from `/api/live-feed` before attaching SSE stream
- [x] `tests/test_integration.py` -- GitHub Copilot -- added API regression test for `/api/live-feed` and updated frontend API allowlist
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestAPI::test_api_live_feed tests/test_integration.py::TestDashboardConsistency::test_no_orphaned_api_fetch_urls tests/test_integration.py::TestDashboardConsistency::test_frontend_api_routes_exist_in_backend -x --tb=short` (3 passed)
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestDashboardConsistency -x --tb=short` (9 passed)

### UX clarification: run-active strip across all tabs
- [x] `dashboard/src/App.js` -- GitHub Copilot -- added a global "Run active" status strip (status + experiment id hint + "Open live view" shortcut) so tab switches do not appear to stop background search
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestDashboardConsistency -x --tb=short` (9 passed)

### Mathspace trust-score explainability refinement (UI legend)
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- added compact trust-score formula legend in Mathspace Impact card
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added matching trust-score formula legend in report impact section
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestDashboardConsistency -x --tb=short` (9 passed)

### Mathspace trust-ranking enhancement (top operators)
- [x] `scientist/analytics.py` -- GitHub Copilot -- added canonical operator trust scoring/labeling and `top_trustworthy_operators` in `mathspace_operator_impact()`
- [x] `scientist/api.py` -- GitHub Copilot -- added endpoint default for `top_trustworthy_operators`
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- surfaced top trustworthy operator chips and trust-% column in Mathspace Impact card
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- surfaced top trustworthy operator chips and trust-% column in report impact section
- [x] `tests/test_integration.py` -- GitHub Copilot -- extended mathspace impact and report schema assertions for trust fields/rankings
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestAPI::test_api_analytics_mathspace_impact_schema tests/test_integration.py::TestAPI::test_api_report_schema -x --tb=short` (2 passed)

### Mathspaces phase 3: analytics + report visibility for operator impact
- [x] `scientist/analytics.py` -- GitHub Copilot -- added canonical `mathspace_operator_impact()` slice (per-operator and per-family tested counts, S1/validation pass rates, baseline-win rates, novelty means) with schema-resilient DB column fallback for legacy notebooks
- [x] `scientist/api.py` -- GitHub Copilot -- added `/api/analytics/mathspace-impact` endpoint and attached `mathspace_operator_impact` to `/api/report`
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- added `MathspaceImpact` card and endpoint fetch wiring for `/api/analytics/mathspace-impact`
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added `MathspaceOperatorImpact` evidence section sourced from report payload
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_api_analytics_mathspace_impact_schema`, extended `test_api_report_schema`, and updated dashboard endpoint allowlist coverage
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestAPI::test_api_analytics_mathspace_impact_schema tests/test_integration.py::TestAPI::test_api_report_schema tests/test_integration.py::TestDashboardConsistency::test_no_orphaned_api_fetch_urls tests/test_integration.py::TestDashboardConsistency::test_frontend_api_routes_exist_in_backend -x --tb=short` (4 passed)

### Mathspaces phase 2 expansion: one new operator per family
- [x] `mathspaces/hyperbolic.py` -- GitHub Copilot -- added `execute_hyp_tangent_nonlinear` (log-map → tanh → exp-map manifold nonlinearity)
- [x] `mathspaces/tropical.py` -- GitHub Copilot -- added `execute_tropical_center` (sequence-wise tropical baseline centering)
- [x] `mathspaces/padic.py` -- GitHub Copilot -- added `execute_padic_gate` (smooth valuation-based gating)
- [x] `mathspaces/clifford.py` -- GitHub Copilot -- added `execute_grade_mix` (vector+bivector blended projection)
- [x] `mathspaces/registry.py` -- GitHub Copilot -- registered new primitives: `hyp_tangent_nonlinear`, `tropical_center`, `padic_gate`, `grade_mix`
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_mathspace_phase2_ops_registered` and `test_mathspace_phase2_ops_execute_shape_and_finite`
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestPackageWiring::test_mathspace_registry_includes_hyp_distance tests/test_integration.py::TestPackageWiring::test_external_op_nonfinite_sanitization_and_telemetry tests/test_integration.py::TestPackageWiring::test_mathspace_phase2_ops_registered tests/test_integration.py::TestPackageWiring::test_mathspace_phase2_ops_execute_shape_and_finite -x --tb=short` (4 passed)

### Mathspaces phase 1 expansion: missing op registration + stability telemetry
- [x] `mathspaces/registry.py` -- GitHub Copilot -- added missing `hyp_distance` primitive registration (2-input hyperbolic distance op) with execute binding
- [x] `synthesis/compiler.py` -- GitHub Copilot -- added external-op output sanitization + lightweight telemetry (`calls`, `nonfinite_elements`, `sanitized_calls`) for custom math-space execute functions
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_mathspace_registry_includes_hyp_distance` and `test_external_op_nonfinite_sanitization_and_telemetry`
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestPackageWiring::test_mathspace_registry_includes_hyp_distance tests/test_integration.py::TestPackageWiring::test_external_op_nonfinite_sanitization_and_telemetry -x --tb=short` (2 passed)

### Report correctness fix: S1 survivor/pass-rate key mapping
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- fixed executive summary and markdown export to read Stage-1 survivors from canonical `summary.stage1_survivors` with legacy fallback `summary.total_s1_passed`
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- corrected survival-rate denominator key to use canonical `summary.total_programs_evaluated` (with legacy fallback)
- [x] `tests/test_integration.py` -- GitHub Copilot -- updated StrategyAdvisor regression assertions for current tier logic and added `test_research_report_uses_stage1_survivors_summary_key`
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestDashboardConsistency::test_strategy_advisor_breakthrough_count_uses_tier tests/test_integration.py::TestDashboardConsistency::test_research_report_uses_stage1_survivors_summary_key tests/test_integration.py::TestAPI::test_api_report tests/test_integration.py::TestAPI::test_api_report_schema -x --tb=short` (4 passed)

### Report correctness verification (no code changes required)
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- reviewed report component logic for consistency with recent overview/count fixes; no stale breakthrough heuristic detected
- [x] `tests/test_integration.py` -- GitHub Copilot -- validated report endpoint contract with focused tests
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestAPI::test_api_report tests/test_integration.py::TestAPI::test_api_report_schema -x --tb=short` (2 passed)

### Terminology alignment across Overview and Leaderboard (copy-only)
- [x] `dashboard/src/App.js` -- GitHub Copilot -- aligned nav labels to simplified wording (`Runs`, `Candidates (All)`, `Decisions (Curated)`)
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- aligned explanatory copy to the updated tab names and clarified decision-view heading
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestDashboardConsistency -x --tb=short` (8 passed)

### Overview summary-card readability pass (copy only)
- [x] `dashboard/src/components/SummaryCards.js` -- GitHub Copilot -- simplified metric labels (`Runs`, `Candidates Tested`, `Pass Rate`, `Novelty Peak`) and shortened helper text for faster glanceability while preserving data and thresholds
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestDashboardConsistency -x --tb=short` (8 passed)

### Overview microcopy simplification (novice-first wording, no behavior changes)
- [x] `dashboard/src/App.js` -- GitHub Copilot -- clarified overview labels: `Experiment setup (advanced)` disclosure title, helper text for advanced setup, and `Activity` tab labels (`Live updates` / `Recent trends`)
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- simplified CTA wording to `Apply recommended action` and `Review in Leaderboard`
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestDashboardConsistency -x --tb=short` (8 passed)

### Overview UX simplification while preserving functionality
- [x] `dashboard/src/App.js` -- GitHub Copilot -- simplified overview flow by (1) collapsing full control surface under an `Advanced controls` disclosure (auto-open when running/expert), (2) introducing a single tabbed `Activity` panel (`Live` / `Recent`) instead of mode-dependent panel swapping, and (3) reducing compact Top Programs rows from 5 to 3 for faster scanning
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestDashboardConsistency -x --tb=short` (8 passed)

### Overview regression coverage: StrategyAdvisor breakthrough count consistency
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_strategy_advisor_breakthrough_count_uses_tier` to lock that StrategyAdvisor counts breakthroughs from canonical `tier` classification (with legacy fallback retained)
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestDashboardConsistency::test_strategy_advisor_breakthrough_count_uses_tier -x --tb=short` (1 passed)

### Overview count alignment: breakthrough headline matches stage tier count
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- switched breakthrough classification to authoritative `tier === "breakthrough"` (with heuristic fallback only when tier is missing) so `Export/Publish ... Breakthroughs` and pipeline badge counts stay consistent
- [x] Validation -- GitHub Copilot -- VS Code diagnostics check: no errors in `StrategyAdvisor.js`

### P1 recursive-learning closure: gating-behavior diagnostics visibility (phase 66)
- [x] `scientist/analytics.py` -- GitHub Copilot -- added canonical `gating_behavior_diagnostics()` aggregation with gate entropy means, collapse-risk buckets, per-mode token-retention summaries, and percentile retention curves
- [x] `scientist/api.py` -- GitHub Copilot -- added `/api/analytics/gating-diagnostics` endpoint and included `gating_behavior_diagnostics` in `/api/report`
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- added scientist-facing `GatingBehaviorDiagnostics` card and wired diagnostics fetch to canonical endpoint payload
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- expanded per-candidate gating diagnostics with explicit collapse-risk labels and token-retention curve rendering
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_api_analytics_gating_diagnostics_schema`, extended `test_api_report_schema`, and updated frontend route allowlist coverage
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py::TestAPI::test_api_analytics_gating_diagnostics_schema tests/test_integration.py::TestAPI::test_api_report_schema tests/test_integration.py::TestDashboardConsistency::test_frontend_api_routes_exist_in_backend -x --tb=short` (3 passed)

### P1 hypothesis-review closure: provenance coverage across notebook + campaign views (phase 65)
- [x] `scientist/runner.py` -- GitHub Copilot -- added `_build_hypothesis_metadata()` and propagated provenance metadata (`source`, `llm_used`, `fallback_used`, `used_context`, `review_status`, `confidence`, `critique`) across investigation/validation/evolution/novelty/scale-up start paths and continuous non-synthesis starts
- [x] `scientist/notebook.py` -- GitHub Copilot -- normalized hypothesis entry logging to derive confidence from `confidence` or `critique_confidence` and summarize structured critique payloads in entry content
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- normalized provenance rendering to handle structured critique objects and critique-confidence fallback
- [x] `dashboard/src/components/LabNotebook.js` -- GitHub Copilot -- expanded provenance label support and robust critique/confidence rendering for structured or string critique metadata
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_start_investigation_records_hypothesis_provenance_metadata` and `test_start_evolution_records_llm_provenance_metadata`
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "start_experiment_records_hypothesis_provenance_metadata or start_investigation_records_hypothesis_provenance_metadata or start_evolution_records_llm_provenance_metadata" -q` (3 passed)

### P1 hypothesis-review closure: manual-start context enforcement for LLM hypothesis generation (phase 64)
- [x] `scientist/llm/context.py` -- GitHub Copilot -- added `build_manual_start_fallback_context()` to guarantee non-empty, testability-focused context when history/analytics context cannot be loaded
- [x] `scientist/runner.py` -- GitHub Copilot -- enforced context passage for manual `start_experiment` when LLM backend is available and wired fallback context both on context-build failure and empty-context edge cases
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_start_experiment_enforces_context_when_llm_available` and validated existing manual-context test path
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "start_experiment_builds_context_for_hypothesis or start_experiment_enforces_context_when_llm_available" -q` (2 passed)

### Scientist verdict-gap closure: explicit preflight critique gate visibility (phase 63)
- [x] `scientist/persona.py` -- GitHub Copilot -- normalized `critique_hypothesis()` output with explicit `gate` (`pass|warn|fail`) and criterion-level `checks` (`testability`, `measurable_metric`, `confound_risk`, `fallback_plan`) for deterministic API/UI consumption
- [x] `scientist/runner.py` -- GitHub Copilot -- persisted preflight critique provenance (`preflight_critique`, `critique`, `critique_confidence`, `review_status=preflight_*`) in manual `start_experiment` metadata path
- [x] `scientist/api.py` -- GitHub Copilot -- extended `/api/experiments/start` response with `hypothesis_critique` and `hypothesis_review_gate` for explicit gate visibility at launch
- [x] `dashboard/src/components/ControlPanel.js` -- GitHub Copilot -- added preflight gate badge plus criterion pass/warn/fail chips in Hypothesis Review panel
- [x] `tests/test_integration.py` -- GitHub Copilot -- added persona/API coverage for critique gate schema and updated metadata provenance expectation to preflight review status
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "hypothesis_critique_returns_gate_and_checks or api_start_returns_preflight_critique_gate or start_experiment_records_hypothesis_provenance_metadata" -q` (3 passed)

### Scientist verdict-gap closure: consolidated routing-mode comparative evidence (phase 62)
- [x] `scientist/analytics.py` -- GitHub Copilot -- added `routing_mode_comparison()` with canonical per-mode aggregates including sample-size/confidence/stability/efficiency labels and normalized `uniform` baseline mode; routed `routing_health()` through canonical comparison
- [x] `scientist/api.py` -- GitHub Copilot -- added `/api/analytics/routing-comparison` endpoint and included `routing_mode_comparison` in `/api/report`
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- consumed canonical routing comparison payload and added explicit Sample / Conf Label / Stability columns in Routing Health card
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- upgraded Routing Mode Comparison card to use canonical comparison payload with sample/confidence/stability labels and token retention
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_api_analytics_routing_comparison_schema`, extended routing-health schema assertions for confidence labels, and updated report schema + API fetch allowlist
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "routing_health or routing_comparison or api_report_schema or no_orphaned_api_fetch_urls" -q` (5 passed)

### Scientist verdict-gap closure: compression tradeoff instrumentation (phase 61)
- [x] `scientist/analytics.py` -- GitHub Copilot -- added `compression_coverage()` canonical aggregation (tested/surviving counts, survival shares, avg compression ratio, memory footprint, quality-retention, loss summary) and expanded canonical mechanism detection to include `token_mixing=compressed_attention`
- [x] `scientist/api.py` -- GitHub Copilot -- added `/api/analytics/compression-coverage` endpoint for scientist-facing compression evidence
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- switched Compression Technique Coverage to backend canonical payload with explicit ratio/memory/quality-retention columns and tested/survivor context
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_api_analytics_compression_coverage_schema` and updated API fetch allowlist for `/api/analytics/compression-coverage`
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "compression_coverage or no_orphaned_api_fetch_urls or api_analytics_math_family_coverage_schema or api_report_schema" -q` (4 passed)

### Scientist verdict-gap closure: QKV alternatives clarity in candidate/report views (phase 60)
- [x] `dashboard/src/components/TopPrograms.js` -- GitHub Copilot -- replaced heuristic-only QKV flags with explicit `qkv_usage` descriptor badges (Full QKV / Q=K=V / QKV-free) and clear fallback labeling
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- aligned quality flags to explicit `qkv_usage` explainability labels with candidate-level tooltip descriptions
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added scientist-facing QKV legend in Discovery Rankings and per-candidate QKV label chip in report rows, plus expanded Alternatives-to-Attention explanatory text
- [x] Validation -- GitHub Copilot -- VS Code diagnostics check: no errors in `TopPrograms.js`, `Leaderboard.js`, and `ResearchReport.js`

### Scientist verdict-gap closure: first-class functional-family evidence in scientist views (phase 59)
- [x] `scientist/api.py` -- GitHub Copilot -- added `math_family_coverage` to `/api/report` payload for scientist-facing report consumption
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added `FunctionalFamilyEvidence` card with functional/exotic coverage counts, survival evidence, and family breakdown table
- [x] `tests/test_integration.py` -- GitHub Copilot -- extended `test_api_report_schema` to require `math_family_coverage` section shape (`families`, `totals`)
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_report_schema or math_family_coverage" -q` (3 passed)

### Scientist verdict-gap closure: backend canonical metrics + packet export completeness (phase 58)
- [x] `scientist/analytics.py` -- GitHub Copilot -- added canonical backend metric builders (`canonical_compression_metrics`, `reproducibility_packet_status`) with stable definitions for compression ratio/memory/quality retention and packet completeness
- [x] `scientist/api.py` -- GitHub Copilot -- attached canonical `compression_metrics` + `reproducibility_packet` to `/api/programs`, `/api/programs/<result_id>`, `/api/leaderboard`, `/api/report` top rows, and `/api/dashboard` top rows; extended `/api/reproducibility-manifest/<result_id>` with `canonical_metrics.compression` and `packet_status`
- [x] `tests/test_integration.py` -- GitHub Copilot -- extended schema checks for programs/program detail/leaderboard/report and added `test_api_reproducibility_manifest_schema`
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_programs or api_program_detail or api_leaderboard_entry_schema or api_report_schema or api_reproducibility_manifest_schema" -q` (6 passed)

### Scientist verdict-gap progress: decision-grade confidence framing + repro packets (phase 57)
- [x] `dashboard/src/components/Leaderboard.js` -- GitHub Copilot -- added per-row promotion-confidence scoring (evidence completeness, uncertainty, repeat runs) and reproducibility packet readiness summary
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- added report-level decision evidence stats (decision-ready counts, baseline win-rate with 95% Wilson CI, average promotion confidence, repro packet completeness) plus per-program confidence/repro indicators in Discovery Rankings
- [x] Validation -- GitHub Copilot -- VS Code diagnostics check: no errors in `Leaderboard.js` and `ResearchReport.js`

### Program row-click crash fix (Top Programs + Leaderboard) (phase 56)
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- normalized `routing_expert_utilization_json` parsing to support array/object payloads and prevented non-array `.map` crash in `GatingDiagnostics`
- [x] Validation -- GitHub Copilot -- VS Code diagnostics check: no errors in `ProgramDetail.js`

### Program row-click blank-page hardening follow-up (phase 55)
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- added non-object node guards in `detectGatingOps` to prevent modal render crash on malformed graph nodes
- [x] `dashboard/src/components/GraphViewer.js` -- GitHub Copilot -- hardened graph normalization for dict/array node payloads, filtering invalid node entries and guarding edge derivation
- [x] Validation -- GitHub Copilot -- VS Code diagnostics check: no errors in `ProgramDetail.js` and `GraphViewer.js`

### Program row-click blank-page fix (phase 54)
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- fixed `TokenMixingTaxonomy` crash by normalizing graph nodes to array (`Object.values(...)` for object-form nodes) and guarding node shape before iteration
- [x] Validation -- GitHub Copilot -- VS Code diagnostics check: no errors in `ProgramDetail.js`

### P1 QKV usage enum groundwork (phase 53)
- [x] `scientist/analytics.py` -- GitHub Copilot -- added `qkv_usage_enum(program)` with deterministic enum classification (`full_qkv`, `q_eq_k_eq_v`, `qkv_free`) from morphology choices and graph ops
- [x] `scientist/api.py` -- GitHub Copilot -- attached `qkv_usage` and `uses_qkv` to `/api/programs`, `/api/programs/<result_id>`, `/api/leaderboard` entries, and report `top_programs`
- [x] `tests/test_integration.py` -- GitHub Copilot -- added schema assertions for new QKV fields on programs, program detail, leaderboard entry, and report top-program rows
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_programs or api_program_detail or api_leaderboard_entry_schema or api_report_schema" -q` (5 passed)

### P1 math-family coverage analytics (phase 52)
- [x] `scientist/analytics.py` -- GitHub Copilot -- added `math_family_coverage()` aggregation over tested/surviving programs with deterministic family classification (euclidean/hyperbolic/tropical/p-adic/clifford/functional)
- [x] `scientist/api.py` -- GitHub Copilot -- added `/api/analytics/math-family-coverage` endpoint
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- added `MathFamilyCoverage` card and endpoint fetch wiring
- [x] `tests/test_integration.py` -- GitHub Copilot -- added API + schema tests for `math-family-coverage` and updated `no_orphaned_api_fetch_urls` known endpoint list
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "math_family_coverage or no_orphaned_api_fetch_urls or analytics_routing_health_schema or grammar_can_generate_functional_primitives" -q` (5 passed)

### P1 synthesis functional primitives wiring (phase 51)
- [x] `synthesis/grammar.py` -- GitHub Copilot -- added functional-category candidate selection in `_apply_parameterized` with budget checks and op-config emission for `integral_kernel`/`fixed_point_iter`
- [x] `synthesis/compiler.py` -- GitHub Copilot -- added explicit `integral_kernel` parameter init and config-aware execution controls (`kernel_scale`, `n_iters`, `damping`) for functional ops
- [x] `synthesis/primitives.py` -- GitHub Copilot -- normalized functional primitive metadata with config keys for `integral_kernel` and `fixed_point_iter`
- [x] `tests/test_integration.py` -- GitHub Copilot -- added `test_grammar_can_generate_functional_primitives` coverage
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "MorphologicalConstraints or FunctionalArchitectureBuild or grammar_can_generate_functional_primitives" -q` (5 passed)

### P1 exotic-model-family foundation (phase 50)
- [x] `morphological_box.py` -- GitHub Copilot -- added functional family options (`integral_kernel_mixing`, `basis_expansion_layer`, `implicit_fixed_point`) with dedicated tags and structural constraints for valid functional pairings
- [x] `arch_builder.py` -- GitHub Copilot -- implemented functional modules (`IntegralKernelMixer`, `BasisExpansionLayer`, `ImplicitFixedPointLayer`) and wired them into token/channel mixer builders
- [x] `tests/test_integration.py` -- GitHub Copilot -- added morphology constraint regressions and torch build/forward smoke test for functional-family specs
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "MorphologicalConstraints or FunctionalArchitectureBuild" -q` (4 passed)

### P3 campaign criteria backend normalization (phase 49)
- [x] `scientist/analytics.py` -- GitHub Copilot -- added `campaign_success_criteria_tracker()` with normalized criteria parsing (bullet/numbered items), `%`/phrase threshold support, criterion-type inference, ratio normalization, and explicit pass/at-risk/not-yet status evaluation
- [x] `scientist/api.py` -- GitHub Copilot -- campaign detail/report endpoints now include `success_criteria_tracker` computed by analytics
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- success tracker now prefers backend `success_criteria_tracker` payload (with compatibility for `observed_text`) and retains local fallback
- [x] `tests/test_integration.py` -- GitHub Copilot -- expanded campaign detail/report schema checks to assert `success_criteria_tracker` list presence
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_campaign_detail_schema or api_campaign_report_schema or api_campaigns_list_schema or no_orphaned_api_fetch_urls" -q` (4 passed)

### P3 campaign criteria-tracker hardening (phase 48)
- [x] `dashboard/src/components/CampaignView.js` -- GitHub Copilot -- strengthened success-criteria evaluation: safer criteria splitting (no decimal breakage), support for `%` and phrase thresholds (`at least`, `no more than`, etc.), criterion-type inference, ratio normalization, and clearer pass/at-risk/not-yet gating by available evidence
- [x] `.current_work.md` -- GitHub Copilot -- released temporary claim and updated partial-status note for per-campaign success-criteria tracker
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_campaigns_list_schema or api_campaign_report_schema or no_orphaned_api_fetch_urls" -q` (3 passed)

---

## Archive date: 2026-02-17

### Moved from Open TODOs (completed items)
- Archived all completed checklist entries from phase 67 and phase 68 sections so `.current_work.md` now retains only open items.

### Moved from Recently Completed (uncommitted)
- P1 completion: unique-architecture vs rerun telemetry visibility (phase 68)
- P1 completion: fingerprint-aware grammar weighting robustness (phase 68)
- P0 completion: report repeated-architecture explainer + grouped/expanded controls (phase 68)
- P0 completion: attach per-row cross-run stability to report rankings (phase 68)
- P0 completion: gate report row actions by backend eligibility (phase 68)
- P0 completion: deduplicate report discovery rankings by fingerprint (phase 68)
- P1 completion: auto-refresh TrendCharts + adaptation linkage summaries (phase 67)
- P1 completion: stabilize trend scoring against tiny runs (phase 67)
- P1 completion: align trajectory threshold and UX message contract (phase 67)
- P0 completion: make queue progression-aware with typed intents + auto-prune (phase 67)
- P0 completion: hide/disable redundant investigation actions for unchanged candidates (phase 67)
- P0 completion: backend eligibility guardrails for investigation/validation starts (phase 67)
- P0 completion: enforce one visible best-next-step on Overview (phase 67)

### Coordination note
- `.current_work.md` was rewritten to active-only format (claims, open TODOs, concise recently completed pointer, and notes).

---

Archive date: 2026-02-20
Source: `.current_work.md` cleanup

- [x] `scientist/runner.py` -- Codex -- Fingerprint refinement intent scoring + learning-guided candidate ranking (`balanced|quality|compression|sparsity|novelty`) with metadata trace.
- [x] `dashboard/src/components/ProgramDetail.js` -- Codex -- Add explicit refine actions for balanced/compression/sparsity intents.
- [x] `tests/test_refinement_intent.py` -- Codex -- Add deterministic unit coverage for refinement intent scoring behavior.
- [x] `tests/test_integration.py` -- Codex -- Add wiring regression for ProgramDetail intent-specific refine actions/payloads.
- [x] `dashboard/src/components/ProgramDetail.js` -- Codex -- Add "Refinement Rationale" panel sourced from graph metadata (`metadata.refinement.*`) for intent/score/parent traceability.
- [x] `tests/test_integration.py` -- Codex -- Add regression coverage for Refinement Rationale panel wiring and metadata usage.
- [x] `scientist/runner.py` -- Codex -- Attach refinement `intent_score_breakdown` metadata (weighted terms/components) for UI attribution tooltip.
- [x] `dashboard/src/components/ProgramDetail.js` -- Codex -- Add intent-score component tooltip and compact top-component summary in Refinement Rationale.
- [x] `tests/test_refinement_intent.py` -- Codex -- Add score-breakdown consistency test (`sum(weighted_terms) == intent_score`).
- [x] `scientist/runner.py` -- Codex -- Strengthen default fingerprint-refinement hypothesis contract (source rule, mutation mechanism, intent weights/formula, success criteria, fallback plan) and add zero-S1 exploration mix.
- [x] `scientist/persona.py` -- Codex -- Improve rule-based refinement critique/checks for underspecified hypotheses (source rule, mutation spec, intent weights, success criteria).
- [x] `tests/test_refinement_intent.py` -- Codex -- Add regression for structured default refinement hypothesis text.
- [x] `tests/test_integration.py` -- Codex -- Add regression for refinement critique missing-spec detection.
- [x] `scientist/api.py` -- Codex -- Add `hypothesis_missing_fields` in start response, derived from critique checks/concerns.
- [x] `scientist/persona.py` -- Codex -- Add structured `missing_fields` derivation to preflight critique normalization.
- [x] `dashboard/src/components/ControlPanel.js` -- Codex -- Render explicit missing-field checklist chips in Hypothesis Review panel.
- [x] `tests/test_integration.py` -- Codex -- Add regressions for API missing-field payload and ControlPanel missing-field UI wiring.
- [x] `scientist/notebook.py` -- Codex -- Add insight-bundle trial + pairwise interaction tables/APIs (`selection_insight_trials`, `selection_insight_interactions`).
- [x] `scientist/runner.py` -- Codex -- Add insight-bundle matching, interaction-aware candidate scoring, trial recording, and pending-trial outcome resolution.
- [x] `tests/test_selection_policy.py` -- Codex -- Add tests for insight interaction learning and insight-aware scoring metadata.
- [x] `scientist/api.py` -- Codex -- Add `/api/analytics/insight-interactions` endpoint with enriched synergy/antagonism payload.
- [x] `dashboard/src/components/LearningPanel.js` -- Codex -- Add “Insight Synergy Matrix” card wired to insight-interactions analytics endpoint.
- [x] `tests/test_integration.py` -- Codex -- Add API/schema/wiring regressions for insight interaction endpoint and LearningPanel matrix.
- [x] `dashboard/src/components/TopPrograms.js` -- Codex -- Clarify program vs fingerprint copy and add deduplicated “Leading Fingerprints” summary table.
- [x] `tests/test_integration.py` -- Codex -- Add copy/wiring regression for TopPrograms clarification + leading fingerprints card.
- [x] `.current_work.md` -- Claude Code -- IA review: Information Architecture & Decision Transparency audit (2026-02-19)
- [x] `dashboard/src/components/StrategyAdvisor.js` -- Claude Code -- IA-DT-1: Add Data Source tooltips to every recommendation in computeStrategy + briefing evidence
- [x] `tests/test_integration.py` -- Claude Code -- IA-DT-1: Add regression tests for Data Source tooltip presence in strategy computation
- [x] `scientist/runner.py` -- Claude Code -- #5: Make per-cycle LLM calls rule-based in continuous mode
- [x] `scientist/runner.py` -- Claude Code -- #6: Remove analyze_results() from continuous mode
- [x] `scientist/persona.py` -- Claude Code -- #5: Force rule-based path for continuous mode methods
- [x] `scientist/api.py` -- Claude Code -- #8: Route detailed fix plans to local qwen agents, not to chat
- [x] `scientist/api.py` -- Claude Code -- #9: Add agent status summarization back to chat
- [x] `scientist/api.py` -- GitHub Copilot -- Align sparse recommendation rationale with executable sparse-driving config for strategy briefing actions.
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- Preserve full recommended-action config passthrough so sparse knobs are not dropped on execute.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add regressions for sparse-action config pass-through and sparse-targeted novelty recommendation config.
- [x] `.current_work.md` -- GitHub Copilot -- Track sparse recommendation logic investigation + implementation progress.
- [x] `.current_work.md` -- GitHub Copilot -- Track report-tab load investigation + date/theme/trend report generation proposal.
- [x] `scientist/api.py` -- GitHub Copilot -- RPT implementation: fast report mode + filtered report query endpoint (date/theme/trend).
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- RPT implementation: report builder controls + fast initial report load wiring.
- [x] `tests/test_integration.py` -- GitHub Copilot -- RPT regressions for report query endpoint and report-tab filtered-report wiring markers.
- [x] `scientist/notebook.py` -- GitHub Copilot -- RPT-4: add report snapshot persistence APIs + table for scoped report caching.
- [x] `scientist/api.py` -- GitHub Copilot -- RPT-4: wire scoped report endpoint to snapshot cache with invalidation on newer completed runs.
- [x] `tests/test_integration.py` -- GitHub Copilot -- RPT-4 regression for scoped report snapshot cache hit behavior.
- [x] `scientist/notebook.py` -- GitHub Copilot -- RPT-4b: add snapshot cache cleanup lifecycle (TTL expiry + per-scope cap) with periodic maintenance on writes.
- [x] `tests/test_integration.py` -- GitHub Copilot -- RPT-4b regression for snapshot cleanup pruning/capping behavior.
- [x] `scientist/api.py` -- GitHub Copilot -- RPT-6: add report-cache diagnostics endpoint with optional cleanup trigger and retention metadata.
- [x] `scientist/notebook.py` -- GitHub Copilot -- RPT-6: add report snapshot stats helper for diagnostics endpoint payload.
- [x] `tests/test_integration.py` -- GitHub Copilot -- RPT-6 regression for report-cache diagnostics endpoint contract.
- [x] `scientist/api.py` -- GitHub Copilot -- Fix experiment-detail JSON serialization for non-finite metric values (Infinity/NaN) in program rows.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add regression for `/api/experiments/<id>` non-finite float sanitization.
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- A0: Make strategy output action-only (no pseudo-execution text; explicit actionable vs advice-only states).
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- A0/A1: Keep chat summary-only and surface concise local-agent milestone/status summaries.
- [x] `scientist/llm/prompts.py` -- GitHub Copilot -- A0: Tighten prompt contract to forbid pseudo-execution prose and require actionable blocks or advice_only.
- [x] `tests/test_integration.py` -- GitHub Copilot -- A0/A1: Add regressions for action-only strategy text and summary-only chat/task language.
- [x] `dashboard/src/components/LiveFeed.js` -- GitHub Copilot -- Fix blank-row rendering by normalizing/filtering live-feed event types before render.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add regression assertion for LiveFeed blank-row guard (renderable-event filtering/normalization).
- [x] `scientist/api.py` -- GitHub Copilot -- Backend live-feed coherence fix so default `/api/live-feed` returns a contiguous latest-experiment stream instead of mixed cross-experiment slices.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add regression for `/api/live-feed` default latest-experiment stream selection.
- [x] `scientist/api.py` -- GitHub Copilot -- Harden SSE serialization for non-finite floats (`NaN/Inf`) with strict JSON-safe encoding.
- [x] `eval/fingerprint.py` -- GitHub Copilot -- Fix sensitivity analysis grad-context so Jacobian probe does not fail under outer `torch.no_grad()`.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add regression for SSE non-finite float sanitization.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Surface `/api/diagnostics/fingerprint` skip counters in Overview Production Readiness card for quick runtime visibility.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add/extend dashboard wiring assertions for fingerprint diagnostics panel markers.
- [x] `.current_work.md` -- GitHub Copilot -- Track claim/completion notes for fingerprint diagnostics UI wiring.
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- Add compact fingerprint diagnostics card in Learning tab using existing diagnostics endpoint data.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add/extend LearningPanel wiring assertions for fingerprint diagnostics endpoint/markers.
- [x] `.current_work.md` -- GitHub Copilot -- Track claim/completion notes for LearningPanel diagnostics wiring.
- [x] `scientist/api.py` -- GitHub Copilot -- Align strategy briefing recommended investigate/validation actions with start-mode eligibility checks to prevent execute-time ineligible result IDs.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add regression coverage ensuring briefing recommendation downgrades when suggested investigation IDs are ineligible.
- [x] `.current_work.md` -- GitHub Copilot -- Track claim/completion notes for strategy recommendation eligibility-alignment fix.
- [x] `scientist/api.py` -- GitHub Copilot -- Fix dashboard static/index fallback handling so missing frontend build does not produce 500 on `/` and `/favicon.ico`.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add regression coverage for missing static build behavior (no 500 recursion via not_found/index handlers).
- [x] `.current_work.md` -- GitHub Copilot -- Track claim/completion notes for dashboard static fallback hardening.
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- Add refinement action traceability panel (started experiment ID, source resolution, live outcomes) so refine/compression/sparsity launches are auditable.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add wiring regression for ProgramDetail refinement trace panel markers and experiment polling hook.
- [x] `.current_work.md` -- GitHub Copilot -- Track claim/completion notes for refinement traceability UX.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Clean up unrelated dashboard consistency checks (`ReportGallery` import/export style expectations) to match current component structure.
- [x] `.current_work.md` -- GitHub Copilot -- Track claim/completion notes for dashboard consistency test cleanup.
- [x] `dashboard/src/components/TopPrograms.js` -- GitHub Copilot -- Clarify row semantics (program row vs fingerprint ID) and curated destination naming for Candidates view.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Align curated tab label with Leaderboard naming to reduce navigation confusion.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Update TopPrograms copy marker assertions for clarified terminology.
- [x] `.current_work.md` -- GitHub Copilot -- Track claim/completion notes for candidate/fingerprint terminology clarification.
- [x] `dashboard/src/components/TopPrograms.js` -- GitHub Copilot -- Add tiny tooltip on Program Fingerprint ID column header clarifying rerun semantics.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add regression marker for TopPrograms fingerprint header tooltip copy.
- [x] `.current_work.md` -- GitHub Copilot -- Track claim/completion notes for fingerprint-header tooltip clarification.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Profile and reduce Overview render/poll CPU noise (dedupe repeated workflow blocks, stabilize polling effect dependencies).
- [x] `dashboard/src/components/AriaStatus.js` -- GitHub Copilot -- Enforce summary-only hypothesis rendering (strip pseudo execution/code fences from status card).
- [x] `tests/test_integration.py` -- GitHub Copilot -- Add regression assertion for AriaStatus hypothesis sanitization and maintain Overview wiring markers.
- [x] **RL-BUG-1: Fix key mismatch in persona mode recommendation** -- `persona.py:2066` reads `negative_results.get("excluded_ops")` but `negative_results_synthesis()` returns `"failed_ops"` as the key. Result: persona NEVER sees failed ops. Fix: change to `negative_results.get("failed_ops")`.
- [x] **RL-GAP-1: Feed refuted hypotheses into new hypothesis generation** -- `_get_past_hypotheses()` now also queries `insights WHERE status='refuted'` and merges them into the past hypotheses list. Context builders (`build_rich_context`, `build_mode_selection_context`) now surface refuted hypotheses with "DO NOT re-test" labels. Past hypotheses limit raised from 5→10 to accommodate both experiment outcomes and refuted insights.
- [x] **RL-GAP-2: Add soft penalties for weak ops (5-20% S1 rate)** -- Added `op_weights` dict to `GrammarConfig` for per-op weight multipliers. `negative_results_synthesis()` now computes `weak_ops` (nonzero but below 50% of mean S1 rate) with linear penalty weights (0.2→1.0). Runner passes these to grammar config. `_pick_op()` and `_apply_parameterized()` now use per-op weights in selection. Verified: relu 6 (penalized) vs 21 (normal) in controlled test.
- [x] **RL-GAP-3: Add selection decision feedback loop** -- Added `decision_outcome_analysis()` to analytics.py that joins `selection_decisions` with `experiments` to compute per-mode success rates, consecutive failures, and penalty multipliers (0.3→1.0). Wired into `_gather_analytics_data()`. Added `_apply_decision_feedback()` to persona.py that adjusts confidence by mode penalty and warns on 3+ consecutive failures. All mode recommendations (LLM and rule-based) now pass through the feedback loop.
- [x] **RL-GAP-4: Make refuted hypotheses block similar re-generation** -- Added `set_refuted_hypotheses()`, `_check_refuted_overlap()` (Jaccard token similarity, threshold=0.45), and `_tokenize_hypothesis()` to persona. Runner calls `_populate_refuted_cache()` before hypothesis generation (both manual and continuous paths) to merge refuted insights from notebook + negative_results_synthesis. `_rule_based_critique()` now flags hypotheses similar to refuted ones with concern + suggestion to pivot. Verified: near-duplicate caught at 50% sim, unrelated passes clean.
- [x] Op exclusion via `runner.py:6236-6240` (0% S1 + confidence >= 0.7 → hard filter)
- [x] Grammar weight learning via `analytics.py:650-708` (quadratic S1 rate ratio)
- [x] Failure pattern tracking via `analytics.py:3210-3228` (top 10 error types)
- [x] Anti-pattern correlations via `analytics.py:3230-3244` (negative structural features)
- [x] Refuted hypothesis collection via `analytics.py:3246-3263` (insights table query)
- [x] Knowledge extraction includes refuted via `runner.py:3837-3838`
- [x] Stagnation agent uses failed ops via `runner.py:1636-1651`
- [x] Campaign reports count refuted via `persona.py:2778`
- [x] Anti-pattern knowledge entries from refuted hypotheses via `persona.py:2730-2743`
- [x] **UX-1: Add fingerprint detail links in Production Readiness workflow + top candidates** -- `dashboard/src/App.js`, `scientist/api.py`
- [x] **UX-2: Clarify curated leaderboard naming to reflect program-focused data** -- `dashboard/src/App.js`, `dashboard/src/components/Leaderboard.js`
- [x] **UX-3: Add score-context tooltip explaining 0-100 is internal composite, not GPT replacement** -- `dashboard/src/components/Leaderboard.js`
- [x] **EP-1: Add Evidence Pack schema + validation gate for recommendations** -- `scientist/evidence.py`, `scientist/runner.py`, `scientist/api.py`
- [x] **EP-2: Add audit enforcement for grammar weight changes (query + entry)** -- `scientist/analytics.py`, `scientist/notebook.py`, `scientist/runner.py`
- [x] **EP-3: Add novelty-claim guardrails (CKA reference + similarity path)** -- `eval/fingerprint.py`, `eval/cka_references.py`, tests
- [x] **EP-4: CI checks + tests for Evidence Pack + audit requirements** -- `tests/test_evidence_pack.py`
- [x] **SP-1: Add multi-objective evidence-based scoring + family uncertainty policy** -- `scientist/runner.py`
- [x] **SP-2: Add durable selection decision logging APIs and family stats persistence** -- `scientist/notebook.py`
- [x] **SP-3: Add decision-log JSON schema + validation helper** -- `scientist/evidence.py`, `scientist/selection_decision.schema.json`
- [x] **SP-4: Add tests for deterministic scoring and plateau safety-valve triggering** -- `tests/test_selection_policy.py`
- [x] **NI-1: Add novelty reference versioning + validity metadata in fingerprint/metrics** -- `eval/fingerprint.py`, `eval/metrics.py`
- [x] **NI-2: Add novelty calibration persistence + baseline calibration utilities** -- `scientist/notebook.py`, `eval/novelty_calibration.py`
- [x] **NI-3: Enforce novelty validity in promotion-critical paths** -- `scientist/runner.py`
- [x] **NI-4: Add automated novelty integrity checker + tests** -- `tools/novelty_integrity_check.py`, `tests/test_novelty_integrity.py`
- [x] **HP-1: Add preregistration model + SQLite tables** -- `scientist/preregistration.py`, `scientist/notebook.py`
- [x] **HP-2: Enforce preregistration before batch execution paths** -- `scientist/runner.py`
- [x] **HP-3: Link experiment detail API and dashboard rationale card** -- `scientist/api.py`, `dashboard/src/components/ExperimentDetail.js`
- [x] **HP-4: Add blocking + linkage tests** -- `tests/test_preregistration.py`
- [x] **CH-1: Add healer module with task state machine + sandbox policy loading** -- `healer/core.py`, `healer/healer_config.json`
- [x] **CH-2: Add SQLite healer task/event logs linked to triggering experiment** -- `scientist/notebook.py`
- [x] **CH-3: Wire runner triggers (exceptions, integrity failures, plateau) to healer invocations** -- `scientist/runner.py`
- [x] **CH-4: Add tests for healer workflow + command guardrails** -- `tests/test_code_healer.py`
- [x] **CH-5: Add API + dashboard visibility for healer task status** -- `scientist/api.py`, `dashboard/src/App.js`, `tests/test_integration.py`
- [x] **IA-DT-1a: Add `dataSources` array to every `computeStrategy()` return path** -- Each of the 10 priority rules in `computeStrategy()` must include a `dataSources` array with entries like `{ metric, value, threshold, comparison, source }` that precisely explains which data point triggered this recommendation.
- [x] **IA-DT-1b: Add `dataSources` extraction from briefing evidence** -- When AI-powered briefing is active, extract data sources from `briefing.evidence` fields (sparse_coverage, learning_trend, avg_recent_s1_rate, pipeline counts) into the same `dataSources` format.
- [x] **IA-DT-1c: Render Data Source tooltip on Recommended Action badge** -- Display a hover tooltip on the "Recommended Action" label showing all data sources as structured lines: "Triggered because: {metric} = {value} ({comparison} {threshold})". Each line links to its source tab.
- [x] **IA-DT-1d: Add regression tests** -- Test that every `computeStrategy()` return includes non-empty `dataSources`, and that briefing evidence produces matching source entries.
- [x] **P0-1: Replace `window.alert/confirm` with inline UI** -- `ExperimentList.js` lines 177, 198 use `window.confirm()` / `window.alert()` for cancel/rerun. Use inline confirmation state toggle + error rendering instead.
- [x] **P0-2: Remove `querySelector` DOM manipulation in StrategyAdvisor** -- `StrategyAdvisor.js` line 751-753 directly mutates a `<details>` element via `document.querySelector('.overview-left details')`. Lift the open state into App.js as a prop + callback.
- [x] **P0-3: Fix undefined `var(--border-color)` CSS token** -- 12+ instances across `StrategyAdvisor.js` and `AriaChatPanel.js` use `var(--border-color)` but only `var(--border)` is defined in App.css. Find-replace all to `var(--border)`.
- [x] **P0-4: Add loading skeleton on initial dashboard fetch** -- `App.js` has no loading state; `SummaryCards` returns `null` when data is empty causing layout shift. Add `isLoading` state + skeleton/spinner.
- [x] **P0-5: Collapse Leaderboard header wall of text** -- `Leaderboard.js` lines 570-597 render 7 `<p>` explanation paragraphs before data. Collapse into a `<details>` element, keep one-liner visible.
- [x] **P1-1: Group ControlPanel mode buttons** -- 7 mode buttons in single flex row wraps badly. Split into Primary (Single/Continuous) + collapsible Advanced (Evolution, Novelty, etc.).
- [x] **P1-2: Add active visual state for Novice/Expert toggle** -- `App.js` mode buttons use `aria-pressed` but no CSS differentiates pressed state. Add `[aria-pressed="true"]` style.
- [x] **P1-3: Add tab nav overflow handling** -- 13 tabs + separators in single row clip on <1400px. Add `overflow-x: auto; white-space: nowrap` + scroll hint.
- [x] **P1-4: Consolidate 3 EventSource connections into shared context** -- `LiveFeed.js`, `AriaChatPanel.js`, `StrategyAdvisor.js` each open independent `/api/events` SSE connection. Create `EventBusContext` + `useEventBus()` hook.
- [x] **P1-5: Add x close button to error banner** -- `App.js` error banner only dismisses via click with tiny "Click to dismiss" text. Add explicit close button + Escape key handler.
- [x] **P1-6: Humanize cycle activity card for Novice mode** -- Raw jargon like `ΔS1 0`, `synthesis`, `n/a` shown in Overview. Replace with natural language: `0 new survivors`, relative timestamps.
- [x] **P1-7: Differentiate chat message roles visually** -- `AriaChatPanel.js` user/aria/system messages look identical except for tiny label. Right-align user messages, center system messages (standard chat pattern).
- [x] **P1-8: Group ControlPanel advanced settings into fieldsets** -- 30+ config fields in Expert mode with no grouping. Add fieldset/section headers: Timing, Graph Gen, Automation, Pipeline.
- [x] **P1-9: Improve progression queue UX** -- Queue banner shows counts not contents. Clear Queue button sits at same visual weight as constructive actions — make it visually distinct (red).
- [x] **P1-10: Fix font-family fallback** -- `App.css` line 23 has `monospace` as last fallback instead of `sans-serif`.
- [x] **P2-1: Add `:focus-visible` styles** -- No keyboard focus indicators defined anywhere. Add global `outline: 2px solid var(--accent-blue)` rule.
- [x] **P2-2: Add shape/symbol alongside color for pass/fail** -- Color is sole differentiator (WCAG 1.4.1 violation). Add check/x/! prefixes to status badges.
- [x] **P2-3: Fix tab delta badge coloring** -- Always green regardless of direction. Loss increases should show red.
- [x] **P2-4: Clarify or remove AriaStatus "Energy" metric** -- Opaque persona metric with no tooltip/explanation.
- [x] **P2-5: Deduplicate `reliabilityColor`/QKV functions** -- Duplicated across Leaderboard, TopPrograms, ExperimentList. Move to `utils/colors.js` / `utils/architecture.js`.
- [x] **P2-6: Fix Activity sub-tab active state** -- Uses barely-perceptible opacity change. Use proper active styling (blue border/text).
- [x] **P2-7: Remove redundant SummaryCards description paragraph** -- "Quick snapshot of..." restates what the card title already says.
- [x] **P2-8: Add `aria-label` to chat input** -- Missing accessible label for screen readers.
- [x] **P2-9: Add accessible labels to StrategyAdvisor pipeline badges** -- Arrow separators need `aria-hidden`, counts need semantic markup.
- [x] **P2-10: Fix LiveFeed double-card nesting** -- LiveFeed wraps itself in `.card` but is already inside a `.card` in Overview.
- [x] **P2-11: Style `<details>` disclosure triangle consistently** -- Browser default varies; replace with custom arrow matching ControlPanel pattern.
- [x] **P2-12: Humanize cycle history entry format** -- `#3 · synthesis · completed · ΔS1 0` to `Cycle 3 — synthesis — 0 new survivors (2m ago)`.
- [x] **RPT-1: Split `/api/report` into fast summary + lazy heavy sections** -- Done in `scientist/api.py` + `ResearchReport.js`: `/api/report` now supports `fast`, `include_heavy`, and fast-first load path from report tab.
- [x] **RPT-2: Remove synchronous narrative generation from critical path** -- Done in `scientist/api.py`: narrative generation is now opt-in via `include_narrative`; fast/report-query paths default to no narrative.
- [x] **RPT-3: Add report query endpoint with date/theme/trend filters** -- Done in `scientist/api.py`: `/api/report/query` with `start_date`, `end_date`, `theme`, `trend`, `limit`, and query metadata in response.
- [x] **RPT-4: Persist report snapshots for reuse** -- Done: added `report_snapshots` persistence in `scientist/notebook.py` and wired `/api/report/query` in `scientist/api.py` to read/write cache entries keyed by query hash + invalidated by latest completed experiment timestamp.
- [x] **RPT-5: Report tab UX for scoped generation** -- Done in `ResearchReport.js`: added builder controls (Date Range, Theme, Trend, Top-K), `Generate Scoped Report`, `Reset to Fast Overview`, and `Load Full Details` actions.
- [x] **RPT-6: Add report-cache diagnostics visibility** -- Done in `scientist/api.py` + `scientist/notebook.py`: added `/api/diagnostics/report-cache` with cache stats (counts/ages/by-scope), retention settings, and optional cleanup trigger.
- [x] **Gradient flow pre-check** -- Added fail-fast zero-gradient precheck at Stage-1 step 0 in `_micro_train`; runs with `zero_grad_precheck_failed` now stop immediately to cut wasted training cycles.
- [x] **Simplicity constraints** -- Added evolve/novelty prescreen simplicity hardening in `runner.prescreen_run_config`: under auto-harden, cap `max_depth` to 3 and `max_ops` to 5.
- [x] **Grammar weight floor** -- Confirmed enforced in `analytics._compute_weights_from_stats` via `round(max(0.5, min(8.0, weight)), 2)`; category starvation floor is active.
- [x] **Mood model grounding** -- Updated `_update_mood_from_results` so `triumphant` requires both novelty and Stage-1 survivors (`n_pass_s1 > 0 and novel > 0`).
- [x] **Exploit winning ops** -- Added champion-bias pass in `runner._execute_experiment` that detects reliable winning ops and boosts structural/sequence category weights for search.
- [x] **Projection ops are the real goldmine** -- Champion-bias now detects reliable projection-op winners (`low_rank_proj`, `shared_basis_proj`, `tied_proj`) and boosts `parameterized` category weight.
- [x] **Frequency domain ban/suppression** -- Added hard category cap in grammar-weight synthesis: `frequency_domain` now clamps to `<= 0.1` in learned weights.
- [x] **Sparse ops underexplored** -- Champion-bias now detects reliable sparse-op winners (`nm_sparse_linear`, `block_sparse_linear`, `semi_structured_2_4_linear`) and boosts `parameterized` search weight.
- [x] **SRC-1: Force sparse-driving knobs when sparse coverage is below target** -- Done in `scientist/api.py`: added sparse coverage summary + config augmentation so low sparse-share recommendations include executable sparse knobs (`morph_focus_sparse`, `morph_ratio`, `use_synthesized_training`, `morph_sparse_weight_storage`, etc.) and deterministic sparse-focused novelty config.
- [x] **SRC-2: Preserve full suggested action config on execute** -- Done in `dashboard/src/components/StrategyAdvisor.js`: start payload now preserves full `suggested_config` keys (except intentional extraction of `hypothesis`/`result_ids`) so sparse knobs are not dropped.
- [x] **SRC-3: Add regression coverage for sparse recommendation execution path** -- Done in `tests/test_integration.py`: added API regression for low sparse-coverage augmentation and UI wiring regression for full suggested-config passthrough and sparse coverage evidence markers.
- [x] **P0: Add production-readiness gate summary in API/dashboard** -- Done in `scientist/api.py`: `/api/dashboard` now returns `production_readiness` with breakthrough counts, decision-ready counts, confidence/repro/CKA coverage, and top-candidate readiness snapshots.
- [x] **P0: Add epic-switch recommendation signal** -- Done in `scientist/api.py`: deterministic `epic_switch_recommendation` (`stay_current_epic` vs `switch_to_scale_up_epic`) with rationale based on readiness gates.
- [x] **P1: Add a dedicated Production Readiness panel in dashboard** -- Added Overview card in `dashboard/src/App.js` driven by `/api/dashboard` `production_readiness` (epic-switch recommendation, readiness counts, top-candidate blockers) with wiring assertion in `tests/test_integration.py`.
- [x] **P1: Add scale-up run template(s)** -- Added deterministic production-readiness templates in `scientist/api.py` (multi-seed stress, robustness re-check, scale-up+efficiency) and one-click run controls in `dashboard/src/App.js`.
- [x] **P1: Add reproducibility-completion workflow** -- Guided path from repro packet `3/6` → `6/6` including manifest/benchmark checks and artifact-backed CKA.
- [x] **Investigate Aria self-correction capabilities** -- Done: chat actions (adjust_config, adjust_grammar, edit_file, spawn_agent) let Aria act on issues she identifies. Codebase agent can do multi-file fixes.
- [x] **Aria autonomous fix loop** -- Done: chat action dispatch executes config/grammar/code changes directly from Aria's LLM responses with safety rails.
- [x] **Aria agent spawning** -- Done: `spawn_agent` action type delegates to existing codebase agent infrastructure.
- [x] **Aria local model sub-agents for cost control** -- Done: Codex implemented Ollama local planner, gated to remote-only primary LLM, with VRAM limits.
- [x] **Local Ollama: fix default model** -- Changed default from `qwen2.5-coder:3b` to `qwen2.5-coder:7b-instruct`.
- [x] **Local Ollama: offload model after task** -- Added `_ollama_offload_model()` using `/api/generate` with `keep_alive: 0`. Called after every planner run (success or failure).
- [x] **Local Ollama: increase subprocess timeout** -- Bumped from 90s to 120s.
- [x] **Local Ollama: multi-3b swarm option** -- Added `qwen2.5-coder:3b` swarm planner mode with up to 3 workers based on VRAM capacity (`ARIA_LOCAL_OLLAMA_3B_MAX_WORKERS`, `ARIA_LOCAL_OLLAMA_MAX_VRAM_GB`, model-size estimate).
- [x] **Local Ollama: 3b-vs-7b routing policy** -- Added policy in code-agent planner: simple read/small-fix goals prefer 3b swarm then fallback to 7b; complex goals prefer 7b single planner.
- [x] **Aria stale-data guardrails** -- Added snapshot-key guard in chat analysis refresh; when idle and no evidence changed, emits one concise "No new evidence since last analysis snapshot." notice and skips full regeneration.
- [x] **Aria run-cause audit trail** -- Done by Codex: `run_trigger_source` tracking for all launch paths exposed in `/api/progress`, `/api/status`, `/api/aria/tools`.
- [x] **Aria analysis cadence control** -- Added chat header cadence selector (`Auto: Off`, `Auto: Run-only`, `Auto: Always`) with localStorage persistence and run-loop gating (default `run-only`).
- [x] **Expose compression signals in briefing payload** -- Added compression coverage summary to `/api/strategy/briefing` `data` and `evidence` payload blocks.
- [x] **Add compactness-focused deterministic action** -- Added deterministic `compact_synthesis` action + rationale + compact `suggested_config` when compression coverage is under-explored.
- [x] **Add compression opportunities API endpoint** -- Added `/api/analytics/compression-opportunities` with ranked opportunities and executable config suggestions.
- [x] **Add low-rank linear primitive family** -- *(Phase 98: `low_rank_proj` with rank=D/4, D²/2 params, registered in grammar/registry.)*
- [x] **Add tied/shared projection primitives** -- *(Phase 98b: `tied_proj` with shared transposed weights, D·r params where r=D/4. Down=W, up=W^T with GELU.)*
- [x] **Add grouped/block-diagonal linear primitives** -- *(Phase 98: `grouped_linear` with 4 groups, D²/4 params, registered in grammar/registry.)*
- [x] **Add bottleneck squeeze-expand primitive** -- *(Phase 98: `bottleneck_proj` D→D/4→D with GELU, D²/2 params, registered in grammar/registry.)*
- [x] **Add structured sparsity primitives** -- *(Phase 100: `nm_sparse_linear`, `block_sparse_linear`, `semi_structured_2_4_linear` with mask/fallback paths.)*
- [x] **Extend graph serialization and compile path** -- *(Already works: OpNode.config dict serializes via to_dict/from_dict; compiler reads config during init; all compression/sparse ops compile correctly.)*
- [x] **Expose compression metadata in metrics** -- *(Phase 98b: `_detect_compression_ops()` scans graph_json for compression primitives; `canonical_compression_metrics` auto-detects mechanism from ops; `compression_primitive_effectiveness()` aggregates per-op survival rates + param efficiency.)*
- [x] **Add prescreen safeguards for compressed ops** -- *(Phase 98c: Added model_dim divisibility-by-4 check in `prescreen_run_config` — auto-rounds up for grouped_linear (g=4), N:M sparse (m=4), and semi_structured_2_4 compatibility.)*
- [x] **Add integration tests for each primitive family** -- *(Phase 98: 17 tests for compression ops; Phase 100: sparse op tests.)*
- [x] **Add dashboard evidence hooks** -- *(Phase 98b: `compression_primitives` array added to both data_block and recommendation_evidence in `/api/strategy/briefing`, showing per-op survival rates, avg loss, and param counts.)*
- [x] **Add one-shot pruning baselines (Wanda/SparseGPT-style)** -- Added optional one-shot pruning baseline pass in Stage 1 (`RunConfig.one_shot_pruning_baseline`) using new pruning utility with Wanda/SparseGPT-style scoring, plus persisted dense-vs-pruned quality-retention metrics.
- [x] **Add N:M structured sparsity primitive (2:4 first)** -- Added `nm_sparse_linear` primitive with configurable `n/m` schema, compile-time param init, and runtime masked linear execution.
- [x] **Add block-sparse linear primitive family (BSR/BSC)** -- Added `block_sparse_linear` primitive with configurable `block_size` + `block_density`, block mask generation, and sparse execution path.
- [x] **Add semi-structured sparse path (2:4 kernel-ready)** -- Added `semi_structured_2_4_linear` primitive with explicit 2:4 pattern metadata and kernel-readiness gating.
- [x] **Add dynamic sparse training mode (RigL-style)** -- *(Phase 104: `RigLScheduler` + `RigLOptimizer` in `training/sparse_training.py`. Fixed-budget mask updates with cosine-decayed drop fraction. Registered as `rigl_sparse` in OPTIMIZER_RECIPES.)*
- [x] **Add sparse kernel compatibility gating** -- Added device/layout compatibility checks for semi-structured 2:4 and explicit dense fallback telemetry (`sparse_telemetry`).
- [x] **Add sparsity telemetry in notebook metrics** -- Added persisted sparse metrics in `program_results` (`sparse_density_*`, fallback counters, N:M compliance, active sparse params estimate, telemetry JSON) sourced from compiled layer telemetry.
- [x] **Add sparsity-aware search constraints** -- Extended `weight_storage` sparse options and added structural validity constraints for sparse settings in `morphological_box.py`.
- [x] **Add sparse benchmark suite** -- Added sparse-focused integration tests covering registration, gradients, fallback behavior, and sparse constraint validation.
- [x] **Add dashboard sparse evidence panel** -- Added sparse evidence summary to `/api/strategy/briefing` (`data.sparse` and `evidence.sparse`) and surfaced sparse chips in StrategyAdvisor (`sparse runs`, `density`, `N:M compliance`, `top sparse op`).
- [x] **Add sparse+quant co-design experiments** -- *(Phase 105: `eval/quantization.py` with fake-quant INT8/INT4 simulation + combined sparse+quant evaluation pipeline. `sparse_quant_codesign_summary()` in analytics.py aggregates quality-retention-per-byte across programs. 8 integration tests.)*
- [x] **Add sparse MoE activation telemetry** -- *(Phase 104: `moe_activation_telemetry()` in analytics.py aggregates per-expert utilization, load balance score, drop rates, routing confidence, and capacity overflow from existing notebook routing columns.)*
- [x] End-to-end Aria loop is complete and operational (LLM briefing, one-click run, autonomous cycle control, SSE auto-refresh, fallback mode, and chat).
- [x] Acceptance criteria are fully met (actionable AI rationale each cycle, no silent-failure path, and working deterministic fallback).
- [x] Detailed phase-by-phase Aria completion history is maintained in `.current_work_archive.md`.
- [x] `synthesis/compiler.py` -- Gemini CLI -- Refactored `_execute_op` into a registry-based dispatch system (complexity < 10). Vectorized `selective_scan` using memory-efficient `F.conv1d` with exponential kernels, eliminating O(S) Python loops. Implemented liveness-based memory management in `CompiledLayer.forward` to aggressively reclaim VRAM.
- [x] `eval/sandbox.py` -- Gemini CLI -- Refactored `_stability_probe` for DRY compliance and improved internal helper abstractions.
- [x] `search/novelty_search.py` -- Gemini CLI -- Vectorized `BehaviorArchive` using NumPy for $O(1)$ distance calculations per population member, eliminating $O(N)$ Python loops.
- [x] `scientist/analytics.py` -- Gemini CLI -- Refactored `experiment_clusters` and `structural_correlations` using NumPy/SciPy. Vectorized K-Means, silhouette scores, and feature normalization, delivering massive orchestration speedups.
- [x] Validation -- Gemini CLI -- 155+ integration tests passed (synthesis, compiler, analytics, novelty). Verified performance and VRAM hygiene.
- [x] `dashboard/src/components/TopPrograms.js` -- GitHub Copilot -- Added small header tooltip on `Program Fingerprint ID`: “Architecture identity for each program row; the same fingerprint can appear multiple times when rerun.”
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended TopPrograms copy regression marker to include fingerprint-header tooltip text.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "top_programs_copy_clarifies_program_vs_fingerprint_and_shows_leading_fingerprints" -x --tb=short` (1 passed).
- [x] `dashboard/src/components/TopPrograms.js` -- GitHub Copilot -- Clarified survivor-table semantics so each row is explicitly a program run and `Program Fingerprint ID` is the architecture identity that can repeat across reruns.
- [x] `dashboard/src/components/TopPrograms.js` -- GitHub Copilot -- Updated curated destination copy to `Leaderboard (Curated)` and renamed deduplicated summary card to `Leading Fingerprints (Deduplicated Architecture IDs)`.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Renamed tab label from `Decisions (Curated)` to `Leaderboard (Curated)` for consistent navigation language.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Updated `test_top_programs_copy_clarifies_program_vs_fingerprint_and_shows_leading_fingerprints` marker assertions for new terminology.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "top_programs_copy_clarifies_program_vs_fingerprint_and_shows_leading_fingerprints or research_report_mentions_deduplicated_fingerprint_rankings or tab_names_match_content" -x --tb=short` (3 passed).
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- Added `Refinement Trace` panel for refine/compression/sparsity actions: captures launched experiment ID + source resolution payload, keeps modal open after launch, polls `/api/experiments/<id>` for live outcome summary, and exposes quick actions (`Open Refinement Run`, `View Top Refined Result`).
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- Extended trace panel with direct clickable refined fingerprint links (`Open Fingerprint`) and `Recent Refinement Launches` history (last 3 runs with status + quick-open actions).
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- Added one-click action-row shortcut `Reopen Last Refined Fingerprint` so users can jump to latest refined candidate without scrolling to trace/history sections.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Updated/extended ProgramDetail wiring assertions for helper-based refinement launch path and new trace panel markers/polling hook.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended ProgramDetail wiring assertions for clickable fingerprint-link and recent-launch-history markers.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended ProgramDetail wiring assertions for `Reopen Last Refined Fingerprint` shortcut wiring (`lastRefinedCandidate`).
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "program_detail_refinement_intent_actions_are_wired or program_detail_refinement_rationale_panel_is_wired or api_start_refine_fingerprint_accepts_graph_fingerprint_prefix or api_start_requires_result_ids_for_refine_fingerprint" -x --tb=short` (4 passed).
- [x] `scientist/api.py` -- GitHub Copilot -- Hardened dashboard static handling (`/`, `/<path>`, 404 fallback, `/favicon.ico`) so missing frontend build artifacts no longer recurse into `send_from_directory(..., "index.html")` and trigger 500s.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_dashboard_root_missing_static_build_returns_503_not_500` and `test_dashboard_favicon_missing_static_build_returns_204` regressions.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "dashboard_root_missing_static_build_returns_503_not_500 or dashboard_favicon_missing_static_build_returns_204 or api_strategy_briefing or api_start_" -x --tb=short` (21 passed).
- [x] `scientist/api.py` -- GitHub Copilot -- Aligned `/api/strategy/briefing` investigate/validation recommendation IDs with `_build_start_mode_eligibility(...)`; AI and deterministic recommendation paths now filter to eligible IDs and downgrade to `continuous` when no executable candidates remain.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added regressions `test_api_strategy_briefing_downgrades_ineligible_investigation_recommendation` and `test_api_strategy_briefing_deterministic_skips_ineligible_screening_investigate` to prevent recommendation/execute mismatch.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "strategy_briefing and (normalizes_ai_mode_alias or adds_sparse_focus_knobs_when_coverage_low or downgrades_ineligible_investigation_recommendation or deterministic_skips_ineligible_screening_investigate or api_strategy_briefing_contract)" -x --tb=short` (5 passed).
- [x] Validation -- GitHub Copilot -- Broader sweep `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_strategy_briefing or strategy_advisor or api_start_ or sparse_morph or compact_synthesis or refine_fingerprint or rerun_experiment" -x --tb=short` (28 passed). Stabilized deterministic regression invariant to enforce "not investigate/investigation when IDs are ineligible" without over-constraining higher-priority deterministic fallback actions.
- [x] `scientist/api.py` -- GitHub Copilot -- Extended non-finite float sanitization to `/api/programs` and `/api/programs/<result_id>` using `_json_safe(...)` so `Infinity/NaN` metrics serialize as `null`.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_programs_sanitizes_non_finite_metrics` and `test_api_program_detail_sanitizes_non_finite_metrics` regressions.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_programs or api_program_detail or api_programs_sanitizes_non_finite_metrics or api_program_detail_sanitizes_non_finite_metrics" -x --tb=short` (5 passed).
- [x] `scientist/api.py` -- GitHub Copilot -- Sanitized `/api/experiments/<id>` and `/api/experiments/<id>/programs` responses via `_json_safe(...)` so non-finite floats (`Infinity`/`NaN`) in program metrics serialize as JSON `null` instead of invalid tokens.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_experiment_detail_sanitizes_non_finite_program_metrics` regression.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_experiment_detail or api_experiment_programs or api_experiment_detail_sanitizes_non_finite_program_metrics" -x --tb=short` (3 passed).
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_strategy_briefing or strategy_advisor or api_start_ or sparse_morph or compact_synthesis or refine_fingerprint or rerun_experiment" -x --tb=short` (23 passed).
- [x] Analysis -- GitHub Copilot -- Identified high-latency sources: `/api/report` wide analytics fan-out + synchronous `aria.generate_report_narrative(data)` in request path + large one-shot payload (`top_programs_expanded`, `recent_experiments`) fetched on tab mount.
- [x] `scientist/api.py` -- GitHub Copilot -- Added report fast-mode controls (`fast`, `include_heavy`, `include_narrative`) and new `/api/report/query` endpoint for date/theme/trend scoped report generation.
- [x] `dashboard/src/components/ResearchReport.js` -- GitHub Copilot -- Switched initial load to fast report mode and added report builder controls for date/theme/trend/top-k generation plus optional full-detail fetch.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_report_fast_mode_contract`, `test_api_report_query_contract`, and `test_research_report_wires_scoped_query_builder_controls` regressions.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_report_fast_mode_contract or api_report_query_contract or api_report_schema or api_report or research_report_wires_scoped_query_builder_controls or research_report_uses_stage1_survivors_summary_key" -x --tb=short` (7 passed).
- [x] `scientist/notebook.py` -- GitHub Copilot -- Added `report_snapshots` table and notebook APIs: `get_latest_completed_experiment_timestamp()`, `get_report_snapshot()`, `save_report_snapshot()`.
- [x] `scientist/api.py` -- GitHub Copilot -- Added snapshot-key generation and cache lookup/save path for `/api/report/query` with `snapshot_cache` metadata (`hit`, `key`, invalidation timestamp).
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_report_query_snapshot_cache_hits_on_repeated_query` and extended query contract to validate `snapshot_cache` presence.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "api_report_query_contract or api_report_query_snapshot_cache_hits_on_repeated_query or api_report_fast_mode_contract or api_report_schema" -x --tb=short` (4 passed).
- [x] `scientist/notebook.py` -- GitHub Copilot -- Added `cleanup_report_snapshots(ttl_seconds, max_rows_per_scope)` and periodic cleanup trigger in `save_report_snapshot`; defaults support env knobs `ARIA_REPORT_SNAPSHOT_TTL_SECONDS` and `ARIA_REPORT_SNAPSHOT_MAX_ROWS_PER_SCOPE`.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_report_snapshot_cleanup_prunes_expired_and_caps_scope` for expiry + bounded-per-scope retention.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "report_snapshot_cleanup_prunes_expired_and_caps_scope or api_report_query_snapshot_cache_hits_on_repeated_query or api_report_query_contract" -x --tb=short` (3 passed).
- [x] `scientist/notebook.py` -- GitHub Copilot -- Added `get_report_snapshot_stats()` to summarize report snapshot counts and age telemetry per scope.
- [x] `scientist/api.py` -- GitHub Copilot -- Added `/api/diagnostics/report-cache` endpoint returning snapshot stats, configured retention, and optional cleanup execution (`?cleanup=1`).
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_report_cache_diagnostics_endpoint` regression for diagnostics payload and cleanup-trigger contract.
- [x] Validation note -- GitHub Copilot -- Targeted API pytest setup is currently blocked by unrelated syntax error in `scientist/runner.py` (`keyword argument repeated: exploratory` at line ~9205). Direct compile checks pass for changed files: `python -m py_compile scientist/notebook.py scientist/api.py tests/test_integration.py`.
- [x] `scientist/api.py` -- GitHub Copilot -- Added sparse coverage summary to strategy evidence and sparse-config augmentation for both AI and deterministic recommended actions so low sparse coverage produces executable sparse-targeted knobs.
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- Updated execute path to preserve complete `suggested_config` payload, preventing sparse-driving fields from being dropped; added sparse coverage/survival evidence chips.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_strategy_briefing_adds_sparse_focus_knobs_when_coverage_low`, updated strategy briefing contract assertions for `sparse_coverage`, and added `test_strategy_advisor_preserves_full_suggested_config_passthrough`.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "strategy_briefing_adds_sparse_focus_knobs_when_coverage_low or strategy_advisor_preserves_full_suggested_config_passthrough or api_strategy_briefing_contract or api_strategy_briefing_normalizes_ai_mode_alias" -x --tb=short` (4 passed).
- [x] `scientist/analytics.py` -- Codex -- Added deterministic attribution report with per-factor success rates, Wilson CIs, BH-FDR correction, and matched-control comparisons.
- [x] `scientist/runner.py` -- Codex -- Added `propose_ablation_suite(candidate_graph, hypothesis) -> list[graphs]`, automatic ablation experiment execution, and grammar-weight update gate enforcing ablation support or strong correlational evidence with uncertainty and queued ablation plan.
- [x] `scientist/notebook.py` -- Codex -- Added `attribution_reports` SQLite table and notebook APIs linking `hypothesis_id`, `supporting_experiments`, `ablation_experiments`, and `outcome`.
- [x] `tests/test_attribution_ablation.py` -- Codex -- Added deterministic attribution and ablation-suite compile/validation unit tests.
- [x] Validation -- Codex -- `pytest tests/test_attribution_ablation.py -x --tb=short`, `pytest tests/test_evidence_pack.py -x --tb=short`, `pytest tests/test_integration.py -k "grammar and weight" -x --tb=short` (all passed).
- [x] `scientist/evidence.py` -- Codex -- Added Evidence Pack schema, validation, novelty-reference guard, and learning-log audit validation.
- [x] `scientist/runner.py` -- Codex -- Attached Evidence Packs to recommendations/decisions, added audit logging helper for grammar weight application, and enforced recommendation application only with evidence.
- [x] `scientist/analytics.py` -- Codex -- Added reproducible audit query metadata for grammar weight learning.
- [x] `scientist/notebook.py` -- Codex -- Added `evidence_pack_json` to decisions + migrations and retrieval.
- [x] `scientist/api.py` -- Codex -- Added Evidence Pack to `/api/aria/recommendation`.
- [x] `eval/fingerprint.py` -- Codex -- Added `similarity_path` provenance to fingerprints.
- [x] `eval/cka_references.py` -- Codex -- Added similarity-path metadata for CKA provenance.
- [x] `tests/test_evidence_pack.py` -- Codex -- Added CI checks for Evidence Pack validation and grammar-weight audit evidence.
- [x] `tests/test_integration.py` -- Codex -- Added API contract check for `/api/aria/recommendation` Evidence Pack payload.
- [x] `.current_work.md` -- Claude Code -- Full UI/UX review of dashboard: Feedback Loop visibility, Data-to-Action mapping, Cognitive Load analysis, State Consistency code audit. Added 14 actionable TODOs (IA-P0 through IA-P3) + 3 Dead Ends + Narrative Layer recommendations.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Wired Overview `Production Readiness` card to fetch `/api/diagnostics/fingerprint` during dashboard refresh and surface compact runtime telemetry (`Fingerprint diagnostics`, `Sensitivity skips`, top skip reasons).
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended `test_dashboard_wires_production_readiness_panel` to assert diagnostics UI markers and endpoint wiring string in `App.js`.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_dashboard_wires_production_readiness_panel or test_api_fingerprint_diagnostics_endpoint or test_api_dashboard" -x --tb=short` (5 passed).
- [x] `dashboard/src/components/LearningPanel.js` -- GitHub Copilot -- Added `FingerprintDiagnosticsCard` and wired Learning tab analytics fetch to `/api/diagnostics/fingerprint`, rendering sensitivity skip totals and top skip reasons.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_learning_panel_wires_fingerprint_diagnostics_card` for endpoint/marker wiring assertions.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_learning_panel_wires_fingerprint_diagnostics_card or test_learning_panel_mentions_unique_vs_rerun_telemetry or test_dashboard_wires_production_readiness_panel or test_api_fingerprint_diagnostics_endpoint" -x --tb=short` (4 passed).
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Removed duplicate Production Readiness reproducibility render blocks (same `Complete repro packet` section was rendered 3x) and removed duplicated per-candidate `workflow` label spans to reduce unnecessary Overview render work.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Stabilized auto-repair polling effect dependency by using a derived active-task key (`activeAutoRepairTaskIdsKey`) instead of full `autoRepairTasks` array dependency churn.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Optimized 2.5s agent-status polling path: replaced O(n²) message scans with single-pass task map construction and skipped no-op `setMessages` updates when task payload did not materially change.
- [x] `dashboard/src/components/AriaStatus.js` -- GitHub Copilot -- Added `sanitizeHypothesisText()` to strip fenced/inline code snippets and truncate noisy long-form pseudo-execution text before rendering in status card.
- [x] `dashboard/src/hooks/useRenderPerf.js` -- GitHub Copilot -- Added lightweight dev-only render timing sampler (`window.__ariaRenderPerf`) with optional debug threshold logging.
- [x] `dashboard/src/components/{AriaStatus,SummaryCards,StrategyAdvisor,AriaChatPanel,TopPrograms,InsightsPanel}.js` -- GitHub Copilot -- Wired Overview-critical components into render profiler for per-component timing capture.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_aria_status_sanitizes_hypothesis_summary` regression to ensure status card uses sanitization path and no longer renders raw `{aria.current_hypothesis}`.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "aria_status_sanitizes_hypothesis_summary or dashboard_wires_production_readiness_panel or dashboard_wires_auto_repair_started_event_to_chat" -x --tb=short` (3 passed).
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "aria_chat_panel_auto_analysis_uses_single_briefing_endpoint or aria_status_sanitizes_hypothesis_summary or dashboard_wires_production_readiness_panel" -x --tb=short` (3 passed).
- [x] `dashboard/src/components/LiveFeed.js` -- GitHub Copilot -- Added `normalizeLiveFeedEvent()` with event-type aliasing and `RENDERABLE_EVENT_TYPES` filtering so unsupported persisted event types (e.g., non-rendered history entries) are dropped instead of producing empty rows.
- [x] `dashboard/src/components/LiveFeed.js` -- GitHub Copilot -- Added generation-history annotations (`annotateGenerationHistory`) so sparse persisted history clearly labels missing generation ranges (e.g., "Generations 1-12 are not in current feed history") and run boundaries (Evolution/Novelty run IDs), preventing misleading visual gaps.
- [x] `scientist/api.py` -- GitHub Copilot -- Updated `/api/live-feed` backend selection strategy: fetches a deeper live-feed window and, when no `experiment_id` is provided, auto-selects the latest experiment ID and returns that coherent stream (instead of mixed cross-experiment events).
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_live_feed_defaults_to_latest_experiment_stream` to enforce default latest-experiment live-feed behavior.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_live_feed_filters_non_renderable_events_to_avoid_blank_rows` regression.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_sse_event_types_handled or test_live_feed_filters_non_renderable_events_to_avoid_blank_rows or test_api_live_feed" -x --tb=short` (3 passed).
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_api_live_feed or test_api_live_feed_defaults_to_latest_experiment_stream or test_live_feed_filters_non_renderable_events_to_avoid_blank_rows" -x --tb=short` (3 passed).
- [x] `scientist/api.py` -- GitHub Copilot -- Added `_json_safe()` recursive serializer and applied it in `/api/events` stream (`event_stream`) so tensor/array/scalar payloads are converted into JSON-safe primitives before `json.dumps`.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_events_serializes_tensor_payloads` regression to ensure SSE event stream handles tensor-valued fields without crashing Werkzeug response iteration.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_api_events_serializes_tensor_payloads or test_api_live_feed or test_sse_event_types_handled" -x --tb=short` (4 passed).
- [x] `scientist/api.py` -- GitHub Copilot -- Added non-finite float sanitization in `_json_safe` (maps `NaN/Inf/-Inf` to `null`) and enforced strict `json.dumps(..., allow_nan=False)` for `/api/events`.
- [x] `eval/fingerprint.py` -- GitHub Copilot -- Ran sensitivity analysis under `torch.enable_grad()`, added non-grad guards, and prevented predictable no-grad warning spam while preserving graceful fallback metrics.
- [x] `eval/fingerprint.py` -- GitHub Copilot -- Added rate-limited skip telemetry (`_record_sensitivity_skip`) to aggregate/debug skipped sensitivity probes without log spam (periodic breakdown by reason).
- [x] `eval/fingerprint.py` -- GitHub Copilot -- Added public `get_sensitivity_skip_stats(reset=False)` helper for runtime diagnostics export.
- [x] `scientist/api.py` -- GitHub Copilot -- Added `/api/diagnostics/fingerprint` endpoint exposing sensitivity-skip counters with optional reset flag.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_fingerprint_diagnostics_endpoint` regression for diagnostics shape/reset path.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_events_sanitizes_non_finite_float_payloads` regression.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_api_events_serializes_tensor_payloads or test_api_events_sanitizes_non_finite_float_payloads or test_api_live_feed or test_fingerprint" -x --tb=short` (8 passed).
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_fingerprint_quality_defaults or test_fingerprint_records_cka_source or test_fingerprint_heuristic_fallback_when_no_artifacts or test_fingerprint_cka_provenance_fields_exist" -x --tb=short` (4 passed).
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_api_fingerprint_diagnostics_endpoint or test_api_events_sanitizes_non_finite_float_payloads or test_fingerprint_quality_defaults or test_fingerprint_records_cka_source" -x --tb=short` (4 passed).
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added collapsible agent-detail controls (`Show task details` / `Hide task details`) so chat stays summary-first while preserving optional deep task diagnostics.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_local_chat_agent_reads_workspace_files` to verify local workspace file-reading/search evidence path.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_workspace_file_index_supports_agent_targeting` to verify Aria file-index/query path for agent spawn targeting.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_edit_file_can_modify_research_test_python_file` to verify Python test-file modification under `research/tests` via Aria edit action rails.
- [x] `scientist/runner.py` -- GitHub Copilot -- Fixed `_execute_edit_file_action` path resolver so direct project paths like `search/evolution.py` resolve correctly (no accidental `research/research/...` nesting).
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_edit_file_can_target_real_search_python_file` to prove Aria can edit a real project file path (`search/evolution.py`) via chat edit rails.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_api_aria_chat_returns_local_hits_and_spawned_agent_summary` to validate `/api/aria/chat` returns local file evidence (`local_code_hits`) and spawned agent metadata/actions in one response contract.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "local_chat_agent_reads_workspace_files or workspace_file_index_supports_agent_targeting or edit_file_can_modify_research_test_python_file or aria_chat_panel_auto_analysis_uses_single_briefing_endpoint" -x --tb=short` (4 passed).
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_edit_file_can_target_real_search_python_file or test_adjust_config_applies_valid_changes or test_edit_file_rejects_non_project_paths" -x --tb=short` (3 passed).
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_api_aria_chat_returns_local_hits_and_spawned_agent_summary" -x --tb=short` (1 passed).
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- Added briefing sanitization to strip non-action code fences from strategy text and explicit `Actionable`/`Advice only` status labels.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added summary-only text compaction for briefing/chat/system updates, concise action summaries, and explicit self-fix capability badge (`Self-fix: .py/.js`).
- [x] `scientist/llm/prompts.py` -- GitHub Copilot -- Strengthened `CHAT_PROMPT` contract to forbid pseudo-execution snippets and require action blocks (or explicit `advice_only` when no safe action exists).
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added regressions for strategy actionability markers, pseudo-code sanitization marker, and summary-only chat/self-fix UI markers.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_aria_chat_panel_auto_analysis_uses_single_briefing_endpoint or test_strategy_advisor_marks_actionability_and_sanitizes_pseudo_code or test_dashboard_wires_production_readiness_panel" -x --tb=short` (3 passed).
- [x] `scientist/api.py` -- GitHub Copilot -- Added `_compute_breakthrough_production_readiness()` and injected `production_readiness` into `/api/dashboard` payload with deterministic epic-switch recommendation and concise top-candidate gate status.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended `test_api_dashboard` to assert production-readiness payload presence and recommendation contract.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_api_dashboard or test_api_dashboard_summary_schema or test_api_aria_chat or dashboard_wires_auto_repair_started_event_to_chat" -x --tb=short` (14 passed).
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added Overview Production Readiness card bound to `data.production_readiness` showing epic switch recommendation, readiness counts, and top-candidate blockers.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `test_dashboard_wires_production_readiness_panel` wiring assertion for Production Readiness panel labels and payload key.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_api_dashboard or test_dashboard_wires_production_readiness_panel or test_dashboard_wires_auto_repair_started_event_to_chat or test_api_aria_chat" -x --tb=short` (15 passed).
- [x] `scientist/api.py` -- GitHub Copilot -- Added `_build_scale_up_templates_for_result()` and exposed `scale_up_templates` in `production_readiness` (top-level and per top-candidate payload).
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added one-click `Run Scale-Up Template` actions in Production Readiness candidate rows and `handleRunProductionTemplate` start flow.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended dashboard/API checks for `scale_up_templates` contract and template action wiring markers.
- [x] `scientist/api.py` -- GitHub Copilot -- Added reproducibility workflow builder and attached `reproducibility_workflow` payload (top-level + candidate-level) under `production_readiness`, including empty-state `reproducibility_workflow: None`.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added “Complete repro packet” progress block, actionable `Run step` controls for runnable workflow steps, and candidate workflow progress labels.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added/updated reproducibility workflow assertions for API payload contract and Production Readiness panel wiring markers.
- [x] Validation -- GitHub Copilot -- `/home/tim/venvs/llm/bin/pytest tests/test_integration.py -k "test_api_dashboard or test_dashboard_wires_production_readiness_panel or test_dashboard_wires_auto_repair_started_event_to_chat or test_api_aria_chat" -x --tb=short` (15 passed).
- [x] `scientist/api.py` -- GitHub Copilot -- Added concise default mode (unless detailed/summary explicitly requested), plus post-formatting to enforce `Simple Answer` + `Action Plan` response structure for regular chat questions.
- [x] `scientist/api.py` -- GitHub Copilot -- Hardened prompt style guidance so non-summary responses avoid narrative babble and stick to short actionable output.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Updated chat endpoint regression to assert default `Simple Answer` + `Action Plan` contract.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added persistent Overview strip (`Active auto-repair tasks`) that tracks auto-repair task metadata, listens for `aria-auto-repair-started`, and polls `/api/aria/agent/status/<task_id>` until terminal state.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended dashboard wiring regression to assert Overview strip and status polling endpoint wiring for active auto-repair visibility.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added `Show completed` toggle so Overview can switch between active-only and full auto-repair history, while keeping active count visible.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Updated dashboard wiring regression assertions for renamed strip label (`Auto-repair tasks`) and completed-toggle text.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Persisted completed-toggle preference via localStorage key `aria_auto_repair_show_completed_v1` (init from storage + write-through effect on toggle changes).
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended dashboard wiring regression to assert completed-toggle persistence key presence.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added `Reset preferences` action in auto-repair strip header to clear persisted completed-toggle preference and return to active-only view.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended dashboard wiring regression to assert reset-preferences control is present.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added `emitAutoRepairStarted` event dispatch from all `/api/experiments/start` error handlers when backend returns `auto_repair_started` + task metadata.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added listener for `aria-auto-repair-started` and system message that includes whether local helper is active or blocked when repair task is launched.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added dashboard wiring contract test for auto-repair event emission/consumption path.
- [x] `scientist/api.py` -- GitHub Copilot -- Added runtime/code-error heuristic and automatic codebase-agent spawn in `/api/experiments/start` failure path; API now returns `auto_repair_started` and `auto_repair_task` when self-repair is launched.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added regression ensuring start-time runtime errors auto-spawn a self-repair task and expose task metadata in the 500 response payload.
- [x] `scientist/api.py` -- GitHub Copilot -- Broadened fix-intent detection (including “needed to fix / what needs fixing” phrasing), forced execution-first mode for any fix request, and gated summary-style fallback responses behind explicit summary intent.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Updated/added chat endpoint regressions for action-first default reply format, broadened fix-intent execution-first routing, and explicit summary-request format behavior.
- [x] `scientist/notebook.py` -- GitHub Copilot -- Made `log_learning_event` backward-compatible with structured extra kwargs (`**event_data`) so runner calls with fields like `changes`, `weights`, `excluded_ops`, `multipliers`, etc. no longer raise unexpected-kwarg errors.
- [x] `scientist/runner.py` -- GitHub Copilot -- Added sparse-focused morphological controls (`morph_focus_sparse`, optional `morph_sparse_weight_storage`) and chat action mode alias (`sparse_morph*`) that forces sparse morphological synthesis profile.
- [x] `scientist/api.py` -- GitHub Copilot -- Added `/api/experiments/start` mode alias normalization + sparse-morph bias helper (`sparse_morph_bias`) for one-click sparse-focused morphological runs.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added regressions for `log_learning_event` extra kwargs compatibility, sparse-morph API start alias behavior, sparse-morph chat-action profile behavior, and RunConfig field presence.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Removed all auto-analysis triggers (init interval, run completion auto-refresh, LLM-configured auto-refresh), switched chat to explicit-request-only behavior, and relabeled manual refresh to `Ask for Action`.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended Aria chat panel regression to assert manual-only cadence text (`Auto: Off (Manual only)`), absence of run/always options, and action-focused manual trigger label.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Consolidated auto-analysis injection to a single briefing message sourced from `/api/strategy/briefing` (includes recommended action inline) to prevent multi-message chat spam.
- [x] `scientist/api.py` -- GitHub Copilot -- Expanded code-fix intent triggers (`"need to fix"`, `"needs fixing"`, `"anything to fix"`) so natural phrasing routes to execution-first autonomous agent handoff.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added regressions for execution-first trigger phrase handling and single-endpoint Aria chat panel auto-analysis path.
- [x] `scientist/runner.py` -- GitHub Copilot -- Added simplicity caps for evolve/novelty in prescreen auto-harden path (`max_depth<=3`, `max_ops<=5`).
- [x] `scientist/persona.py` -- GitHub Copilot -- Grounded mood transitions so novelty without Stage-1 survivorship no longer sets `triumphant`.
- [x] `scientist/analytics.py` -- GitHub Copilot -- Verified grammar-weight floor remains enforced at `0.5` minimum (documentation/todo closure).
- [x] `scientist/runner.py` -- GitHub Copilot -- Added Stage-1 step-0 zero-gradient fail-fast (`zero_grad_precheck_failed`) to avoid wasting micro-train steps on dead-gradient candidates.
- [x] `scientist/analytics.py` -- GitHub Copilot -- Added hard learned-weight suppression cap for `frequency_domain` category (`<=0.1`).
- [x] `scientist/runner.py` -- Claude Code -- Added missing `import math` (used by Codex's zero_grad_precheck at step-0). Added structural novelty scoring in gradient flow pre-check path so zero-grad programs still record novelty_score.
- [x] `tests/test_integration.py` -- Claude Code -- Fixed `test_prescreen_run_config_caps_evolution_depth_and_ops` to match Codex's simplicity caps (max_depth=3, max_ops=5 instead of 12, 20).
- [x] `scientist/runner.py` -- GitHub Copilot -- Added champion-bias weighting that boosts structural/sequence/parameterized categories when historical winning, projection, and sparse ops show reliable S1 success.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added persisted auto-analysis cadence policy selector and stale-evidence guardrails to avoid repetitive idle analysis spam.
- [x] `scientist/api.py` -- Claude Code -- Changed default model `qwen2.5-coder:3b` → `qwen2.5-coder:7b-instruct`, added `_ollama_offload_model()` (POST keep_alive:0 to free VRAM after task), bumped subprocess timeout 90s → 120s.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Extended each agent task card to show `planner_backend`, `main_llm_backend`, and local Ollama usage/block reason (including estimated VRAM when available).
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added real-time local helper badge (`Local LM`) in chat header by polling `/api/aria/tools`, displaying enabled/blocked state and concise reason.
- [x] `scientist/api.py` -- GitHub Copilot -- Added strict local `ollama run` planner for code-agent read/fix planning, enabled only when primary LLM backend is remote (`anthropic`/`openai`) and local model estimated VRAM is strictly `< ARIA_LOCAL_OLLAMA_MAX_VRAM_GB` (default `10`). Added helper status in `/api/aria/tools` under `local_ollama_helper`.
- [x] `scientist/api.py` -- GitHub Copilot -- Added local `qwen2.5-coder:3b` swarm planner (up to 3 workers by VRAM), plus simple-vs-complex routing policy between 3b swarm and 7b planner; exposed worker capacity and policy metadata in `/api/aria/tools`.
- [x] `scientist/llm/prompts.py` -- Claude Code -- Conversational CHAT_PROMPT with action block format (adjust_config, adjust_grammar, start_experiment, edit_file, spawn_agent).
- [x] `scientist/api.py` -- Claude Code -- `_parse_chat_actions()` regex extraction, action execution loop with spawn_agent delegation, increased max_tokens 768→2048, updated tools endpoint.
- [x] `scientist/runner.py` -- Claude Code -- `execute_chat_action()` dispatch, `_execute_edit_file_action()` with safety rails, `_grammar_weight_overrides`, `_detect_plateau()` circuit breaker (auto-stops after 5 barren cycles).
- [x] `scientist/persona.py` -- Claude Code -- Fixed rule-based mode recommendation repetition with cooldown-aware triggers requiring both time gap AND new data.
- [x] `scientist/llm/context.py` -- Claude Code -- `_build_session_delta()` showing what changed recently to prevent stale repetition.
- [x] `dashboard/src/components/AriaChatPanel.js` -- Claude Code -- Action result rendering, spawn_agent support, increased chat viewport 220→400px.
- [x] `tests/test_integration.py` -- Claude Code -- 18 new tests for actions, safety rails, plateau detection, session delta, mode cooldowns.
- [x] `scientist/api.py` -- GitHub Copilot -- Implemented `run_trigger_source` tracking for all API/chat launch paths (`ui_start`, `ui_rerun`, `cycle_control`, `chat_action`) and exposed trigger payload in `/api/progress`, `/api/status`, and `/api/aria/tools`.
- [x] `scientist/api.py` -- GitHub Copilot -- Added `/api/aria/tools` capability endpoint reporting codebase-agent/tool availability, LLM readiness, runner state, and continuous-cycle state.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Changed periodic auto-analysis refresh to run only when `isRunning` or `autonomousMode` is active, preventing repetitive overnight strategy spam while idle.
- [x] `scientist/api.py` -- GitHub Copilot -- Updated chat to default fix-oriented requests to execution-first mode (spawn agent + concise handoff) unless user explicitly asks for detailed analysis.
- [x] `scientist/api.py` -- GitHub Copilot -- Fixed self-fix phrasing path so requests like "fix itself" reliably trigger code-agent spawn (no null task handoff).
- [x] `scientist/api.py` -- GitHub Copilot -- Added explicit self-fix intent handling so requests like “fix yourself” spawn a codebase agent and immediately return a short deterministic handoff instead of long analysis.
- [x] `scientist/api.py` -- GitHub Copilot -- Added brief-response intent detection (e.g., “don’t babble”, “be concise”) and shorter LLM output constraints for non-deterministic chat paths.
- [x] `scientist/api.py` -- GitHub Copilot -- Expanded self-repair trigger phrases so requests like “spin off agents and fix issues yourself” reliably auto-spawn background codebase-agent tasks.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added de-duplication for auto-injected briefing/strategy/recommendation messages so repeated refresh cycles do not spam identical analysis blocks.
- [x] `scientist/api.py` -- GitHub Copilot -- Removed extension allowlist in code-agent target resolution so Aria can modify any existing file within workspace boundaries.
- [x] `scientist/api.py` -- GitHub Copilot -- Updated code-agent planning/context wording from Python/JavaScript-only to generic workspace-file edits.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added live polling of `/api/aria/agent/status/<task_id>` for spawned `agent_task`s and in-message rendering of task status, summary, and applied/proposed/skipped edit counts.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added inline applied-edits listing and one-time completion system notifications when background codebase-agent tasks reach terminal state.
- [x] `scientist/api.py` -- GitHub Copilot -- Added background codebase-agent task engine with safe edit application (`find`/`replace`) across `.py/.js/.ts/.tsx`, syntax validation, timeout-protected LLM planning, and task lifecycle tracking.
- [x] `scientist/api.py` -- GitHub Copilot -- Added `/api/aria/agent/spawn` and `/api/aria/agent/status/<task_id>` endpoints, and wired `/api/aria/chat` to auto-spawn agent tasks for self-repair/design-fix requests (with `agent_task` metadata in responses).
- [x] `scientist/api.py` -- GitHub Copilot -- Extended `/api/aria/chat` local code-hit metadata with `abs_path` for editor-link generation.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Rendered top code hits as clickable `vscode://file...:line` links in the local evidence panel.
- [x] `dashboard/src/components/AriaChatPanel.js` -- GitHub Copilot -- Added a compact per-message local evidence panel for Aria replies, showing `local_tools_used` and top `local_code_hits` returned by `/api/aria/chat`.
- [x] `scientist/api.py` -- GitHub Copilot -- Added local chat tooling hooks for `/api/aria/chat` (runtime snapshot + recent experiment probe + workspace code search), injects findings into LLM context, and returns `local_tools_used`/`local_code_hits` metadata in chat responses.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Passes active `progress.experiment_id` to LiveFeed so replay/history can be scoped to the current run.
- [x] `dashboard/src/components/LiveFeed.js` -- GitHub Copilot -- Replays persisted live-feed history only for the active experiment and clears stale events when no active experiment ID is available.
- [x] `dashboard/src/App.js` -- GitHub Copilot -- Added `handleRerunExperiment()` and wired Overview `ControlPanel` to restart the latest experiment via `/api/experiments/<id>/rerun`.
- [x] `dashboard/src/components/ControlPanel.js` -- GitHub Copilot -- Added `Restart Last Experiment` action in Overview run controls (enabled when a recent experiment ID is available).
- [x] `scientist/runner.py` -- GitHub Copilot -- Added full traceback logging (`logger.error(...\ntraceback)`) across experiment/investigation/validation/evolution/novelty/scale-up failure handlers.
- [x] `scientist/api.py` -- GitHub Copilot -- Added `refine_fingerprint` start mode with fingerprint-prefix resolution (`graph_fingerprints`/`fingerprints`) and structured `refine_resolution` response payload.
- [x] `scientist/runner.py` -- GitHub Copilot -- Added `start_fingerprint_refinement()` and mutation-centered generation path (`model_source="fingerprint_refine"`) that mutates selected source graphs and falls back safely when needed.
- [x] `dashboard/src/components/ProgramDetail.js` -- GitHub Copilot -- Added `Refine Fingerprint` action button to launch a refinement run directly from the selected program fingerprint.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added API regression coverage for `refine_fingerprint` input validation and fingerprint-prefix launch path.
- [x] `scientist/runner.py` -- Claude Code -- Added logger.info calls at every key phase: continuous session start (config summary + limits), mode selection (mode + reasoning), experiment start (hypothesis text), graph generation, periodic evaluation progress (every 10 programs or 15s), S1 survivor announcements (★ with loss/params), experiment completion (full funnel summary), Aria summary text, and cycle completion (ΔS1). Added logger.debug for micro-train steps at start/midpoint/end. All 366 tests pass.
- [x] `__main__.py` -- GitHub Copilot -- Switched dashboard startup to `debug=False` so Flask reloader/debug restarts do not repeatedly drop browser SSE connections while backend training continues.
- [x] `scientist/runner.py` -- GitHub Copilot -- Updated graph validation calls to pass active run limits (`max_ops`, `max_depth`) instead of validator defaults, preventing false `Too many ops` failures when configs intentionally exceed 20.
- [x] `tests/test_runner_mode_selection.py` -- GitHub Copilot -- Added regression coverage asserting run-config limit forwarding in both execution and candidate-generation validation paths.
- [x] `scientist/analytics.py` -- Claude Code -- Added `sparse_coverage()` method: queries sparse_density_mean, graph_json sparse ops, pruning_method, and training_program_json for RigL runs. Returns n_sparse_tested, survival rate, avg density, RigL/pruning run counts.
- [x] `scientist/llm/context.py` -- Claude Code -- Added sparsity coverage section to `build_mode_selection_context()` (sparse share, survival, density, RigL/pruning counts) and `build_briefing_context()` (new `sparse_coverage` parameter). LLM now sees sparse data.
- [x] `scientist/llm/prompts.py` -- Claude Code -- Added decision criterion #7 to `MODE_SELECTION_PROMPT` for sparse-focused synthesis when sparsity coverage < 15%. Added sparsity guidance to `BRIEFING_PROMPT` (model_source="morphological_box", use_synthesized_training=true).
- [x] `scientist/persona.py` -- Claude Code -- Added sparsity exploration guardrail in `_rule_based_mode_recommendation()`: triggers sparse-focused synthesis cycle (morph box + synthesized training + one-shot pruning) when sparse share < 15% and every 4th experiment.
- [x] `scientist/runner.py` -- Claude Code -- Wired `analytics.sparse_coverage()` into `_gather_analytics_data()` so sparse data flows through to both context builders and the rule-based fallback.
- [x] `scientist/api.py` -- Claude Code -- Wired `sparse_coverage` into briefing endpoint's `build_briefing_context()` call.
- [x] `scientist/runner.py` -- GitHub Copilot -- Applied per-cycle mode config overrides in `run_aria_cycle`, added cycle-level prescreening after overrides, and injected compression-focus steering when compressed coverage is under target.
- [x] `scientist/persona.py` -- GitHub Copilot -- Added rule-based priority for compact/compression synthesis when compression coverage is underrepresented.
- [x] `scientist/llm/context.py` -- GitHub Copilot -- Added compression coverage/survival signals to mode-selection context for LLM decisions.
- [x] `tests/test_runner_mode_selection.py` -- GitHub Copilot -- Added regression tests for cycle override application and compression steering trigger.
- [x] `eval/sandbox.py` -- Claude Code -- Added `is_cuda_fatal()` for sticky CUDA error detection (device-side assert, illegal memory, context destroyed), `cuda_health_check()` probe, specific `cuda_fatal` error categorization in `safe_eval`, and safe `empty_cache()` in cleanup. Complements Copilot's runner-level circuit breaker.
- [x] `tests/test_integration.py` -- Claude Code -- Added `TestSandboxCudaDetection` with 7 tests covering fatal vs non-fatal error classification, OOM exclusion, mock device-side assert in safe_eval, and result serialization. All 363 tests pass.
- [x] `scientist/runner.py` -- GitHub Copilot -- Added CUDA preflight health probe + recent device-side-assert streak detection in `prescreen_run_config`, with automatic CPU fallback to prevent repeated 0/0 launch failures.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added regression tests for probe-failure fallback and recent CUDA-assert streak fallback behavior.
- [x] `eval/quantization.py` -- Claude Code -- Created fake-quantization utilities: `fake_quantize_tensor()` (per-tensor symmetric INT8/INT4 simulation), `apply_fake_quantization()` (in-place model quantization), `evaluate_sparse_quant_quality()` (combined sparsity+quantization quality-retention-per-byte pipeline).
- [x] `scientist/analytics.py` -- Claude Code -- Added `sparse_quant_codesign_summary()` to ExperimentAnalytics: aggregates quality-retention and compression ratios (INT8/INT4) across programs with sparse/pruning data, computes quality-per-byte metric.
- [x] `tests/test_integration.py` -- Claude Code -- Added `TestQuantizationUtils` with 8 tests covering fake-quant tensor ops, model quantization, zero preservation, and analytics summary. All 354 tests pass.
- [x] `README.md` -- GitHub Copilot -- Added project overview, validated startup/run commands, parent-directory launch requirement, and troubleshooting notes for HYDRA/CKA warnings.
- [x] `training/sparse_training.py` -- Claude Code -- Created `RigLScheduler` (drop smallest-magnitude, grow largest-gradient, cosine-decayed drop fraction) and `RigLOptimizer` (AdamW + integrated RigL mask updates). Fixed parameter budget, evolving sparsity topology.
- [x] `training/optimizer_synthesis.py` -- Claude Code -- Added `rigl_sparse` to OPTIMIZER_RECIPES and SynthesizedOptimizer.create() dispatch. Passes sparsity/total_steps kwargs through.
- [x] `scientist/analytics.py` -- Claude Code -- Added `moe_activation_telemetry()`: aggregates per-expert utilization (avg/min/max), load balance score (1 - CV), routing drop rates, confidence stats, capacity overflow counts from existing notebook routing columns. All 346 tests pass.
- [x] `eval/pruning.py` -- GitHub Copilot -- Added one-shot pruning utilities (`apply_one_shot_pruning`, `estimate_lm_ce_loss`) with Wanda/SparseGPT-style scoring and sparsity accounting.
- [x] `scientist/runner.py` -- GitHub Copilot -- Added optional one-shot pruning baseline config knobs + prescreen safeguards and integrated dense-vs-pruned quality-retention metrics into Stage 1 outputs.
- [x] `scientist/notebook.py` -- GitHub Copilot -- Added pruning metric columns in `program_results` schema/migration (`pruning_*` fields) for persistence.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added regression coverage for pruning utility sparsity behavior and runner micro-train pruning metric emission.
- [x] `scientist/runner.py` -- Claude Code -- Added model_dim divisibility-by-4 check to `prescreen_run_config`. Auto-rounds up when not divisible by 4, preventing uneven splits in grouped_linear (g=4), N:M sparse (m=4), and semi_structured_2_4. All 344 tests pass. Weight Compression Primitives Initiative now 10/10 complete.
- [x] `scientist/api.py` -- GitHub Copilot -- Added `_compute_sparse_evidence()` aggregation and included sparse briefing payload blocks (`data.sparse`, `evidence.sparse`) in `/api/strategy/briefing`.
- [x] `dashboard/src/components/StrategyAdvisor.js` -- GitHub Copilot -- Added sparse evidence chips in the "Why this was chosen" section (sparse runs, density, N:M compliance, top sparse op).
- [x] `tests/test_integration.py` -- GitHub Copilot -- Extended strategy briefing contract test to assert sparse payload presence.
- [x] `scientist/runner.py` -- GitHub Copilot -- Added `_extract_sparse_metrics` aggregation from compiled layer op telemetry and wired metrics into `program_results` payload.
- [x] `scientist/notebook.py` -- GitHub Copilot -- Added sparse telemetry columns to schema + migration map for notebook persistence.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `TestSparseTelemetryPersistence` covering runner aggregation and notebook column migration.
- [x] `synthesis/primitives.py` -- GitHub Copilot -- Added built-in sparse parameterized primitives: `nm_sparse_linear`, `block_sparse_linear`, and `semi_structured_2_4_linear` with explicit config schema.
- [x] `synthesis/compiler.py` -- GitHub Copilot -- Added sparse mask builders (N:M and block), runtime sparse execution paths, and `sparse_telemetry` fallback/density tracking for compatibility gating.
- [x] `eval/flops.py` -- GitHub Copilot -- Added sparse-effective FLOP estimation for N:M, block-sparse, and semi-structured 2:4 ops using density-aware compute scaling.
- [x] `morphological_box.py` -- GitHub Copilot -- Added sparse weight storage options (`semi_structured_2_4`, `block_sparse`) and structural sparse validity constraints.
- [x] `tests/test_integration.py` -- GitHub Copilot -- Added `TestSparsePrimitives` coverage for registration, shape/gradient flow, fallback telemetry, and sparse morphology constraints.
- [x] `search/novelty_search.py` -- Codex -- Added novelty feature sanitization (`_sanitize_unit_feature`) and bounded RMS behavior distance to prevent outlier/NaN fingerprint fields from inflating live-feed novelty magnitudes.
- [x] `tests/test_novelty_search_scaling.py` -- Codex -- Added regression tests ensuring novelty distance/archive scores remain finite and bounded under extreme/non-finite fingerprint values.


---

Archive date: 2026-02-20
Source: `.current_work.md` cleanup (comment removals)

<!-- GitHub Copilot reproducibility-workflow claims released — completed -->
<!-- GitHub Copilot scale-up-template claims released — completed -->
<!-- GitHub Copilot production-readiness panel claims released — completed -->
<!-- GitHub Copilot survivor-mining claim released — deferred to separate pass -->
<!-- GitHub Copilot 3b-swarm routing claim released — completed -->
<!-- GitHub Copilot champion-bias claim released — completed -->
<!-- GitHub Copilot frequency-domain suppression claim released — completed -->
<!-- GitHub Copilot zero-grad precheck claim released — completed -->
<!-- GitHub Copilot mood-grounding claim released — completed -->
<!-- GitHub Copilot simplicity-constraints claim released — completed -->
<!-- GitHub Copilot Aria cadence+stale-evidence claim released — completed -->
<!-- GitHub Copilot Aria planner-path badge claim released — completed -->
<!-- GitHub Copilot Aria local-helper badge claim released — completed -->
<!-- GitHub Copilot Ollama local-helper claim released — completed -->
<!-- GitHub Copilot run-cause audit trail claim released — completed -->
<!-- GitHub Copilot Aria tools+idle-refresh claims released — completed -->
<!-- GitHub Copilot Aria concise self-fix claim released — completed -->
<!-- GitHub Copilot Aria trigger+dedup claims released — completed -->
<!-- GitHub Copilot Aria file-type rails claim released — completed -->
<!-- GitHub Copilot Aria agent progress UI claim released — completed -->
<!-- GitHub Copilot Aria codebase-agent claim released — completed -->
<!-- GitHub Copilot Aria clickable source links claims released — completed -->
<!-- GitHub Copilot Aria chat evidence panel claim released — completed -->
<!-- GitHub Copilot local chat tools claim released — completed -->
<!-- GitHub Copilot live-feed scoping claims released — completed -->
<!-- GitHub Copilot restart+traceback claims released — completed -->
<!-- GitHub Copilot fingerprint-refinement claims released — completed -->
<!-- Claude Code experiment logging claims released — completed -->
<!-- GitHub Copilot dashboard SSE stability claim released — completed -->
<!-- Claude Code sparsity-awareness wiring claims released — completed -->
<!-- Claude Code sandbox CUDA hardening claims released — completed -->
<!-- GitHub Copilot validation-limit propagation claims released — completed -->
<!-- GitHub Copilot Phase 96 claims released — completed -->
<!-- GitHub Copilot Phase 97 claims released — completed -->
<!-- GitHub Copilot Phase 99 claims released — completed -->
<!-- GitHub Copilot Phase 100 claims released — completed -->
<!-- GitHub Copilot Phase 101 claims released — completed -->
<!-- GitHub Copilot Phase 102 claims released — completed -->
<!-- GitHub Copilot Phase 103 claims released — completed -->
<!-- GitHub Copilot README docs refresh claim released — completed -->
<!-- GitHub Copilot prescreen CUDA hardening claims released — completed -->
<!-- GitHub Copilot compression-direction hardening claims released — completed -->
<!-- Claude Code Phase 104 claims released — completed -->
<!-- Claude Code Phase 95 claims released — completed -->
<!-- Claude Code Phase 93 claims released — completed -->
<!-- Claude Code Phase 92 claims released — completed -->
<!-- Claude Code learning-loop + compound-ops claims released — completed -->
<!-- Claude Code Phase 76 claims released — completed -->
<!-- Claude Code Decision Packet claims released — completed -->
<!-- Claude Code ResearchReport queue/handoff claims released — completed -->
<!-- Claude Code negative-results claims released — completed -->
<!-- Claude Code score unification + reproducibility manifest claims released — completed -->
<!-- Claude Code attention taxonomy + deltas claims released — completed -->
<!-- Claude Code Phase 64 Aria AI briefing claims released — completed -->
