# Native-First Runner Plan Archive

Archived from `NATIVE_RUNNER_PLAN.md` on 2026-02-21 to keep the active plan focused.

## Completed Step History

- Step 1 implemented: added `research/scientist/native_runner_adapter.py` scaffold with explicit state detection and strict/non-strict policy handshake.
- Step 2 implemented: capability + semantic-warning passthrough is now wired into `native_runner_capability_report()` and compile-path logging/telemetry in `research/scientist/native_runner.py`.
- Step 3 implemented: first true non-strict reuse call now probes Designer runtime using real workflow conversion + compile invocation with parity guard in `research/scientist/native_runner_adapter.py`.
- Step 4 implemented: fallback-rate counters + optional fail-fast threshold guard added in `research/scientist/native_runner.py`.
- Step 5 implemented: contract compatibility tests added in `research/tests/test_native_runner_contracts.py` (IR schema + ABI header symbol checks).
- Step 6 implemented: CI workflow added at `../.github/workflows/research-native-ci.yml` (Python 3.11/3.12 matrix).
- Step 7 implemented: adapter-vs-legacy parity tests added for representative graph families (`simple`, `residual`, `attention-like`) in `research/tests/test_native_runner_parity.py`.
- Step 8 implemented: long-run fallback telemetry soak harness added in `research/scientist/native_runner_soak.py` with regression coverage in `research/tests/test_native_runner_soak.py`.
- Step 8 implemented: live native telemetry now injected into `/api/status`, `/api/progress`, and `/api/dashboard` progress payloads (plus `/api/system/status`) and rendered in dashboard `ControlPanel` status/progress cards.
- Step 9 implemented: selective execution-mode scaffold added in `research/scientist/native_runner.py` via `NATIVE_RUNNER_EXECUTION_MODE` (`probe`|`selective`) with explicit `selective_execution` and `execution_path` telemetry (safe legacy compile retained).
- Step 10 implemented: fallback telemetry stress-soak and API freshness smoke tests added (`research/tests/test_native_runner_adapter.py`, `research/tests/test_integration.py`) and passing; `/api/progress` now verified to reflect fresh compile counter updates.
- Step 11 implemented: selective mode now performs a real native safe-path activation by executing `relu` + `add` via `ctypes` on tiny buffers when candidate criteria pass; activation success/failure is surfaced in telemetry (`selective_execution.activation`, `execution_path`, and activation counters).
- Step 12 implemented: compile/eval A/B contract test added for selective mode (`research/tests/test_native_runner_parity.py`) verifying output parity against legacy and asserting active selective execution telemetry.
- Step 13 implemented: selective mode now supports opt-in designer layer replacement (`NATIVE_RUNNER_SELECTIVE_LAYER_EXEC=1`) by compiling per-layer workflows and swapping compatible layers with adapter modules.
- Step 14 implemented: selective layer replacement success/failure regressions added (`research/tests/test_native_runner_adapter.py`) with explicit `execution_path` + `selective_execution.layer_build` assertions.
- Step 15 implemented: sustained selective guardrail telemetry added (`selective_guardrail`) with configurable threshold (`NATIVE_RUNNER_SELECTIVE_GUARDRAIL_WINDOW`), trigger/reset behavior, and trigger counters in `research/scientist/native_runner.py`.
- Step 16 implemented: guardrail regression coverage added in `research/tests/test_native_runner_adapter.py` (trigger + reset) and `/api/progress` exposure validated in `research/tests/test_integration.py`.
- Step 17 implemented: `selective_guardrail` is now surfaced in dashboard `ControlPanel` badges and native runner status details (idle + running views), including active/ok state, window progress, trigger count, and last reason.
- Step 18 implemented: rolling guardrail event history (`triggered` / `cleared`) added to `selective_guardrail.history` in `research/scientist/native_runner.py`, exposed via `/api/progress`, and rendered in dashboard `ControlPanel` as recent history.
- Step 19 implemented: selective layer replacement now enforces compatibility guardrails (layer-index bounds, required input routing, and tensor output shape contract checks) with explicit `skip_reasons`/`skipped_layers` telemetry.
- Step 20 implemented: guardrail history events now include UTC `timestamp` plus `source` tags for timeline correlation in `research/scientist/native_runner.py`; adapter/integration assertions and dashboard `ControlPanel` history rendering were updated to display/verify this metadata.
- Step 21 implemented: selective layer replacement now emits richer per-layer diagnostics (`layer_results` with `layer_index`, `workflow_id`, `input_node_id`, `applied`, `skip_reason`, `error`) for canary traceability.
- Step 22 implemented: opt-in strict selective layer mode (`NATIVE_RUNNER_SELECTIVE_LAYER_STRICT=1`) now fail-fast rejects incompatible replacements; regression coverage added in `research/tests/test_native_runner_adapter.py`.
- Step 23 implemented: dashboard observability upgraded to expose selective execution details (`execution_path`, candidate reason, layer strict mode, applied/skipped counters, per-layer `layer_results`) in `ControlPanel` and compact `StatusBar` badges.
- Step 24 implemented: guardrail history retention boundary coverage added in `research/tests/test_native_runner_adapter.py`, verifying `selective_guardrail.history` truncates to the last 25 events while preserving chronological order and timestamp/source metadata.
- Step 25 implemented: deterministic selective canary benchmark harness added in `research/scientist/native_runner_canary.py`, comparing probe vs selective-layer compile latency under fixed seeds; regression coverage added in `research/tests/test_native_runner_canary.py`.
- Step 26 implemented: `/api/system/status` now includes lightweight `native_runner_canary` summary via a cached, env-gated canary benchmark payload (`NATIVE_RUNNER_CANARY_STATUS_ENABLED`, TTL/seed/iteration controls) with integration coverage for default and enabled payload shapes.
- Step 27 implemented: dashboard `ControlPanel` now surfaces `native_runner_canary` summary from `/api/system/status` (status, probe→selective latency, delta, sample count, seed) in both native-runner live card and running stats strip.
- Step 28 implemented: dashboard canary visibility controls added in `ControlPanel` (toggle summary badge + toggle refresh hint), with concise staleness/cached hinting sourced from `native_runner_canary.age_s` and `cached`.
- Step 29 implemented: compact API-facing selective layer summary added at `selective_execution.layer_build.summary` (`applied_layers`, `skipped_layers`, `error_layers`, `top_skip_reasons`, strict-fail marker) with adapter regression assertions.
- Step 30 implemented: manual canary refresh path added end-to-end via `/api/system/status?refresh_canary=1` (TTL bypass in API canary cache) plus dashboard `ControlPanel` action button (`Refresh canary now`) and integration coverage for cache-bypass behavior.
- Step 31 implemented: dashboard canary refresh control now has a cooldown indicator/lockout (`Refresh in Ns`) to prevent rapid repeated refresh spam, with inline cooldown hinting in `ControlPanel`.
- Step 32 implemented: optional guardrail mini-timeline sparkline added in dashboard `ControlPanel` using recent `selective_guardrail.history` events (`█` triggered, `░` cleared) in both status badge and native runner card.
- Step 33 implemented: explicit canary force-refresh endpoint added (`POST /api/native-runner/canary/refresh`) and dashboard refresh action switched to this route with in-flight UI state; integration tests now cover disabled and enabled force-refresh semantics.
- Step 34 implemented: minimal C runtime ABI implementation added in `research/runtime/native/src/runner_abi.c` covering init/capability/strict/fallback/compile/execute/release so `runner_abi.h` symbols are now smoke-testable.
- Step 35 implemented: runtime ABI smoke coverage added (`research/tests/test_native_runner_abi_smoke.py`) and CI updated to build native runtime before running ABI tests (`.github/workflows/research-native-ci.yml`).
- Step 34 implemented: guardrail sparkline legend/help tooltip added in dashboard `ControlPanel` (`Legend: █ triggered, ░ cleared, · other`) with hover detail retained for recent event timeline context.
- Step 35 implemented: optional dashboard cooldown override setting added in `ControlPanel` (`Cooldown (s)` input, clamped 0–60) and wired into canary refresh lockout duration.
- Step 36 implemented: dashboard canary UI preferences (`show summary`, `show hint`, `cooldown`) now persist via localStorage in `ControlPanel`, with safe fallback defaults and bounded cooldown restore.
- Step 37 implemented: Rust scheduler (WS-C) fully integrated with C kernels via FFI. `aria-scheduler` now links `aria_native_runtime_static` and `libscipy_openblas` (via RPATH), exposing `execute_graph` to Python.
- Step 38 implemented: `native_runner.py` updated with `dispatch_graph_native` and Rust scheduler availability probe in selective activation logic.
- Step 39 implemented: dashboard `ControlPanel` now provides `Reset canary prefs` action to clear persisted localStorage canary settings and restore defaults (`show summary=true`, `show hint=true`, `cooldown=8s`).
- Step 40 implemented: `runner_abi.c` compile/execute upgraded from dummy smoke behavior to deterministic first-family execution (native_ir.v1 schema gate, bounded compile params, handle-owned logits buffers, token-conditioned logits, seq-limit enforcement, release-safe lifecycle).
- Step 41 implemented: ABI smoke coverage expanded (`test_native_runner_abi_smoke.py`) to validate deterministic logits replay, released-handle rejection, unsupported-schema compile failure, and execute seq-overflow guardrails; CI build target mismatch fixed (`c-kernels`) with a backward-compatible `build` alias in `runtime/native/Makefile`.
- Step 42 implemented: `native_runner.py` now supports optional runner ABI prepare path (`NATIVE_RUNNER_ABI_EXEC=1`) that compiles the first graph via `nr_compile`, executes a tiny token smoke via `nr_execute`, and attaches a reusable ABI session handle to the compiled model for future native-token execution.
- Step 43 implemented: adapter regressions added for ABI integration behavior (`test_native_runner_adapter.py`) covering successful ABI session attachment and strict-mode fail-fast on ABI prepare failure.
- Step 44 implemented: `safe_eval` now performs optional inference-time token execution through attached ABI session (`NATIVE_RUNNER_ABI_INFER_PROBE`), recording `native_abi_probe` telemetry while leaving forward/backward training path unchanged.
- Step 45 implemented: added dedicated ABI inference-probe regression tests (`test_native_runner_abi_inference_probe.py`) for success and non-fatal probe failure paths.
- Step 40 implemented: optional compact/expanded toggle for native telemetry details added in dashboard `ControlPanel` (persisted preference), with compact mode hiding verbose guardrail/layer detail blocks while preserving core status metrics.
- Step 42 implemented: inline confirmation message added in dashboard `ControlPanel` when `Reset canary prefs` is used (auto-dismiss toast-like hint confirming defaults restored).
- Step 43 implemented: dashboard `ControlPanel` now supports telemetry verbosity presets (`compact`/`default`/`debug`) with persisted profile state, backward-compatible preset inference from existing canary preferences, and one-click profile application for summary/hint/cooldown/detail settings.
- Step 44 implemented: lightweight canary control tooltips added in dashboard `ControlPanel` for cooldown and reset controls (clear hover guidance for cooldown semantics and default-reset behavior).
- Step 45 implemented: non-persistent custom preset hint added in dashboard `ControlPanel` to indicate when telemetry controls diverge from named presets (`compact`/`default`/`debug`) and guide users back to a preset selection.
- Step 46 implemented: running-state native runner card now exposes telemetry preset quick-actions (`compact`/`default`/`debug`) for one-click verbosity switching during active experiments.
- Step 47 implemented: canary preference persistence now excludes the derived `custom` telemetry preset value, storing only named presets to reduce localStorage churn while preserving runtime custom-state hinting via inferred settings.
- Step 48 implemented: targeted dashboard regression checks added for telemetry preset transitions via `dashboard/src/components/ControlPanel.test.js` and shared preset helper extraction (`controlPanelTelemetryPresets.js`) to validate idle/running parity, custom divergence detection, and persistence normalization.
- Step 49 implemented: wired dashboard telemetry preset regression checks into CI (`.github/workflows/research-native-ci.yml`) with Node setup + focused `ControlPanel.test.js` run on the Python 3.11 matrix lane.
- Step 50 implemented: hardened runner ABI compile/execute path toward a real first supported IR family by requiring supported unary `op_name` graphs at compile-time (`relu`/`gelu`/`silu`/`sigmoid`/`tanh`/`exp`), executing the selected native unary kernel during `nr_execute`, and updating Python ABI prepare logic to select family-compatible graphs with explicit `compile_message` telemetry.
- Step 51 implemented: extended ABI first-family support to a minimal two-op chain (`unary + add`) by requiring both op markers at compile-time, invoking native `add` then unary kernels during execute with deterministic add-vector initialization, and adding deterministic smoke/adapter coverage for supported and rejected graph-family cases.
- Step 52 implemented: extended ABI first-family support to a minimal three-op chain (`unary + add + mul`) by requiring add+mul markers at compile-time, invoking native `add` then `mul` then unary kernels during execute with deterministic vector initialization, and updating smoke/adapter coverage for accepted/rejected family cases.
- Step 53 implemented: extended ABI first-family support to a minimal four-op chain (`unary + add + mul + matmul`) by requiring add+mul+matmul markers at compile-time, running deterministic `add -> mul -> matmul -> unary` execution, and updating smoke/adapter coverage for family selection and rejection cases.
- Step 54 implemented: extended ABI first-family support to a minimal five-op chain (`unary + add + mul + matmul + linear`) by requiring add+mul+matmul+linear markers at compile-time, running deterministic `add -> mul -> matmul -> linear -> unary` execution, and updating smoke/adapter coverage for family selection and rejection cases.
- Step 55 implemented: extended ABI first-family support to a minimal six-op chain (`unary + add + mul + matmul + linear + softmax`) by requiring add+mul+matmul+linear+softmax markers at compile-time, running deterministic `add -> mul -> matmul -> linear -> softmax -> unary` execution, and updating smoke/adapter coverage for family selection and rejection cases.
- Step 56 implemented: extended ABI first-family support to a minimal seven-op chain (`unary + add + mul + matmul + linear + softmax + rmsnorm`) by requiring add+mul+matmul+linear+softmax+rmsnorm markers at compile-time, running deterministic `add -> mul -> matmul -> linear -> softmax -> rmsnorm -> unary` execution, and updating smoke/adapter coverage for family selection and rejection cases.
- Step 57 implemented: extended ABI first-family support to a minimal eight-op chain (`unary + add + mul + matmul + linear + softmax + rmsnorm + sub`) by requiring add+mul+matmul+linear+softmax+rmsnorm+sub markers at compile-time, running deterministic `add -> mul -> matmul -> linear -> softmax -> rmsnorm -> sub -> unary` execution, and updating smoke/adapter coverage for family selection and rejection cases.
- Step 58 implemented: tightened ABI first-family admission by requiring an explicit `exp` marker alongside the eight-op chain (`add + mul + matmul + linear + softmax + rmsnorm + sub`), with matching Python candidate gating and focused smoke/adapter coverage updates.
- Step 59 implemented: tightened ABI first-family admission to require ordered family markers (`exp -> add -> mul -> matmul -> linear -> softmax -> rmsnorm -> sub`) in C compile gates and Python ABI candidate selection, with focused smoke/adapter out-of-order rejection coverage.
- Step 60 implemented: tightened ABI first-family admission to require explicit dependency-chain links across the ordered family (`exp -> add -> mul -> matmul -> linear -> softmax -> rmsnorm -> sub`) using node `input_ids` checks in C compile gates and Python ABI candidate gating, with focused unlinked-family rejection coverage.
- Step 61 implemented: evolved ABI first-family topology checks from direct-link requirements to transitive ancestor-path validation across the ordered family chain via `input_ids` ancestry in both C compile gates and Python ABI candidate selection, with focused transitively linked acceptance coverage.
- Step 62 implemented: enforced explicit-edge topology parity for ABI first-family admission by requiring transitive chain reachability in IR `edges` (when present) in both C compile gates and Python ABI candidate selection, with focused edge-mismatch rejection coverage.
- Step 63 implemented: tightened ABI first-family topology admission back to strict direct-link parity by requiring immediate parent links across the ordered family chain in both `input_ids` and explicit `edges` (when present) in C compile gates and Python ABI candidate selection, with focused transitive-link rejection coverage.
- Step 64 implemented: tightened explicit-edge declaration parity so declared `edges` arrays are always enforced for ordered-family direct links (including rejecting declared-empty edges), in both C compile gates and Python ABI candidate selection, with focused declared-empty-edge rejection coverage.
- Step 65 implemented: tightened first-family identity constraints to require exactly one occurrence of each required marker op (`exp/add/mul/matmul/linear/softmax/rmsnorm/sub`) in both C compile gates and Python ABI candidate selection, with focused duplicate-marker rejection coverage.
- Step 66 implemented: tightened Python ABI candidate selection to enforce required direct family-chain links in `input_ids` independently from explicit `edges` so edge-only links cannot satisfy input linkage parity, with focused edge-only-link rejection coverage (now aligned with C compile gates).
