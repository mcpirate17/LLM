id: swiglu_mlp
version: 1.0.0
name: Swiglu Mlp
category: channel_mixing
tags:
- standard
- channel_mixing
status: approved
description: SwiGLU MLP channel mixer (Gated Linear Unit with SiLU).
inputs:
- name: x
  dtype: tensor
  shape:
  - B
  - S
  - D
outputs:
- name: y
  dtype: tensor
  shape:
  - B
  - S
  - D
params:
  mlp_ratio:
    type: float
    default: 3.0
    description: Expansion factor for the hidden dimension.
implementation:
  native: null
  rust: null
  cython: null
  python: kernel_fallback.py
performance:
  has_params: true
  param_formula: D*D*3.5
  preserves_gradient: true
  numerically_risky: false
constraints:
  incompatible_with: []
help_md: '### Technical

  SwiGLU MLP (baseline)


  Inputs: x:tensor[B, S, D].

  Outputs: y:tensor[B, S, D].

  Params: mlp_ratio (float, default 3.0).


  ### Plain Language

  SwiGLU MLP (baseline)


  High-performance gated feed-forward block using SiLU activation.

  '
