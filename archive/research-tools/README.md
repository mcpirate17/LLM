# Research Tools Archive

This directory is the quarantine location for maintenance scripts removed from
the active `research/tools/` package.

Archived scripts kept here are not part of the runtime import surface, test
discovery surface, or normal research entrypoints. Restore a script to
`research/tools/` only after re-reviewing its imports, side effects, and current
owner.
